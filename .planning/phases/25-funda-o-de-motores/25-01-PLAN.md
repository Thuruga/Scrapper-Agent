---
phase: 25-funda-o-de-motores
plan: 01
type: execute
wave: 1
depends_on: ["25-00"]
files_modified:
  - api/routes_brands.py
autonomous: true
requirements: [COMP-02]
must_haves:
  truths:
    - "detect_engine returns 'unknown' for a Wake Commerce domain (fbitsstatic.net in HTML) instead of 'vtex'"
    - "detect_engine returns 'unknown' when all probes fail/timeout instead of falling back to 'vtex'"
    - "A brand whose detected engine is 'unknown' is saved with is_active=False and does not raise an HTTP error"
    - "Wake probe runs BEFORE the VTEX HTML check and uses fbitsstatic.net, not the generic 'vtex' substring"
  artifacts:
    - path: "api/routes_brands.py"
      provides: "Hardened detect_engine (unknown fallback + Wake probe) and create_brand unknown→inactive branch"
      contains: "return \"unknown\""
  key_links:
    - from: "api/routes_brands.py:create_brand"
      to: "api/routes_brands.py:detect_engine"
      via: "engine == 'auto' detection then unknown branch"
      pattern: "detect_engine"
    - from: "api/routes_brands.py:create_brand"
      to: "services.brand_service.set_active"
      via: "auto-deactivate unknown engine after add_brand"
      pattern: "set_active"
---

<objective>
Implement COMP-02 Block 1 in `api/routes_brands.py`:
1. Harden `detect_engine` so it returns `"unknown"` (D-01) instead of the current
   unconditional `return "vtex"` fallback at L53, removing the loose `"vtex" in html_lower`
   condition that false-positives on Wake/marketplace pages.
2. Insert a positive Wake Commerce probe (D-02) using the `fbitsstatic.net` CDN marker —
   ordered BEFORE the hardened VTEX HTML check (Pitfall 1). Wake recognition still returns
   `"unknown"` (Wake is unsupported this milestone) but logs the detection for confidence.
3. Treat transient total-failure/timeout as inconclusive → `"unknown"` (D-03).
4. In `create_brand`, when the detected engine is `"unknown"`, persist the brand with
   `is_active=False` without raising an HTTP error (D-04), and surface the state in the response.

Purpose: Stop the silent VTEX masking that lets an unsupported-platform brand register as
VTEX and return "0 produtos". This is the foundation gate for the whole milestone (Phase 26
relies on `detect_engine` reconfirming engine at add-time; an unknown brand auto-deactivates
rather than polluting search).

Output: Modified `api/routes_brands.py` (detect_engine + create_brand). Turns RED tests from
Plan 00 green (test_engine_detection.py).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-funda-o-de-motores/25-CONTEXT.md
@.planning/phases/25-funda-o-de-motores/25-RESEARCH.md
@.planning/phases/25-funda-o-de-motores/25-VALIDATION.md

@api/routes_brands.py
@core/models.py
</context>

<artifacts_this_phase_produces>
Symbols this plan creates/changes (exclude from drift verification):
- `detect_engine` returning the new `"unknown"` value (previously only "shopify"/"vtex")
- Wake Commerce probe branch inside `detect_engine` keyed on `fbitsstatic.net`
- `create_brand` branch that auto-deactivates `engine == "unknown"` brands (calls `brand_service.set_active(..., False)`)
- Log marker for Wake detection (e.g. `wake_detected` / `fbitsstatic.net` log line)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden detect_engine — unknown fallback + Wake probe + redirect-safe probes</name>
  <files>api/routes_brands.py</files>
  <read_first>
    - api/routes_brands.py (detect_engine L14-53: Shopify probe L22-30, VTEX API probe L32-38, HTML fallback L40-50, loose `"vtex" in html_lower` L45, final `return "vtex"` L53)
    - .planning/phases/25-funda-o-de-motores/25-RESEARCH.md (§"Wake Commerce Detection", §"VTEX/Shopify Hardening" — the 6-step detection order table; §"Common Pitfalls" 1 & 5; §"Security Domain" — Spoofing/redirect threat)
    - tests/test_engine_detection.py (the RED contract from Plan 00 — TestDetectEngine cases)
    - core/session_manager.py (SessionManager.get_session — shared aiohttp session this function uses)
  </read_first>
  <action>
    Modify `detect_engine` in `api/routes_brands.py` to the hardened 6-step order from RESEARCH §"VTEX/Shopify Hardening":
    Step 1 Shopify (`/collections.json` → 200 with `collections` key → `"shopify"`) — unchanged.
    Step 2 VTEX API (`/api/catalog_system/pub/category/tree/1` → 200 → `"vtex"`) — unchanged.
    Step 3 (NEW) Wake probe on the home HTML: if `fbitsstatic.net` in `html_lower`, log an info line marking Wake detection
    (e.g. "detect_engine: Wake Commerce detected for %s (fbitsstatic.net)") and `return "unknown"`. This MUST run before the VTEX HTML check.
    Step 4 Hardened VTEX HTML: keep only `vtexassets.com` OR `vtexcommercestable.com`; REMOVE the loose `"vtex" in html_lower`
    third condition (it false-positives on Wake/marketplace pages — Pitfall 1).
    Step 5 Hardened Shopify HTML: `cdn.shopify.com` OR `window.shopify` (existing) — keep.
    Step 6 Replace the final `return "vtex"` (L53) with `return "unknown"` (D-01, D-03 — transient/total failure is inconclusive).

    Security (Spoofing, RESEARCH §Security Domain): the home-page HTML GET follows redirects by default in aiohttp; a malicious
    redirect could point detection at an attacker domain. Add `allow_redirects=False` to the home-page HTML probe GET (Step 3-5 share
    the one HTML fetch), OR validate the final response URL host equals the target domain before reading HTML. Apply the same hardening
    to the probe GETs as practical. Keep the existing per-probe try/except + `logger.debug` so a single probe failure does not abort the chain.

    Do NOT add a real Wake engine, do NOT reclassify already-registered brands, do NOT change the Shopify/VTEX API probe URLs.
    No fenced code blocks in this action — implement against the identifiers named above.
  </action>
  <verify>
    <automated>cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -m pytest tests/test_engine_detection.py -k "shopify or vtex or wake or all_probes_fail" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - api/routes_brands.py detect_engine no longer contains `return "vtex"` as the final unconditional fallback; final return is `return "unknown"`
    - api/routes_brands.py detect_engine no longer contains the loose `"vtex" in html_lower` condition
    - api/routes_brands.py detect_engine contains a `fbitsstatic.net` check that returns `"unknown"` and is positioned before the `vtexassets.com`/`vtexcommercestable.com` check
    - The home-page HTML probe GET uses `allow_redirects=False` (or validates final URL host == target domain)
    - `python -m pytest tests/test_engine_detection.py -k wake -x` exits 0 (GREEN)
    - `python -m pytest tests/test_engine_detection.py -k all_probes_fail -x` exits 0 (GREEN)
    - `python -m pytest tests/test_engine_detection.py -k "shopify or vtex" -x` exits 0 (no regression on supported platforms)
  </acceptance_criteria>
  <done>detect_engine returns "shopify"/"vtex" for genuine signals, "unknown" for Wake (fbitsstatic.net) and for total probe failure; redirect-safe; all four TestDetectEngine cases green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: create_brand auto-deactivates unknown engine (D-04)</name>
  <files>api/routes_brands.py</files>
  <read_first>
    - api/routes_brands.py (create_brand L56-66: engine=="auto" → detect_engine; returns brand_service.add_brand)
    - services/brand_service.py (add_brand L188-198 — creates DynamicBrand with default is_active=True; note DynamicBrandCreate has no is_active field)
    - core/models.py (DynamicBrandCreate L210-225 — no is_active; DynamicBrand L228-232 — is_active default True)
    - .planning/phases/25-funda-o-de-motores/25-RESEARCH.md (§"D-04: Unknown Engine → Auto-Deactivate on Create", §"Common Pitfalls" 2)
    - tests/test_engine_detection.py (TestCreateBrandUnknown contract from Plan 00)
  </read_first>
  <action>
    Modify `create_brand` in `api/routes_brands.py` so that after engine detection (when `brand_data.engine == "auto"` resolves to
    a value), if the resolved engine is `"unknown"`, the brand is saved with `is_active=False` and the route returns HTTP 200 with the
    brand reflecting `engine="unknown"` and `is_active=False` — NOT an HTTP error (D-04).

    Implementation per RESEARCH §"D-04" "Simpler alternative": call `brand_service.add_brand(brand_data)` then, when engine is `"unknown"`,
    call `brand_service.set_active(saved.brand_key, False)` and return that result. `set_active` is provided by Plan 02 (Wave 1, parallel —
    this plan depends on Plan 00 only; the executor must coordinate so set_active exists before this task runs its integration test, OR mock
    set_active in the unit portion). Because `DynamicBrandCreate` has no `is_active` field, do NOT try to set it on the create model — set it
    on the persisted brand via `set_active` (avoids Pitfall 2 where the brand stays active).

    Preserve the existing behavior for non-unknown engines: normal `add_brand` path, brand stays active (default True).
    Keep the existing try/except → HTTPException(400) wrapper for genuine errors (it must NOT fire for the unknown-engine case).
    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -m pytest tests/test_engine_detection.py -k CreateBrandUnknown -x -q</automated>
  </verify>
  <acceptance_criteria>
    - api/routes_brands.py create_brand branches on `brand_data.engine == "unknown"` and calls `brand_service.set_active(..., False)` after add_brand
    - create_brand returns HTTP 200 (no HTTPException) when engine is "unknown"; the returned brand has engine="unknown" and is_active=False
    - Non-unknown engines unchanged: brand persists with is_active=True
    - `python -m pytest tests/test_engine_detection.py -k CreateBrandUnknown -x` exits 0 (GREEN)
    - `python -m pytest tests/test_engine_detection.py -x -q` full file exits 0
  </acceptance_criteria>
  <done>Adding a brand on an unsupported platform yields a saved, inactive, engine="unknown" brand surfaced in the response; the chokepoint (Plan 02) then excludes it from search — zero silent VTEX masking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → POST /brands/ | Untrusted `domain`/`engine` input crosses into detect_engine and persistence. |
| detect_engine → external storefront | Outbound HTTP probes to an operator-supplied domain; response (and redirects) are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-SR | Spoofing | detect_engine home-page HTML probe | mitigate | Add `allow_redirects=False` to the home-page probe GET (or validate final URL host == target domain) so a malicious redirect cannot make an attacker page masquerade as the target's platform. Implemented in Task 1. |
| T-25-01-WK | Tampering | detect_engine VTEX HTML fallback | mitigate | Remove loose `"vtex" in html_lower`; require `vtexassets.com`/`vtexcommercestable.com`, and probe `fbitsstatic.net` (Wake) first → unsupported platform cannot register as VTEX. Implemented in Task 1. |
| T-25-01-UA | Tampering | create_brand unknown-engine path | mitigate | Unknown engine is persisted inactive (is_active=False) via set_active, never silently active — chokepoint excludes it. Implemented in Task 2. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH §"Package Legitimacy Audit" confirms zero new dependencies. |
</threat_model>

<verification>
Per-task: `python -m pytest tests/test_engine_detection.py tests/test_brand_active.py -x -q` after each commit.
Plan gate: `python -m pytest tests/test_engine_detection.py -x -q` fully green (all COMP-02 cases).
Manual (per VALIDATION Manual-Only): with live network, `detect_engine("www.shop2gether.com.br")` → expect `"unknown"`,
log shows `fbitsstatic.net` marker. Not required for plan completion (network/anti-bot risk).
</verification>

<success_criteria>
- ROADMAP SC-1: detect_engine returns "unknown" for a Wake domain; the brand does not enter search (via Plan 02 chokepoint + auto-deactivate)
- detect_engine never returns "vtex" as an unconditional fallback
- Unknown-engine brand is saved inactive without HTTP error
- All COMP-02 tests in tests/test_engine_detection.py green; no regression in supported-platform detection
</success_criteria>

<output>
Create `.planning/phases/25-funda-o-de-motores/25-01-SUMMARY.md` when done.
</output>
