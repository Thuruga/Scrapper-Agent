---
phase: 29-diagn-stico-de-categorias-vazias-erro
plan: 03
type: execute
wave: 3
depends_on: ["29-02"]
files_modified:
  - api/routes_diagnostic.py
  - api/__init__.py
autonomous: true
requirements: [DIAG-01]
user_setup: []
must_haves:
  truths:
    - "GET /diagnostic/brands/{brand_key} returns the synchronous diagnostic report for one brand (404 if the brand is unknown)"
    - "GET /diagnostic/all returns the synchronous diagnostic report for all brands incl. inactive"
    - "The endpoints are synchronous (async def, no BackgroundTasks, no job/WebSocket) and the route layer holds zero business logic"
    - "Both endpoints inherit X-API-Key auth from api_router"
  artifacts:
    - path: "api/routes_diagnostic.py"
      provides: "Thin GET endpoints delegating to the diagnostic service"
      contains: "/diagnostic/brands/"
    - path: "api/__init__.py"
      provides: "diagnostic_router registered on api_router (auth-protected)"
      contains: "diagnostic_router"
  key_links:
    - from: "api/routes_diagnostic.py"
      to: "services.category_diagnostic_service.run_brand_diagnostic"
      via: "await delegation from thin route"
      pattern: "run_brand_diagnostic"
    - from: "api/__init__.py"
      to: "api.routes_diagnostic"
      via: "api_router.include_router(diagnostic_router)"
      pattern: "include_router\\(diagnostic_router\\)"
---

<objective>
Expose the diagnostic service through a thin, synchronous FastAPI route and register it on the auth-protected `api_router`. This completes the DIAG-01 server contract the frontend (29-04) will consume.

Purpose: D-05/D-06 — synchronous, ephemeral. The endpoint fires the probes and returns the full report in the same response, no job/WebSocket/cache/persistence. Route stays thin; all logic lives in the service (consistent with Phase 25 D-06).
Output: new `api/routes_diagnostic.py` with `GET /diagnostic/brands/{brand_key}` and `GET /diagnostic/all`; `api/__init__.py` updated to include the router (inherits `Depends(verify_api_key)`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-CONTEXT.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-RESEARCH.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md
@api/__init__.py
@tests/test_category_diagnostic.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create api/routes_diagnostic.py (thin synchronous GET endpoints)</name>
  <files>api/routes_diagnostic.py</files>
  <read_first>
    - api/routes_category.py (the thin-route + brand-validation pattern: `get_categories` ~L95-104 — `brand.lower()`, `brand_service.get_brand(...)` → `raise HTTPException(status_code=404, ...)` before delegating; `router = APIRouter()` declaration ~L32; note these routes are `async def` with NO BackgroundTasks)
    - services/category_diagnostic_service.py (the `run_brand_diagnostic` / `run_all_brands_diagnostic` signatures created in 29-02 — `run_brand_diagnostic` returns None for an unknown brand)
    - services/brand_service.py (`get_brand` for the 404 guard)
    - .planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md (the "api/routes_diagnostic.py" section: exact endpoint shapes and the explicit "no BackgroundTasks" note)
  </read_first>
  <action>
    Create `api/routes_diagnostic.py`. Imports: `from fastapi import APIRouter, HTTPException`; `from services.brand_service import brand_service`; `from services.category_diagnostic_service import run_brand_diagnostic, run_all_brands_diagnostic`. Declare `router = APIRouter()`.

    `@router.get("/diagnostic/brands/{brand_key}")` → `async def diagnose_brand(brand_key: str)`: lower-case `brand_key`; if `not brand_service.get_brand(brand_key)` → `raise HTTPException(status_code=404, detail=f"Marca '{brand_key}' não encontrada.")` (V5 input validation — validate the path param against the brand registry, never interpolate it raw into a URL); otherwise `result = await run_brand_diagnostic(brand_key)` and return it (if the service still returns None, raise the same 404).

    `@router.get("/diagnostic/all")` → `async def diagnose_all()`: return `await run_all_brands_diagnostic()` (D-01 — includes inactive brands, which is the diagnostic-specific exception to the [ARCH] active_only chokepoint).

    Both handlers are plain `async def` — NO `BackgroundTasks`, NO job_id, NO WebSocket (D-05 synchronous). Zero business logic in the route: enumeration, probing, classification all live in the service.
  </action>
  <verify>
    <automated>python -c "import api.routes_diagnostic as r; paths=sorted({route.path for route in r.router.routes}); print(paths); assert '/diagnostic/brands/{brand_key}' in paths and '/diagnostic/all' in paths"</automated>
  </verify>
  <acceptance_criteria>
    - `api/routes_diagnostic.py` imports cleanly and `router.routes` exposes exactly the two paths `/diagnostic/brands/{brand_key}` and `/diagnostic/all`.
    - Source assertion: `grep -n "BackgroundTasks\|add_task\|job_id\|websocket" api/routes_diagnostic.py` returns NO match (D-05 synchronous).
    - Source assertion: `grep -n "get_brand" api/routes_diagnostic.py` matches (404 guard present — V5 input validation, path-traversal mitigation T-29-05).
    - Both handlers `await` a `services.category_diagnostic_service` function; no probe/classify/enumeration logic appears in the route file (`grep -n "aiohttp\|Semaphore\|_CATEGORY_INDEX\|resolve" api/routes_diagnostic.py` returns NO match).
  </acceptance_criteria>
  <done>
    The thin synchronous diagnostic route exists with brand-validation 404 guard, delegating to the service, with no background/job/WebSocket machinery and no business logic.
  </done>
</task>

<task type="auto">
  <name>Task 2: Register diagnostic_router on api_router and un-skip the endpoint test</name>
  <files>api/__init__.py, tests/test_category_diagnostic.py</files>
  <read_first>
    - api/__init__.py (the router-aggregation block L12-31: how sub-routers are imported and added; `api_router = APIRouter(dependencies=[Depends(verify_api_key)])` at L23 — the diagnostic router inherits X-API-Key auth automatically when included here)
    - tests/test_category_diagnostic.py (the `TestEndpoint` placeholder marked `@pytest.mark.skip` in 29-01 — replace the skip with a real offline test)
    - .planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md ("Router registration" section)
  </read_first>
  <action>
    In `api/__init__.py`: add `from api.routes_diagnostic import router as diagnostic_router` alongside the other route imports (L12-19), and add `api_router.include_router(diagnostic_router)` to the protected-router block (after L31, before the public_router section). This grants inherited `Depends(verify_api_key)` auth (V2) — do NOT add a separate auth decorator. Do NOT add the diagnostic router to `public_router`.

    In `tests/test_category_diagnostic.py`: replace the `@pytest.mark.skip` `TestEndpoint` placeholder with a real OFFLINE test using FastAPI `TestClient`. Build the app/router, monkeypatch `api.routes_diagnostic.run_brand_diagnostic` (and `run_all_brands_diagnostic`) to return a canned `BrandDiagnosticResult` / `DiagnosticReportResponse` so NO network occurs, and send the X-API-Key header expected by `verify_api_key` (read the expected key from `config.settings` / the existing test convention). Assert: `GET /diagnostic/brands/{known}` → 200 with a `categories` list; `GET /diagnostic/brands/{unknown}` → 404 (monkeypatch `brand_service.get_brand` to return None for the unknown key); `GET /diagnostic/all` → 200 with a `brands` list. If wiring a TestClient against the real auth dependency is impractical offline, instead unit-test the route functions directly (call `diagnose_brand`/`diagnose_all` as coroutines with `asyncio.run`, monkeypatching the service + `brand_service.get_brand`) — either approach must stay offline.
  </action>
  <verify>
    <automated>python -m pytest tests/test_category_diagnostic.py -q && python -c "from api import api_router; print(any('/diagnostic/all'==getattr(rt,'path','') for rt in api_router.routes))"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from api import api_router; assert any(getattr(rt,'path','')=='/diagnostic/all' for rt in api_router.routes)"` exits 0 (router registered on the auth-protected aggregate).
    - Source assertion: `grep -n "diagnostic_router" api/__init__.py` matches in BOTH the import block and an `api_router.include_router(...)` line; `grep -n "public_router.include_router(diagnostic_router)" api/__init__.py` returns NO match (diagnostic is NOT public).
    - `python -m pytest tests/test_category_diagnostic.py -q` exits 0 with ZERO skips — the `TestEndpoint` test now runs offline and passes (200 for known brand, 404 for unknown, 200 for /all).
    - The endpoint test performs no real network I/O (services monkeypatched).
  </acceptance_criteria>
  <done>
    The diagnostic router is registered on the auth-protected api_router (not public), the previously-skipped endpoint test now runs offline and is GREEN, and the full diagnostic test file passes with no skips.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /diagnostic/* | Untrusted HTTP request crosses here; `brand_key` is a user-supplied path param |
| api_router auth | X-API-Key (`verify_api_key`) gates every diagnostic request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Tampering | brand_key path param → probe URL | mitigate | Validate `brand_key` against `brand_service.get_brand()` (404 if unknown) before any service call; the user-supplied key never reaches a URL — the probe URL is built from the internal `brand_data.domain`, not the raw param |
| T-29-06 | Spoofing | unauthenticated access to /diagnostic/* | mitigate | Router included on `api_router` which carries `Depends(verify_api_key)`; explicitly NOT added to `public_router` |
| T-29-07 | Denial of service | /diagnostic/all triggering many probes | mitigate | Concurrency/timeout bounds enforced in the service layer (29-02 T-29-03); route adds no unbounded fan-out |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `python -m pytest tests/test_category_diagnostic.py -q` GREEN with no skips.
- `python -m pytest tests/ -q` does not regress.
- `from api import api_router` succeeds and exposes `/diagnostic/brands/{brand_key}` and `/diagnostic/all` under the auth-protected aggregate; diagnostic endpoints absent from `public_router`.
</verification>

<success_criteria>
- DIAG-01 endpoint surface complete: synchronous GET per-brand + all, thin route, auth-inherited, 404 on unknown brand.
- Endpoint test runs offline and is GREEN; no BackgroundTasks/job/WebSocket; no new packages.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created symbols):
- Module `api/routes_diagnostic.py` with `router`, `diagnose_brand`, `diagnose_all`
- Route paths: `GET /diagnostic/brands/{brand_key}`, `GET /diagnostic/all`
- Registration symbol in api/__init__.py: `diagnostic_router`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-03-SUMMARY.md` when done
</output>
