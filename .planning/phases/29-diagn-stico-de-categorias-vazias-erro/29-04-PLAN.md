---
phase: 29-diagn-stico-de-categorias-vazias-erro
plan: 04
type: execute
wave: 4
depends_on: ["29-03"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/pages/DiagnosticPage.tsx
  - frontend/src/App.tsx
autonomous: false
requirements: [DIAG-02]
user_setup: []
must_haves:
  truths:
    - "A new 'Saúde de Categorias' (Diagnóstico) tab is reachable from the sidebar and renders via the App renderTab switch"
    - "The panel groups results by brand; each category shows an ok/vazia/erro/sem-probe status chip"
    - "Each category row is expandable, revealing http_status, error_detail and the probed URL"
    - "There is a per-brand 'Diagnosticar' button and a 'Diagnosticar todas' button at the top, each with a loading state"
    - "Inactive brands are visually distinct (opacity 0.55) and not confused with 'erro'"
  artifacts:
    - path: "frontend/src/pages/DiagnosticPage.tsx"
      provides: "Brand-grouped health panel with status chips and expandable rows"
      contains: "DiagnosticPage"
      min_lines: 80
    - path: "frontend/src/api/client.ts"
      provides: "ApiClient.getDiagnostic(brandKey?) GET method"
      contains: "getDiagnostic"
  key_links:
    - from: "frontend/src/pages/DiagnosticPage.tsx"
      to: "ApiClient.getDiagnostic"
      via: "fetch on button click"
      pattern: "ApiClient.getDiagnostic"
    - from: "frontend/src/App.tsx"
      to: "DiagnosticPage"
      via: "renderTab case 'diagnostic'"
      pattern: "case 'diagnostic'"
---

<objective>
Deliver DIAG-02: a new operator-facing panel that visualizes category health by brand/engine, consuming the synchronous diagnostic endpoint from 29-03. The operator can spot broken (erro) vs empty (possibly seasonal/vazia) categories at a glance without running a full scan.

Purpose: Observability UI — a dedicated tab (D-13) that does not pollute existing screens, following the project's list/card page conventions and the existing inactive-brand visual treatment (D-03). The trigger is on-demand at two granularities — a per-brand "Diagnosticar" button and a top-level "Diagnosticar todas" (D-04), each producing a fresh result every time.
Output: `ApiClient.getDiagnostic`; new `frontend/src/pages/DiagnosticPage.tsx`; `App.tsx` wired with a new `renderTab` case, sidebar entry, and header title; a human-verify checkpoint of the rendered panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-CONTEXT.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-RESEARCH.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ApiClient.getDiagnostic and the DiagnosticPage panel component</name>
  <files>frontend/src/api/client.ts, frontend/src/pages/DiagnosticPage.tsx</files>
  <read_first>
    - frontend/src/api/client.ts (the static-method + `request<T>(endpoint, options?, signal?)` core ~L12-37 which already attaches X-API-Key and parses errors; the GET-without-body methods `getHistoryList()`/`getHistoryDetail()` ~L90-95 — mirror their shape; no business logic belongs in the client)
    - frontend/src/App.tsx (READ these exact regions: `MonitoredCategoriesPage` ~L1754 for the `useState`+`useEffect`+fetch+loading list-of-cards pattern; `SettingsPage` brand list ~L1699-1710 for the inactive treatment `style={b.is_active === false ? { opacity: 0.55 } : undefined}` at L1705 and the "Inativa" badge; `CrossMarketplacePage` for the `sonner` `toast.error(...)` error pattern)
    - .planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md (the "frontend/src/pages/DiagnosticPage.tsx" and "frontend/src/api/client.ts" sections: per-brand loadingMap, STATUS_CHIP/STATUS_LABEL maps, exact inactive replication)
  </read_first>
  <action>
    In `frontend/src/api/client.ts`: add a static method `getDiagnostic(brandKey?: string)` that returns `this.request<any>(endpoint)` where `endpoint = brandKey ? \`/diagnostic/brands/${encodeURIComponent(brandKey)}\` : '/diagnostic/all'` — mirroring `getHistoryList()`. No new logic in `request<T>` (it already handles auth/errors).

    Create `frontend/src/pages/DiagnosticPage.tsx` exporting a `DiagnosticPage` component accepting `{ brands }` (same prop shape other pages get from App). State: `results` (the brand→categories report; key the brand results so per-brand re-runs merge into the combined view), `loadingMap: Record<string, boolean>` for per-brand spinners (D-16), and a separate `loadingAll` flag. Handlers: `runBrand(brandKey)` sets `loadingMap[brandKey]=true`, awaits `ApiClient.getDiagnostic(brandKey)`, merges that brand's result into `results`, `toast.error` on failure, clears the flag in `finally`; `runAll()` sets `loadingAll=true`, awaits `ApiClient.getDiagnostic()`, replaces `results`, clears in `finally`.

    Render: a top bar with a "Diagnosticar todas" button (disabled + spinner while `loadingAll`). Then one card/block per brand. Per brand: brand name + an "Inativa" badge and `style={brand.is_active === false ? { opacity: 0.55 } : undefined}` on the brand-info wrapper (D-03 — replicate SettingsPage L1705 exactly); a per-brand "Diagnosticar" button (disabled + spinner while `loadingMap[brand.brand_key]`). Inside each brand: a list of category rows. Each row shows the category label + a status chip using `STATUS_CHIP`/`STATUS_LABEL` maps — chip classes `ok→bg-green-500`, `empty→bg-yellow-500`, `error→bg-red-500`, `no_probe→bg-gray-400` (all `text-white`, `px-2 py-0.5 rounded text-xs font-medium`); labels in PT-BR `{ ok:'OK', empty:'Vazia', error:'Erro', no_probe:'Sem probe' }` (D-14 — PT-BR labels only at the display layer; the API stays English). Each row is expandable (local `expanded` state per row, e.g. a Set of row keys) revealing `http_status`, `error_detail` and `probed_url` (D-15). Use `sonner` `toast.error` for failures. Import status chip styling via Tailwind classes only (no new CSS file).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "DiagnosticPage|client.ts" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/api/client.ts` contains a static `getDiagnostic` that builds `/diagnostic/brands/{key}` or `/diagnostic/all` and calls `this.request`.
    - `frontend/src/pages/DiagnosticPage.tsx` exists, exports `DiagnosticPage`, and `cd frontend && npx tsc --noEmit` reports NO errors in `DiagnosticPage.tsx` or `client.ts`.
    - Source assertion: `grep -n "ApiClient.getDiagnostic" frontend/src/pages/DiagnosticPage.tsx` matches; `grep -n "opacity: 0.55" frontend/src/pages/DiagnosticPage.tsx` matches (D-03); `grep -n "probed_url\|error_detail\|http_status" frontend/src/pages/DiagnosticPage.tsx` matches (D-15 expandable detail).
    - Source assertion: `grep -n "no_probe" frontend/src/pages/DiagnosticPage.tsx` matches (D-02 marker rendered distinctly); status chips cover ok/empty/error/no_probe.
    - Per-brand loading state exists (`grep -n "loadingMap\|Record<string, boolean>" frontend/src/pages/DiagnosticPage.tsx` matches — D-16) plus a "Diagnosticar todas" trigger.
  </acceptance_criteria>
  <done>
    ApiClient.getDiagnostic exists; DiagnosticPage renders brand-grouped cards with ok/vazia/erro/sem-probe chips, expandable detail rows (http_status/error_detail/probed_url), per-brand + "todas" loading triggers, and the inactive opacity treatment — typechecks clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the Diagnóstico tab into App.tsx (renderTab + sidebar + header)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx (EXACT regions verified: `renderTab` switch L2107-2117 — add a case before `default`; sidebar `<nav>` L2135-2173 — add a `SidebarItem` before the `<div className="sidebar-spacer" />` at L2166, in the observability group; header `<h1>` ternary L2182-2190 — add a `'diagnostic'` arm; the Lucide icons already imported at the top of the file — pick an existing import like `Activity`/`HeartPulse` or add one to the existing lucide-react import line)
    - frontend/src/pages/DiagnosticPage.tsx (the component created in Task 1 — confirm its prop signature `{ brands }`)
    - .planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md ("Pattern 7: Frontend — nova aba" with the exact L-numbers)
  </read_first>
  <action>
    In `frontend/src/App.tsx`: import `DiagnosticPage` from `./pages/DiagnosticPage`. Add `case 'diagnostic': return <DiagnosticPage brands={brands} />;` to the `renderTab` switch (L2107-2117) before `default`. Add a `SidebarItem` in the `<nav>` (between "Categorias" and the `sidebar-spacer`, L2160-2166) with `label="Saúde de Categorias"`, `active={activeTab === 'diagnostic'}`, `onClick={() => navigateTab('diagnostic')}`, and an icon from `lucide-react` (e.g. `Activity` or `HeartPulse` — add to the existing lucide-react import if not already present). Add the header arm `activeTab === 'diagnostic' ? 'Saúde de Categorias' :` into the `<h1>` ternary chain (L2182-2190). Do not alter the activeTab type machinery beyond adding the new string literal where the tab union/state is declared (search for where `activeTab` literals like `'monitored_categories'` are typed and add `'diagnostic'`).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "App.tsx" | head -20; cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -n "case 'diagnostic'" frontend/src/App.tsx` matches and `grep -n "DiagnosticPage" frontend/src/App.tsx` matches the import + usage.
    - Source assertion: `grep -n "Saúde de Categorias" frontend/src/App.tsx` matches in BOTH the sidebar `SidebarItem` label and the header `<h1>` ternary.
    - Source assertion: `grep -n "navigateTab('diagnostic')" frontend/src/App.tsx` matches (sidebar nav wired).
    - `cd frontend && npx tsc --noEmit` reports NO new errors in App.tsx, and `cd frontend && npm run build` succeeds (exit 0).
  </acceptance_criteria>
  <done>
    The Diagnóstico tab is reachable from the sidebar, renders DiagnosticPage via renderTab, shows the correct header title, and the frontend builds clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the category-health panel (DIAG-02)</name>
  <what-built>
    A new "Saúde de Categorias" tab in the sidebar rendering a brand-grouped diagnostic panel: per-brand "Diagnosticar" buttons + a "Diagnosticar todas" button (each with a loading state), ok/vazia/erro/sem-probe status chips per category, expandable rows revealing http_status/error_detail/probed_url, and the inactive-brand opacity treatment. Backend probe + endpoint from 29-02/29-03 are live and synchronous.
  </what-built>
  <how-to-verify>
    1. Start backend and frontend (the project's usual dev commands), open the app.
    2. Click the new "Saúde de Categorias" item in the sidebar — confirm the tab opens and the header reads "Saúde de Categorias".
    3. Click "Diagnosticar" on one of the 5 VTEX brands (Levi's, Calvin Klein, Zapalla, Austral, Track & Field). Confirm a loading state shows on that brand's button, then the categories appear with status chips. Confirm at least one category shows OK (green) with a product count.
    4. Expand a category row — confirm it reveals http_status, error_detail (if any), and the probed URL.
    5. Click "Diagnosticar todas" — confirm the top button shows loading, then all brands populate. Confirm a non-VTEX / unmapped brand (or marketplaces) shows the "Sem probe" gray marker and is NOT probed.
    6. Confirm an inactive brand (if any) renders dimmed (opacity) with an "Inativa" badge — visually distinct from an "Erro" chip.
    7. Confirm the three states are visually distinguishable: a category with products = OK (green), a category returning 200 + 0 products = Vazia (yellow), and a category that 404/500/403s = Erro (red).
  </how-to-verify>
  <resume-signal>Type "approved" if the panel renders the three states correctly with expandable detail and inactive/no-probe distinctions, or describe the issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend /diagnostic/* | The panel calls the auth-protected endpoint via ApiClient (X-API-Key attached in `request<T>`) |
| backend report → DOM | `error_detail` / `probed_url` strings rendered into the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08 | Information disclosure | error_detail/probed_url rendered in UI | accept | Internal ops tool (ASVS L1); exposing the probed URL is the intended mechanism for the operator to spot a wrong de/para (D-12/D-15) |
| T-29-09 | XSS | rendering backend strings as React text | mitigate | Render via JSX text/`{value}` (React auto-escapes); do NOT use `dangerouslySetInnerHTML` for error_detail/probed_url |
| T-29-10 | Spoofing | unauthenticated API call | mitigate | `ApiClient.request<T>` attaches X-API-Key; backend endpoint inherits `verify_api_key` (29-03 T-29-06) |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit); zustand/sonner/lucide-react already present |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` clean for client.ts, DiagnosticPage.tsx, App.tsx.
- `cd frontend && npm run build` exits 0.
- Human verification (Task 3) confirms the three states, expandable detail, no_probe marker, and inactive distinction render correctly against the live backend.
</verification>

<success_criteria>
- DIAG-02 delivered: brand-grouped health panel reachable from a new tab, with ok/vazia/erro/sem-probe chips, expandable http_status/error_detail/probed_url, per-brand + "todas" loading triggers, and inactive distinction.
- Frontend typechecks and builds; no new packages.
- Operator-confirmed three-state distinction via human-verify checkpoint.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created symbols):
- ApiClient method: `getDiagnostic(brandKey?)` in frontend/src/api/client.ts
- Component: `frontend/src/pages/DiagnosticPage.tsx` (`DiagnosticPage`), with local `STATUS_CHIP`/`STATUS_LABEL` maps, `loadingMap`, `runBrand`, `runAll`, `runAll` triggers
- App.tsx additions: new `renderTab` case `'diagnostic'`, new `SidebarItem` ("Saúde de Categorias"), new header `<h1>` arm, new `activeTab` literal `'diagnostic'`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-04-SUMMARY.md` when done
</output>
