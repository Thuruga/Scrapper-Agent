---
phase: 29-diagn-stico-de-categorias-vazias-erro
plan: 02
type: execute
wave: 2
depends_on: ["29-01"]
files_modified:
  - core/models.py
  - services/category_diagnostic_service.py
autonomous: true
requirements: [DIAG-01]
user_setup: []
must_haves:
  truths:
    - "For each VTEX brand, every mapped category is probed with one raw aiohttp GET that reports the TRUE http_status (no retry, no per-category stable-domain fallback, no Playwright)"
    - "200 + non-empty list → ok; 200 + empty list → empty; anything else (non-200, HTML body, JSON parse error, timeout, network error) → error with http_status + error_detail"
    - "The base URL is resolved ONCE per brand (public vs stable *.vtexcommercestable.com.br) before the per-category probes"
    - "Non-VTEX or unmapped brands appear with a no_probe marker and are never probed"
    - "Inactive brands are included (active_only=False) and carry is_active in the result"
  artifacts:
    - path: "services/category_diagnostic_service.py"
      provides: "probe_category, classify helper, base-url resolver, brand-category enumeration, run_brand_diagnostic, run_all_brands_diagnostic"
      contains: "async def probe_category"
      min_lines: 120
    - path: "core/models.py"
      provides: "CategoryDiagnosticResult, BrandDiagnosticResult, DiagnosticReportResponse with Literal status enum"
      contains: "class CategoryDiagnosticResult"
  key_links:
    - from: "services/category_diagnostic_service.py"
      to: "brand_service.list_brands(active_only=False)"
      via: "enumerate all brands incl. inactive (D-01)"
      pattern: "list_brands\\("
    - from: "services/category_diagnostic_service.py"
      to: "aiohttp.ClientSession.get"
      via: "raw probe, NOT _request_json (D-08)"
      pattern: "session\\.get\\("
---

<objective>
Implement the diagnostic service and its Pydantic response models so the Wave 0 contract test (29-01) turns GREEN. This is the core of DIAG-01: a dedicated, non-resilient probe that reports the true health of each mapped category in three inviolable states.

Purpose: Deliver observability — the deliberate negation of the scraper's resilience (D-07/D-08), so empty/error categories are reported truthfully instead of masked by retries, stable-domain auto-heal, Playwright, or the full-text search fallback.
Output: `core/models.py` extended with the three diagnostic models; new `services/category_diagnostic_service.py` with probe + classify + per-brand base-url resolution + enumeration + `run_brand_diagnostic` / `run_all_brands_diagnostic`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-CONTEXT.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-RESEARCH.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md
@tests/test_category_diagnostic.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add diagnostic Pydantic response models to core/models.py</name>
  <files>core/models.py</files>
  <read_first>
    - core/models.py (the `BrandSearchResult` block ~L136-143 and the `DynamicBrand`/`CategoryMapping` blocks ~L199-233 — copy the BaseModel + Optional-defaults-None style; place new models after the DynamicBrand section to keep domain grouping; `Literal` is already the project's enum idiom in Pydantic v2)
    - .planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md (the "core/models.py — New Pydantic response models" section: exact field shapes)
  </read_first>
  <action>
    Add three Pydantic v2 BaseModel classes to core/models.py (import `Literal` from typing if not already imported):
    - `CategoryDiagnosticResult`: fields `slug: str`, `label: str`, `status: Literal["ok", "empty", "error", "no_probe"]`, `http_status: Optional[int] = None`, `error_detail: Optional[str] = None`, `total_count: Optional[int] = None`, `probed_url: Optional[str] = None`.
    - `BrandDiagnosticResult`: fields `brand_key: str`, `brand_name: str`, `domain: str`, `is_active: bool`, `engine: str`, `categories: List[CategoryDiagnosticResult]`.
    - `DiagnosticReportResponse`: field `brands: List[BrandDiagnosticResult]`.
    Use the ENGLISH status enum strings only (RESEARCH finding #5). `no_probe` is a coverage marker, NOT a 4th health state (D-11) — it is valid only on categories belonging to non-VTEX/unmapped brands (D-02).
  </action>
  <verify>
    <automated>python -c "from core.models import CategoryDiagnosticResult, BrandDiagnosticResult, DiagnosticReportResponse; m=CategoryDiagnosticResult(slug='polos', label='Polos', status='ok'); print(m.status, DiagnosticReportResponse(brands=[]).model_dump())"</automated>
  </verify>
  <acceptance_criteria>
    - `from core.models import CategoryDiagnosticResult, BrandDiagnosticResult, DiagnosticReportResponse` succeeds.
    - `CategoryDiagnosticResult(slug='x', label='y', status='ok')` validates; `status='vazia'` raises a Pydantic ValidationError (proves the Literal enum is ENGLISH-only).
    - `DiagnosticReportResponse(brands=[]).model_dump()` returns `{"brands": []}`.
    - Optional fields (`http_status`, `error_detail`, `total_count`, `probed_url`) default to None.
  </acceptance_criteria>
  <done>
    The three diagnostic models exist in core/models.py with the ENGLISH Literal status enum and Optional-None defaults, importable and constructible.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create services/category_diagnostic_service.py (probe + classify + resolve + enumerate + orchestration)</name>
  <files>services/category_diagnostic_service.py</files>
  <read_first>
    - services/vtex_api_scraper.py (CRITICAL — read these exact ranges to know what to AVOID and what to reuse: `fetch_categories` + public↔stable auto-discovery L46-130 for the base-url resolver D-10; `_request_json` L228-294 which is the resilient path the probe MUST NOT use D-08; `scrape_category_paged` L472-675 — especially the `resources` header read L546-559 and the search-by-path URL shape `/api/catalog_system/pub/products/search/{path}?_from=0&_to=9`; `search` L677-852 — the full-text fallback at L833 the probe MUST NOT use D-07)
    - services/category_mapping.py (`_CATEGORY_INDEX` build and `resolve_category_for_brands` L191-249 — note the `vtex_fq_path` caveat at L227-233 where fq paths starting with `C:/`/`B:` cannot become a friendly URL; `get_canonical_categories` L143)
    - services/brand_service.py (`list_brands(active_only=False)` ~L207 default returns all brands incl. inactive D-01; `get_brand`)
    - services/engines/factory.py (`engine_factory.get_engine` falls through to VTEXEngine for engine="unknown" — DO NOT use the factory to decide probe eligibility; check `brand_data.engine == "vtex"` directly D-02)
    - core/models.py (the models added in Task 1)
    - tests/test_category_diagnostic.py (the contract this module must satisfy)
    - .planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md (Patterns 1-4 give the verified probe/resolver/enumeration/concurrency shapes)
  </read_first>
  <action>
    Create `services/category_diagnostic_service.py`. Module-level imports: `asyncio`, `logging`, `aiohttp`, `from curl_cffi.requests import AsyncSession`, `from typing import Optional, List, Dict`, `from services.brand_service import brand_service`, `from services.category_mapping import _CATEGORY_INDEX`, `from services.vtex_api_scraper import VtexApiClient`, and the three models from core.models. Define `_PROBE_HEADERS = {"Accept": "application/json", "User-Agent": "Mozilla/5.0 (compatible; DiagnosticProbe/1.0)"}`.

    `async def probe_category(session, base_url, path, semaphore) -> dict`: build the URL — if `path` starts with `C:/` or `B:` use the fq-filter form `{base_url}/api/catalog_system/pub/products/search?fq={path}&_from=0&_to=9`, otherwise the path form `{base_url}/api/catalog_system/pub/products/search/{path.lstrip('/')}?_from=0&_to=9`. Under `async with semaphore:` do a single `async with session.get(url, timeout=aiohttp.ClientTimeout(total=10))`. Classification (D-09/D-11/D-12): http_status != 200 → error (`error_detail=f"HTTP {http_status}"`); Content-Type contains "text/html" → error (`error_detail="HTML instead of JSON (possible headless/WAF block)"`); `resp.json(content_type=None)` raising → error (`error_detail=f"JSON parse error: {e}"`); parse the `resources` header (`x-y/total`) into `total_count = int(header.split('/')[-1])` inside try/except (absent/malformed → total_count None, NOT an error — D-12/Pitfall 6); body is a non-empty list → status "ok"; else → status "empty" (total_count or 0). Wrap in `except asyncio.TimeoutError:` → error `error_detail="Timeout"`, http_status None; `except Exception as exc:` → error `error_detail=f"Network error: {exc}"`, http_status None. Always return dict with keys `status, http_status, error_detail, total_count, probed_url` (probed_url = the exact URL built — D-12/D-15 expose it). NEVER call `_request_json`, `VtexApiClient._request_json`, or `VTEXEngine.search` (D-07/D-08).

    `async def _resolve_base_url(domain) -> str` (D-10, once per brand): strip scheme/trailing slash; try `https://{domain}/api/catalog_system/pub/category/tree/1` with `AsyncSession(impersonate="chrome", timeout=15)` and `_PROBE_HEADERS`; if status 200 and body parses to a non-empty list → return `https://{domain}` (public works); otherwise extract account via `VtexApiClient._discover_account_from_html(domain, response.text)` and return `https://{account}.vtexcommercestable.com.br`. This resolution is per-brand only, NOT per-category auto-heal.

    `def _get_brand_category_paths(brand_key) -> List[dict]`: combine hardcoded `_CATEGORY_INDEX` entries where `brand_key.lower() in cat.brands` (use that brand's `.path`) with the brand's `DynamicBrand.mappings` (`canonical_slug`, `vtex_fq_path`, `label`), de-duplicating by slug. Return dicts `{slug, label, path}` where `path` is the friendly path when available else the raw `vtex_fq_path` (probe handles the `C:/`/`B:` fq form).

    `async def run_brand_diagnostic(brand_key) -> Optional[BrandDiagnosticResult]`: lower-case the key; `brand_data = brand_service.get_brand(brand_key)`; return None if not found (route maps to 404). Read `engine = getattr(brand_data, "engine", "vtex")`. If `engine != "vtex"` OR the brand has zero mapped categories → return a BrandDiagnosticResult whose `categories` is one `CategoryDiagnosticResult(status="no_probe")` per known category (or a single synthetic no_probe entry when there are none) and DO NOT probe (D-02). For VTEX brands with mappings: resolve base URL once via `_resolve_base_url(brand_data.domain)`; create one `aiohttp.ClientSession(headers=_PROBE_HEADERS)`; `asyncio.Semaphore(5)`; `asyncio.gather` the per-category probes; map each probe dict + slug/label into `CategoryDiagnosticResult`. Populate `is_active=getattr(brand_data,"is_active",True)` (D-03 flag is carried to the UI) and `engine`.

    `async def run_all_brands_diagnostic() -> DiagnosticReportResponse`: iterate `brand_service.list_brands(active_only=False)` (D-01 — include inactive), run each brand's diagnostic (reuse `run_brand_diagnostic` per brand; bound overall concurrency with a global `asyncio.Semaphore(10)` so "Diagnosticar todas" does not flood VTEX — Pitfall 4), and return `DiagnosticReportResponse(brands=[...])`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_category_diagnostic.py::TestClassifier tests/test_category_diagnostic.py::TestBrandFilter -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_category_diagnostic.py::TestClassifier -q` exits 0 — all 11+ classifier rows GREEN (200+items→ok, 200+empty→empty, 200+empty-no-header→empty, 404/500/403/429→error, html→error, timeout→error "Timeout", network→error "Network...", malformed-resources→total_count None).
    - `python -m pytest tests/test_category_diagnostic.py::TestBrandFilter -q` exits 0 — non-VTEX brand yields only `no_probe` categories and `probe_category` is never invoked for it (D-02).
    - Source assertion: `grep -n "_request_json\|\.search(" services/category_diagnostic_service.py` returns NO match (probe does not route through the resilient path or the full-text-fallback search — D-07/D-08).
    - Source assertion: `grep -cn "list_brands(active_only=False)\|list_brands()" services/category_diagnostic_service.py` ≥ 1 (D-01); and `grep -n "getattr(brand_data, \"engine\"" services/category_diagnostic_service.py` matches (engine checked directly, not via factory — D-02).
    - `python -c "import services.category_diagnostic_service as s; print(hasattr(s,'probe_category') and hasattr(s,'run_brand_diagnostic') and hasattr(s,'run_all_brands_diagnostic'))"` prints True.
  </acceptance_criteria>
  <done>
    The diagnostic service exists and the Wave 0 classifier + brand-filter tests are GREEN; the probe is a raw aiohttp GET that bypasses `_request_json`/`search()`, resolves base URL once per brand, includes inactive brands, and marks non-VTEX brands no_probe.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostic service → VTEX brand sites | Server-side outbound HTTP to brand-controlled VTEX URLs (the probe). The `domain` originates from internal `brand_service` records, not end-user input. |
| brand-mapping data → probe URL | `path`/`vtex_fq_path` come from internal brand mappings; interpolated into the probe URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02 | Information disclosure | probe → arbitrary internal host (SSRF) | mitigate | Probe target host derives only from internal `brand_data.domain` / resolved stable domain — never from request body; `aiohttp` default does not follow cross-host redirects to internal hosts here because no user-supplied raw URL is accepted |
| T-29-03 | Denial of service | run_all_brands_diagnostic | mitigate | Per-brand `Semaphore(5)` + global `Semaphore(10)` + per-probe `ClientTimeout(total=10)` bound concurrency and duration (Pitfall 4) |
| T-29-04 | Information disclosure | error_detail strings | accept | `error_detail` exposes internal exception text — acceptable for an internal ASVS-L1 ops tool not exposed externally |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit); aiohttp/curl_cffi already established dependencies |
</threat_model>

<verification>
- `python -m pytest tests/test_category_diagnostic.py -q` is GREEN except the explicitly-skipped `TestEndpoint` placeholder (endpoint lands in 29-03).
- `python -m pytest tests/ -q` does not regress existing tests.
- Manual source review confirms no `_request_json`, no `VTEXEngine.search`, no `BackgroundTasks`, and base-url resolution is once per brand.
</verification>

<success_criteria>
- DIAG-01 backend delivered: three-state classifier + raw probe + per-brand base-url resolution + no_probe marker + inactive inclusion.
- Wave 0 classifier and brand-filter tests GREEN; ENGLISH status enum in the contract.
- No resilient-path leakage (no _request_json / search fallback); no new packages.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created symbols):
- Models in core/models.py: `CategoryDiagnosticResult`, `BrandDiagnosticResult`, `DiagnosticReportResponse` (status enum values: `ok`, `empty`, `error`, `no_probe`)
- Module `services/category_diagnostic_service.py` with functions: `probe_category`, `_resolve_base_url`, `_get_brand_category_paths`, `run_brand_diagnostic`, `run_all_brands_diagnostic`; constant `_PROBE_HEADERS`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-02-SUMMARY.md` when done
</output>
