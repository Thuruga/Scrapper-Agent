---
phase: 29-diagn-stico-de-categorias-vazias-erro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_category_diagnostic.py
autonomous: true
requirements: [DIAG-01]
must_haves:
  truths:
    - "A failing (RED) offline contract test exists for the three-state classifier covering ok/empty/error and the no_probe marker"
    - "The test matrix mocks the raw HTTP layer with _FakeResp/_FakeSession — no network, no WAF"
  artifacts:
    - path: "tests/test_category_diagnostic.py"
      provides: "Classifier + probe contract test matrix (DIAG-01) and brand-filter no_probe test (D-02)"
      contains: "class TestClassifier"
      min_lines: 90
  key_links:
    - from: "tests/test_category_diagnostic.py"
      to: "services.category_diagnostic_service"
      via: "import probe_category / classify / run_brand_diagnostic"
      pattern: "from services.category_diagnostic_service import"
---

<objective>
Wave 0 (RED): create the offline/deterministic contract test for the category-health classifier and raw probe. The test encodes the full three-state classification matrix (DIAG-01) BEFORE the implementation exists, so it MUST fail at this wave (import error / NameError is an acceptable RED). The implementing plan (29-02) makes it GREEN.

Purpose: Lock the inviolable three-state contract (D-11) as executable truth so no later executor can silently collapse `empty` into `error` or invent a 4th health state. Establishes the sampling point for Nyquist.
Output: `tests/test_category_diagnostic.py` with `TestClassifier`, `TestBrandFilter` (no_probe), and an endpoint placeholder marked skip until 29-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-CONTEXT.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-RESEARCH.md
@.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-PATTERNS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RED contract test for the three-state classifier and raw probe</name>
  <files>tests/test_category_diagnostic.py</files>
  <read_first>
    - tests/test_vtex_api_client.py (the `_FakeResp` / `_FakeSession` pattern at L128-154, and the `asyncio.run()` driving style — the project has NO pytest-asyncio configured by convention; drive coroutines with `asyncio.run`)
    - tests/test_brand_active.py (the `BrandManagerService.__new__` + `.brands = {}` isolation pattern for brand-service-dependent tests, used for the no_probe brand-filter test)
    - .planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-RESEARCH.md (the "Validation Architecture" section: Phase Requirements → Test Map, the mock strategy snippets, and the "Edge Case Classification Matrix")
    - .planning/codebase/TESTING.md (offline/deterministic philosophy — no network/WAF)
  </read_first>
  <behavior>
    Classifier/probe matrix (target symbol: `probe_category(session, base_url, path, semaphore)` in `services.category_diagnostic_service`, returning a dict with keys `status`, `http_status`, `error_detail`, `total_count`, `probed_url`):
    - 200 + non-empty JSON list + header `resources: "0-9/47"` → status `"ok"`, http_status 200, total_count 47, error_detail None
    - 200 + empty JSON list `[]` + header `resources: "0-0/0"` → status `"empty"`, http_status 200, total_count 0, error_detail None
    - 200 + empty JSON list `[]` WITHOUT a `resources` header → status `"empty"`, total_count 0 (NOT an error — D-12 stale mapping path)
    - 404 → status `"error"`, http_status 404, error_detail contains "404"
    - 500 → status `"error"`, http_status 500
    - 403 → status `"error"`, http_status 403
    - 429 → status `"error"`, http_status 429
    - 200 + Content-Type `text/html; charset=utf-8` → status `"error"`, error_detail contains "HTML"
    - timeout (session.get raises `asyncio.TimeoutError`) → status `"error"`, http_status None, error_detail == "Timeout"
    - network error (session.get raises `aiohttp.ClientError`/`Exception`) → status `"error"`, http_status None, error_detail contains "Network"
    - resources header present but malformed (e.g. "abc") → total_count None, NOT an error
    Brand filter (target symbol: `run_brand_diagnostic(brand_key)` in the same module):
    - A brand whose `DynamicBrand.engine` is NOT "vtex" (e.g. "shopify" or "unknown") yields a BrandDiagnosticResult whose categories carry the `"no_probe"` status marker and NO real probe is issued (D-02).
  </behavior>
  <action>
    Create `tests/test_category_diagnostic.py`. Define a local extended `_FakeResp` mirroring tests/test_vtex_api_client.py but adding a `headers` dict (init signature `(status, json_data=None, content_type="application/json", headers=None)`; `self.headers = {"Content-Type": content_type, **(headers or {})}`) and an `async json(self, content_type=None)`. Define `_FakeSession` with `def get(self, url, timeout=None): return self._resp` (sync method returning the async-context-manager `_FakeResp`, matching how the probe calls `async with session.get(...)`).

    Drive every coroutine with `asyncio.run(...)`. Import the not-yet-existing symbols at top: `from services.category_diagnostic_service import probe_category, run_brand_diagnostic` (this import failing is the intended RED — do NOT add a try/except guard around it).

    Class `TestClassifier`: one test method per row of the behavior matrix above, asserting on the returned dict keys. For the timeout/network rows, give `_FakeSession.get` a variant that raises (parametrize or define `_RaisingSession(exc)`).

    Class `TestBrandFilter`: build an isolated `BrandManagerService` instance per the test_brand_active.py pattern, register one brand with `engine="shopify"` (or "unknown"), monkeypatch `category_diagnostic_service.brand_service` (or the module-level brand_service it imports) to the isolated instance, call `run_brand_diagnostic` and assert every returned category has `status == "no_probe"` and that the probe network layer was never invoked (e.g. monkeypatch `probe_category` to raise if called).

    Class `TestEndpoint`: add a single placeholder method decorated `@pytest.mark.skip(reason="GET /diagnostic endpoint lands in 29-03")` so the endpoint sampling point is reserved in the file. Do NOT exercise FastAPI here.

    No fenced implementation code beyond the fake helpers; assertions express the contract. Use ENGLISH status strings only (`ok`/`empty`/`error`/`no_probe`) per RESEARCH finding #5.
  </action>
  <verify>
    <automated>python -m pytest tests/test_category_diagnostic.py -q 2>&1 | grep -E "error|ModuleNotFoundError|ImportError|collected" </automated>
  </verify>
  <acceptance_criteria>
    - File `tests/test_category_diagnostic.py` exists and is collected by pytest (collection does not silently produce 0 tests).
    - `python -m pytest tests/test_category_diagnostic.py -q` FAILS at this wave (RED) because `services.category_diagnostic_service` does not yet exist — the failure is an ImportError/ModuleNotFoundError, NOT a syntax error in the test file.
    - `python -c "import ast; ast.parse(open('tests/test_category_diagnostic.py').read())"` exits 0 (test file is syntactically valid Python).
    - The file contains classes `TestClassifier`, `TestBrandFilter`, `TestEndpoint`, and at least 11 distinct classifier/probe assertions covering ok/empty/empty-no-header/404/500/403/429/html/timeout/network/malformed-resources.
    - All status assertions use the ENGLISH enum strings (`"ok"`, `"empty"`, `"error"`, `"no_probe"`) — grep finds none of the Portuguese variants (`"vazia"`, `"erro"`) as asserted status values.
  </acceptance_criteria>
  <done>
    The RED contract test exists, is syntactically valid, is collected, fails only due to the missing implementation module, and encodes the full DIAG-01 three-state matrix + D-02 no_probe marker offline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked HTTP | All HTTP is replaced by `_FakeResp`/`_FakeSession`; no real network egress occurs from this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | test reaching live VTEX | mitigate | Test mocks the session layer (`_FakeSession`); no real URL is fetched — enforced by the offline pattern from test_vtex_api_client.py |
| T-29-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new packages); nothing to verify |
</threat_model>

<verification>
- `python -m pytest tests/test_category_diagnostic.py -q` runs offline (no network) and fails RED due to missing implementation module.
- `python -m pytest tests/ -q` still collects (this plan does not break the existing suite — it only adds a new file whose import target is absent, isolated to its own module).
</verification>

<success_criteria>
- Offline RED contract test for DIAG-01 three-state classifier + D-02 no_probe marker exists and is collected.
- Matrix covers all 11+ edge cases from the RESEARCH classification matrix using ENGLISH status strings.
- No network access; no new packages.
</success_criteria>

<artifacts_this_phase_produces>
This plan creates (exclude from drift verification — newly created symbols):
- Test module: `tests/test_category_diagnostic.py` (classes `TestClassifier`, `TestBrandFilter`, `TestEndpoint`)
- Local test helpers: `_FakeResp` (extended), `_FakeSession`, `_RaisingSession`
NOTE: this test REFERENCES symbols created in 29-02 (`probe_category`, `run_brand_diagnostic`) and 29-03 (the endpoint) — those are produced downstream, not here.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/29-diagn-stico-de-categorias-vazias-erro/29-01-SUMMARY.md` when done
</output>
