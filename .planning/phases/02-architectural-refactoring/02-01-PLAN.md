# Plan: 02-01 - Engine Abstraction Layer Completion

**Phase:** 02 - Architectural Refactoring
**Goal:** Finalizar a camada de Engine para suportar múltiplos motores de e-commerce e remover dependências diretas da VTEX nas rotas e orquestradores.

## Context
A infraestrutura básica da Engine (BaseEngine, VTEXEngine, EngineFactory) já está presente, mas as rotas da API ainda chamam serviços específicos da VTEX (vtex_catalog, vtex_search) diretamente. Precisamos migrar essas chamadas para a abstração da Engine.

## Proposed Changes

### [Backend]

#### [MODIFY] [base_engine.py](file:///c:/Users/arthur.correia/Documents/Pessoal/scrapper/services/engines/base_engine.py)
- Adicionar método `get_catalog()`.
- Adicionar método `search()`.

#### [MODIFY] [vtex_engine.py](file:///c:/Users/arthur.correia/Documents/Pessoal/scrapper/services/engines/vtex_engine.py)
- Implementar `get_catalog()` chamando `vtex_catalog.get_categories()`.
- Implementar `search()` chamando `vtex_search.search_all_brands()` (refatorado).

#### [MODIFY] [routes_category.py](file:///c:/Users/arthur.correia/Documents/Pessoal/scrapper/api/routes_category.py)
- Substituir `vtex_catalog.get_categories(brand_key)` por `engine.get_catalog()`.

#### [MODIFY] [routes_brands.py](file:///c:/Users/arthur.correia/Documents/Pessoal/scrapper/api/routes_brands.py)
- Substituir chamadas diretas ao `VtexApiClient.fetch_categories` por `engine.discover_categories()`.

#### [MODIFY] [routes_search.py](file:///c:/Users/arthur.correia/Documents/Pessoal/scrapper/api/routes_search.py)
- Refatorar para usar a abstração da Engine em vez de chamar `vtex_search` diretamente.

## Tasks

- [ ] **Task 1**: Estender `BaseEngine` e `VTEXEngine` com `get_catalog` e `search`.
- [ ] **Task 2**: Refatorar `routes_category.py` para injetar a engine.
- [ ] **Task 3**: Refatorar `routes_brands.py` para usar a descoberta via engine.
- [ ] **Task 4**: Refatorar `routes_search.py` para usar a abstração.
- [ ] **Task 5**: Limpeza de imports e verificação final.

## Verification Plan

### Automated Tests
- Criar script `scratch/verify_engine_abstraction.py` para validar o retorno dos novos métodos da Engine.

### Manual Verification
1. Abrir dashboard e navegar pelas abas (Monitores, Categorias, Busca).
2. Validar que as listagens de categorias e buscas continuam funcionando para todas as marcas.
3. Testar a descoberta automática em "Configurações" ao adicionar ou visualizar uma marca.
