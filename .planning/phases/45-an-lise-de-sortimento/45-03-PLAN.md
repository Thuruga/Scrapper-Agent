---
phase: 45-an-lise-de-sortimento
plan: 03
type: execute
wave: 3
depends_on:
  - 45-02
files_modified:
  - frontend/src/App.tsx
  - frontend/src/App.css
  - frontend/src/api/client.ts
  - frontend/src/api/client.phase45.test.ts
autonomous: true
requirements:
  - SORT-01
must_haves:
  truths:
    - "The UI has a dedicated `Sortimento` page/tab separate from monitoring and category-scan flows, per D-11/D-12."
    - "The top of the page shows latest-vs-previous deltas with absolute and percentage changes, or an explicit `baseline inicial` state when no previous snapshot exists, per D-14/D-17."
    - "The lower section shows current distributions for `available_colors`, `available_sizes`, and `composition`, using the backend payload instead of recomputing from raw artifacts, per D-08/D-10 and D-14."
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "Dedicated sortiment page, sidebar navigation, and dashboard rendering"
    - path: "frontend/src/api/client.ts"
      provides: "Typed sortiment client methods and response contracts"
    - path: "frontend/src/App.css"
      provides: "Page-specific layout/chart styling that stays inside the existing frontend visual system"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/api/client.ts"
      via: "typed sortiment fetch/sync/toggle/run/dashboard calls"
      pattern: "Sortiment|sortiment"
    - from: "frontend/src/App.tsx"
      to: "backend/api/routes_sortiment.py"
      via: "dedicated page actions and dashboard reads"
      pattern: "/sortiment/"
---

<objective>
Ship the dedicated Phase 45 dashboard page so operators can manage the separate sortiment registry and inspect truthful latest-versus-previous assortment deltas plus current distributions from the existing React shell.

Purpose: satisfy D-11 through D-17 with a first-class UI surface rather than backend-only artifacts.
Output: typed client methods, a dedicated `Sortimento` tab/page, and dashboard rendering for baseline, deltas, and current distributions.

Artifacts this phase produces:
- Typed sortiment client contracts in `frontend/src/api/client.ts` and `frontend/src/api/client.phase45.test.ts`
- A dedicated `Sortimento` sidebar page in `frontend/src/App.tsx`
- Supporting layout/chart styles in `frontend/src/App.css`
</objective>

<execution_context>
@/home/zallu/.codex/gsd-core/workflows/execute-plan.md
@/home/zallu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-an-lise-de-sortimento/45-CONTEXT.md
@.planning/phases/45-an-lise-de-sortimento/45-RESEARCH.md
@.planning/phases/45-an-lise-de-sortimento/45-PATTERNS.md
@.planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-03-SUMMARY.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-05-SUMMARY.md
@frontend/src/App.tsx
@frontend/src/App.css
@frontend/src/api/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the typed frontend client for Phase 45</name>
  <files>frontend/src/api/client.ts, frontend/src/api/client.phase45.test.ts</files>
  <action>Add explicit TypeScript contracts for the sortiment registry rows, manual-run response, manifest metadata, and dashboard payload returned by Plan 45-02. Extend `ApiClient` with encoded methods for registry list/sync/update, manual category run, and dashboard fetch using the same request pattern already used by monitor and shipping features. Preserve the server-owned boundary established in Phase 44: page actions may send only persisted category ID and explicit booleans, never free-form URL/brand/delta inputs from the browser.</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>`frontend/src/api/client.ts` exports typed Phase 45 client methods, and the compile-time contract file proves the sortiment payload surface is usable from `App.tsx` without `any`-shaped rewrites.</done>
</task>

<task type="auto">
  <name>Task 2: Build the dedicated sortiment page and dashboard</name>
  <files>frontend/src/App.tsx, frontend/src/App.css</files>
  <action>Create a dedicated `Sortimento` page inside the existing `App.tsx` shell instead of nesting this feature under `MonitoringPage` or the category modal, per D-11/D-12. Add a sidebar item and header label, then build a page that loads the separate sortiment registry, offers one-way sync plus enable/disable and manual-run actions, and renders the Phase 45 dashboard from backend payloads only. The top section must show latest-versus-previous deltas with both absolute and percentage values per D-14/D-17; when `baseline=true`, render an explicit `baseline inicial` state and snapshot metadata instead of fake zero deltas per D-16. The lower section must show current distributions for `available_colors`, `available_sizes`, and `composition` only, using cards/charts/tables that reuse existing `GlassCard`, Recharts, and CSS language per D-13. Keep React’s default escaping; do not introduce `dangerouslySetInnerHTML`, a new global store, or browser-side recomputation of delta rules.</action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>The app has a dedicated `Sortimento` page with sidebar navigation, registry controls, explicit baseline handling, top delta cards, and lower current-distribution visuals for exactly the three v1 dimensions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `sortiment API payload -> React dashboard` | Snapshot labels, bucket names, and evidence metadata come from backend JSON and must be rendered safely in the browser. |
| `operator actions -> sortiment page` | Toggle, sync, and manual-run actions change server state and must stay inside the typed API boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-08 | Tampering | `frontend/src/api/client.ts` request payloads | mitigate | Keep page actions constrained to persisted category IDs and explicit booleans so the browser cannot inject alternate URL/brand/runtime values into Phase 45 requests. |
| T-45-09 | Information Disclosure | `frontend/src/App.tsx` dashboard rendering | mitigate | Render backend strings through normal React text nodes only and avoid `dangerouslySetInnerHTML` when showing bucket labels, evidence, and baseline messaging. |
| T-45-10 | Integrity | baseline/delta UI state | mitigate | Branch explicitly on backend `baseline`/`previous_snapshot_at` fields so the page never invents delta cards when the first snapshot has no predecessor. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs are required for Phase 45. |
</threat_model>

<verification>
- `cd frontend && npm run build`
- `rg -n "Sortimento|baseline inicial|available_colors|available_sizes|composition" frontend/src/App.tsx frontend/src/api/client.ts frontend/src/App.css`
</verification>

<success_criteria>
- The sidebar exposes a dedicated `Sortimento` page that is separate from monitor/category-scan pages and uses the shared application shell.
- The page can load the separate registry, trigger one-way sync and manual runs, and render explicit baseline or latest-vs-previous delta states without browser-side snapshot math.
- The dashboard’s lower section shows current distributions for color, size, and composition only, reusing the existing frontend visual language rather than creating a parallel UI system.
</success_criteria>

<output>
Create `.planning/phases/45-an-lise-de-sortimento/45-03-SUMMARY.md` when done
</output>
