---
phase: 45-an-lise-de-sortimento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/config.py
  - backend/services/sortiment_registry_service.py
  - backend/services/sortiment_artifact_service.py
  - backend/tests/test_sortiment_registry_service.py
autonomous: true
requirements:
  - SORT-01
must_haves:
  truths:
    - "The backend owns a separate sortiment registry seeded one-way from `backend/data/monitored_categories.json`, not the price-monitor registry, per D-05/D-06/D-07."
    - "Phase 45 storage is local JSON only, with immutable per-category snapshot naming and lightweight manifest lookup, not SQLite, per D-01/D-02/D-03."
    - "Typed contracts already constrain Phase 45 to `available_colors`, `available_sizes`, and `composition`, with baseline/delta fields ready for D-08 and D-15/D-17."
  artifacts:
    - path: "backend/services/sortiment_registry_service.py"
      provides: "Separate registry load/save/sync helpers for Phase 45 per D-05/D-06/D-07"
    - path: "backend/services/sortiment_artifact_service.py"
      provides: "Canonical snapshot + manifest helpers for one-file-per-category-per-run storage per D-02/D-03/D-04"
    - path: "backend/core/models.py"
      provides: "Typed registry, snapshot, manifest, and dashboard response models for Phase 45"
  key_links:
    - from: "backend/services/sortiment_registry_service.py"
      to: "backend/data/monitored_categories.json"
      via: "one-way seed sync keyed by source monitor identity"
      pattern: "monitored_categories\\.json"
    - from: "backend/services/sortiment_artifact_service.py"
      to: "backend/core/models.py"
      via: "manifest/snapshot model validation before write"
      pattern: "model_validate|model_dump"
---

<objective>
Establish the JSON-backed sortiment foundation so every later Phase 45 change builds on a separate registry and immutable artifact contract instead of the stale SQLite roadmap language.

Purpose: lock D-01 through D-08 into code-level contracts before any cron, API, or UI wiring starts.
Output: additive Pydantic/config contracts plus service helpers for registry sync and snapshot/manifest storage.

Artifacts this phase produces:
- Typed sortiment registry, manifest, snapshot-bucket, and dashboard response models in `backend/core/models.py`
- JSON-backed sortiment registry ownership in `backend/services/sortiment_registry_service.py`
- Immutable snapshot/manifest helper layer in `backend/services/sortiment_artifact_service.py`
</objective>

<execution_context>
@/home/zallu/.codex/gsd-core/workflows/execute-plan.md
@/home/zallu/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-an-lise-de-sortimento/45-CONTEXT.md
@.planning/phases/45-an-lise-de-sortimento/45-RESEARCH.md
@.planning/phases/45-an-lise-de-sortimento/45-PATTERNS.md
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-CONTEXT.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-01-SUMMARY.md
@backend/core/models.py
@backend/config.py
@backend/services/category_monitor_service.py
@backend/services/stock_summary_service.py
@backend/data/monitored_categories.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed Phase 45 contracts and settings</name>
  <files>backend/core/models.py, backend/config.py, backend/tests/test_sortiment_registry_service.py</files>
  <behavior>
    - Test 1: sortiment registry rows validate `source_monitor_id`, preserve operator-owned `enabled` state, and default new synced rows to `enabled=False` per D-05/D-06/D-07.
    - Test 2: snapshot/dashboard models accept only `available_colors`, `available_sizes`, and `composition`, and expose baseline/delta metadata (`baseline`, `latest_snapshot_at`, `previous_snapshot_at`, `delta_abs`, `delta_pct`) for D-08 and D-15 through D-17.
  </behavior>
  <action>Extend `backend/core/models.py` with additive Pydantic models for the Phase 45 registry row, immutable snapshot bucket/evidence records, per-category manifest, and dashboard payload. Add explicit settings in `backend/config.py` for the independent sortiment flow (cron interval, max products per category, evidence cap) without introducing any SQLite path or DB migration per D-01. Hard-limit the dimension contract to `available_colors`, `available_sizes`, and `composition` per D-08 so later backend/UI code cannot silently expand v1 scope.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_sortiment_registry_service.py -x -q</automated>
  </verify>
  <done>Core models and settings encode the Phase 45 registry/snapshot/dashboard contract, and tests prove disabled-by-default sync semantics plus dimension/baseline validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement separate registry sync and immutable artifact helpers</name>
  <files>backend/services/sortiment_registry_service.py, backend/services/sortiment_artifact_service.py, backend/tests/test_sortiment_registry_service.py</files>
  <behavior>
    - Test 1: one-way sync reads `backend/data/monitored_categories.json`, seeds only missing rows into the sortiment registry, never mutates the monitor file, and sets newly seeded rows `enabled=False` per D-05/D-06/D-07.
    - Test 2: artifact helpers generate one canonical snapshot filename per category per run plus a lightweight manifest that resolves `latest_snapshot` and `previous_snapshot` per D-02/D-03.
    - Test 3: helper write paths persist only aggregate-ready metadata and capped evidence records, never a full normalized catalog payload, per D-04.
  </behavior>
  <action>Create `backend/services/sortiment_registry_service.py` as the owner of the separate JSON registry, with idempotent load/save/sync helpers keyed by `source_monitor_id` so re-syncs preserve prior operator choices while ingesting new monitor categories per D-05/D-06/D-07. Create `backend/services/sortiment_artifact_service.py` using the safe JSON/file-ID pattern from `stock_summary_service.py` to own canonical filenames, per-category manifests, and snapshot directory boundaries per D-02/D-03. Do not couple this code to `routes_monitor.py`, do not write SQLite tables, and do not allow full product lists into the persisted snapshot contract per D-01/D-04.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_sortiment_registry_service.py -x -q</automated>
  </verify>
  <done>The sortiment registry is a separate local-JSON owner with one-way seeding from monitored categories, and artifact helpers can address immutable per-run snapshots plus latest/previous manifests without storing whole catalogs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `backend/data/monitored_categories.json -> sortiment_registry_service` | Price-monitor JSON is treated as untrusted seed input before it becomes Phase 45's separate registry. |
| `sortiment_artifact_service -> backend/data/sortiment_*` | Backend code writes operator-owned analytics artifacts to local disk; filename generation must stay inside fixed directories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | `sortiment_registry_service` seed sync | mitigate | Validate synced rows with Pydantic, dedupe by `source_monitor_id`, and never write back into `monitored_categories.json` while applying D-05/D-06/D-07. |
| T-45-02 | Tampering | `sortiment_artifact_service` filenames/manifests | mitigate | Reuse safe artifact IDs and hard-coded sortiment data roots so category values cannot escape the intended snapshot directory per D-02/D-03. |
| T-45-03 | Information Disclosure | snapshot contract | mitigate | Constrain persisted snapshot payloads to aggregates plus capped evidence records and reject full normalized catalog persistence per D-04. |
| T-45-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs are required for Phase 45. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_sortiment_registry_service.py -x -q`
- `rg -n "monitored_categories\\.json|enabled.*False|latest_snapshot|previous_snapshot" backend/services/sortiment_registry_service.py backend/services/sortiment_artifact_service.py backend/core/models.py`
</verification>

<success_criteria>
- A separate sortiment registry service exists and seeds from `backend/data/monitored_categories.json` one-way, defaulting new rows to disabled while preserving later operator changes.
- Typed models/settings encode JSON-only storage, immutable per-run snapshot naming, manifest pointers, and the exact v1 dimensions required by D-01 through D-08.
- Artifact helpers can persist and resolve latest/previous snapshot metadata without any SQLite schema, DB migration, or full-catalog snapshot payload.
</success_criteria>

<output>
Create `.planning/phases/45-an-lise-de-sortimento/45-01-SUMMARY.md` when done
</output>
