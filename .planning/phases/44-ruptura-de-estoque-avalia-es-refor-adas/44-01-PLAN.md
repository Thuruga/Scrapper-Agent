---
phase: 44-ruptura-de-estoque-avalia-es-refor-adas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/config.py
  - backend/services/stock_summary_service.py
  - backend/services/vtex_api_scraper.py
  - backend/services/shopify_api_client.py
  - backend/tests/test_stock_summary_service.py
  - backend/tests/test_vtex_api_client.py
  - backend/tests/test_shopify_variation_stock.py
autonomous: true
requirements: [STOCK-01, STOCK-02, REVW-01]
user_setup: []

must_haves:
  truths:
    - "STOCK-01 D-01/D-03: stock_availability=None is counted as unknown_stock and never affects rupture_pct."
    - "STOCK-01 D-04: product-level stock_availability is True when any variation/SKU has stock and False only when every exposed variation/SKU is unavailable."
    - "STOCK-01 D-02: a scan/run summary can be serialized with total_products, in_stock_count, out_of_stock_count, unknown_stock_count, verified_stock_count, rupture_pct, brand, scan_id or monitor_id, and scanned_at."
    - "STOCK-02 D-06/D-08/D-09: scan product records can hold stock_depth_* fields and explicit stock_depth_state values without treating failures as numeric zero."
    - "REVW-01 D-10/D-11/D-14: products can carry compact on-demand review comments without changing normal search into full-comment fetching."
  artifacts:
    - path: "backend/core/models.py"
      provides: "Phase 44 additive Pydantic contracts"
      contains: "class StockRuptureSummary"
    - path: "backend/services/stock_summary_service.py"
      provides: "Pure rupture math and JSON/local scan artifact helpers"
      exports: ["compute_stock_summary", "ensure_scan_product_ids", "persist_monitor_stock_summary", "load_monitor_stock_summary", "persist_category_job_stock_summaries", "load_category_job_stock_summaries"]
    - path: "backend/tests/test_shopify_variation_stock.py"
      provides: "Shopify variation aggregation regression coverage for D-04"
    - path: "backend/tests/test_vtex_api_client.py"
      provides: "VTEX variation aggregation regression coverage for D-04"
    - path: "backend/services/vtex_api_scraper.py"
      provides: "VTEX product-level stock aggregation from item/seller AvailableQuantity"
      contains: "AvailableQuantity"
    - path: "backend/services/shopify_api_client.py"
      provides: "Shopify product-level stock aggregation from variants[].available"
      contains: "variants"
    - path: "backend/config.py"
      provides: "Conservative stock probe and review pagination defaults"
      contains: "MAX_REVIEW_PAGES"
  key_links:
    - from: "backend/services/stock_summary_service.py"
      to: "backend/core/models.py"
      via: "returns StockRuptureSummary and uses additive scan product fields"
      pattern: "StockRuptureSummary"
    - from: "backend/config.py"
      to: "backend/services/stock_summary_service.py"
      via: "no runtime dependency; later services consume settings added here"
      pattern: "STOCK_PROBE_"
---

<objective>
Define the shared Phase 44 contracts and the pure stock rupture summary helper used by every later plan.

Purpose: Prevent the rupture, stock-depth, and review-comment plans from inventing separate field names or state semantics. This plan also verifies Phase 37 persistence reality before any scan integration chooses storage.
Output: additive Pydantic models/fields, conservative config keys, and `stock_summary_service.py` with deterministic summary math and JSON/local scan artifact helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-RESEARCH.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-VALIDATION.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-PATTERNS.md
@backend/core/models.py
@backend/config.py
@backend/services/shipping/base.py
@backend/services/vtex_api_scraper.py
@backend/services/shopify_api_client.py
</context>

## Artifacts this phase produces

**Files created:**
- `backend/services/stock_summary_service.py` - pure rupture summary and scan artifact helper module.
- `backend/tests/test_stock_summary_service.py` - hermetic tests for STOCK-01 formula, model defaults, scan product IDs, and JSON/local persistence helpers.
- `backend/tests/test_shopify_variation_stock.py` - hermetic Shopify variation stock aggregation regression tests for D-04.

**Symbols created or extended:**
- `core.models.StockRuptureSummary`
- `core.models.StockDepthResult`
- `core.models.ReviewComment`
- `core.models.ReviewCommentsResult`
- `core.models.RawProductBronze.scan_product_id`
- `core.models.RawProductBronze.stock_depth_estimate`
- `core.models.RawProductBronze.stock_depth_state`
- `core.models.RawProductBronze.stock_depth_checked_at`
- `core.models.RawProductBronze.stock_depth_source`
- `core.models.RawProductBronze.stock_depth_label`
- `core.models.RawProductBronze.reviews_state`
- `core.models.RawProductBronze.review_comments`
- `core.models.RawProductBronze.review_product_id`
- `core.models.SearchProductResult.scan_product_id`
- `core.models.SearchProductResult.stock_depth_estimate`
- `core.models.SearchProductResult.stock_depth_state`
- `core.models.SearchProductResult.stock_depth_checked_at`
- `core.models.SearchProductResult.stock_depth_source`
- `core.models.SearchProductResult.stock_depth_label`
- `core.models.SearchProductResult.reviews_state`
- `core.models.SearchProductResult.review_comments`
- `core.models.SearchProductResult.review_product_id`
- `config.Settings.MAX_REVIEW_PAGES`
- `config.Settings.STOCK_PROBE_QUANTITY`
- `config.Settings.STOCK_PROBE_THROTTLE_SECONDS`
- `config.Settings.STOCK_PROBE_TIMEOUT_SECONDS`
- `config.Settings.MAX_STOCK_DEPTH_PROBES_PER_BRAND`
- `services.stock_summary_service.compute_stock_summary`
- `services.stock_summary_service.ensure_scan_product_ids`
- `services.stock_summary_service.persist_monitor_stock_summary`
- `services.stock_summary_service.load_monitor_stock_summary`
- `services.stock_summary_service.persist_category_job_stock_summaries`
- `services.stock_summary_service.load_category_job_stock_summaries`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive Phase 44 model and config contracts</name>
  <files>backend/core/models.py, backend/config.py, backend/tests/test_stock_summary_service.py</files>
  <read_first>
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md (D-01 through D-14 and D-16)
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-PATTERNS.md (`backend/core/models.py` and `backend/config.py` sections)
    - backend/core/models.py (`RawProductBronze`, `SearchProductResult`, `ShippingInfo`)
    - backend/config.py (`Settings`)
  </read_first>
  <behavior>
    - Test 1: constructing `StockRuptureSummary` with zero verified products accepts `rupture_pct=None`.
    - Test 2: `RawProductBronze` and `SearchProductResult` remain valid when all new Phase 44 fields are omitted.
    - Test 3: `ReviewComment` serializes only compact fields: `review_id`, `rating`, `title`, `text`, `author`, `created_at`, `source_provider`, `source_ref`.
    - Test 4: settings expose conservative defaults for `MAX_REVIEW_PAGES`, `STOCK_PROBE_QUANTITY`, `STOCK_PROBE_THROTTLE_SECONDS`, `STOCK_PROBE_TIMEOUT_SECONDS`, and `MAX_STOCK_DEPTH_PROBES_PER_BRAND`.
  </behavior>
  <action>
    Before editing source files, check whether a Backstage MCP coding standards tool such as `backstage_get_coding_standards` is callable. If it is available, call it and follow the returned Python/FastAPI guidance. If it is unavailable, record `Backstage standards MCP unavailable; continuing with in-repo Phase 33/41 patterns` in `44-01-SUMMARY.md` and continue. Add `StockRuptureSummary`, `StockDepthResult`, `ReviewComment`, and `ReviewCommentsResult` to `backend/core/models.py`. Extend `RawProductBronze` and `SearchProductResult` with the exact additive fields listed in the artifacts section; every field must have a safe default so historical JSON/search records still validate. Include `review_product_id: Optional[str] = None` as the provider product reference used by on-demand review comments, separate from SKU and URL. Add the config keys listed in the artifacts section to `Settings`, with defaults: `MAX_REVIEW_PAGES=2`, `STOCK_PROBE_QUANTITY=999`, `STOCK_PROBE_THROTTLE_SECONDS=2.0`, `STOCK_PROBE_TIMEOUT_SECONDS=8`, and `MAX_STOCK_DEPTH_PROBES_PER_BRAND=3`.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_stock_summary_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/core/models.py` contains `class StockRuptureSummary`, `class StockDepthResult`, `class ReviewComment`, and `class ReviewCommentsResult`.
    - `RawProductBronze` and `SearchProductResult` both contain all `scan_product_id`, `stock_depth_*`, `reviews_state`, `review_comments`, and `review_product_id` fields from the artifacts section.
    - `config.Settings` contains all five Phase 44 config keys with the exact defaults named in the action.
    - The new model/config tests pass with `python -m pytest backend/tests/test_stock_summary_service.py -q`.
  </acceptance_criteria>
  <done>Shared Phase 44 model and config contracts exist and are backward compatible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce D-04 variation-level stock aggregation before summaries</name>
  <files>backend/services/vtex_api_scraper.py, backend/services/shopify_api_client.py, backend/tests/test_vtex_api_client.py, backend/tests/test_shopify_variation_stock.py, backend/tests/test_stock_summary_service.py</files>
  <read_first>
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md (D-01 through D-04)
    - backend/services/vtex_api_scraper.py (`parse_product_dict` and paged search stock aggregation)
    - backend/services/shopify_api_client.py (`_map_to_bronze` and search result mapping)
    - backend/tests/test_vtex_api_client.py (existing VTEX parse fixture)
  </read_first>
  <behavior>
    - Test 1: VTEX product with first SKU out of stock and later SKU/seller `AvailableQuantity > 0` yields `stock_availability=True` in `RawProductBronze`.
    - Test 2: VTEX product/search result with all exposed SKU/seller `AvailableQuantity <= 0` yields product-level `stock_availability=False` or `available=False`.
    - Test 3: VTEX paged search with `only_in_stock=True` keeps a product when any SKU/seller has stock and filters it only when none do.
    - Test 4: Shopify product with any `variants[].available is True` yields `stock_availability=True` or `available=True`; all variants false yields `False`.
    - Test 5: summary tests do not compute stock from SKU/variation arrays; summaries consume only normalized product-level `stock_availability`.
  </behavior>
  <action>
    Add scan-level regression tests for D-04 before wiring rupture consumers. Cover VTEX `parse_product_dict` and the paged search branch that reads `items[].sellers[].commertialOffer.AvailableQuantity`; cover Shopify `_map_to_bronze` and search mapping that read `variants[].available`. If the current parser code fails these tests, update only the stock aggregation expressions in `backend/services/vtex_api_scraper.py` and `backend/services/shopify_api_client.py` to implement D-04: product-level stock is `True` when any exposed variation/SKU has stock and `False` only when every exposed variation/SKU is unavailable. Do not change Wake, SFCC, or Zara stock logic in this task; those paths expose scalar or text-level stock signals in the current codebase, not per-variation arrays. Record that scan-path audit in `44-01-SUMMARY.md`.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_stock_summary_service.py backend/tests/test_vtex_api_client.py backend/tests/test_shopify_variation_stock.py -q</automated>
  </verify>
  <acceptance_criteria>
    - VTEX parser/search tests prove product-level stock is `True` when any SKU/seller has stock and `False` only when no SKU/seller is available.
    - Shopify parser/search tests prove product-level stock is `True` when any variant is available and `False` only when all variants are unavailable.
    - The D-04 scan-path audit states that Wake, SFCC, and Zara currently expose scalar/text stock signals rather than variation arrays.
    - `compute_stock_summary` tests consume only product-level `stock_availability` values and do not inspect SKU or variation arrays.
    - Plan 44-02 summary wiring remains blocked on 44-01, so rupture summaries consume D-04-verified product-level stock semantics.
  </acceptance_criteria>
  <done>D-04 variation aggregation is verified at scan/parser level before any rupture summary integration consumes `stock_availability`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement pure rupture summary and scan artifact helpers</name>
  <files>backend/services/stock_summary_service.py, backend/tests/test_stock_summary_service.py</files>
  <read_first>
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md (D-01 through D-04, D-15, D-16)
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-RESEARCH.md (`Pure Rupture Summary Helper`, `Open Questions`, and `Validation Architecture`)
    - backend/services/category_monitor_service.py (`DATA_DIR` and local JSON pattern)
    - backend/services/shipping/base.py (`get_field`, state/value separation pattern)
  </read_first>
  <behavior>
    - Test 1: products with stock `True`, `False`, and `None` produce `in_stock_count=1`, `out_of_stock_count=1`, `unknown_stock_count=1`, `verified_stock_count=2`, `rupture_pct=0.5`.
    - Test 2: all products with `stock_availability is None` produce `verified_stock_count=0` and `rupture_pct=None`.
    - Test 3: `ensure_scan_product_ids` adds deterministic `scan_product_id` values based on `scan_id`, `brand`, `url`, and title/name without overwriting existing IDs.
    - Test 4: monitor and manual-job summary helpers write/read JSON under `backend/data` without requiring SQLite when Phase 37 artifacts are absent.
  </behavior>
  <action>
    Verify persistence reality before implementation by checking for `backend/data/analytics.db`, `backend/services/*analytics*.py`, and Phase 37 summary/plan artifacts. In this workspace those are expected to be absent; implement JSON/local helpers and do not create a SQLite schema. If a valid existing Phase 37 SQLite service is present during execution, call that existing service from thin adapter functions only; do not invent tables or run schema push. Create `backend/services/stock_summary_service.py` with `compute_stock_summary(products, brand, scan_id=None, monitor_id=None, scanned_at=None)`, `ensure_scan_product_ids(products, brand, scan_id)`, `persist_monitor_stock_summary(monitor_id, summary)`, `load_monitor_stock_summary(monitor_id)`, `persist_category_job_stock_summaries(job_id, summaries)`, and `load_category_job_stock_summaries(job_id)`. The formula must implement D-01 exactly: `rupture_pct = out_of_stock_count / verified_stock_count` and `verified_stock_count = in_stock_count + out_of_stock_count`; `None` stock is `unknown_stock_count` and not denominator data.
    Consume `stock_availability` as the D-04-verified product-level aggregate from Task 2; do not recompute SKU/variation stock inside summary helpers.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_stock_summary_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `compute_stock_summary` never counts `stock_availability is None` in `verified_stock_count`.
    - `compute_stock_summary` returns `rupture_pct=None` when `verified_stock_count == 0`.
    - `ensure_scan_product_ids` returns dict-like products with stable `scan_product_id` and preserves every existing product field.
    - JSON/local helpers use filenames `stock_summary_{monitor_id}.json` and `category_scan_summaries_{job_id}.json` under `backend/data`.
    - Summary helpers treat product-level `stock_availability` as final input from D-04-verified parser/scan paths.
    - No ORM migration/schema-push file is added by this plan.
  </acceptance_criteria>
  <done>Rupture math and JSON/local scan artifact helpers are deterministic, tested, and ready for scan wiring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scan products -> summary math | Scraped products are untrusted provider data and may omit or mis-shape stock fields. |
| local artifact files -> API responses | JSON files under `backend/data` become operator-visible API output. |
| Phase 37 persistence check -> implementation choice | Existing SQLite artifacts may or may not exist; this plan must not invent schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | `compute_stock_summary` | mitigate | Treat only literal `True` and `False` as verified stock; all other values count as unknown and never change `rupture_pct`. |
| T-44-01A | Tampering | variation-level stock aggregation | mitigate | VTEX and Shopify scan/parser tests enforce D-04: product stock is true when any variation/SKU is available and false only when none are available. |
| T-44-02 | Information Disclosure | review model fields | mitigate | Persist/return compact `ReviewComment` fields only; no `raw_payload`, `payload`, or provider response blob fields. |
| T-44-03 | Tampering | stock-depth model fields | mitigate | `stock_depth_estimate` is optional and separate from `stock_depth_state`; failures cannot be encoded as estimate `0`. |
| T-44-04 | Repudiation | persistence choice | mitigate | Executor records the persistence reality check result in `44-01-SUMMARY.md`; no silent SQLite assumptions. |
| T-44-SC | Tampering | package installs | accept | No npm/pip/cargo install is planned in this file; if an executor adds one, stop and run the package legitimacy gate first. |
</threat_model>

<verification>
Run:

```powershell
python -m pytest backend/tests/test_stock_summary_service.py backend/tests/test_vtex_api_client.py backend/tests/test_shopify_variation_stock.py -q
```

Before closing the plan, inspect `git diff -- backend/core/models.py backend/config.py backend/services/stock_summary_service.py backend/services/vtex_api_scraper.py backend/services/shopify_api_client.py backend/tests/test_stock_summary_service.py backend/tests/test_vtex_api_client.py backend/tests/test_shopify_variation_stock.py` and confirm every new field is additive, parser changes are limited to D-04 stock aggregation if needed, and summary helpers do not inspect SKU/variation arrays.
</verification>

<success_criteria>
- STOCK-01 denominator semantics are executable as tested pure logic.
- STOCK-01 D-04 variation aggregation is tested in VTEX and Shopify scan/parser paths before summary wiring runs.
- STOCK-02 and REVW-01 have shared additive contracts for later plans.
- Phase 37 persistence uncertainty is resolved by a recorded check and JSON/local helpers when SQLite artifacts are absent.
</success_criteria>

<output>
Create `.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-01-SUMMARY.md` when done.
</output>
