---
phase: 44-ruptura-de-estoque-avalia-es-refor-adas
plan: 02
type: execute
wave: 2
depends_on: [44-01]
files_modified:
  - backend/services/category_monitor_service.py
  - backend/services/orchestrator.py
  - backend/services/orchestrator_multi.py
  - backend/api/routes_monitor.py
  - backend/api/routes_category.py
  - backend/tests/test_stock_summary_service.py
  - backend/tests/test_phase44_routes.py
autonomous: true
requirements: [STOCK-01]
user_setup: []

must_haves:
  truths:
    - "STOCK-01 D-02: scheduled category monitor runs persist one reusable rupture summary per monitor execution."
    - "STOCK-01 D-01/D-03: manual category scan summaries use the same denominator and unknown_stock semantics as scheduled monitor summaries."
    - "STOCK-01 D-04: scheduled and manual summary wiring consumes the D-04-verified product-level stock_availability produced by Plan 44-01."
    - "STOCK-01 D-15: automated verification does not treat zero Hugo Boss products as success; Hugo Boss remains a dependency risk until the VTEX-IO category-scan todo is resolved."
  artifacts:
    - path: "backend/services/category_monitor_service.py"
      provides: "Scheduled monitor stock summary persistence and scan_product_id enrichment"
      contains: "compute_stock_summary"
    - path: "backend/services/orchestrator.py"
      provides: "Single-brand manual scan stock summary persistence"
      contains: "job_id"
    - path: "backend/services/orchestrator_multi.py"
      provides: "Multi-brand manual scan stock summaries per brand"
      contains: "persist_category_job_stock_summaries"
    - path: "backend/api/routes_monitor.py"
      provides: "GET /monitor/category/{monitor_id}/stock-summary"
      contains: "stock-summary"
    - path: "backend/api/routes_category.py"
      provides: "GET /scrape-category/{job_id}/stock-summary"
      contains: "stock-summary"
  key_links:
    - from: "backend/services/category_monitor_service.py"
      to: "backend/services/stock_summary_service.py"
      via: "compute summary after products are scraped and before local JSON save"
      pattern: "compute_stock_summary"
    - from: "backend/services/orchestrator_multi.py"
      to: "backend/services/stock_summary_service.py"
      via: "persist per-brand summaries under the manual scan job_id"
      pattern: "persist_category_job_stock_summaries"
---

<objective>
Wire STOCK-01 into every category scan surface that already exists: scheduled category monitors, single-brand manual scans, and multi-brand manual scans.

Purpose: The operator must receive the same rupture semantics whether a scan came from the 10-minute monitor or a manual category job. This plan deliberately uses Aramis/another working brand for automated verification and records Hugo Boss as a dependency risk until `.planning/todos/pending/hugoboss-vtex-io-category-scan.md` is resolved.
Output: scan products enriched with `scan_product_id`, summary JSON artifacts, and read endpoints for scheduled and manual scan summaries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-RESEARCH.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-VALIDATION.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-PATTERNS.md
@.planning/todos/pending/hugoboss-vtex-io-category-scan.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-01-SUMMARY.md
@backend/services/stock_summary_service.py
@backend/services/category_monitor_service.py
@backend/services/orchestrator.py
@backend/services/orchestrator_multi.py
@backend/api/routes_monitor.py
@backend/api/routes_category.py
</context>

## Artifacts this phase produces

**Endpoint paths created:**
- `GET /monitor/category/{monitor_id}/stock-summary`
- `GET /scrape-category/{job_id}/stock-summary`

**Fields persisted on scheduled monitor product records:**
- `scan_product_id`

**Manual scan artifacts created:**
- `backend/data/category_scan_summaries_{job_id}.json`

**Scheduled monitor artifacts created:**
- `backend/data/stock_summary_{monitor_id}.json`

**Functions/signatures extended:**
- `services.orchestrator.run_orchestrator(..., job_id: str | None = None)`
- `services.orchestrator_multi.run_multi_orchestrator(...)` writes per-brand stock summaries through `persist_category_job_stock_summaries`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist scheduled monitor rupture summaries beside monitored products</name>
  <files>backend/services/category_monitor_service.py, backend/api/routes_monitor.py, backend/tests/test_stock_summary_service.py, backend/tests/test_phase44_routes.py</files>
  <read_first>
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-01-SUMMARY.md (final field/function names)
    - backend/services/stock_summary_service.py (`compute_stock_summary`, `ensure_scan_product_ids`, monitor persistence helpers)
    - backend/services/category_monitor_service.py (`run_category_scan`)
    - backend/api/routes_monitor.py (`get_monitored_products`, local JSON read pattern)
    - .planning/todos/pending/hugoboss-vtex-io-category-scan.md (Hugo Boss risk)
  </read_first>
  <behavior>
    - Test 1: `run_category_scan` persists monitored products with `scan_product_id` and leaves original fields intact.
    - Test 2: `run_category_scan` persists `stock_summary_{monitor_id}.json` with D-01 counts and `rupture_pct=None` when every product is unknown.
    - Test 3: `GET /monitor/category/{monitor_id}/stock-summary` returns the persisted summary.
    - Test 4: an empty Hugo Boss-like scan is not asserted as a successful STOCK-01 proof; tests use synthetic products or a working non-Hugo-Boss monitor fixture.
  </behavior>
  <action>
    Before editing source files, perform the Backstage MCP coding standards check if a callable standards MCP exists; record an unavailable-tool exception in `44-02-SUMMARY.md` if it does not. In `run_category_scan`, convert each yielded product to a dict using `model_dump(mode="json")` when available, call `ensure_scan_product_ids(scraped_products, brand, monitor_id)`, compute `summary = compute_stock_summary(...)`, write the enriched products to the existing `monitored_products_{monitor_id}.json`, and call `persist_monitor_stock_summary(monitor_id, summary)`. Update the matching monitor row with `last_scraped_at` and a compact `last_stock_summary` dict. In `routes_monitor.py`, add `GET /monitor/category/{monitor_id}/stock-summary` that reads `load_monitor_stock_summary(monitor_id)` and returns the summary dict or a 404 detail when no scan summary exists. Do not call stock-depth or review-comment services here.
    Consume the Plan 44-01 D-04 product-level `stock_availability` value as final input; do not recompute stock from SKU, seller, variant, or size arrays in scheduled summary wiring.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_stock_summary_service.py backend/tests/test_phase44_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Scheduled monitor products are still saved to `monitored_products_{monitor_id}.json`.
    - Every persisted scheduled monitor product has `scan_product_id`.
    - `stock_summary_{monitor_id}.json` is written after each successful or empty scheduled scan.
    - The new monitor summary endpoint uses `load_monitor_stock_summary` and never recomputes from caller-provided products.
    - Scheduled summary wiring does not inspect SKU/seller/variant arrays; D-04 aggregation remains owned by Plan 44-01 scan/parser tests.
    - No assertion or code path treats a Hugo Boss scan with zero products as Phase 44 success.
  </acceptance_criteria>
  <done>Scheduled category monitor runs persist and expose rupture summaries using the shared helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist manual category scan rupture summaries by job_id</name>
  <files>backend/services/orchestrator.py, backend/services/orchestrator_multi.py, backend/api/routes_category.py, backend/tests/test_stock_summary_service.py, backend/tests/test_phase44_routes.py</files>
  <read_first>
    - backend/services/stock_summary_service.py (`compute_stock_summary`, `ensure_scan_product_ids`, manual-job persistence helpers)
    - backend/services/orchestrator.py (`run_orchestrator`)
    - backend/services/orchestrator_multi.py (`BrandJobResult`, `run_multi_orchestrator`, `consolidate_and_save`)
    - backend/api/routes_category.py (`scrape_category`, `scrape_category_multi`)
  </read_first>
  <behavior>
    - Test 1: `scrape_category` passes its generated `job_id` into `run_orchestrator`.
    - Test 2: `run_orchestrator(..., job_id="job-1")` writes a one-brand `category_scan_summaries_job-1.json` artifact.
    - Test 3: `run_multi_orchestrator` writes one summary per brand under the shared `job_id`.
    - Test 4: `GET /scrape-category/{job_id}/stock-summary` returns all persisted manual summaries.
  </behavior>
  <action>
    Extend `run_orchestrator` with optional `job_id: str | None = None` without changing existing callers. When `job_id` is provided, enrich `produtos_validos` with `ensure_scan_product_ids(..., marca, job_id)`, compute one `StockRuptureSummary`, persist it through `persist_category_job_stock_summaries(job_id, [summary])`, include `stock_summary` in the final WebSocket/log dict, and keep Excel generation compatible by flattening product dicts as before. In `routes_category.scrape_category`, pass the generated `job_id` into `run_orchestrator`. In `run_multi_orchestrator`, after all `BrandJobResult` values are collected, compute one summary per brand using each result's products and persist the list under the same `job_id`; include `stock_summaries` in the final log payload. In `routes_category.py`, add `GET /scrape-category/{job_id}/stock-summary` that returns `{"job_id": job_id, "summaries": [...]}` from `load_category_job_stock_summaries`.
    Consume the Plan 44-01 D-04 product-level `stock_availability` value as final input; do not recompute stock from SKU, seller, variant, or size arrays in manual summary wiring.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_stock_summary_service.py backend/tests/test_phase44_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_orchestrator` remains callable with its old arguments.
    - Single-brand and multi-brand manual scans persist `category_scan_summaries_{job_id}.json`.
    - Manual summary counts use `stock_availability` with the exact D-01 denominator semantics.
    - Manual summary wiring does not inspect SKU/seller/variant arrays; D-04 aggregation remains owned by Plan 44-01 scan/parser tests.
    - `scrape_category` and `scrape_category_multi` responses keep existing response keys and add no blocking work before returning.
    - The manual summary endpoint does not create or mutate summaries on read.
  </acceptance_criteria>
  <done>Manual category scan jobs expose persisted per-brand rupture summaries by job_id.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine output -> local JSON | Scraper output may contain missing or malformed stock fields. |
| background job -> WebSocket/API | Summary data becomes operator-visible status. |
| existing Hugo Boss mappings -> STOCK-01 UAT | Hugo Boss category monitoring is known to return zero products until the VTEX-IO scan todo is resolved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-05 | Tampering | scheduled/manual summary integration | mitigate | Use shared `compute_stock_summary`; never duplicate denominator math in routes or orchestrators. |
| T-44-06 | Repudiation | scan summary artifacts | mitigate | Persist summaries with `scan_id` or `monitor_id`, `brand`, and `scanned_at`; expose read endpoints for audit. |
| T-44-07 | Denial of Service | manual scan summary generation | accept | Summary computation is a linear pass over already-collected products and adds no outbound requests. |
| T-44-08 | Tampering | Hugo Boss verification | mitigate | Automated tests and UAT notes must use synthetic products or a working non-Hugo-Boss brand unless the HB VTEX-IO todo is resolved; zero HB products is not success. |
</threat_model>

<verification>
Run:

```powershell
python -m pytest backend/tests/test_stock_summary_service.py backend/tests/test_phase44_routes.py -q
```

Human check before UAT: if testing with Hugo Boss, first verify `.planning/todos/pending/hugoboss-vtex-io-category-scan.md` is resolved and a live category scan returns real products. Otherwise use Aramis, Reserva, or another known working category target for STOCK-01 UAT and keep Hugo Boss recorded as dependency risk.
</verification>

<success_criteria>
- Scheduled category monitors and manual category scans use the same summary helper.
- Summaries persist by monitor_id or job_id and are retrievable through API routes.
- Unknown stock remains visible as `unknown_stock_count` and never becomes zero rupture.
</success_criteria>

<output>
Create `.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-02-SUMMARY.md` when done.
</output>
