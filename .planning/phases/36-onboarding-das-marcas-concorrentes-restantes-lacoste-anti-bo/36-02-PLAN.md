---
phase: 36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo
plan: 02
type: execute
wave: 1
depends_on: ["36-01"]
files_modified:
  - backend/config.py
  - backend/services/engines/sfcc_antibot_fetcher.py
  - backend/services/engines/sfcc_engine.py
  - backend/tests/test_sfcc_antibot_fetcher.py
  - backend/tests/test_sfcc_engine.py
autonomous: true
requirements: [COMP-03]
user_setup: []
must_haves:
  truths:
    - "Este plano so executa se 36-01 REPORT.md for GO_TECHNICAL ou GO_ACTIVATION (D-11)."
    - "Um fetcher dedicado aplica stealth e fetch de HTML apenas para Lacoste/SFCC flagada, sem mudar globalmente BrowserManager (D-09/D-10)."
    - "SFCCEngine usa o fetcher anti-bot para Lacoste e preserva BrowserManager para demais marcas SFCC."
    - "Falhas/bloqueios anti-bot viram BrandSearchResult.error diagnostico, nunca sucesso vazio silencioso (D-12)."
    - "Produtos continuam passando pelo parser SFCC, Quality Gates e filtro masculino existentes (D-07)."
  artifacts:
    - path: "backend/services/engines/sfcc_antibot_fetcher.py"
      provides: "Fetcher Playwright stealth dedicado para SFCC-Lacoste"
      contains: "class SFCCAntiBotFetcher"
    - path: "backend/config.py"
      provides: "Flags e parametros conservadores para anti-bot SFCC"
      contains: "SFCC_ANTIBOT_ENABLED_BRANDS"
    - path: "backend/services/engines/sfcc_engine.py"
      provides: "Roteamento condicional para o fetcher dedicado"
      contains: "SFCCAntiBotFetcher"
    - path: "backend/tests/test_sfcc_antibot_fetcher.py"
      provides: "Testes hermeticos do fetcher"
    - path: "backend/tests/test_sfcc_engine.py"
      provides: "Regressao de roteamento Lacoste vs demais SFCC"
  key_links:
    - from: "backend/services/engines/sfcc_engine.py"
      to: "backend/services/engines/sfcc_antibot_fetcher.py"
      via: "helper condicional para search/PDP fetch quando brand_key em SFCC_ANTIBOT_ENABLED_BRANDS"
    - from: "backend/config.py"
      to: "SFCCEngine"
      via: "settings.SFCC_ANTIBOT_ENABLED_BRANDS e parametros de timeout/sleep"
    - from: "SFCCAntiBotFetcher"
      to: "playwright_stealth.Stealth"
      via: "apply_stealth_sync(context ou page)"
---

<objective>
Implementar o caminho de producao condicional ao GO do spike: um fetcher dedicado/flagado para SFCC-Lacoste que aplica `playwright-stealth`, preserva o `BrowserManager` global, e integra no menor ponto possivel do `SFCCEngine` (D-09/D-10/D-11).

Purpose: se o spike provou produto real, o sistema precisa transformar esse caminho em busca operavel sem regressao nos demais engines.

Output: `SFCCAntiBotFetcher`, flags em `config.py`, wiring no `SFCCEngine`, testes hermeticos.

**Gate:** antes de executar, ler `.planning/spikes/008-lacoste-antibot-zara-recheck/REPORT.md`. Se o veredito Lacoste for `NO-GO`, parar e criar summary informando que este plano foi pulado por D-04.
</objective>

<context>
@.planning/phases/36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo/36-CONTEXT.md
@.planning/phases/36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo/36-RESEARCH.md
@.planning/spikes/008-lacoste-antibot-zara-recheck/REPORT.md
@backend/core/browser_manager.py
@backend/services/engines/sfcc_engine.py
@backend/services/engines/sfcc_parser.py
@backend/tests/test_sfcc_engine.py
@backend/config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar testes RED para fetcher dedicado e roteamento Lacoste</name>
  <files>backend/tests/test_sfcc_antibot_fetcher.py, backend/tests/test_sfcc_engine.py</files>
  <behavior>
    - `SFCCAntiBotFetcher.fetch_html(url)` aplica `Stealth().apply_stealth_sync` ao context/page antes de navegar.
    - `SFCCAntiBotFetcher.fetch_html` levanta ou retorna erro diagnostico quando o HTML contem `Access Denied` ou e curto demais.
    - `SFCCEngine("lacoste")` usa `SFCCAntiBotFetcher` quando `lacoste` esta em `settings.SFCC_ANTIBOT_ENABLED_BRANDS`.
    - `SFCCEngine` para uma marca SFCC nao flagada continua usando `BrowserManager.fetch_html`.
    - Bloqueio no fetcher vira `BrandSearchResult.error`, nao `products=[]` com `error=None`.
  </behavior>
  <action>
    Criar `backend/tests/test_sfcc_antibot_fetcher.py` com mocks hermeticos de `playwright.sync_api.sync_playwright` e/ou seams internos do fetcher; nenhum teste deve abrir browser real. Estender `backend/tests/test_sfcc_engine.py` para cobrir roteamento condicional: patchar `services.engines.sfcc_antibot_fetcher.SFCCAntiBotFetcher.fetch_html` para Lacoste e `core.browser_manager.BrowserManager.fetch_html` para outra marca. Escrever os testes antes da implementacao; eles devem falhar por ImportError/AttributeError ate a Task 2.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_sfcc_antibot_fetcher.py backend/tests/test_sfcc_engine.py -q --tb=short</automated>
  </verify>
  <done>Testes existem, sao hermeticos, expressam D-09/D-10/D-12, e falham apenas porque fetcher/wiring ainda nao existem.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar SFCCAntiBotFetcher e wiring condicional no SFCCEngine</name>
  <files>backend/config.py, backend/services/engines/sfcc_antibot_fetcher.py, backend/services/engines/sfcc_engine.py</files>
  <behavior>
    - Fetcher usa Playwright sync dentro de `asyncio.to_thread`, como BrowserManager.
    - Fetcher aplica stealth com API local `Stealth().apply_stealth_sync(...)`.
    - Config centraliza marcas/timeout/sleep/concorrencia, sem numeros magicos.
    - SFCCEngine escolhe fetcher dedicado so para Lacoste/flag.
    - Erros de bloqueio propagam mensagem clara para `BrandSearchResult.error`.
  </behavior>
  <action>
    Em `backend/config.py`, adicionar campos de Settings: `SFCC_ANTIBOT_ENABLED_BRANDS: List[str] = Field(default_factory=lambda: ["lacoste"], ...)`, `SFCC_ANTIBOT_TIMEOUT_MS: int = 35000`, `SFCC_ANTIBOT_EXTRA_SLEEP_SECONDS: float = 3.0`, `SFCC_ANTIBOT_MIN_HTML_BYTES: int = 1000`. Criar `backend/services/engines/sfcc_antibot_fetcher.py` com `class SFCCAntiBotFetchError(Exception)` e `class SFCCAntiBotFetcher` contendo `async def fetch_html(self, url: str, wait_selector: Optional[str]=None, timeout: Optional[int]=None, extra_sleep: Optional[float]=None) -> str`. A implementacao deve espelhar `BrowserManager.fetch_html`: funcao sync interna, `sync_playwright`, Chromium headless, args `BrowserManager.CHROMIUM_ARGS` mais `--disable-blink-features=AutomationControlled`, contexto desktop pt-BR, `Stealth().apply_stealth_sync(context)` ou page antes de navegar, `page.goto(..., wait_until="domcontentloaded")`, wait_selector opcional, sleep, `page.content()`, cleanup em finally. Detectar bloqueios: `Access Denied`, HTTP 403 visivel, HTML abaixo de `SFCC_ANTIBOT_MIN_HTML_BYTES`, ausencia total de marcadores uteis quando combinada com assinatura de erro; levantar `SFCCAntiBotFetchError` com mensagem diagnostica.

    Em `sfcc_engine.py`, adicionar helper privado para buscar HTML (`_fetch_html` ou equivalente) que usa `SFCCAntiBotFetcher` somente quando `self.brand_key.lower()` esta em `settings.SFCC_ANTIBOT_ENABLED_BRANDS`; caso contrario, usa `BrowserManager.fetch_html`. Aplicar o helper na busca e nos PDPs, preservando `sfcc_parser.py`, Quality Gates, shipping `None` e demais comportamento. Capturar `SFCCAntiBotFetchError` no search para retornar `BrandSearchResult.error` com mensagem clara (D-12).

    Nao usar proxy, ScraperAPI, BrightData, CAPTCHA solving, browser headed/manual ou perfil persistente real (D-03). Nao alterar `BrowserManager`.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_sfcc_antibot_fetcher.py backend/tests/test_sfcc_engine.py -q --tb=short</automated>
  </verify>
  <done>Fetcher dedicado implementado, SFCCEngine roteia Lacoste/flag por ele, demais marcas seguem BrowserManager, bloqueios viram erro diagnostico, testes verdes.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-05 | Tampering | Global browser behavior | mitigate | Nao alterar BrowserManager; fetcher dedicado com testes de roteamento. |
| T-36-06 | DoS | PDP concurrency vs anti-bot | mitigate | Reusar max_results modesto e concorrencia conservadora; config central. |
| T-36-07 | Repudiation | Anti-bot errors | mitigate | Mensagens diagnosticas em `BrandSearchResult.error` e logs. |
| T-36-08 | Information Disclosure | Persistent profile/cookies | mitigate | Contexto efemero, sem user_data_dir/perfil real. |
</threat_model>

<verification>
- `python -m pytest backend/tests/test_sfcc_antibot_fetcher.py backend/tests/test_sfcc_engine.py -q --tb=short`
- `python -m pytest backend/tests/ -q`
</verification>

## Acceptance Criteria

<success_criteria>
- D-09/D-10 cumpridos: fetcher especifico/flagado, BrowserManager global intacto.
- D-12 cumprido: bloqueio anti-bot retorna erro diagnostico.
- D-07 cumprido: produto continua no pipeline SFCC existente.
</success_criteria>

## Execution Handoff

<output>
Criar `36-02-SUMMARY.md` registrando se o plano executou por GO ou foi pulado por NO-GO, arquivos alterados e testes rodados.
</output>
