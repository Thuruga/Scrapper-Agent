---
phase: 36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo
plan: 03
type: execute
wave: 2
depends_on: ["36-02"]
files_modified:
  - backend/data/brands.json
  - .planning/spikes/008-lacoste-antibot-zara-recheck/activation_smoke.py
  - .planning/phases/36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo/36-03-SUMMARY.md
autonomous: true
requirements: [COMP-03]
user_setup: []
must_haves:
  truths:
    - "Este plano so executa se 36-02 passou e o REPORT.md permite ao menos GO_TECHNICAL."
    - "Lacoste so fica is_active=true se activation smoke confirmar >=3 produtos repetiveis com titulo + URL Lacoste + preco (D-06)."
    - "Se houver apenas GO_TECHNICAL, Lacoste permanece inativa e o summary registra follow-up; nao ha ativacao fraca."
    - "search_all_brands('polo', brands=['lacoste']) retorna produtos reais apos ativacao."
    - "Suite backend completa permanece verde."
  artifacts:
    - path: ".planning/spikes/008-lacoste-antibot-zara-recheck/activation_smoke.py"
      provides: "Smoke live repetivel para D-06"
      contains: "search_all_brands"
    - path: "backend/data/brands.json"
      provides: "Lacoste is_active=true somente se activation GO"
      contains: "\"lacoste\""
    - path: "36-03-SUMMARY.md"
      provides: "Resultado final de ativacao ou nao ativacao"
  key_links:
    - from: "activation_smoke.py"
      to: "EngineFactory.search_all_brands"
      via: "busca Lacoste isolada por query"
    - from: "activation_smoke.py"
      to: "backend/data/brands.json"
      via: "ativa Lacoste somente se D-06 confirmado"
---

<objective>
Validar o caminho integrado e aplicar a decisao operacional: ativar Lacoste somente se D-06 for comprovado, ou registrar GO tecnico sem ativacao. Rodar regressao para garantir que o fetcher dedicado nao quebrou outros engines.

Purpose: a phase so deve terminar com Lacoste ativa se a busca e repetivel e robusta o suficiente; caso contrario, preservar inatividade evita poluir a busca com falhas.

Output: smoke script, `brands.json` atualizado apenas em activation GO, summary final.
</objective>

<context>
@.planning/spikes/008-lacoste-antibot-zara-recheck/REPORT.md
@.planning/phases/36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo/36-CONTEXT.md
@backend/services/engines/factory.py
@backend/services/engines/sfcc_engine.py
@backend/services/brand_service.py
@backend/data/brands.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar activation_smoke.py para repetir busca Lacoste via engine real</name>
  <files>.planning/spikes/008-lacoste-antibot-zara-recheck/activation_smoke.py</files>
  <action>
    Criar script isolado `.planning/spikes/008-lacoste-antibot-zara-recheck/activation_smoke.py`. Ele deve bootstrapar a raiz do repo no `sys.path`, importar `engine_factory` de `services.engines.factory`, e executar duas rodadas de `await engine_factory.search_all_brands("polo", brands=["lacoste"], max_per_brand=3)`; se `polo` falhar, tentar `camisa`. O script deve imprimir e retornar JSON/resumo com query usada, contagem por rodada, produto(s) com `product_name`, `url`, `price_full`, e erro se houver. Validar que cada produto tem URL contendo `lacoste.com.br` e preco positivo. Nao alterar `brands.json` nesta tarefa.
  </action>
  <verify>
    <automated>python -c "import ast,io; p='.planning/spikes/008-lacoste-antibot-zara-recheck/activation_smoke.py'; src=io.open(p,encoding='utf-8').read(); ast.parse(src); assert 'search_all_brands' in src; assert 'lacoste' in src; print('OK')"</automated>
  </verify>
  <done>activation_smoke.py existe, e sintaticamente valido, usa engine real e testa repetibilidade de `polo`/`camisa` sem editar dados.</done>
</task>

<task type="auto">
  <name>Task 2: Aplicar decisao de ativacao baseada no smoke D-06</name>
  <files>backend/data/brands.json, .planning/phases/36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo/36-03-SUMMARY.md</files>
  <action>
    Executar `python .planning/spikes/008-lacoste-antibot-zara-recheck/activation_smoke.py`. Se o smoke comprovar duas rodadas com pelo menos 3 produtos reais no total por rodada, todos com titulo + URL Lacoste + preco positivo, editar `backend/data/brands.json` e definir `lacoste.is_active=true`. Se o smoke retornar apenas >=1 produto ou resultado instavel, manter `is_active=false` e registrar "GO tecnico / nao ativar ainda" no summary, conforme D-06. Se retornar erro/bloqueio, manter `is_active=false` e registrar NO-GO operacional.

    Em todos os casos, criar `36-03-SUMMARY.md` com: veredito final, query usada, contagens, exemplos de produtos, decisao `is_active`, e outcome Zara do REPORT. Nao marcar Zara como implementada.
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('backend/data/brands.json',encoding='utf-8')); lac=d['lacoste']; assert lac['engine']=='sfcc'; assert isinstance(lac['is_active'], bool); print('lacoste_active=', lac['is_active'])"</automated>
  </verify>
  <done>Lacoste ativa somente se D-06 foi comprovado; caso contrario permanece inativa com summary explicito. Zara continua apenas documentada.</done>
</task>

<task type="auto">
  <name>Task 3: Rodar regressao backend e registrar fechamento da phase</name>
  <files>.planning/phases/36-onboarding-das-marcas-concorrentes-restantes-lacoste-anti-bo/36-03-SUMMARY.md</files>
  <action>
    Rodar `python -m pytest backend/tests/test_sfcc_antibot_fetcher.py backend/tests/test_sfcc_engine.py -q --tb=short` e depois `python -m pytest backend/tests/ -q`. Acrescentar ao `36-03-SUMMARY.md` os comandos e resultados. Se Lacoste ficou ativa, incluir tambem o output resumido do `activation_smoke.py`. Se algum teste falhar, corrigir antes de fechar ou registrar bloqueio no summary.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_sfcc_antibot_fetcher.py backend/tests/test_sfcc_engine.py -q --tb=short</automated>
    <automated>python -m pytest backend/tests/ -q</automated>
  </verify>
  <done>Testes SFCC/anti-bot e suite backend verdes; summary final registra veredito, ativacao e regressao.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-09 | Tampering | `brands.json` activation | mitigate | Ativar somente apos smoke D-06; JSON check garante engine sfcc e boolean. |
| T-36-10 | Repudiation | Live smoke evidence | mitigate | Summary registra query, exemplos e comandos. |
| T-36-11 | DoS | Full backend regression | mitigate | Rodar suite antes de fechar; corrigir regressao. |
</threat_model>

<verification>
- `python .planning/spikes/008-lacoste-antibot-zara-recheck/activation_smoke.py`
- `python -m pytest backend/tests/test_sfcc_antibot_fetcher.py backend/tests/test_sfcc_engine.py -q --tb=short`
- `python -m pytest backend/tests/ -q`
</verification>

## Acceptance Criteria

<success_criteria>
- Lacoste `is_active=true` somente com activation GO; caso contrario permanece inativa.
- Busca isolada da Lacoste retorna produtos reais se ativa.
- Sem regressao nos testes backend.
- Summary final registra Lacoste e Zara.
</success_criteria>

## Execution Handoff

<output>
Atualizar/criar `36-03-SUMMARY.md` com veredito final e comandos de validacao.
</output>
