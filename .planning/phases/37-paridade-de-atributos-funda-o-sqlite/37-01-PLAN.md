---
phase: 37-paridade-de-atributos-fundacao-sqlite
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/services/product_contract.py
  - backend/tests/test_product_contract.py
autonomous: true
requirements: [PARID-01, PARID-03]
must_haves:
  truths:
    - "One shared canonical export contract exists with fixed leading columns in English."
    - "Alias normalization is additive: raw specification keys survive alongside canonical aliases."
    - "product_name and product_description fall back from raw_title/raw_description without renaming the bronze model."
    - "product_code remains null unless a visible commercial code is actually present."
  artifacts:
    - path: "backend/services/product_contract.py"
      provides: "shared canonical column order, alias map, and export-row projector"
      contains: "CANONICAL_PRODUCT_COLUMNS"
    - path: "backend/core/models.py"
      provides: "typed home for product_code on RawProductBronze"
      contains: "product_code"
    - path: "backend/tests/test_product_contract.py"
      provides: "Wave-0 contract tests for canonical projection and additive aliases"
      contains: "test_build_canonical_product_row"
---

<objective>
Establish the shared Phase 37 contract before touching individual engines. This plan creates the canonical product/export vocabulary, adds the only missing typed field (`product_code`) to the bronze model, and locks the behavior with pure tests.

Purpose: without a shared projector, each export surface will keep flattening product payloads differently and any engine parity work in later plans will still produce column drift.
Output: one pure helper module, one additive model field, and Wave-0 contract tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-CONTEXT.md
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-RESEARCH.md
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-PATTERNS.md
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - contract tests for canonical projection and additive aliases</name>
  <files>backend/tests/test_product_contract.py</files>
  <action>
    Create a pure test file that goes red first and locks the phase contract:

    - fixed `CANONICAL_PRODUCT_COLUMNS` order exactly matching `37-CONTEXT.md`
    - `build_canonical_product_row(...)` falls back from `raw_title` -> `product_name` and `raw_description` -> `product_description`
    - `product_code` stays `None` when absent
    - alias normalization adds canonical keys like `composition` without deleting raw keys like `Composição do produto`
    - list fields (`available_colors`, `available_sizes`) preserve list shape in the projector and blank/`None` semantics for missing scalar fields
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_product_contract.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive model field for product_code</name>
  <files>backend/core/models.py</files>
  <action>
    Add `product_code: Optional[str] = None` to `RawProductBronze` only. Do not rename `raw_title` or `raw_description`, and do not force this field onto `SearchProductResult` yet.
  </action>
  <verify>
    <automated>python -c "from core.models import RawProductBronze; p=RawProductBronze(url='https://x',brand='b',raw_title='t',raw_description='d',price_full=1,image_url='https://i'); assert p.product_code is None; print('OK')"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Shared projector and alias helper - GREEN</name>
  <files>backend/services/product_contract.py</files>
  <action>
    Implement a pure helper module with:

    - `CANONICAL_PRODUCT_COLUMNS`
    - a small alias map for canonical spec keys
    - `normalize_specifications_aliases(specs)` that is additive
    - `build_canonical_product_row(product_like)` that accepts dict/model-like payloads and emits one canonical row with blanks/`None` semantics

    Keep this module I/O-free and route-free. The projector may optionally append extra raw/spec keys later, but the canonical leading fields must be fixed and deterministic now.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_product_contract.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/test_product_contract.py -q`
- `python -m pytest backend/tests/test_quality_gates.py -q`
</verification>

<success_criteria>
- A shared canonical contract exists in code.
- `RawProductBronze` has additive support for `product_code`.
- The canonical projector is pure, deterministic, and tested.
</success_criteria>

<output>
Create `.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-01-SUMMARY.md` when done.
</output>
