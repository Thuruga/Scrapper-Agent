---
phase: 37-paridade-de-atributos-fundacao-sqlite
plan: 03
type: execute
wave: 3
depends_on: [37-01, 37-02]
files_modified:
  - backend/api/routes_search.py
  - backend/services/orchestrator.py
  - backend/services/orchestrator_multi.py
  - backend/tests/test_export_search_contract.py
autonomous: true
requirements: [PARID-01, PARID-02]
must_haves:
  truths:
    - "Comparative export leads with the fixed canonical English columns."
    - "Single-brand and multi-brand category exports lead with the same canonical English columns."
    - "Missing fields stay blank/null in export instead of dropping products or reshaping columns."
    - "No new endpoint or UI surface is introduced for this phase."
---

<objective>
Unify the final user-visible export behavior across all Excel-producing paths. This plan consumes the shared projector from Plan 01 and the richer engine data from Plan 02 so the operator sees one stable contract regardless of export surface.

Purpose: Phase 37 only succeeds if the user can trust the Excel columns to mean the same thing across comparative search and category scans.
Output: all export surfaces share one canonical leading column block and one blanks policy.
</objective>

<context>
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-CONTEXT.md
@backend/services/product_contract.py
@backend/api/routes_search.py
@backend/services/orchestrator.py
@backend/services/orchestrator_multi.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - export contract tests across all surfaces</name>
  <files>backend/tests/test_export_search_contract.py</files>
  <action>
    Add a regression suite that proves:

    - `POST /search/export` emits the canonical leading columns in fixed order
    - single-brand category export uses the same leading columns
    - multi-brand category export uses the same leading columns
    - sparse rows remain present with blanks rather than dropping columns or products
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_export_search_contract.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Comparative export consumes shared canonical projector</name>
  <files>backend/api/routes_search.py</files>
  <action>
    Replace the route-local pandas/specification flattening logic in the comparative export with the shared canonical projector. Preserve existing download behavior, filename behavior, and blank-row semantics.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_export_search_contract.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Category orchestrators consume the same shared projector</name>
  <files>backend/services/orchestrator.py, backend/services/orchestrator_multi.py</files>
  <action>
    Route single-brand and multi-brand Excel generation through the same canonical projector and fixed leading column list. Allow extra raw/spec columns only after the canonical block, not instead of it.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_export_search_contract.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/test_export_search_contract.py -q`
- `python -m pytest -q`
</verification>

<success_criteria>
- All Excel-producing paths share one canonical leading contract.
- The phase changes export shape only, not UI behavior or route surface area.
- Products with partial attributes still export successfully with blanks.
</success_criteria>

<output>
Create `.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-03-SUMMARY.md` when done.
</output>
