---
phase: 34
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - backend/services/banner_extraction_service.py
  - backend/tests/fixtures/banner_carousels.html
  - backend/tests/test_banner_extraction.py
autonomous: true
requirements: [BANNER-01, BANNER-02, BANNER-03]
must_haves:
  truths:
    - "All image slides in the principal first-viewport desktop carousel are collected at 1366×768, including lazy/srcset/background slides and image slides after videos (BANNER-01, BANNER-03)."
    - "The largest desktop source and exact rendered URL are recorded and original bytes/format are preserved (D-09, D-10)."
    - "Friendly order-description-brand metadata and SHA-256 are emitted without changing the proven prototype behavior (D-11, D-12)."
  artifacts:
    - path: backend/services/banner_extraction_service.py
      provides: Production collector promoted from the validated prototype
    - path: backend/tests/fixtures/banner_carousels.html
      provides: Deterministic carousel variants and false positives
    - path: backend/tests/test_banner_extraction.py
      provides: Pure helper and local browser fixture coverage
  key_links:
    - from: backend/services/banner_extraction_service.py
      to: testes/extrair_banners.py
      via: promoted detection behavior
    - from: backend/services/banner_extraction_service.py
      to: backend/services/banner_storage_service.py
      via: normalized original-byte asset writes
---

<objective>
Promote the proven desktop banner prototype into a cancellable, deterministic production collector.

Purpose: satisfy all-slide extraction without coupling live retail sites to automated tests.
Output: collector service plus local carousel fixtures and tests.
</objective>

<artifacts_produced>
- `backend/services/banner_extraction_service.py`
- `backend/tests/fixtures/banner_carousels.html`
- `backend/tests/test_banner_extraction.py`
</artifacts_produced>

<execution_context>
@testes/extrair_banners.py
@testes/README.md
@.planning/phases/34-extra-o-de-banners-desktop/34-RESEARCH.md
@.planning/phases/34-extra-o-de-banners-desktop/34-VALIDATION.md
@backend/services/banner_storage_service.py
@backend/core/browser_manager.py
</execution_context>

<context>
The reference run completed 13/13 brands with 37 images and 3 video slides. Preserve its broad DOM detection and sequential resource profile; refactor into dependency-injectable helpers rather than importing the CLI script.
</context>

<tasks>

<task type="auto" id="34-02-01">
  <name>Promote normalized detection and original-byte download logic</name>
  <read_first>
    <file>testes/extrair_banners.py</file>
    <file>backend/core/banner_models.py</file>
    <file>backend/services/banner_storage_service.py</file>
  </read_first>
  <action>Implement a service accepting a registered brand, cancellation signal and progress callback. Reuse the prototype's 1366×768 context, large-first-viewport scoring, `img`/`picture`/`srcset`/lazy/CSS background discovery, generic carousel-next traversal and video awareness. Normalize URLs; choose the largest desktop source while recording the rendered URL; reject non-HTTP(S), localhost/private/link-local targets and URLs not discovered from the loaded brand page; enforce navigation/download timeouts and byte limits. Stream original bytes to the storage service, preserving validated content type/extension, SHA-256 and friendly order-description-brand metadata.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_banner_extraction.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Pure helper tests cover srcset choice, CSS URL parsing, safe URL policy, filenames and candidate scoring.</criterion>
    <criterion>Cancellation is checked before/after blocking calls, between slides and before downloads.</criterion>
    <criterion>Collector returns normalized banner/video/screenshot metadata without owning run approval.</criterion>
  </acceptance_criteria>
  <done>Production collector preserves reference behavior behind a reusable service boundary.</done>
</task>

<task type="auto" id="34-02-02">
  <name>Prove carousel traversal with deterministic browser fixtures</name>
  <read_first>
    <file>backend/services/banner_extraction_service.py</file>
    <file>testes/resultado/summary.json</file>
  </read_first>
  <action>Create one self-contained local HTML fixture representing stacked/hidden slides, `srcset`, lazy attributes, CSS backgrounds, video between image slides, generic next controls, repeated images, logos/products and lower-page false positives. Exercise it through Playwright without external network. Assert ordered unique image slides, video metadata, first-viewport screenshot, complete-image preservation and cooperative cancellation. Make browser tests skip with an explicit reason only when Chromium is unavailable; pure unit coverage must always run.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_banner_extraction.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Fixture yields every intended hero image once and excludes known false positives.</criterion>
    <criterion>An image after a video slide is still discovered.</criterion>
    <criterion>A cancellation signal prevents remaining carousel/download work.</criterion>
  </acceptance_criteria>
  <done>All-slide behavior is reproducible without hitting a live retailer.</done>
</task>

</tasks>

<threat_model>
- T-34-03 Medium — SSRF: accept only active registered brand entry pages and public HTTP(S) assets discovered from that page; unit-test private/file/data schemes and redirects.
- T-34-02 High — resource exhaustion: one browser/job, sequential brands, bounded slides/download bytes/timeouts and cooperative cancellation; fixture tests assert termination.
</threat_model>

<verification>
`python -m pytest backend/tests/test_banner_extraction.py -q`
</verification>

<success_criteria>
- Deterministic fixtures cover the DOM/carousel variants proven by the spike.
- Original desktop source and rendered URL/viewport evidence are both retained.
- The collector is safe to call from the asynchronous job layer.
</success_criteria>

<output>
After completion, create `.planning/phases/34-extra-o-de-banners-desktop/34-02-SUMMARY.md`.
</output>

