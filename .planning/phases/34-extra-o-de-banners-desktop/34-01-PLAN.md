---
phase: 34
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/banner_models.py
  - backend/services/banner_storage_service.py
  - backend/services/banner_report_service.py
  - backend/tests/test_banner_models.py
  - backend/tests/test_banner_storage.py
autonomous: true
requirements: [BANNER-02, BANNER-04]
must_haves:
  truths:
    - "Original image bytes are stored once by SHA-256 while each run retains its own friendly order-description-brand filename (D-09, D-10, D-11, D-12)."
    - "Only fully approved immutable runs enter newest-first 30-day history; partial/cancelled/review drafts do not (D-05, D-06, D-07, D-08, D-13, D-15, D-16)."
    - "Every candidate starts selected and approval removes deselected references (D-14, D-15)."
  artifacts:
    - path: backend/core/banner_models.py
      provides: Typed banner, brand progress and run lifecycle contracts
    - path: backend/services/banner_storage_service.py
      provides: Content-addressed blobs, atomic run persistence, approval, history and cleanup
    - path: backend/services/banner_report_service.py
      provides: Deterministic JSON, CSV and self-contained HTML gallery artifacts
    - path: backend/tests/test_banner_storage.py
      provides: Deduplication, lifecycle, retention and path-safety proofs
  key_links:
    - from: backend/services/banner_storage_service.py
      to: backend/core/banner_models.py
      via: Pydantic run serialization
    - from: backend/services/banner_storage_service.py
      to: backend/data/banner_assets
      via: digest-only physical paths
---

<objective>
Create the safe domain and persistence foundation for banner runs before browser or API concerns are added.

Purpose: make deduplication, immutable approval, 30-day history and cleanup independently testable.
Output: typed models, a content-addressed repository, and focused unit tests.
</objective>

<artifacts_produced>
- `backend/core/banner_models.py`
- `backend/services/banner_storage_service.py`
- `backend/services/banner_report_service.py`
- `backend/tests/test_banner_models.py`
- `backend/tests/test_banner_storage.py`
</artifacts_produced>

<execution_context>
@.planning/phases/34-extra-o-de-banners-desktop/34-CONTEXT.md
@.planning/phases/34-extra-o-de-banners-desktop/34-RESEARCH.md
@.planning/phases/34-extra-o-de-banners-desktop/34-PATTERNS.md
@.planning/phases/34-extra-o-de-banners-desktop/34-VALIDATION.md
@backend/core/models.py
@backend/services/search_history_service.py
</execution_context>

<context>
The prototype already proves extraction. This plan owns only stable domain/storage behavior and must not import UI or Playwright. Store data below `backend/data`, but tests must inject temporary roots.
</context>

<tasks>

<task type="auto" id="34-01-01">
  <name>Define banner run contracts and safe content-addressed storage</name>
  <read_first>
    <file>backend/core/models.py</file>
    <file>backend/services/search_history_service.py</file>
    <file>.planning/phases/34-extra-o-de-banners-desktop/34-CONTEXT.md</file>
  </read_first>
  <action>Create enums/models for run status (`RUNNING`, `REVIEW`, `COMPLETED`, `PARTIAL`, `CANCELLED`, `FAILED`), brand status, logical banner metadata, video slides, and run summaries. Preserve source URL and exact rendered URL at 1366×768, natural/rendered dimensions, MIME, original extension, byte count, digest and screenshot reference. Implement a repository with an injectable root, SHA-256 streaming writes, MIME-extension allowlist, sanitized friendly filename metadata in `NN-description-brand.ext` form, and physical paths derived only from a validated 64-hex digest plus allowlisted extension. Use a lock and same-directory temporary file plus `os.replace` for metadata writes.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_banner_models.py backend/tests/test_banner_storage.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Two identical byte payloads produce one physical asset and distinct logical references may use different friendly names.</criterion>
    <criterion>Malicious brand/description/extension values cannot escape the configured root.</criterion>
    <criterion>Model serialization round-trips every field needed by review/history.</criterion>
  </acceptance_criteria>
  <done>Safe models and deduplicated physical storage pass unit tests.</done>
</task>

<task type="auto" id="34-01-02">
  <name>Implement run lifecycle, approval, retention and garbage collection</name>
  <read_first>
    <file>backend/core/banner_models.py</file>
    <file>backend/services/banner_storage_service.py</file>
    <file>backend/tests/test_banner_storage.py</file>
  </read_first>
  <action>Add draft/run creation and updates, newest-first completed-history list/detail/delete, 30-day cleanup, and orphan blob collection computed from all persisted references. Approval is valid only from `REVIEW`, requires at least one known banner ID, keeps selected references only, deletes newly orphaned blobs safely, timestamps the run, and transitions once to immutable `COMPLETED`. Reject repeated approval and any approval of partial/cancelled/failed runs. Keep drafts queryable separately but exclude them from history. Add a report service that deterministically materializes UTF-8 JSON, CSV and a self-contained safe HTML gallery from any terminal run, including per-brand errors, video counts and screenshot references; regenerate the approved artifacts after deselection so final history contains only approved banners.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_banner_storage.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Only `COMPLETED` runs appear in history and reopen without extraction.</criterion>
    <criterion>Approval removes deselected logical references and cannot be changed afterward.</criterion>
    <criterion>Expired/deleted runs are removed while a shared blob survives until its final reference disappears.</criterion>
    <criterion>Terminal and approved runs expose consistent JSON, CSV and HTML gallery artifacts with per-brand failure evidence.</criterion>
  </acceptance_criteria>
  <done>The full storage lifecycle is atomic, deduplicated and test-covered.</done>
</task>

</tasks>

<threat_model>
- T-34-01 High — path traversal: physical paths accept only a strict SHA-256 digest and allowlisted extension; friendly names never form physical paths; tests inject `../`, separators and hostile MIME values.
- T-34-02 High — resource exhaustion/corruption: streaming byte limits, atomic metadata replacement, 30-day cleanup and derived-reference garbage collection are tested.
</threat_model>

<verification>
`python -m pytest backend/tests/test_banner_models.py backend/tests/test_banner_storage.py -q`
</verification>

<success_criteria>
- Content-addressed assets retain original formats without duplicate bytes.
- Review/approval/history transitions enforce all locked lifecycle decisions.
- JSON, CSV, visual gallery and per-brand screenshots remain auditable production artifacts for BANNER-04.
- No external text controls a filesystem path.
</success_criteria>

<output>
After completion, create `.planning/phases/34-extra-o-de-banners-desktop/34-01-SUMMARY.md`.
</output>
