---
phase: 34
plan: 03
type: execute
wave: 3
depends_on: ["34-01", "34-02"]
files_modified:
  - backend/services/banner_job_service.py
  - backend/api/routes_banners.py
  - backend/api/__init__.py
  - backend/tests/test_banner_routes.py
autonomous: true
requirements: [BANNER-01, BANNER-02, BANNER-03, BANNER-04]
must_haves:
  truths:
    - "A job accepts selected active brands, reports incremental per-brand progress/results and supports best-effort stop that preserves completed session results (D-02, D-03, D-04)."
    - "Cancelled or partial jobs never enter history; only a fully successful run enters explicit review (D-05, D-08, D-13)."
    - "All candidates start selected; approval accepts the final selected IDs once, removes false positives and creates immutable 30-day history (D-06, D-07, D-14, D-15, D-16)."
  artifacts:
    - path: backend/services/banner_job_service.py
      provides: Sequential multi-brand orchestration, progress and cancellation
    - path: backend/api/routes_banners.py
      provides: Start/status/stop/approve/history/asset contracts
    - path: backend/tests/test_banner_routes.py
      provides: API lifecycle and negative-state tests
  key_links:
    - from: backend/api/routes_banners.py
      to: backend/services/banner_job_service.py
      via: authenticated job endpoints
    - from: backend/services/banner_job_service.py
      to: backend/core/job_manager.py
      via: job-scoped asyncio.Event
    - from: backend/services/banner_job_service.py
      to: backend/core/websocket.py
      via: progress events plus authoritative persisted status
---

<objective>
Expose the storage and collector as a secure long-running banner workflow with cancellation, review and history.

Purpose: establish the complete backend contract consumed by the final UI.
Output: job orchestrator, authenticated routes and API tests.
</objective>

<artifacts_produced>
- `backend/services/banner_job_service.py`
- `backend/api/routes_banners.py`
- `backend/tests/test_banner_routes.py`
</artifacts_produced>

<execution_context>
@backend/api/routes_category.py
@backend/api/routes_jobs.py
@backend/api/__init__.py
@backend/core/job_manager.py
@backend/core/websocket.py
@backend/services/brand_service.py
@backend/services/banner_extraction_service.py
@backend/services/banner_storage_service.py
@.planning/phases/34-extra-o-de-banners-desktop/34-CONTEXT.md
</execution_context>

<context>
HTTP routes inherit API-key authentication from `api_router`; WebSocket is an optimization only. Every status response must contain enough state for reconnect/poll reconciliation.
</context>

<tasks>

<task type="auto" id="34-03-01">
  <name>Build sequential multi-brand job orchestration and progress</name>
  <read_first>
    <file>backend/api/routes_category.py</file>
    <file>backend/core/job_manager.py</file>
    <file>backend/core/websocket.py</file>
    <file>backend/services/brand_service.py</file>
    <file>backend/services/banner_extraction_service.py</file>
  </read_first>
  <action>Implement a job service that validates a non-empty unique subset of `list_brands(active_only=True)`, creates a `RUNNING` draft and job-scoped cancellation event, runs brands sequentially through `asyncio.to_thread`, persists each progress/result transition, and emits structured messages containing job ID, brand key/status/count, overall counts, candidate metadata and terminal state. On stop, set the shared event; after the current blocking call returns, skip pending brands as cancelled and retain completed results in the draft. Transition to `REVIEW` only if every selected brand completed; otherwise use `CANCELLED`, `PARTIAL` or `FAILED` and never create history.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_banner_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Fake collectors prove brands execute sequentially and candidates become visible incrementally.</criterion>
    <criterion>Stop sets backend cancellation, preserves completed results and marks pending brands cancelled.</criterion>
    <criterion>Errors are isolated per brand and prevent review/history without erasing successful brand data.</criterion>
  </acceptance_criteria>
  <done>Backend jobs have authoritative, reconnectable and cancellable state.</done>
</task>

<task type="auto" id="34-03-02">
  <name>Expose authenticated banner lifecycle routes and prove state gates</name>
  <read_first>
    <file>backend/api/__init__.py</file>
    <file>backend/api/routes_history.py</file>
    <file>backend/services/banner_job_service.py</file>
    <file>backend/services/banner_storage_service.py</file>
  </read_first>
  <action>Add a `/banners` router with POST `/jobs`, GET `/jobs/{job_id}`, POST `/jobs/{job_id}/stop`, POST `/jobs/{job_id}/approve`, GET `/history`, GET `/history/{run_id}`, DELETE `/history/{run_id}`, safe GET asset/screenshot routes, and GET `/runs/{run_id}/reports/{format}` for JSON/CSV/HTML artifacts. Register it under `api_router`. Approval accepts selected candidate IDs and delegates all one-shot/state validation to the repository. Asset/report lookup resolves persisted identifiers only, uses `FileResponse` with original media type/friendly name, and never accepts arbitrary paths. Tests override the collector/repository root, verify auth inheritance, invalid brand/job/selection responses, cancellation, partial exclusion, report generation, approval immutability, reopen and delete.</action>
  <verify>
    <automated>python -m pytest backend/tests/test_banner_routes.py -q</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Start returns promptly with a job ID and GET returns full current state.</criterion>
    <criterion>Only REVIEW can approve and only COMPLETED appears under history.</criterion>
    <criterion>Asset endpoints cannot be coerced into reading an arbitrary local file.</criterion>
  </acceptance_criteria>
  <done>The backend vertical contract is authenticated and fully state-gated.</done>
</task>

</tasks>

<threat_model>
- T-34-04 Medium — cross-job access/cancellation: validate UUID-like IDs, isolate flags/state, inherit API-key protection, and test unknown/wrong-state jobs.
- T-34-01 High — arbitrary file read: asset routes resolve validated persisted digest/reference records only and test traversal-shaped identifiers.
</threat_model>

<verification>
`python -m pytest backend/tests/test_banner_routes.py -q`
</verification>

<success_criteria>
- The complete workflow is available through authenticated HTTP plus job progress events.
- Cancellation and partial failure preserve session evidence but cannot pollute history.
- Approval is explicit, filtered and immutable.
</success_criteria>

<output>
After completion, create `.planning/phases/34-extra-o-de-banners-desktop/34-03-SUMMARY.md`.
</output>
