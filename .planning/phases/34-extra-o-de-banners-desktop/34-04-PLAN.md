---
phase: 34
plan: 04
type: execute
wave: 4
depends_on: ["34-03"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/stores/bannerStore.ts
  - frontend/src/App.tsx
  - frontend/src/App.css
autonomous: false
requirements: [BANNER-01, BANNER-02, BANNER-03, BANNER-04]
must_haves:
  truths:
    - "A dedicated Banners tab exposes one final-user extraction button and comparative-search-style brand selection, all active brands selected initially (D-01, D-02)."
    - "Per-brand progress and incremental galleries survive tab switches; Stop cancels backend work and keeps completed session results (D-03, D-04)."
    - "Completed runs require explicit all-selected-by-default review, remove deselected false positives, then become immutable reopenable/deletable 30-day history (D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-16)."
  artifacts:
    - path: frontend/src/stores/bannerStore.ts
      provides: Persistent-in-module banner job/review/history state and event identity guard
    - path: frontend/src/App.tsx
      provides: Dedicated tab, selection, progress, gallery, approval and history UI
    - path: frontend/src/App.css
      provides: Responsive complete-image gallery using existing design tokens
  key_links:
    - from: frontend/src/stores/bannerStore.ts
      to: frontend/src/api/client.ts
      via: banner lifecycle calls and polling/event reconciliation
    - from: frontend/src/App.tsx
      to: frontend/src/stores/bannerStore.ts
      via: BannersPage actions/state
    - from: frontend/src/App.tsx
      to: backend/api/routes_banners.py
      via: ApiClient contracts
---

<objective>
Deliver the final-user desktop banner workflow in a dedicated dashboard tab and verify the full vertical slice.

Purpose: let users select brands, extract, stop, review, approve and reopen banners without CLI access.
Output: typed API/store integration, complete page, styling, build verification and conversational UAT checkpoint.
</objective>

<artifacts_produced>
- `frontend/src/stores/bannerStore.ts`
- updated `frontend/src/api/client.ts`
- updated `frontend/src/App.tsx`
- updated `frontend/src/App.css`
</artifacts_produced>

<execution_context>
@.planning/phases/34-extra-o-de-banners-desktop/34-UI-SPEC.md
@.planning/phases/34-extra-o-de-banners-desktop/34-CONTEXT.md
@frontend/src/stores/searchStore.ts
@frontend/src/api/client.ts
@frontend/src/App.tsx
@frontend/src/App.css
@backend/api/routes_banners.py
</execution_context>

<context>
Reuse the current hand-rolled glass UI and installed icons. Do not add a component package, persistence middleware, or mobile-banner extraction.
</context>

<tasks>

<task type="auto" id="34-04-01">
  <name>Add banner API contracts and module-scoped Zustand workflow</name>
  <read_first>
    <file>frontend/src/api/client.ts</file>
    <file>frontend/src/stores/searchStore.ts</file>
    <file>backend/api/routes_banners.py</file>
  </read_first>
  <action>Add typed client methods for start/status/stop/approve/history/detail/delete, report links and authenticated asset/screenshot URLs. Create `bannerStore.ts` owning active brands/selection, job ID, per-brand progress, incremental candidates, candidate selection, terminal state and history. Start clears the prior session, stop calls the backend (not just AbortController), and polling/WebSocket reconciliation checks active job identity before applying late state. Initialize candidate IDs selected as they arrive. Keep Zustand module-scoped without browser persistence so it survives tab unmount but not reload; a status detail can restore an active/review run.</action>
  <verify>
    <automated>cd frontend; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>All API operations use the existing API-key client contract.</criterion>
    <criterion>A late response/event for an old job cannot overwrite the current job.</criterion>
    <criterion>Stop invokes the backend and completed candidates remain in store.</criterion>
  </acceptance_criteria>
  <done>Frontend state faithfully models the backend lifecycle across tab changes.</done>
</task>

<task type="auto" id="34-04-02">
  <name>Build dedicated selection, progress, review and history interface</name>
  <read_first>
    <file>.planning/phases/34-extra-o-de-banners-desktop/34-UI-SPEC.md</file>
    <file>frontend/src/App.tsx</file>
    <file>frontend/src/App.css</file>
    <file>frontend/src/stores/bannerStore.ts</file>
  </read_first>
  <action>Add the `Images` sidebar item/tab/title and `BannersPage`. Reuse SearchPage brand chips with all active brands selected by default and select-all/clear-all controls. Implement start/stop button, accessible overall and per-brand progress, incremental complete-image (`object-fit: contain`) gallery, selected-by-default card/checkbox toggles, stored original and viewport screenshot links, approval count/disabled state/irreversible confirmation, completion/cancellation copy and toasts, plus a collapsed newest-first approved history with reopen/delete confirmations. Partial/cancelled data stays in current session and never appears in history. Use only existing tokens/classes plus tightly scoped banner selectors and one-column responsive safety below 768px.</action>
  <verify>
    <automated>cd frontend; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>Every copy, state, accessibility and styling detail in 34-UI-SPEC is represented.</criterion>
    <criterion>False positives can be deselected before one-shot approval and disappear afterward.</criterion>
    <criterion>History reopens saved assets without starting a new job.</criterion>
  </acceptance_criteria>
  <done>The dedicated user-facing tab covers the entire locked workflow.</done>
</task>

<task type="checkpoint:human-verify" id="34-04-03" gate="blocking">
  <name>Verify end-to-end behavior and live desktop extraction</name>
  <read_first>
    <file>.planning/phases/34-extra-o-de-banners-desktop/34-VALIDATION.md</file>
    <file>.planning/phases/34-extra-o-de-banners-desktop/34-UI-SPEC.md</file>
  </read_first>
  <action>Run the full automated regression, launch backend/frontend, then follow the UI-SPEC/manual UAT: all-selected default and toggles; start with several brands; watch incremental progress; stop and confirm no history; run to review; deselect a false positive; approve; reopen/delete history; repeat an unchanged banner to inspect deduplication; finally smoke all 13 active sites against first-viewport screenshots.</action>
  <verify>
    <automated>python -m pytest backend/tests -q; cd frontend; npm run lint; npm run build</automated>
  </verify>
  <acceptance_criteria>
    <criterion>User explicitly approves the UI and live extraction evidence.</criterion>
    <criterion>No cancelled/partial run appears in history.</criterion>
    <criterion>All active-brand heroes can be reviewed against desktop viewport evidence.</criterion>
  </acceptance_criteria>
  <done>Automated gates are green and the user accepts the live user-facing workflow.</done>
</task>

</tasks>

<threat_model>
- T-34-05 Medium — stale asynchronous state: active-job identity guards every poll/event/action response; build/lint plus cancellation flow verify no old job clobbers current state.
- T-34-04 Medium — client-only cancellation illusion: Stop must call the authenticated backend stop route; store tests/build and UAT observe server terminal state.
</threat_model>

<verification>
- `python -m pytest backend/tests -q`
- `cd frontend && npm run lint && npm run build`
- Human verification described in task 34-04-03.
</verification>

<success_criteria>
- A nontechnical final user completes the whole workflow from one dedicated tab.
- State survives tab navigation and accurately communicates stop/partial/review/history distinctions.
- Explicit approval is the only path to immutable history.
</success_criteria>

<output>
After approval, create `.planning/phases/34-extra-o-de-banners-desktop/34-04-SUMMARY.md`.
</output>
