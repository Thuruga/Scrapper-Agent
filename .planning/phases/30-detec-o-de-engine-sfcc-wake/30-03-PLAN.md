---
phase: 30-detec-o-de-engine-sfcc-wake
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - backend/tests/test_engine_detection.py
autonomous: true
requirements: [COMP-05]
must_haves:
  truths:
    - "The existing 4 detection scenarios remain present as a regression base (shopify→shopify, vtex→vtex, wake→wake updated, all-fail→unknown)"
    - "A new test asserts detect_engine returns 'sfcc' when BrowserManager.fetch_html (mocked via AsyncMock) returns HTML containing demandware.static"
    - "A new test asserts detect_engine returns 'wake' for fbitsstatic.net HTML (previously asserted 'unknown')"
    - "A new test asserts detect_engine returns 'unknown' for a 403 domain whose rendered HTML lacks any demandware marker (anti-false-positive, SC-4)"
    - "The suite verifies SC-3: a brand whose detected engine is sfcc or wake stays active (NOT auto-deactivated by the D-04 unknown branch) — without modifying create_brand"
    - "pytest backend/tests/test_engine_detection.py exits 0"
  artifacts:
    - path: "backend/tests/test_engine_detection.py"
      provides: "Regression base + SFCC/Wake/anti-false-positive + SC-3-stays-active coverage"
      contains: "BrowserManager.fetch_html"
  key_links:
    - from: "backend/tests/test_engine_detection.py"
      to: "api.routes_brands.BrowserManager.fetch_html"
      via: "patch(..., new=AsyncMock(return_value=<fixture html with demandware.static>))"
      pattern: "patch\\(\\s*[\"']api\\.routes_brands\\.BrowserManager\\.fetch_html"
---

<objective>
Convert `backend/tests/test_engine_detection.py` from its v2.0 RED baseline into the GREEN regression+expansion suite for Phase 30 (D-11). Keep the 4 existing scenarios as a regression base, add a browser mock seam for the new SFCC path, add the SFCC / Wake / 403-anti-false-positive detection cases, and add an SC-3 test proving an sfcc/wake brand stays ACTIVE through `create_brand`'s existing D-04 logic (without modifying `create_brand`).

Purpose: Prove SC-1, SC-2, SC-3, and SC-4 are satisfied by the code changes from plans 30-01 and 30-02. This is the verification artifact for the whole phase.

Output: Modified test file with the updated Wake assertion, three new detection cases, the `BrowserManager.fetch_html` AsyncMock seam, and an SC-3 stays-active integration test. `pytest backend/tests/test_engine_detection.py -q` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-detec-o-de-engine-sfcc-wake/30-CONTEXT.md
@.planning/phases/30-detec-o-de-engine-sfcc-wake/30-PATTERNS.md
@.planning/phases/30-detec-o-de-engine-sfcc-wake/30-01-PLAN.md
@.planning/phases/30-detec-o-de-engine-sfcc-wake/30-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Wake assertion and add the SFCC browser mock seam + new detection cases</name>
  <files>backend/tests/test_engine_detection.py</files>
  <read_first>
    - backend/tests/test_engine_detection.py — the WHOLE file. Reuse `_make_mock_response` (L25-34) and `_make_mock_session` (L37-54) as-is for the HTTP probes. The patch-target convention is `patch("api.routes_brands.<ClassName>.<method>", new=AsyncMock(...))` (see the SessionManager patch at L68-74). The existing scenarios to keep: `test_shopify_detected_via_collections_json` (L64), `test_vtex_detected_via_category_tree` (L76), `test_wake_commerce_returns_unknown` (L92 — its assertion flips), `test_all_probes_fail_returns_unknown` (L117 — extend so the browser also returns no marker).
    - backend/api/routes_brands.py `detect_engine` AFTER plan 30-01 lands — the SFCC probe now lazily imports `BrowserManager` and calls `BrowserManager.fetch_html`. The mock target is therefore `api.routes_brands.BrowserManager.fetch_html` (the name `BrowserManager` is resolvable on the module once the lazy import executes; patch the attribute on the imported class path used by the probe).
    - .planning/phases/30-detec-o-de-engine-sfcc-wake/30-PATTERNS.md §"test_engine_detection.py — new test cases" — the exact `test_sfcc_detected_via_browser` skeleton, the AsyncMock seam, and the 4-scenario list.
    - .planning/phases/30-detec-o-de-engine-sfcc-wake/30-CONTEXT.md D-11.
  </read_first>
  <action>
    Update the module docstring so it no longer claims these are RED tests / that Wake returns "unknown"; it is now the Phase 30 GREEN regression+expansion suite. (1) In `test_wake_commerce_returns_unknown`, change the assertion from `"unknown"` to `"wake"` and rename the test to reflect the new behavior (e.g. `test_wake_commerce_detected_returns_wake`); update its docstring. (2) Add `test_sfcc_detected_via_browser`: build a mock session where `collections.json` and `category/tree/1` return 404 and the home HTML probe also fails (e.g. 404/403 or no demandware), patch BOTH `api.routes_brands.SessionManager.get_session` (returning that session) AND `api.routes_brands.BrowserManager.fetch_html` (`new=AsyncMock(return_value='<link rel="stylesheet" href="https://www.demandware.static/sites/...">')`), call `asyncio.run(detect_engine("www.lacoste.com.br"))`, assert `== "sfcc"`. (3) Add `test_sfcc_anti_false_positive_403_no_demandware` (SC-4, Zara/Inditex case): all HTTP probes return 403 or raise, `BrowserManager.fetch_html` returns generic HTML WITHOUT `demandware.static`/`demandware.edgesuite.net`, assert `== "unknown"`. (4) Extend `test_all_probes_fail_returns_unknown` so it ALSO patches `BrowserManager.fetch_html` to return marker-free HTML, then assert `== "unknown"`. Use the exact marker strings `demandware.static` / `demandware.edgesuite.net` / `fbitsstatic.net`. Do NOT modify production code in this plan; tests only.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_engine_detection.py::TestDetectEngine -q</automated>
  </verify>
  <acceptance_criteria>
    - `test_engine_detection.py` still contains the shopify and vtex regression tests, unchanged in intent.
    - The Wake test now asserts `result == "wake"` (not `"unknown"`).
    - A new test patches `api.routes_brands.BrowserManager.fetch_html` with an `AsyncMock` returning HTML containing `demandware.static` and asserts `detect_engine(...) == "sfcc"`.
    - A new anti-false-positive test asserts `detect_engine(...) == "unknown"` when HTTP probes 403 and the rendered HTML has no demandware marker (SC-4).
    - `test_all_probes_fail_returns_unknown` patches `BrowserManager.fetch_html` to marker-free HTML and still asserts `"unknown"`.
    - `python -m pytest tests/test_engine_detection.py::TestDetectEngine -q` exits 0.
  </acceptance_criteria>
  <done>The detection suite is GREEN and proves SC-1, SC-2, and SC-4 against the plan 30-01 code.</done>
</task>

<task type="auto">
  <name>Task 2: Add SC-3 stays-active test (verifying existing D-04 logic, no create_brand change)</name>
  <files>backend/tests/test_engine_detection.py</files>
  <read_first>
    - backend/api/routes_brands.py L72-94 — `create_brand` and the D-04 rule (L85-90: `if saved.engine == "unknown": ... set_active(False)`). The `unknown` branch is what auto-deactivates; for `sfcc`/`wake` it MUST NOT fire, so the brand stays active. This plan VERIFIES this — it does NOT modify `create_brand`.
    - backend/tests/test_engine_detection.py L144-219 — `TestCreateBrandUnknown.test_unknown_engine_brand_saved_inactive`: the existing pattern that patches `routes_brands_module.detect_engine` (AsyncMock), `brand_service.add_brand` (returns a fake `DynamicBrand`), and `brand_service.set_active` (`create=True`) to drive `create_brand` without I/O. Mirror this seam.
    - .planning/phases/30-detec-o-de-engine-sfcc-wake/30-CONTEXT.md — `code_context` note: "create_brand ... a lógica D-04 já garante SC-3 automaticamente ... Nenhuma mudança em create_brand é necessária."
  </read_first>
  <action>
    Add a test (e.g. `test_sfcc_brand_stays_active` in a `TestCreateBrandActive` class, plus optionally a `wake` parametrization or sibling) mirroring the existing `test_unknown_engine_brand_saved_inactive` seam. Patch `routes_brands_module.detect_engine` to an `AsyncMock(return_value="sfcc")`, patch `brand_service.add_brand` to return a fake `DynamicBrand(engine="sfcc", is_active=True, ...)`, and patch `brand_service.set_active` (`create=True`) with a `MagicMock` so you can ASSERT it is NOT called (the D-04 deactivation branch must not fire for non-unknown engines). Call `asyncio.run(routes_brands_module.create_brand(brand_create_data))` with `engine="auto"`. Assert `result.engine == "sfcc"`, `result.is_active is True`, and that `set_active` was NOT called with `False` (e.g. `set_active.assert_not_called()`), proving SC-3 holds via the EXISTING D-04 logic without any `create_brand` modification. Add an analogous `wake` case (same assertions with `engine="wake"`). Do NOT modify `create_brand` or any production code.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_engine_detection.py -q</automated>
  </verify>
  <acceptance_criteria>
    - A new test drives `create_brand` with detect_engine mocked to `"sfcc"` and asserts the returned brand has `engine == "sfcc"` and `is_active is True`.
    - The test asserts `brand_service.set_active` was NOT called to deactivate (the D-04 `unknown` branch does not fire), proving SC-3 via existing logic.
    - An analogous `wake` case exists with the same stays-active assertions.
    - No production file is modified by this plan (`git diff --name-only` shows only `backend/tests/test_engine_detection.py`).
    - `python -m pytest tests/test_engine_detection.py -q` exits 0 (full file: regression + SFCC + Wake + anti-false-positive + SC-3 stays-active).
  </acceptance_criteria>
  <done>SC-3 is proven against the existing D-04 logic; sfcc/wake brands persist active without any create_brand change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → mocked `BrowserManager.fetch_html` / `SessionManager.get_session` | Tests inject fixture HTML in place of live network/browser I/O; no real external fetch occurs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Tampering (test integrity) | Mock seam hides a real regression | mitigate | The anti-false-positive case (403 + marker-free HTML → `"unknown"`) and the SC-3 stays-active case (`set_active.assert_not_called`) are negative/behavioral assertions that fail loudly if detection over-matches or create_brand regresses. The suite asserts exact engine strings, not truthiness. |
| T-30-09 | Denial of Service | Test accidentally performing real browser/network I/O | mitigate | Both `SessionManager.get_session` and `BrowserManager.fetch_html` are patched with mocks in every new case; no test launches a real Playwright browser or hits the network, keeping the suite fast (<60s) and hermetic. |

No npm/pip/cargo installs in this plan — package legitimacy gate not applicable. (`pytest` is an existing dev dependency; no new install task.)
</threat_model>

<verification>
- SC-1: `test_sfcc_detected_via_browser` asserts `"sfcc"`.
- SC-2: `test_wake_commerce_detected_returns_wake` asserts `"wake"`.
- SC-3: `test_sfcc_brand_stays_active` (+ wake) assert `is_active is True` and `set_active` not called to deactivate.
- SC-4: `test_sfcc_anti_false_positive_403_no_demandware` asserts `"unknown"`; `test_all_probes_fail_returns_unknown` (extended) asserts `"unknown"`.
- Whole-phase gate: `cd backend && python -m pytest tests/test_engine_detection.py -q` exits 0.
</verification>

<success_criteria>
- The full detection suite is GREEN and maps each of SC-1..SC-4 to at least one passing test.
- Regression base (shopify, vtex) still passes.
- No production code modified in this plan.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 30-03)

- **Modified test file:** `backend/tests/test_engine_detection.py`.
- **New test functions:** `test_sfcc_detected_via_browser` (SC-1), `test_sfcc_anti_false_positive_403_no_demandware` (SC-4), `test_wake_commerce_detected_returns_wake` (renamed from `test_wake_commerce_returns_unknown`, SC-2), `test_sfcc_brand_stays_active` and a `wake` sibling/parametrization (SC-3); extended `test_all_probes_fail_returns_unknown` (SC-4).
- **New mock seam:** `patch("api.routes_brands.BrowserManager.fetch_html", new=AsyncMock(return_value=<fixture html>))` — the browser injection point for the SFCC path (D-11).
- **Possibly new test class:** `TestCreateBrandActive` (SC-3 stays-active), mirroring `TestCreateBrandUnknown`.
- **No production files modified.**
</artifacts_produced>

<output>
Create `.planning/phases/30-detec-o-de-engine-sfcc-wake/30-03-SUMMARY.md` when done.
</output>
