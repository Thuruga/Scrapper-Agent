---
phase: 31-engine-sfcc-browser-p-blico-lacoste-hugoboss
plan: 03
type: execute
wave: 2
depends_on: ["31-02"]
files_modified:
  - backend/services/engines/sfcc_engine.py
  - backend/services/engines/sfcc_parser.py
  - backend/tests/test_sfcc_engine.py
autonomous: true
requirements: [COMP-03]
user_setup: []

must_haves:
  truths:
    - "discover_categories() returns category dicts (name/path/id) parsed from the rendered home/menu when nav links are present — D-05"
    - "discover_categories() returns [] without crashing when nav is missing/empty or any exception occurs — D-06 graceful stub"
    - "get_catalog() returns the frontend group shape [{group, items:[{label, path}]}] built from discover_categories — D-05"
    - "Phase 31 still closes via search even if catalog falls back to the stub — D-06"
  artifacts:
    - path: "backend/services/engines/sfcc_engine.py"
      provides: "Real discover_categories() (home/menu nav extraction with [] fallback) and get_catalog() (group-shape wrapper), replacing the Wave 1 minimal stubs"
      contains: "async def discover_categories"
    - path: "backend/services/engines/sfcc_parser.py"
      provides: "extract_nav_categories(html, base_domain) helper for nav-link extraction + dedup"
      contains: "def extract_nav_categories"
  key_links:
    - from: "backend/services/engines/sfcc_engine.py"
      to: "core.browser_manager.BrowserManager.fetch_html"
      via: "await fetch_html(home_url, wait_selector='nav', extra_sleep=2.0)"
      pattern: "wait_selector"
    - from: "backend/services/engines/sfcc_engine.py"
      to: "backend/services/engines/sfcc_parser.py"
      via: "import extract_nav_categories"
      pattern: "extract_nav_categories"
    - from: "backend/services/engines/sfcc_engine.py"
      to: "get_catalog frontend consumer"
      via: "get_catalog wraps discover_categories into [{group, items}]"
      pattern: "\"group\""
---

<objective>
Deliver the catalog/category-discovery expansion (D-05): real `discover_categories()` and `get_catalog()` so Lacoste/HugoBoss appear in the category-monitoring screen like VTEX/Shopify. Per the D-06 research gate (verdict: FEASIBLE WITH CAVEATS), the implementation extracts navigation links from the rendered home/menu and MUST degrade gracefully to `return []` on any exception or empty nav — never crashing and never blocking the search delivery already shipped in Plan 02.

Purpose: Closes the explicitly-expanded scope (D-05) while honoring the pragmatic guard (D-06). This is sequenced AFTER search (Plan 02) precisely because it was never spike-validated; the stub fallback makes the risk safe.
Output: `sfcc_engine.py` `discover_categories`/`get_catalog` upgraded from Wave 1 stubs to real implementations; `extract_nav_categories` helper added to `sfcc_parser.py`; `test_discover_categories_stub` plus a populated-nav test in `test_sfcc_engine.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-CONTEXT.md
@.planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-RESEARCH.md
@.planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-PATTERNS.md
@.planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-VALIDATION.md
@.planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-02-SUMMARY.md
@backend/services/engines/shopify_engine.py
@backend/services/engines/sfcc_engine.py
@backend/services/engines/sfcc_parser.py
---
NOTE: this plan EDITS sfcc_engine.py and sfcc_parser.py created by Plans 01/02. It runs in Wave 2 (after Plan 02 merges) precisely to avoid file-ownership conflicts. Read the current state of both files before editing.
</context>

<artifacts_this_phase_produces>
See 31-01-PLAN.md § artifacts_this_phase_produces for the canonical inventory. This plan adds/upgrades:

**Upgraded in `sfcc_engine.py` (this plan — replaces the Wave 1 `[]` stubs):**
- `SFCCEngine.discover_categories() -> List[Dict]` — real home/menu nav extraction with `[]` fallback (D-06)
- `SFCCEngine.get_catalog() -> List[Dict]` — group-shape wrapper over discover_categories

**New function in `sfcc_parser.py` (this plan):**
- `extract_nav_categories(html, base_domain) -> List[Dict]` — nav `<a>` link extraction, same-domain filter, dedup by path

**New constant (this plan):**
- Category-group label constant (e.g. `CATALOG_GROUP_LABEL = "Coleções / Categorias"`) mirroring ShopifyEngine.get_catalog
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add extract_nav_categories helper to sfcc_parser.py</name>
  <files>backend/services/engines/sfcc_parser.py</files>
  <read_first>
    - backend/services/engines/sfcc_parser.py (current state from Plan 01 — extend it, do not rewrite existing functions)
    - .planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-RESEARCH.md (§ CRQ-2 implementation approach — nav find + a[href] extraction + dedup by path; § D-06 Verdict Summary)
    - .planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-PATTERNS.md (§ sfcc_engine.py discover_categories pattern — the nav/role=navigation lookup and the {name, path, id} dict shape)
    - backend/services/engines/shopify_engine.py:21-57 (discover_categories noise-filtering precedent: skip empty/short/noise labels)
  </read_first>
  <behavior>
    - extract_nav_categories(html_with_nav, "lacoste.com.br") returns a list of {"name","path","id"} dicts, one per same-domain category-like nav link, deduped by path
    - extract_nav_categories("<html><body><div>no nav</div></body></html>", "lacoste.com.br") returns []
    - external/empty/too-short anchor labels are filtered out
  </behavior>
  <action>
    Add `extract_nav_categories(html, base_domain) -> List[Dict[str, Any]]` to `sfcc_parser.py` (keep it a pure parser function — no browser, no I/O). Parse with BeautifulSoup, locate `soup.find("nav")` or `soup.find(attrs={"role": "navigation"})`; if neither found, return `[]`. For each `a[href]` under nav: take `href` and `a.get_text(strip=True)` (`.get_text()` only — Tampering mitigation T-31-07); keep only links whose href starts with `/` (same-domain relative) and whose label length > 2 and is not pure noise; build `{"name": label, "path": href, "id": href}`. Dedup by `path` preserving order. Apply a light noise filter mirroring ShopifyEngine.discover_categories (skip terms like account/login/cart/help/politica/conta etc.). Return the list. This function carries the parsing risk so the engine method stays thin.
  </action>
  <verify>
    <automated>pytest backend/tests/test_sfcc_engine.py::TestSFCCCategoryDiscovery -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_sfcc_engine.py::TestSFCCCategoryDiscovery -x` exits 0.
    - `extract_nav_categories` returns `[]` for HTML with no nav element and a non-empty deduped list for HTML containing nav anchors.
    - Function is pure (no import of BrowserManager / no network) — `python -c "from services.engines.sfcc_parser import extract_nav_categories"` exits 0.
  </acceptance_criteria>
  <done>extract_nav_categories added to sfcc_parser.py; returns {name,path,id} dicts deduped by path, [] when nav absent; pure parser (no browser).</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade SFCCEngine.discover_categories + get_catalog (real, with D-06 stub fallback)</name>
  <files>backend/services/engines/sfcc_engine.py, backend/tests/test_sfcc_engine.py</files>
  <read_first>
    - backend/services/engines/sfcc_engine.py (current state from Plan 02 — replace the minimal `[]` discover_categories/get_catalog stubs with real implementations)
    - .planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-PATTERNS.md (§ sfcc_engine.py — discover_categories + graceful stub pattern; § get_catalog pattern; § BrowserManager.fetch_html call signature with wait_selector/extra_sleep)
    - .planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-RESEARCH.md (§ CRQ-2 full discover_categories code; § D-06 Verdict Summary — return [] on any exception; § Open Questions #3 forgiving selector)
    - backend/services/engines/shopify_engine.py:59-70 (get_catalog group-shape: [{"group": ..., "items": [{"label", "path"}]}])
    - backend/services/engines/sfcc_parser.py (extract_nav_categories from Task 1)
    - .planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-VALIDATION.md (§ Per-Task Verification Map — test_discover_categories_stub behavior)
  </read_first>
  <action>
    Replace the Wave 1 minimal stubs in `sfcc_engine.py`. Implement `discover_categories()`: resolve brand via lazy `from services.brand_service import brand_service`; if no brand return `[]`; wrap in try/except — inside, `html = await BrowserManager.fetch_html(f"https://www.{brand.domain}", wait_selector="nav", extra_sleep=2.0)` (forgiving wait for JS-driven menu per Open Question #3), then `return extract_nav_categories(html, brand.domain)`. On ANY exception, `logger.warning(...)` and `return []` — this is the D-06 graceful stub, NOT a failure. Implement `get_catalog()` mirroring ShopifyEngine: `flat = await self.discover_categories(); return [{"group": CATALOG_GROUP_LABEL, "items": [{"label": c["name"], "path": c["path"]} for c in flat]}]` (define `CATALOG_GROUP_LABEL = "Coleções / Categorias"` as a module constant). Then ADD to `test_sfcc_engine.py` a populated-nav test in `TestSFCCCategoryDiscovery` (mock `_BROWSER_FETCH_TARGET` AsyncMock returning HTML with a `<nav>` containing two category anchors; assert `discover_categories()` returns those two as dicts and `get_catalog()` wraps them into the group shape) — keep the existing `test_discover_categories_stub` (empty-nav → `[]`). Do NOT alter search/calculate_shipping from Plan 02. Keep the engine thin: nav parsing lives in `extract_nav_categories`.
  </action>
  <verify>
    <automated>pytest backend/tests/test_sfcc_engine.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_sfcc_engine.py -x` exits 0 (full SFCC suite green: parser, factory, search, AND both category tests — stub + populated).
    - `discover_categories()` returns `[]` (no crash) when BrowserManager yields no-nav HTML or raises; returns the parsed category dicts when nav anchors are present.
    - `get_catalog()` returns `[{"group": "Coleções / Categorias", "items": [...]}]` shape; items have `label` and `path` keys.
    - Plan 02 behavior preserved: `pytest backend/tests/test_sfcc_engine.py::TestSFCCEngineSearch -x` and `::TestSFCCFactory -x` still green.
    - Full suite green: `pytest backend/tests/ -ra` exits 0.
  </acceptance_criteria>
  <done>discover_categories/get_catalog upgraded to real implementations with D-06 [] fallback; populated-nav + stub tests both green; search/shipping behavior from Plan 02 unchanged; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SFCC home/menu HTML → nav parser | Untrusted rendered nav markup crosses into extract_nav_categories |
| nav anchor href → category path | Anchor hrefs become catalog paths surfaced to the frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Tampering | `extract_nav_categories` parsing scraped nav markup | mitigate | BeautifulSoup `.get_text(strip=True)` extracts label text only (no markup execution); only same-domain relative hrefs (`startswith("/")`) are kept — external/javascript: hrefs are dropped (V5 Input Validation) |
| T-31-08 | Denial of Service | nav render under JS-dropdown / anti-bot / partial HTML | mitigate | `discover_categories` try/except returns `[]` on ANY exception (D-06 graceful stub) — category failure never crashes the engine and never blocks search delivery |
| T-31-SC | Tampering | npm/pip/cargo installs | accept | No new packages; all deps already pinned in backend/requirements.txt |
</threat_model>

<verification>
- `pytest backend/tests/test_sfcc_engine.py -x` green (full SFCC suite including both category tests).
- Full suite: `pytest backend/tests/ -ra` green (no regression in search/factory/Phase-30 detection).
- Manual smoke (operator, per 31-VALIDATION § Manual-Only, D-06 confirm): after merge, call `discover_categories()` against the live BR stores; if it returns items, spot-check they are valid category paths in the monitoring UI; if it returns `[]`, confirm the graceful stub is acceptable per D-06 and record that catalog moves to a follow-up phase.
</verification>

<success_criteria>
- D-05: `discover_categories()`/`get_catalog()` return real category data from the rendered home/menu, surfacing Lacoste/HugoBoss in the category-monitoring screen.
- D-06: on infeasible/empty/exception, both degrade to `[]`/empty group with no crash — and Phase 31 still closes via search (Plan 02 untouched).
- Full SFCC test suite green; no regression to search, factory, or Phase-30 detection.
</success_criteria>

<output>
Create `.planning/phases/31-engine-sfcc-browser-p-blico-lacoste-hugoboss/31-03-SUMMARY.md` when done. Record: whether live discover_categories returned real categories or fell back to the D-06 stub, the nav selector used, and (if stub) the recommendation to move full catalog to a follow-up phase.
</output>
