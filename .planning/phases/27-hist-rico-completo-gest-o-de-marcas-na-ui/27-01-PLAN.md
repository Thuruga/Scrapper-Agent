---
phase: 27-hist-rico-completo-gest-o-de-marcas-na-ui
plan: 01
type: execute
wave: 1
depends_on: ["27-00"]
files_modified:
  - api/routes_search.py
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "POST /search (comparative) persists a history record with type='search'"
    - "The persisted record is COMPLETED with results = the inner List[BrandSearchResult], matching what SearchPage reopen expects (Resolution A)"
    - "The persisted query is the raw search term (request.query), not a composed display label"
    - "On a search exception, the record is marked FAILED with error set and the exception re-raised"
  artifacts:
    - path: "api/routes_search.py"
      provides: "HIST-01 persistence (create_job + COMPLETED/FAILED update) inside search_products"
      contains: "search_history_service.create_job"
  key_links:
    - from: "api/routes_search.py (search_products)"
      to: "services.search_history_service.create_job / update_job"
      via: "create_job(type='search') then update_job(COMPLETED, results=result.model_dump(mode='json')['results'])"
      pattern: "update_job\\(.*COMPLETED"
    - from: "api/routes_search.py (search_products)"
      to: "stored results shape"
      via: "result.model_dump(mode='json')['results'] (inner list, NOT the wrapper)"
      pattern: "model_dump\\(mode=\"json\"\\)\\[\"results\"\\]"
---

<objective>
HIST-01 backend: make `POST /search` (the synchronous comparative search, `search_products`) persist its result to the search history so it appears in the history list and can be reopened without re-scraping. Mirror the existing `POST /search/cross-marketplace` persistence block (same file, lines 413-450) with TWO mandatory deviations dictated by the result-shape landmine (RESEARCH Pitfall 1 / Resolution A).

Purpose: Today only SKU searches persist; comparative searches vanish. This closes the "incomplete history" half of HIST-01 (D-07) and turns the Wave 0 tests GREEN.
Output: `api/routes_search.py` `search_products` now does create_job(type="search") → search → update_job(COMPLETED, inner-list) with a try/except that marks FAILED on error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-RESEARCH.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-PATTERNS.md
@api/routes_search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist comparative search in POST /search (create + COMPLETED/FAILED) with the inner-list shape contract</name>
  <files>api/routes_search.py</files>
  <behavior>
    - After search succeeds: a SearchHistory record exists with type="search", status="COMPLETED", query=request.query (raw), results=inner List[BrandSearchResult].
    - On search exception: the record is status="FAILED" with error=str(e), and the exception is re-raised (so the HTTP error response is unchanged).
    - The stored results is a list, never the ComparisonResult wrapper dict (guards SearchPage silent-empty-render).
  </behavior>
  <read_first>
    - api/routes_search.py — `search_products(request: SearchRequest)` (lines 140-179). Currently: computes `all_brands`/`target_brands` (157-161), calls `brand_results = await engine_factory.search_all_brands(...)` (165-173), returns `ComparisonResult(query, brands_searched=target_brands, results=brand_results)` (175-179). NO persistence, NO try/except today.
    - api/routes_search.py — the cross-marketplace persistence ANALOG (lines 413-450): `import uuid; job_id = str(uuid.uuid4()); from services.search_history_service import search_history_service; create_job(job_id, query=..., brands=..., type="cross")` then `try: result = await ...; update_job(job_id, "COMPLETED", results=result); return result; except Exception as e: update_job(job_id, "FAILED", error=str(e)); raise`. COPY THIS STRUCTURE but with the two deviations below — do NOT copy verbatim.
    - services/search_history_service.py — `create_job(job_id, query, brands, type="search")` (52), `update_job(job_id, status, results=None, error=None)` (58). `_save_history` uses `model_dump(mode="json")` so passing JSON-serializable values is required.
    - core/models.py — `ComparisonResult` has `.model_dump(mode="json")` yielding `{"query", "brands_searched", "results": [...]}`; the `["results"]` slice is the inner `List[BrandSearchResult]` to store.
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-RESEARCH.md — "Stored Result Shape Contract" table + Pitfall 1 (store inner list) + Pitfall 2 (store raw query) + Pitfall 6 (do NOT echo job_id; response_model=ComparisonResult at line 133 strips extra fields).
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-PATTERNS.md — the "Adapted block for the comparative endpoint" showing the two deviations explicitly.
    - tests/test_search_history_comparative.py (created in 27-00) — the four assertions this change must satisfy.
  </read_first>
  <action>
    Modify `search_products` in `api/routes_search.py` to persist history per D-07, mirroring the cross-marketplace block (413-450) with two REQUIRED deviations (RESEARCH Resolution A):

    1. After `target_brands` is computed (around line 161) and BEFORE the `engine_factory.search_all_brands(...)` call: generate `job_id = str(uuid.uuid4())`, import `search_history_service` (lazy import inside the function, mirroring the analog at line 415), and call `search_history_service.create_job(job_id=job_id, query=request.query, brands=target_brands, type="search")`.
       - DEVIATION 1 (Pitfall 2): pass `query=request.query` — the RAW term, NOT a composed "Reserva, Aramis · N marcas" label. The reopen handler (SearchPage, App.tsx:656) dumps `res.query` back into the search box; a label string there would corrupt it. Label composition happens in the frontend (27-03).

    2. Wrap the existing `engine_factory.search_all_brands(...)` call AND the `ComparisonResult(...)` construction in a `try:` block. On success: build the `result = ComparisonResult(query=request.query, brands_searched=target_brands, results=brand_results)` as today, then call `search_history_service.update_job(job_id=job_id, status="COMPLETED", results=result.model_dump(mode="json")["results"])` and `return result`.
       - DEVIATION 2 (Pitfall 1 — highest risk): store `result.model_dump(mode="json")["results"]` (the INNER list), NOT the whole `result`/`ComparisonResult`. SearchPage reopen sets `setResults({ results: res.results, ... })` expecting `res.results` to BE the array; storing the wrapper causes a silent empty render.

    3. Add `except Exception as e:` that calls `search_history_service.update_job(job_id=job_id, status="FAILED", error=str(e))` then `raise` (re-raise so the HTTP error contract is unchanged) — mirrors the analog 444-450 (Pitfall 5).

    Do NOT change `response_model=ComparisonResult` (line 133) and do NOT try to echo `job_id` in the response (Pitfall 6 — it would be stripped; the frontend refetches the list instead). Do NOT convert the endpoint to async-job/polling (STATE.md [ARCH] decision — keep create+complete in the same sync request, D-07). `uuid` import may be lazy inside the function (matching the analog) or hoisted — either is acceptable.
  </action>
  <verify>
    <automated>python -m pytest tests/test_search_history_comparative.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_search_history_comparative.py -x` exits 0 (all 4 tests GREEN, including test_persisted_results_shape_is_inner_list and test_search_failure_marks_failed).
    - `python -m pytest tests/ -x` exits 0 (full backend suite green — no regression).
    - `api/routes_search.py` `search_products` contains `search_history_service.create_job(` with `type="search"` and `query=request.query`.
    - The COMPLETED update stores `result.model_dump(mode="json")["results"]` (grep for `model_dump(mode="json")["results"]` returns a hit inside search_products).
    - An `except Exception` branch calls `update_job(...status="FAILED"...)` then `raise`.
    - No `job_id` is added to the returned ComparisonResult; `response_model=ComparisonResult` unchanged.
  </acceptance_criteria>
  <done>POST /search persists a type="search", COMPLETED record with the inner-list results and raw query on success, and a FAILED record on exception; all 27-00 tests pass and the full backend suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /search | Untrusted JSON body crosses here; validated by `SearchRequest` (Pydantic) before reaching persistence |
| route → search_history_service → data/search_history.json | Trusted server writes the result to a local JSON file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | Tampering | POST /search request body | mitigate | `SearchRequest` Pydantic validation already enforces field types + `max_per_brand` cap (<=50); persistence stores only validated/derived values |
| T-27-01-02 | Denial of Service (disk) | Large `ComparisonResult` bloating `search_history.json` | accept | Bounded by existing 30-day `cleanup_old_records` + `max_per_brand` cap; identical to existing cross-marketplace storage — no new risk (RESEARCH Security Domain) |
| T-27-01-03 | Tampering | Stored search term re-rendered later | accept | React auto-escapes on render (27-03); stored `query` is the raw user term, no HTML execution path; no `dangerouslySetInnerHTML` |
| T-27-01-SC | Tampering | npm/pip installs | accept | This plan installs ZERO packages (RESEARCH Package Legitimacy Audit: zero external packages). No legitimacy gate required |

No HIGH threats. Persistence reuses an already-shipped, already-tested pattern; no new auth surface, no new external input.
</threat_model>

<verification>
- `python -m pytest tests/test_search_history_comparative.py -x` → GREEN (was RED after 27-00).
- `python -m pytest tests/ -x` → full suite GREEN.
- Manual smoke (optional): run a comparative search via the UI/API; confirm a new `type="search"` COMPLETED entry appears in `data/search_history.json` with `results` as a JSON array.
</verification>

<success_criteria>
HIST-01 backend complete: comparative searches are persisted as reopenable, correctly-shaped history records (Success Criterion #1, backend half).
</success_criteria>

<output>
Create `.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-01-SUMMARY.md` when done
</output>
