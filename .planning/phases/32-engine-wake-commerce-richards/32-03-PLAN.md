---
phase: 32-engine-wake-commerce-richards
plan: 03
type: execute
wave: 2
depends_on: ["32-02"]
files_modified:
  - backend/tests/test_wake_engine.py
  - backend/tests/test_sfcc_engine.py
autonomous: true
requirements: [COMP-04]
must_haves:
  truths:
    - "EXECUTA SOMENTE SE o veredito do spike 32-01 for GO (caso NO-GO, WakeEngine deferido e este plano nao executa)"
    - "test_wake_engine.py cobre SC-2 (search retorna produtos), SC-3 (factory retorna WakeEngine), SC-4 (token ausente->error; shipping None), D-06 (campo opcional), D-08 (stubs) — todos com SessionManager mockado"
    - "O teste obsoleto test_factory_wake_still_raises em test_sfcc_engine.py e removido/substituido (deixa de ser valido apos o wiring)"
    - "A suite completa (225 testes existentes + novos Wake) fica verde — sem regressao"
  artifacts:
    - path: "backend/tests/test_wake_engine.py"
      provides: "Testes hermeticos do WakeEngine cobrindo SC-2/SC-3/SC-4/D-06/D-08 com SessionManager mockado"
      contains: "class TestWakeFactory"
      min_lines: 80
    - path: "backend/tests/test_sfcc_engine.py"
      provides: "Remocao do test_factory_wake_still_raises (marcador 'Phase 32 pendente' agora invalido)"
  key_links:
    - from: "backend/tests/test_wake_engine.py"
      to: "core.session_manager.SessionManager.get_session"
      via: "mock seam (patch) — sem rede real"
      pattern: "SessionManager.get_session"
    - from: "backend/tests/test_wake_engine.py"
      to: "services.engines.wake_engine.WakeEngine"
      via: "import + assertIsInstance via EngineFactory.get_engine"
      pattern: "WakeEngine"
---

<objective>
Wave 2 — Cobertura de testes do `WakeEngine` e remocao do guard obsoleto. Criar `backend/tests/test_wake_engine.py` com testes hermeticos (SessionManager mockado) cobrindo SC-2/SC-3/SC-4/D-06/D-08, remover o teste `test_factory_wake_still_raises` (que afirmava "Phase 32 pendente" e agora e invalido), e garantir que a suite completa permanece verde. Atende COMP-04 (validacao automatizada de SC-2/SC-3/SC-4).

Purpose: O 32-VALIDATION mapeia cada SC para um teste automatizado nomeado; este plano os cria. Sem eles, SC-2/SC-3/SC-4 nao tem prova automatizada e o gate da phase (suite verde antes de `/gsd-verify-work`) nao pode ser cumprido. O teste antigo `test_factory_wake_still_raises` passou a falhar quando o engine foi plugado (32-02) — deve ser substituido por `test_factory_returns_wake_engine`.

Output: `test_wake_engine.py` (novo) + `test_sfcc_engine.py` (guard obsoleto removido); suite completa verde.

**PRECONDICAO DE GATE (D-01/D-03):** Como 32-02, este plano executa **SOMENTE SE** o veredito do spike 32-01 for **GO**. Em NO-GO, o `WakeEngine` e deferido e este plano nao executa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-engine-wake-commerce-richards/32-RESEARCH.md
@.planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md
@.planning/phases/32-engine-wake-commerce-richards/32-VALIDATION.md
</context>

<artifacts_this_phase_produces>
> Simbolos/arquivos criados por ESTE plano (excluir do drift-check):
> - Arquivo: `backend/tests/test_wake_engine.py`
> - Classes de teste: `TestWakeFactory`, `TestWakeEngineSearch`, `TestWakeTokenFailure`, `TestWakeModels`, `TestWakeStubs`
> - Metodos de teste: `test_factory_returns_wake_engine`, `test_search_returns_products`, `test_calculate_shipping_returns_none`, `test_missing_token_returns_error`, `test_model_wake_token_optional`, `test_discover_categories_stub`
> - Fixture/constante de teste: `_GRAPHQL_RESPONSE`, `_SESSION_GET_TARGET`
> (Engine de 32-02 e spike de 32-01 listados nos respectivos planos. Esta lista cobre simbolos NOVOS de teste; `WakeEngine`/`wake_access_token` foram criados em 32-02.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Criar test_wake_engine.py — SC-2/SC-3/SC-4/D-06/D-08 com SessionManager mockado</name>
  <files>backend/tests/test_wake_engine.py</files>
  <read_first>
    - backend/tests/test_sfcc_engine.py (analog de testes hermeticos: estrutura de classes, mock seam, fixture de resposta, TestSFCCFactory L218-249, padrao de patch)
    - backend/tests/test_engine_detection.py (padrao _make_mock_session / _make_mock_response para aiohttp)
    - backend/services/engines/wake_engine.py (a unidade sob teste — assinaturas reais de search/_resolve_token/calculate_shipping/stubs, criado em 32-02)
    - backend/core/models.py (DynamicBrandCreate com wake_access_token; BrandSearchResult; SearchProductResult)
    - .planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md §"test_wake_engine.py" (todos os padroes de teste: mock seam SessionManager.get_session, _GRAPHQL_RESPONSE, TestWakeFactory, TestWakeEngineSearch, TestWakeTokenFailure)
    - .planning/phases/32-engine-wake-commerce-richards/32-VALIDATION.md §"Per-Task Verification Map" (nomes exatos de classe::metodo esperados por SC)
  </read_first>
  <action>
    Criar `backend/tests/test_wake_engine.py` espelhando `test_sfcc_engine.py`. Mock seam: `_SESSION_GET_TARGET = "core.session_manager.SessionManager.get_session"` (aiohttp, NAO `BrowserManager.fetch_html`). Fixture `_GRAPHQL_RESPONSE` = dict JSON com `data.search.products.edges[0].node` contendo `productName`, `aliasComplete`, `prices.price` (float em reais, ex.: 799.0), `images[].url`, `available` — usar a forma confirmada no REPORT.md do spike 32-01.

    Criar as classes/metodos com os nomes EXATOS do 32-VALIDATION:
    - `TestWakeFactory::test_factory_returns_wake_engine` (SC-3): patch `services.engines.factory.brand_service.get_brand` retornando mock com `.engine="wake"`; `EngineFactory.get_engine("richards")` -> `assert isinstance(engine, WakeEngine)`.
    - `TestWakeEngineSearch::test_search_returns_products` (SC-2): mockar `SessionManager.get_session` para um session cujo `.post(...)` retorna um async-context-manager com `.json()` -> `_GRAPHQL_RESPONSE`; patch `brand_service.get_brand` com mock tendo `wake_access_token="tcs_loja_test"`, `domain="www.richards.com.br"`, `brand_name="Richards"`; `asyncio.run(engine.search("camisa", max_results=3))` -> `assert len(result.products) >= 1` e que o primeiro produto tem titulo+url+preco (url comecando com `https://www.richards.com.br/`, nao fbits.net — Armadilha 2).
    - `TestWakeEngineSearch::test_calculate_shipping_returns_none` (SC-4/D-08): `asyncio.run(engine.calculate_shipping(product={}, zipcode="01310-000"))` -> `assert result is None`.
    - `TestWakeTokenFailure::test_missing_token_returns_error` (SC-4/D-07): brand mock com `wake_access_token=None` e auto-extract falhando (patch `SessionManager.get_session` com `side_effect=Exception` OU retornando HTML sem token); rodar via `EngineFactory().search_all_brands("camisa", brands=["richards"])` -> `assert result[0].error is not None and result[0].error != ""` (nunca lista vazia silenciosa).
    - `TestWakeModels::test_model_wake_token_optional` (D-06): `DynamicBrandCreate(brand_key="x",brand_name="X",domain="x.com")` valida com `wake_access_token is None`; com valor explicito preserva.
    - `TestWakeStubs::test_discover_categories_stub` (D-08): `asyncio.run(engine.discover_categories())` -> `assert result == []`.

    Seguir o padrao de mock do async context manager (`__aenter__`/`__aexit__`) do PATTERNS para o `session.post(...)`. Testes 100% hermeticos — nenhuma rede real (a rede e validada UMA vez pelo spike 32-01). Ajustar import paths conforme o conftest/raiz do projeto (mesma convencao de `test_sfcc_engine.py`).

    Atende COMP-04 SC-2/SC-3/SC-4 e D-06/D-08 (per 32-VALIDATION §"Per-Task Verification Map").
  </action>
  <acceptance_criteria>
    - `test_wake_engine.py` existe e contem as classes `TestWakeFactory`, `TestWakeEngineSearch`, `TestWakeTokenFailure`, `TestWakeModels`, `TestWakeStubs`
    - `test_wake_engine.py` usa o mock seam `core.session_manager.SessionManager.get_session` e NAO faz rede real
    - `pytest backend/tests/test_wake_engine.py -q --tb=short` sai 0 (todos os testes verdes)
    - `pytest backend/tests/test_wake_engine.py::TestWakeFactory::test_factory_returns_wake_engine backend/tests/test_wake_engine.py::TestWakeEngineSearch::test_search_returns_products backend/tests/test_wake_engine.py::TestWakeTokenFailure::test_missing_token_returns_error -x` sai 0
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest backend/tests/test_wake_engine.py -q --tb=short</automated>
  </verify>
  <done>test_wake_engine.py existe e passa: SC-2 (search produtos), SC-3 (factory WakeEngine), SC-4 (token ausente->error + shipping None), D-06 (campo opcional), D-08 (stubs) — tudo hermetico com SessionManager mockado.</done>
</task>

<task type="auto">
  <name>Task 2: Remover o guard obsoleto test_factory_wake_still_raises + regressao da suite completa</name>
  <files>backend/tests/test_sfcc_engine.py</files>
  <read_first>
    - backend/tests/test_sfcc_engine.py (L235-249 — test_factory_wake_still_raises, o teste a remover; confirmar que e a unica referencia ao guard wake NotImplementedError neste arquivo)
    - .planning/phases/32-engine-wake-commerce-richards/32-RESEARCH.md §"test_sfcc_engine.py — TestSFCCFactory.test_factory_wake_still_raises (L235-249)" e §"Estado da Arte" (teste descontinuado apos o wiring)
    - .planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md §"Remocao obrigatoria"
  </read_first>
  <action>
    Em `backend/tests/test_sfcc_engine.py`, **remover** o metodo `test_factory_wake_still_raises` (L235-249) — ele afirma `pytest.raises(NotImplementedError)` para `engine="wake"`, o que deixou de ser verdade quando 32-02 plugou o `WakeEngine`. A cobertura positiva equivalente (`test_factory_returns_wake_engine`) ja vive em `test_wake_engine.py` (Task 1), entao este e um delete limpo — NAO mover o teste, apenas remover (a asercao de factory para wake agora e responsabilidade de `test_wake_engine.py`). NAO alterar nenhum outro teste do arquivo SFCC. Confirmar que nenhuma outra parte de `test_sfcc_engine.py` depende do guard wake.

    Em seguida, rodar a suite COMPLETA para garantir zero regressao: `python -m pytest backend/tests/ -q`. Os 225 testes existentes (menos o removido) + os novos testes Wake (Task 1) devem ficar verdes. Se algum teste existente quebrar, investigar e corrigir antes de fechar o plano (a substituicao do guard nao deve ter efeito colateral fora do branch wake).

    Atende a regressao (32-VALIDATION §"Sampling Rate" — gate da phase: suite completa verde) e o §"Estado da Arte" (teste descontinuado removido).
  </action>
  <acceptance_criteria>
    - `grep -c "test_factory_wake_still_raises" backend/tests/test_sfcc_engine.py` retorna 0
    - `grep -c "NotImplementedError" backend/tests/test_sfcc_engine.py` (filtrando comentarios) retorna 0 para o contexto wake
    - `python -m pytest backend/tests/ -q` sai 0 (suite completa verde, sem regressao)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest backend/tests/ -q && python -c "import io; assert 'test_factory_wake_still_raises' not in io.open('backend/tests/test_sfcc_engine.py',encoding='utf-8').read(); print('OK')"</automated>
  </verify>
  <done>test_factory_wake_still_raises removido de test_sfcc_engine.py; suite completa (existentes + novos Wake) verde, sem regressao.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testes -> (nenhuma rede) | Testes hermeticos com SessionManager mockado; nenhuma chamada externa real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-05 | Repudiation | cobertura do caminho de token ausente (`test_missing_token_returns_error`) | mitigate | Teste prova que token nao resolvido vira `BrandSearchResult.error` (D-07) e nunca 0 produtos silenciosos — garante a observabilidade da falha |
| T-32-07 | Tampering | teste fazendo rede real em vez de mock (flakiness / dependencia externa) | mitigate | Mock seam obrigatorio em `SessionManager.get_session`; a unica verificacao com rede real e o spike manual 32-01 (32-VALIDATION §"Manual-Only Verifications") |
| T-32-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo (pytest/pytest-asyncio ja instalados, 32-RESEARCH §"Auditoria de Legitimidade" [OK]) |
</threat_model>

<verification>
- `python -m pytest backend/tests/test_wake_engine.py -q --tb=short` verde (SC-2/SC-3/SC-4/D-06/D-08).
- `python -m pytest backend/tests/ -q` verde (regressao — gate da phase).
- `test_factory_wake_still_raises` removido.
</verification>

<success_criteria>
- COMP-04 SC-2/SC-3/SC-4 cobertos por testes automatizados nomeados (conforme 32-VALIDATION).
- D-06/D-08 cobertos.
- Suite completa verde antes de `/gsd-verify-work` (gate da phase); guard obsoleto removido.
</success_criteria>

<output>
Create `.planning/phases/32-engine-wake-commerce-richards/32-03-SUMMARY.md` when done.
</output>
