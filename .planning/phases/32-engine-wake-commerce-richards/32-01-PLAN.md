---
phase: 32-engine-wake-commerce-richards
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/spikes/007-wake-graphql-token-confirmation/experiment.py
  - .planning/spikes/007-wake-graphql-token-confirmation/REPORT.md
autonomous: false
requirements: [COMP-04]
must_haves:
  truths:
    - "Um spike isolado roda da raiz e tenta extrair o TCS-Access-Token do storefront Wake (Richards primário, Shop2gether fallback) e bater no endpoint GraphQL com esse token (D-01)"
    - "O spike produz um REPORT.md com veredito explícito GO ou NO-GO (D-04)"
    - "GO exige >=1 produto com titulo + URL + preco retornado via GraphQL (D-02)"
    - "O spike confirma empiricamente as suposicoes A1-A6 do RESEARCH (campos aliasComplete/images dentro de search, formato do preco, prefixo do token)"
    - "O spike vive fora de backend/ ate o GO (D-04)"
  artifacts:
    - path: ".planning/spikes/007-wake-graphql-token-confirmation/experiment.py"
      provides: "Script isolado e reproduzivel que confirma o fluxo GraphQL + TCS-Access-Token da Wake"
      contains: "storefront-api.fbits.net/graphql"
    - path: ".planning/spikes/007-wake-graphql-token-confirmation/REPORT.md"
      provides: "Veredito GO/NO-GO + evidencia (produto extraido, token usado, endpoint, campos confirmados)"
      contains: "Veredito"
  key_links:
    - from: ".planning/spikes/007-wake-graphql-token-confirmation/experiment.py"
      to: "https://storefront-api.fbits.net/graphql"
      via: "SessionManager.get_session() POST aiohttp com header TCS-Access-Token"
      pattern: "TCS-Access-Token"
    - from: "experiment.py"
      to: "REPORT.md"
      via: "saida dupla (stdout + escrita do REPORT.md com veredito)"
      pattern: "REPORT"
---

<objective>
Wave 0 — GATE de confirmacao (spike). Construir `.planning/spikes/007-wake-graphql-token-confirmation/experiment.py` e produzir um `REPORT.md` com veredito explicito **GO/NO-GO** sobre o fluxo GraphQL + `TCS-Access-Token` da Wake, validado empiricamente contra a **Richards** (primario) com fallback para **Shop2gether** (D-01). Este spike e o gate que destrava (ou defere) o build do `WakeEngine` nos planos seguintes.

Purpose: O caminho GraphQL + token da Wake e HIGH confidence **documental** mas **NAO** testado empiricamente (Blocker registrado em STATE.md e suposicoes A1-A6 em 32-RESEARCH.md). Construir o engine completo sobre um caminho nao-provado e proibido por D-03. Este spike confirma o caminho fim-a-fim antes de qualquer codigo de engine, atendendo COMP-04 SC-1.

Output: `experiment.py` (isolado, reproduzivel, fora de `backend/` por D-04) + `REPORT.md` com veredito GO/NO-GO e evidencia.

**Regra de gate (trava esta phase):** Se o veredito for **GO** (>=1 produto com titulo+URL+preco via GraphQL+token, por D-02), os planos 32-02 e 32-03 executam normalmente. Se for **NO-GO**, **para no gate**: registra-se o veredito, o `WakeEngine` e deferido para uma phase de follow-up (D-03), e os planos 32-02/32-03 **NAO sao executados** nesta phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-engine-wake-commerce-richards/32-CONTEXT.md
@.planning/phases/32-engine-wake-commerce-richards/32-RESEARCH.md
@.planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md
@.planning/spikes/CONVENTIONS.md
</context>

<artifacts_this_phase_produces>
> Lista de TODOS os simbolos/arquivos criados nesta phase (usada pelo source-grounding do plan-review-convergence para excluir simbolos novos da verificacao de drift). Esta phase como um todo cria:
> - Arquivo: `.planning/spikes/007-wake-graphql-token-confirmation/experiment.py` (este plano)
> - Arquivo: `.planning/spikes/007-wake-graphql-token-confirmation/REPORT.md` (este plano)
> - Funcao auxiliar do spike: `extract_token_from_html(html: str) -> Optional[str]` (este plano)
> - Constante do spike: `_TOKEN_RE` (regex `storefrontAccessToken`) (este plano)
> - Constante do spike: `GRAPHQL_ENDPOINT = "https://storefront-api.fbits.net/graphql"` (este plano)
> - (Planos 32-02/32-03, condicionais ao GO): classe `WakeEngine(BaseEngine)`, metodo `WakeEngine._resolve_token`, campo de modelo `wake_access_token: Optional[str]`, constantes `GRAPHQL_ENDPOINT`/`DEFAULT_MAX_RESULTS`/`_TOKEN_RE` em `wake_engine.py`, arquivo `backend/services/engines/wake_engine.py`, arquivo `backend/tests/test_wake_engine.py`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Criar experiment.py — auto-extracao do token + POST GraphQL Wake (Richards + fallback Shop2gether)</name>
  <files>.planning/spikes/007-wake-graphql-token-confirmation/experiment.py</files>
  <read_first>
    - .planning/spikes/001-brand-gate-impact/experiment.py (analog de bootstrap: ROOT/sys.path/os.chdir; padrao de saida dupla stdout + REPORT.md)
    - .planning/spikes/CONVENTIONS.md (layout do spike, reuso de servicos reais, sem deps novas)
    - backend/core/session_manager.py (SessionManager.get_session() — aiohttp compartilhado; reusar para GET do HTML e POST do GraphQL)
    - .planning/phases/32-engine-wake-commerce-richards/32-RESEARCH.md §"Padrao 4" (query GraphQL `WakeSearch($q,$first)`), §"Padrao 5" (regex `_TOKEN_RE`), §"Log de Suposicoes" (A1-A6), §"Convencao de Spikes"
  </read_first>
  <action>
    Criar o script isolado `experiment.py` em `.planning/spikes/007-wake-graphql-token-confirmation/`. Bootstrap exatamente como o analog 001: `ROOT = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "..", ".."))`, `sys.path.insert(0, ROOT)`, `os.chdir(ROOT)` — confirmar que ROOT aponta para a raiz do repo (007 fica em `.planning/spikes/007-.../`, 3 niveis acima). O script e assincrono: usar `asyncio.run(main())` no `if __name__ == "__main__"`.

    Definir constantes de modulo: `GRAPHQL_ENDPOINT = "https://storefront-api.fbits.net/graphql"` e `_TOKEN_RE = re.compile(r"""storefrontAccessToken\s*:\s*['"]([^'"]+)['"]""", re.IGNORECASE)`. Definir `extract_token_from_html(html: str) -> Optional[str]` que aplica `_TOKEN_RE.search(html)` e retorna `match.group(1)` ou `None`. Incluir a estrategia de fallback descrita no RESEARCH (Padrao 5 "ALTERNATIVA"): se `storefrontAccessToken` nao for encontrado, tentar regex `tcs_[a-zA-Z0-9_]+` em qualquer `<script>` inline.

    Alvos: lista `[("Richards", "www.richards.com.br"), ("Shop2gether", "www.shop2gether.com.br")]` — Richards primario, Shop2gether fallback (D-01). Para cada alvo, em sequencia ate o primeiro GO:
    1. Reusar `session = await SessionManager.get_session()`. GET `https://{domain}` com `allow_redirects=False` (mitigacao T-32 Open Redirect — mesmo padrao de `routes_brands.py:44`). Extrair o token via `extract_token_from_html`.
    2. Se token encontrado, montar o body GraphQL usando a query `WakeSearch($q: String!, $first: Int!)` do RESEARCH Padrao 4 (campos `productName`, `aliasComplete`, `prices { price listPrice }`, `images { url }`, `available`). Usar **variaveis GraphQL** (`variables: {"q": "camisa", "first": 5}`), nunca interpolacao de string (mitigacao Tampering / query injection). POST para `GRAPHQL_ENDPOINT` com header `TCS-Access-Token: <token>` via `session.post(url, json={"query": ..., "variables": ...}, headers=...)`.
    3. Parsear a resposta JSON: extrair os nos de `data.search.products.edges[].node`. Registrar para CADA suposicao A1-A6: A1 (token extraido funciona no header — resposta retorna produtos), A2 (`aliasComplete` presente dentro de `search`), A3 (`images.url` presente dentro de `search`), A4 (formato de `prices.price` — float em reais vs centavos), A5 (Richards usa o padrao `clientConfig.storefrontAccessToken`), A6 (busca nao exige reCAPTCHA/sessao).

    Saida dupla: imprimir resumo no stdout E reservar os dados estruturados para o REPORT.md (Task 2 ou o proprio script escreve). NAO imprimir o token completo em texto claro — mascarar (ex.: primeiros 8 chars + `...`) no stdout e no REPORT, registrando apenas o prefixo observado (ex.: `tcs_loja_...`). Sem dependencias novas — usar aiohttp (via SessionManager), `re`, e opcionalmente `beautifulsoup4` (ja no projeto) para parsing HTML.

    Implementa o gate D-01/D-02; confirma empiricamente as suposicoes ASSUMED A1-A6 (per 32-RESEARCH §"Log de Suposicoes").
  </action>
  <acceptance_criteria>
    - `experiment.py` existe em `.planning/spikes/007-wake-graphql-token-confirmation/` e contem a string literal `storefront-api.fbits.net/graphql`
    - `experiment.py` contem `allow_redirects=False` no GET do HTML (mitigacao Open Redirect)
    - `experiment.py` usa variaveis GraphQL (`variables`/`$q`) e NAO interpolacao de string na query (mitigacao Tampering) — verificar ausencia de f-string montando o corpo `search(query: "..."` com o termo do usuario
    - `experiment.py` reusa `SessionManager.get_session()` (nao instancia `aiohttp.ClientSession` propria)
    - `experiment.py` NAO imprime o token completo em texto claro (mascara aplicada)
    - `python -c "import ast; ast.parse(open(r'.planning/spikes/007-wake-graphql-token-confirmation/experiment.py', encoding='utf-8').read())"` sai 0 (sintaxe valida)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import ast,io; src=io.open('.planning/spikes/007-wake-graphql-token-confirmation/experiment.py',encoding='utf-8').read(); ast.parse(src); assert 'storefront-api.fbits.net/graphql' in src; assert 'allow_redirects=False' in src; assert 'TCS-Access-Token' in src; print('OK')"</automated>
  </verify>
  <done>experiment.py existe, sintaticamente valido, bate no endpoint GraphQL com header TCS-Access-Token via variaveis GraphQL, faz GET com allow_redirects=False, e mascara o token no output.</done>
</task>

<task type="auto">
  <name>Task 2: Rodar o spike e gerar REPORT.md com veredito GO/NO-GO + evidencia</name>
  <files>.planning/spikes/007-wake-graphql-token-confirmation/REPORT.md</files>
  <read_first>
    - .planning/spikes/007-wake-graphql-token-confirmation/experiment.py (o script a executar — criado na Task 1)
    - .planning/phases/32-engine-wake-commerce-richards/32-RESEARCH.md §"Formato do REPORT.md (veredito GO/NO-GO)" (template canonico do REPORT)
    - .planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md §"REPORT.md (doc)" (formato canonico verificado contra spikes 003-006)
  </read_first>
  <action>
    Executar `python .planning/spikes/007-wake-graphql-token-confirmation/experiment.py` a partir da raiz do repo. Capturar a evidencia real e escrever (ou completar) o `REPORT.md` seguindo o template canonico do RESEARCH §"Formato do REPORT.md": secoes `## Veredito` (**GO** ou **NO-GO** — D-02: GO exige >=1 produto com titulo+URL+preco via GraphQL+token), `## Evidencia` (endpoint, header com token mascarado + URL de origem, query, resposta com >=1 produto), `## Campos confirmados` (tabela productName/aliasComplete/prices.price/images.url/available — sim/nao + valor exemplo, resolvendo A2/A3), `## Formato do preco` (float em reais vs centavos — resolve A4), `## Token auto-extraido` (estrategia, URL de origem, prefixo observado — resolve A1/A5; NUNCA o token completo), `## Alvo testado` (checkbox Richards primario / Shop2gether fallback).

    O veredito deve ser **explicito e inequivoco** na primeira linha apos `## Veredito` — o gate desta phase depende disto: GO -> planos 32-02/32-03 executam; NO-GO -> para no gate, `WakeEngine` deferido (D-03). Se o spike der NO-GO (ex.: 401/403, 0 produtos, token nao extraivel em nenhum dos dois alvos), registrar o motivo concreto e a suposicao A1-A6 que falhou.

    Registra o veredito do gate (D-04) e resolve as suposicoes A1-A6 (per 32-RESEARCH §"Log de Suposicoes"). Atende COMP-04 SC-1.
  </action>
  <acceptance_criteria>
    - `REPORT.md` existe em `.planning/spikes/007-wake-graphql-token-confirmation/`
    - `REPORT.md` contem uma secao `## Veredito` com a string `GO` ou `NO-GO` de forma explicita
    - `REPORT.md` contem as secoes `## Evidencia`, `## Campos confirmados`, `## Formato do preco`, `## Token auto-extraido`, `## Alvo testado`
    - `REPORT.md` NAO contem o token completo em texto claro (apenas prefixo/mascara)
    - Em caso de GO: a secao Evidencia lista >=1 produto com productName + aliasComplete (ou URL) + prices.price
  </acceptance_criteria>
  <verify>
    <automated>python -c "import io; t=io.open('.planning/spikes/007-wake-graphql-token-confirmation/REPORT.md',encoding='utf-8').read(); assert '## Veredito' in t; assert ('GO' in t or 'NO-GO' in t); assert '## Campos confirmados' in t; print('OK')"</automated>
  </verify>
  <done>REPORT.md existe com veredito explicito GO/NO-GO, evidencia estruturada (produto/token mascarado/endpoint/campos confirmados) e resolve as suposicoes A1-A6. Este e o gate que destrava ou defere os planos 32-02/32-03.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (GATE): Revisar o veredito GO/NO-GO do REPORT.md e decidir se 32-02/32-03 executam</name>
  <action>Verificacao humana do gate (sem implementacao automatizada nesta tarefa). O operador inspeciona o `REPORT.md` produzido na Task 2 e decide GO/NO-GO (D-03) — ver how-to-verify para os passos exatos. Em GO, 32-02/32-03 executam; em NO-GO, o `WakeEngine` e deferido e 32-02/32-03 nao executam.</action>
  <what-built>O spike `experiment.py` foi executado contra a Richards (e/ou Shop2gether) e o `REPORT.md` foi gerado com um veredito GO/NO-GO. Esta e a unica verificacao manual da phase (confirmacao empirica contra API externa viva da Wake — nao mockavel, e o ponto do gate, per 32-VALIDATION §"Manual-Only Verifications").</what-built>
  <how-to-verify>
    1. Abrir `.planning/spikes/007-wake-graphql-token-confirmation/REPORT.md`.
    2. Confirmar o veredito na secao `## Veredito`:
       - **GO** exige (D-02): >=1 produto real com **titulo + URL + preco** retornado via GraphQL quando o header `TCS-Access-Token` foi enviado. Conferir que a secao `## Evidencia` mostra o produto e que `## Campos confirmados` marca productName/aliasComplete/prices.price como presentes.
       - **NO-GO**: o REPORT registra o motivo (ex.: 401/403, 0 produtos, token nao extraivel) e qual suposicao A1-A6 falhou.
    3. Conferir que o token NAO aparece em texto claro (apenas prefixo `tcs_loja_...` ou mascara).
    4. Decidir o gate (D-03):
       - Se **GO**: aprovar para que os planos 32-02 (engine) e 32-03 (testes) executem.
       - Se **NO-GO**: o `WakeEngine` e deferido para phase de follow-up; os planos 32-02/32-03 NAO executam nesta phase. Registrar a decisao.
  </how-to-verify>
  <verify>
    <human-check>Veredito GO/NO-GO confirmado no REPORT.md; em GO, >=1 produto com titulo+URL+preco evidenciado; token mascarado. Decisao de gate registrada (D-03).</human-check>
  </verify>
  <done>Veredito do gate revisado e decisao registrada: GO libera 32-02/32-03; NO-GO defere o WakeEngine (D-03).</done>
  <resume-signal>Digite "GO — aprovado" (libera 32-02/32-03) ou "NO-GO — deferir engine" (para no gate, defere WakeEngine per D-03), ou descreva o problema observado no REPORT.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spike -> storefront Wake (home page) | GET HTTP externo para extrair o token publico do HTML inline |
| spike -> storefront-api.fbits.net (GraphQL) | POST HTTP externo com o termo de busca e o header TCS-Access-Token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Elevation of Privilege | GET da home page para extracao do token (`experiment.py`) | mitigate | `allow_redirects=False` no aiohttp GET — mesmo padrao de T-25-01-SR ja aplicado em `routes_brands.py:44`; evita seguir redirect aberto |
| T-32-02 | Tampering | query GraphQL com o termo de busca (`experiment.py`) | mitigate | Usar variaveis GraphQL (`$q: String!`, `variables={"q": ...}`) — nunca interpolacao de string; o sanitamento server-side e responsabilidade da Wake |
| T-32-03 | Information Disclosure | token de storefront no stdout / REPORT.md | mitigate | Token e publico de storefront (entregue no HTML a qualquer visitante), mas o REPORT/stdout mascaram o valor (apenas prefixo `tcs_loja_...`) para nao normalizar log de credenciais |
| T-32-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo instalado nesta phase (32-RESEARCH §"Auditoria de Legitimidade": aiohttp/pydantic/pytest/bs4/pytest-asyncio ja instalados, slopcheck [OK]). Sem [ASSUMED]/[SUS]/[SLOP]. |
</threat_model>

<verification>
- `experiment.py` valido e executavel da raiz (`python .planning/spikes/007-wake-graphql-token-confirmation/experiment.py`).
- `REPORT.md` com veredito GO/NO-GO explicito (COMP-04 SC-1).
- Token mascarado em todo output.
- Sem dependencias novas instaladas.
</verification>

<success_criteria>
- COMP-04 SC-1 atendido: spike de confirmacao produz veredito GO/NO-GO registrado **antes** de qualquer codigo de engine (D-01/D-02/D-03/D-04).
- O veredito governa a execucao dos planos 32-02/32-03 (GO = executam; NO-GO = deferidos).
- Suposicoes A1-A6 do 32-RESEARCH resolvidas no REPORT.
</success_criteria>

<output>
Create `.planning/phases/32-engine-wake-commerce-richards/32-01-SUMMARY.md` when done. Registrar o veredito (GO/NO-GO) de forma proeminente no SUMMARY — ele determina se 32-02/32-03 executam.
</output>
