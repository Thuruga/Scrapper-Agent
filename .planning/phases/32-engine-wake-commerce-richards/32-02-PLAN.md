---
phase: 32-engine-wake-commerce-richards
plan: 02
type: execute
wave: 1
depends_on: ["32-01"]
files_modified:
  - backend/core/models.py
  - backend/services/engines/wake_engine.py
  - backend/services/engines/factory.py
autonomous: true
requirements: [COMP-04]
must_haves:
  truths:
    - "EXECUTA SOMENTE SE o veredito do spike 32-01 (REPORT.md) for GO — caso NO-GO, este plano NAO executa e o WakeEngine fica deferido (D-03)"
    - "Uma marca com engine='wake' resolve para um WakeEngine via EngineFactory.get_engine — nunca mais NotImplementedError (SC-3, D-09)"
    - "WakeEngine.search retorna um BrandSearchResult com >=1 produto real (titulo+URL+preco) via GraphQL Wake, nunca via o caminho VTEX (SC-2, D-10/D-11)"
    - "O TCS-Access-Token e resolvido POR LOJA (override da marca -> cache -> auto-extracao do storefront) e enviado em cada request GraphQL (SC-4, D-05/D-06)"
    - "Token nao resolvido levanta ValueError claro (capturado por _search_one como BrandSearchResult.error) — nunca 0 produtos silenciosos (SC-4, D-07)"
    - "calculate_shipping retorna None; discover_categories/get_catalog retornam [] sem crash (D-08)"
    - "wake_access_token e campo opcional em DynamicBrandCreate/DynamicBrand sem quebrar marcas existentes (D-06)"
  artifacts:
    - path: "backend/core/models.py"
      provides: "Campo opcional wake_access_token em DynamicBrandCreate (herdado por DynamicBrand)"
      contains: "wake_access_token"
    - path: "backend/services/engines/wake_engine.py"
      provides: "WakeEngine(BaseEngine) — busca GraphQL Wake + resolucao de token por loja + stubs graciosos"
      contains: "class WakeEngine(BaseEngine)"
      min_lines: 120
    - path: "backend/services/engines/factory.py"
      provides: "Wiring: engine_type=='wake' instancia WakeEngine (import lazy), substitui NotImplementedError"
      contains: "WakeEngine"
  key_links:
    - from: "backend/services/engines/factory.py"
      to: "backend/services/engines/wake_engine.py"
      via: "import lazy dentro de get_engine para engine_type=='wake'"
      pattern: "from services.engines.wake_engine import WakeEngine"
    - from: "backend/services/engines/wake_engine.py"
      to: "https://storefront-api.fbits.net/graphql"
      via: "SessionManager.get_session() POST aiohttp com header TCS-Access-Token e variaveis GraphQL"
      pattern: "TCS-Access-Token"
    - from: "backend/services/engines/wake_engine.py"
      to: "BaseEngine.validate_single / filter_mens_fashion"
      via: "Quality Gates + filtro CAT-01 antes do retorno"
      pattern: "validate_single|filter_mens_fashion"
---

<objective>
Wave 1 — Build do `WakeEngine`. Adicionar o campo opcional `wake_access_token` ao modelo de marca, implementar `backend/services/engines/wake_engine.py` (busca real via GraphQL Wake + resolucao de token por loja + stubs graciosos) e plugar o engine na `EngineFactory` substituindo o guard `NotImplementedError`. Atende COMP-04 SC-2, SC-3, SC-4.

Purpose: Com a Richards rotulada `wake` (Phase 30) mas o engine ainda lancando `NotImplementedError` (`factory.py:57-60`), buscas para lojas Wake falham. Este plano entrega o caminho GraphQL real — confirmado empiricamente pelo spike 32-01 — para que o operador busque produtos da Richards via a API publica da Wake, nao via o caminho VTEX (que retorna 0 produtos para lojas Wake).

Output: `models.py` com `wake_access_token` opcional; `wake_engine.py` com `WakeEngine`; `factory.py` com o branch `wake` ativo.

**PRECONDICAO DE GATE (D-01/D-03):** Este plano executa **SOMENTE SE** o `REPORT.md` do spike 32-01 (Wave 0) registrar veredito **GO**. Se o veredito for **NO-GO**, este plano **NAO** deve ser executado — o `WakeEngine` e deferido para uma phase de follow-up (D-03). O executor DEVE ler `.planning/spikes/007-wake-graphql-token-confirmation/REPORT.md` como primeira acao e abortar com mensagem clara se o veredito nao for GO. Alem disso, o spike pode ter resolvido suposicoes A2/A3/A4 (campos `aliasComplete`/`images` dentro de `search`, formato do preco) de forma diferente do RESEARCH — o parser deve seguir a **evidencia do REPORT**, nao a query assumida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-engine-wake-commerce-richards/32-CONTEXT.md
@.planning/phases/32-engine-wake-commerce-richards/32-RESEARCH.md
@.planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md
@.planning/spikes/007-wake-graphql-token-confirmation/REPORT.md
</context>

<artifacts_this_phase_produces>
> Simbolos/arquivos criados por ESTE plano (excluir do drift-check de simbolos novos):
> - Arquivo: `backend/services/engines/wake_engine.py`
> - Classe: `WakeEngine(BaseEngine)` em `wake_engine.py`
> - Metodos: `WakeEngine.__init__`, `WakeEngine.get_engine_name`, `WakeEngine.search`, `WakeEngine.calculate_shipping`, `WakeEngine.discover_categories`, `WakeEngine.get_catalog`, `WakeEngine.run_bulk_scrape`, `WakeEngine.get_product_details`, `WakeEngine._resolve_token`
> - Constantes em `wake_engine.py`: `GRAPHQL_ENDPOINT`, `DEFAULT_MAX_RESULTS`, `_TOKEN_RE`
> - Atributo de instancia: `WakeEngine._token_cache`
> - Campo de modelo: `wake_access_token: Optional[str]` em `DynamicBrandCreate` (herdado por `DynamicBrand`) em `backend/core/models.py`
> - Branch novo em `factory.py`: `if engine_type == "wake": ... return WakeEngine(brand_key)`
> (Spike de 32-01 e testes de 32-03 listados nos respectivos planos.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adicionar campo opcional wake_access_token ao modelo de marca (D-06)</name>
  <files>backend/core/models.py</files>
  <read_first>
    - backend/core/models.py (L207-233 — DynamicBrandCreate e DynamicBrand; campos opcionais existentes review_provider/review_store_id/vtex_account/engine/logo_url como padrao)
    - .planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md §"backend/core/models.py — MODIFICACAO"
  </read_first>
  <behavior>
    - DynamicBrandCreate aceita wake_access_token ausente (default None) — `DynamicBrandCreate(brand_key="x", brand_name="X", domain="x.com")` valida sem erro
    - DynamicBrandCreate aceita wake_access_token explicito — `DynamicBrandCreate(..., wake_access_token="tcs_loja_abc")` preserva o valor
    - DynamicBrand (subclasse) herda o campo sem alteracao adicional
    - Marcas existentes em data/brands.json (sem o campo) continuam validas
  </behavior>
  <action>
    Em `backend/core/models.py`, adicionar uma unica linha em `DynamicBrandCreate` logo apos `logo_url` (L225), espelhando os campos opcionais ja existentes (`vtex_account`/`review_store_id`): `wake_access_token: Optional[str] = None` com comentario indicando que e o override manual do token publico de storefront Wake (D-06). NAO alterar `DynamicBrand` — ele e subclasse de `DynamicBrandCreate` e herda o campo automaticamente. NAO mexer em nenhum outro campo nem no `field_validator` de `domain`. Campo `Optional` com default `None` garante que marcas existentes sem o campo continuam validas.

    Implementa D-06 (campo opcional por loja para o override do TCS-Access-Token).
  </action>
  <acceptance_criteria>
    - `grep -n "wake_access_token" backend/core/models.py` retorna >=1 linha dentro de DynamicBrandCreate
    - `python -c "from backend.core.models import DynamicBrandCreate; b=DynamicBrandCreate(brand_key='x',brand_name='X',domain='x.com'); assert b.wake_access_token is None; b2=DynamicBrandCreate(brand_key='x',brand_name='X',domain='x.com',wake_access_token='tcs_loja_abc'); assert b2.wake_access_token=='tcs_loja_abc'"` sai 0 (ajustar import path conforme conftest/raiz do projeto)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest backend/tests/test_wake_engine.py::TestWakeModels -q --tb=short 2>/dev/null || python -c "import io; assert 'wake_access_token' in io.open('backend/core/models.py',encoding='utf-8').read(); print('OK')"</automated>
  </verify>
  <done>wake_access_token: Optional[str] = None presente em DynamicBrandCreate; DynamicBrand herda; marcas existentes continuam validas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar WakeEngine — busca GraphQL + resolucao de token por loja + stubs graciosos (D-05/D-07/D-08/D-10/D-11)</name>
  <files>backend/services/engines/wake_engine.py</files>
  <read_first>
    - backend/services/engines/sfcc_engine.py (analog ESTRUTURAL: __init__ fino, get_engine_name, search()->BrandSearchResult, calculate_shipping->None L245-255, stubs de categoria, ordem Quality Gates filter_mens_fashion->validate_single->SearchProductResult)
    - backend/services/engines/shopify_engine.py (analog de TRANSPORTE: engine nao-VTEX via aiohttp HTTP-API, sem browser)
    - backend/services/engines/base_engine.py (contrato BaseEngine: metodos abstratos run_bulk_scrape/discover_categories/get_catalog/search/get_product_details/calculate_shipping/get_engine_name; validate_single L160, validate_and_filter L106, filter_mens_fashion L166)
    - backend/core/session_manager.py (SessionManager.get_session() — aiohttp compartilhado para GET HTML e POST GraphQL)
    - backend/core/models.py (BrandSearchResult, SearchProductResult, RawProductBronze — alvo dos Quality Gates e do retorno)
    - .planning/spikes/007-wake-graphql-token-confirmation/REPORT.md (evidencia empirica: campos confirmados de search, formato do preco, prefixo do token — o parser SEGUE este REPORT)
    - .planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md §"wake_engine.py" (todos os padroes) + §"Padroes Compartilhados" (resolucao brand_key->dominio, Quality Gates)
    - .planning/phases/32-engine-wake-commerce-richards/32-RESEARCH.md §"Padrao 4" (query), §"Padrao 5" (regex token), §"Armadilhas 1-5" (token override vs auto-extract, aliasComplete relativo, images em search, prices.price vs buyBox, cache por instancia)
  </read_first>
  <behavior>
    - search('camisa') com SessionManager mockado retornando o JSON GraphQL -> BrandSearchResult com >=1 SearchProductResult (titulo+url+preco) (SC-2)
    - search() com token resolvivel (override da marca) NAO faz GET de auto-extracao (precedencia override > cache > auto-extract)
    - search() sem token resolvivel (override None E auto-extract falha) -> raise ValueError com mensagem clara contendo o brand_key (SC-4/D-07)
    - calculate_shipping(product, zipcode) -> None (D-08)
    - discover_categories() -> [] ; get_catalog() -> [] (D-08, sem crash)
    - URL do produto montada como https://{domain}/{aliasComplete} (nao apontando para storefront-api.fbits.net) (Armadilha 2)
  </behavior>
  <action>
    Criar `backend/services/engines/wake_engine.py` com `class WakeEngine(BaseEngine)`, espelhando `sfcc_engine.py` para estrutura e `shopify_engine.py` para transporte HTTP. Imports: `from __future__ import annotations`, `logging`, `re`, typing, `from core.models import BrandSearchResult, SearchProductResult`, `from core.session_manager import SessionManager`, `from services.engines.base_engine import BaseEngine`. NAO importar `BrowserManager` (Wake e API pura, sem render — D-11).

    Constantes de modulo: `GRAPHQL_ENDPOINT = "https://storefront-api.fbits.net/graphql"`, `DEFAULT_MAX_RESULTS: int = 10`, `_TOKEN_RE = re.compile(r"""storefrontAccessToken\s*:\s*['"]([^'"]+)['"]""", re.IGNORECASE)`.

    `__init__(self, brand_key: str) -> None`: fino — `self.brand_key = brand_key`; `self._token_cache: Optional[str] = None` (cache POR INSTANCIA, nunca de classe — Armadilha 5). `get_engine_name(self) -> str: return "Wake"`.

    `async def _resolve_token(self, brand=None) -> Optional[str]` com precedencia clara (Armadilha 1): (1) override `brand.wake_access_token` (getattr ou dict.get); (2) `self._token_cache`; (3) auto-extracao — `session = await SessionManager.get_session()`, GET `https://{domain}` com `allow_redirects=False` (mitigacao Open Redirect), aplicar `_TOKEN_RE.search(html)`, cachear em `self._token_cache`; (4) retornar `None`. NUNCA usar variavel de classe para o cache.

    `async def search(self, query, max_results=DEFAULT_MAX_RESULTS, sort=None, only_in_stock=False, zipcode=None, include_shipping=False) -> BrandSearchResult`: resolver brand via `from services.brand_service import brand_service` (lazy) -> `brand_service.get_brand(self.brand_key)`; extrair `domain` e `brand_name` com o padrao getattr/dict de `sfcc_engine.py` L121-145. Chamar `token = await self._resolve_token(brand)`; se `not token`, `raise ValueError(f"Token Wake nao resolvido para '{self.brand_key}'. Configure wake_access_token na marca ou verifique o storefront.")` (D-07 — capturado por `_search_one` como `BrandSearchResult.error`, nunca 0 produtos silenciosos). Montar o body GraphQL com a query `WakeSearch($q: String!, $first: Int!)` usando **variaveis** (`variables={"q": query.strip(), "first": max_results}`) — nunca interpolacao de string (mitigacao Tampering). POST via `session.post(GRAPHQL_ENDPOINT, json={...}, headers={"TCS-Access-Token": token})`. Parsear `data.search.products.edges[].node` conforme os campos CONFIRMADOS no REPORT.md do spike (Armadilhas 2/3/4): montar URL como `f"https://{domain}/{alias.lstrip('/')}"` (NAO apontar para fbits.net), preco em reais via passthrough de float positivo (NAO dividir por 100 se o REPORT confirmou reais), imagem de `images[].url` (ou tratar ausencia conforme REPORT). Aplicar Quality Gates na ordem obrigatoria (PATTERNS §"Quality Gates"): `filter_mens_fashion(parsed_dicts)` (CAT-01) -> `validate_single(p)` por produto -> construir `SearchProductResult(brand=brand_name, product_name=..., url=..., price_full=..., image_url=..., available=...)`. Retornar `BrandSearchResult(brand_key=self.brand_key, brand_name=brand_name, products=validated, total_found=len(validated))`. NUNCA usar o caminho VTEX (anti-padrao do RESEARCH).

    Contrato BaseEngine restante (D-08): `async def calculate_shipping(self, product, zipcode) -> Optional[Dict]: return None` (espelha sfcc_engine.py L245-255 e shopify_engine.py — sem badge "Frete Gratis" indevido); `async def discover_categories(self) -> List[Dict]: return []`; `async def get_catalog(self) -> List[Dict]: return []`; `async def run_bulk_scrape(self, category_url, log_callback=None, cancel_event=None, zipcode=None, include_shipping=False): return` (stub — yield nada); `async def get_product_details(self, product_url) -> Optional[Dict]: return None`. Implementar TODO o contrato para nao levantar TypeError na instanciacao.

    Implementa D-05 (auto-extracao + override por loja), D-07 (erro claro de token), D-08 (stubs + shipping None), D-10 (single-query, sem enriquecimento PDP), D-11 (aiohttp via SessionManager, sem browser). Se o REPORT do spike indicou que a query `products(filters:{search:[...]})` retorna `aliasComplete`/`images` melhor que `search(query:...)`, usar a query confirmada pelo spike.
  </action>
  <acceptance_criteria>
    - `wake_engine.py` contem `class WakeEngine(BaseEngine)` e a string `storefront-api.fbits.net/graphql`
    - `wake_engine.py` contem `return None` em calculate_shipping e `return []` em discover_categories/get_catalog
    - `wake_engine.py` NAO importa BrowserManager (`grep -c "BrowserManager" wake_engine.py` filtrando comentarios == 0)
    - `wake_engine.py` usa `variables` na query GraphQL (sem f-string interpolando `query` no corpo `search(query: "..."`)
    - `python -c "from backend.services.engines.wake_engine import WakeEngine; e=WakeEngine('richards'); assert e.get_engine_name()=='Wake'"` sai 0 (ajustar import path conforme conftest)
    - `pytest backend/tests/test_wake_engine.py::TestWakeEngineSearch::test_search_returns_products -x` sai 0 (apos 32-03 criar os testes; ate la, a verificacao automatizada e a importabilidade + grep)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import io; src=io.open('backend/services/engines/wake_engine.py',encoding='utf-8').read(); assert 'class WakeEngine(BaseEngine)' in src; assert 'storefront-api.fbits.net/graphql' in src; assert 'variables' in src; code=[l for l in src.splitlines() if not l.strip().startswith('#')]; assert sum('BrowserManager' in l for l in code)==0; print('OK')"</automated>
  </verify>
  <done>WakeEngine implementado: search via GraphQL com token por loja, _resolve_token com precedencia override->cache->auto-extract, ValueError claro sem token, calculate_shipping None, stubs []; contrato BaseEngine completo sem TypeError; sem BrowserManager; query via variaveis.</done>
</task>

<task type="auto">
  <name>Task 3: Plugar WakeEngine na EngineFactory — substituir NotImplementedError (D-09, SC-3)</name>
  <files>backend/services/engines/factory.py</files>
  <read_first>
    - backend/services/engines/factory.py (TODO — em especial L42-50 bloco sfcc com import lazy, e L52-60 guard wake NotImplementedError a substituir; _search_one L92-105 que captura o ValueError de token)
    - .planning/phases/32-engine-wake-commerce-richards/32-PATTERNS.md §"factory.py — MODIFICACAO" e §"Import lazy dentro de get_engine"
  </read_first>
  <action>
    Em `backend/services/engines/factory.py`, substituir o bloco `if engine_type == "wake": raise NotImplementedError(...)` (L57-60) por uma copia exata do padrao do bloco `sfcc` (L48-50): `if engine_type == "wake":` seguido de `from services.engines.wake_engine import WakeEngine  # noqa: PLC0415` e `return WakeEngine(brand_key)`. Import LAZY dentro de `get_engine` (mesmo padrao do `SFCCEngine` em L48-50) para preservar seguranca contra import circular. Substituicao cirurgica — NAO alterar nenhuma outra linha de `factory.py` (nem `search_all_brands`, nem `_search_one` L92-105, que ja capturam o `ValueError` de token como `BrandSearchResult.error` por D-07). O branch `wake` fica logo apos o branch `sfcc`. Atualizar/remover o comentario L52-56 "Phase 32 pending" que descrevia o guard antigo.

    Implementa D-09 (factory wiring com import lazy) e SC-3 (engine_type=='wake' retorna WakeEngine, nunca NotImplementedError).
  </action>
  <acceptance_criteria>
    - `factory.py` contem `from services.engines.wake_engine import WakeEngine` e `return WakeEngine(brand_key)`
    - `factory.py` NAO contem mais `raise NotImplementedError` para o branch wake (grep do bloco wake sem NotImplementedError)
    - `pytest backend/tests/test_wake_engine.py::TestWakeFactory::test_factory_returns_wake_engine -x` sai 0 (apos 32-03)
  </acceptance_criteria>
  <verify>
    <automated>python -c "import io; src=io.open('backend/services/engines/factory.py',encoding='utf-8').read(); assert 'from services.engines.wake_engine import WakeEngine' in src; assert 'return WakeEngine(brand_key)' in src; import re; m=re.search(r'engine_type == \"wake\":(.*?)(return VTEXEngine|engine_type ==)', src, re.S); assert m and 'NotImplementedError' not in m.group(1); print('OK')"</automated>
  </verify>
  <done>factory.py instancia WakeEngine(brand_key) com import lazy para engine_type=='wake'; NotImplementedError removido do branch wake; nenhuma outra linha alterada.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador/scheduler -> WakeEngine.search | termo de busca (`query`) atravessa para a GraphQL da Wake |
| WakeEngine -> storefront Wake (home page) | GET HTTP externo para auto-extrair o token publico |
| WakeEngine -> storefront-api.fbits.net (GraphQL) | POST HTTP externo com header TCS-Access-Token por loja |
| data/brands.json -> WakeEngine | override `wake_access_token` (token publico de storefront) lido por loja |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Elevation of Privilege | GET da home page para auto-extracao do token (`WakeEngine._resolve_token`) | mitigate | `allow_redirects=False` no aiohttp GET — mesmo padrao de T-25-01-SR em `routes_brands.py:44` |
| T-32-02 | Tampering | query GraphQL com `query` do operador (`WakeEngine.search`) | mitigate | Variaveis GraphQL (`$q: String!`, `variables={"q": ...}`) — nunca interpolacao de string; sanitamento server-side e da Wake (32-RESEARCH §"Dominio de Seguranca", ASVS V5) |
| T-32-04 | Information Disclosure | token de storefront commitado em `wake_access_token` (brands.json git-tracked) | accept | Aceito por design (D-06): e o token PUBLICO de storefront, entregue no HTML a qualquer visitante — nao e segredo de servidor. Normalmente o campo fica vazio (auto-extract resolve) |
| T-32-05 | Repudiation | token ausente/erro retornando 0 produtos silenciosos (`WakeEngine.search`) | mitigate | `raise ValueError` claro -> capturado por `_search_one` (factory.py:92-105) como `BrandSearchResult.error` (D-07); operador ve o erro explicitamente |
| T-32-06 | Spoofing/Information Disclosure | vazamento de token entre marcas via cache compartilhado (asyncio.gather) | mitigate | Cache como atributo de INSTANCIA (`self._token_cache`), nunca de classe — cada `get_engine(brand_key)` cria nova instancia (Armadilha 5) |
| T-32-SC | Tampering | npm/pip installs | accept | Nenhum pacote novo (32-RESEARCH §"Auditoria de Legitimidade": todos [OK], ja instalados). Sem [ASSUMED]/[SUS]/[SLOP] |
</threat_model>

<verification>
- `python -m pytest backend/tests/ -q` permanece verde (regressao — 225 testes existentes nao quebram; novos testes Wake chegam no 32-03).
- WakeEngine importavel e instanciavel; factory resolve `wake` para WakeEngine.
- Sem BrowserManager no wake_engine.py; query via variaveis GraphQL.
</verification>

<success_criteria>
- COMP-04 SC-2: `WakeEngine.search` retorna >=1 produto real (titulo+URL+preco) via GraphQL Wake (validado por teste em 32-03 com SessionManager mockado).
- COMP-04 SC-3: `EngineFactory.get_engine` para `engine="wake"` retorna `WakeEngine`, nunca `NotImplementedError`.
- COMP-04 SC-4: token por loja em cada request; ausencia/erro -> `BrandSearchResult.error` claro (nunca 0 produtos silenciosos); `calculate_shipping`->None (sem badge "Frete Gratis").
- D-06: `wake_access_token` opcional no modelo sem quebrar marcas existentes.
</success_criteria>

<output>
Create `.planning/phases/32-engine-wake-commerce-richards/32-02-SUMMARY.md` when done.
</output>
