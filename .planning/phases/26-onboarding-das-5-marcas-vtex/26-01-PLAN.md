---
phase: 26-onboarding-das-5-marcas-vtex
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_vtex_brand_onboarding_contract.py
autonomous: true
requirements: [COMP-01]
must_haves:
  truths:
    - "Contract test file exists and is collectable by pytest without ImportError"
    - "Tests assert engine=='vtex', is_active is True, mappings persisted with valid canonical_slug, vtex_fq_path is relative, brand in list_brands(active_only=True)"
    - "Test runs fully offline (no network, no real file/Supabase I/O)"
  artifacts:
    - path: "tests/test_vtex_brand_onboarding_contract.py"
      provides: "Offline deterministic contract test for COMP-01 (D-10b)"
      min_lines: 60
      contains: "class TestBrandContract"
  key_links:
    - from: "tests/test_vtex_brand_onboarding_contract.py"
      to: "services.brand_service.BrandManagerService"
      via: "in-memory __new__ factory, _check_reload + _save mocked"
      pattern: "BrandManagerService.__new__"
    - from: "tests/test_vtex_brand_onboarding_contract.py"
      to: "services.category_mapping.resolve_category_for_brands"
      via: "import + call against in-memory brand_service"
      pattern: "resolve_category_for_brands"
---

<objective>
Create the offline, deterministic contract test that pins down the final state COMP-01 requires after onboarding the 5 VTEX brands. This is the Nyquist Wave 0 scaffold (D-10b): it asserts the post-onboarding contract — `engine="vtex"`, `is_active=True`, `mappings` persisted with valid canonical slugs and relative `vtex_fq_path`, brand visible via `list_brands(active_only=True)`, and `resolve_category_for_brands` produces a valid URL — entirely in memory, with no network and no real I/O.

Purpose: The seed script in plan 26-02 is validated against this contract. Per the live-test rejection in D-10, the per-brand live search smoke (success criterion 1) is manual-only; this test is the automated half of the two-layer verification.
Output: `tests/test_vtex_brand_onboarding_contract.py` with `class TestBrandContract`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-onboarding-das-5-marcas-vtex/26-CONTEXT.md
@.planning/phases/26-onboarding-das-5-marcas-vtex/26-RESEARCH.md
@.planning/phases/26-onboarding-das-5-marcas-vtex/26-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (26-01) creates ONLY the contract test. The full set of symbols introduced by Phase 26 (so the source-grounding drift pass can exclude newly-created identifiers):

- `tests/test_vtex_brand_onboarding_contract.py` — NEW
  - `_make_service_with_vtex_brand(brand_key, engine, is_active, mappings)` — test factory
  - `class TestBrandContract` with methods:
    - `test_engine_is_vtex`
    - `test_brand_is_active`
    - `test_mappings_persisted`
    - `test_brand_in_active_list`
    - `test_vtex_fq_path_is_relative`
    - `test_resolve_category_returns_valid_url`
- `scripts/onboard_vtex_brands.py` — NEW (created in plan 26-02): `BRAND_TABLE`, `AUSTRAL_DOMAIN_CANDIDATES`, `CANONICAL_KEYWORDS`, `normalize()`, `auto_match()`, `onboard_brand()`, `resolve_austral_domain()`, `discover_and_match()`, `print_and_confirm()`, `persist_mappings()`, `main()`

No symbols are added to `api/`, `services/`, or `core/`. `services/category_mapping.py` is NOT edited (D-07).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create offline contract test scaffold for onboarding (COMP-01, D-10b)</name>
  <files>tests/test_vtex_brand_onboarding_contract.py</files>
  <read_first>
    - tests/test_brand_active.py — EXACT analog: copy the `_make_service_with_brands` factory shape (`BrandManagerService.__new__`, `svc.brands={}`, `svc.last_modified=0`, `svc.updated_event=asyncio.Event()`, `svc._check_reload = unittest.mock.MagicMock()`) and the `with unittest.mock.patch.object(svc, "_save")` idiom for write tests
    - services/brand_service.py — confirm public methods: `add_brand`, `set_active`, `update_mappings`, `list_brands(active_only=...)`, `get_brand`; confirm `_save` is the I/O sink to mock
    - core/models.py (lines 195-239) — `CategoryMapping(canonical_slug, vtex_fq_path, label)`, `DynamicBrand`, `DynamicBrandCreate` exact field names
    - services/category_mapping.py (lines 45-118 and 191-250) — `_RAW_CATEGORIES` is a list of dicts each with a `"slug"` key (the canonical slugs are camisas, polos, camisetas, calcas, bermudas, jaquetas, infantil); `resolve_category_for_brands(category_slug, brand_keys)` returns a dict mapping brand_key→{url,path,label} and reads `brand_data.mappings`
    - .planning/phases/26-onboarding-das-5-marcas-vtex/26-PATTERNS.md — test analog snippets (note the correction below before copying verbatim)
  </read_first>
  <action>
    Create tests/test_vtex_brand_onboarding_contract.py mirroring tests/test_brand_active.py structure (no pytest-asyncio; plain class with methods; in-memory service via `BrandManagerService.__new__`).

    CRITICAL CORRECTION to the PATTERNS.md analog: do NOT write `from services.category_mapping import resolve_category_for_brands, CANONICAL_SLUGS`. There is NO `CANONICAL_SLUGS` export in services/category_mapping.py (only `_RAW_CATEGORIES`, a list of dicts) — importing it raises ImportError and the file fails to collect. Import only `resolve_category_for_brands`. Define the valid-slug set inside the test as the literal `VALID_SLUGS = {"camisas", "polos", "camisetas", "calcas", "bermudas", "jaquetas", "infantil"}` (these are the `slug` values from `_RAW_CATEGORIES`), OR derive it via `from services.category_mapping import _RAW_CATEGORIES` then `{c["slug"] for c in _RAW_CATEGORIES}`. Prefer deriving from `_RAW_CATEGORIES` so the set stays in sync with the source of truth (D-04 anchor).

    Write a factory `_make_service_with_vtex_brand(brand_key="levis", engine="vtex", is_active=True, mappings=None)` that builds an in-memory `BrandManagerService` (copy the `__new__` + `_check_reload=MagicMock()` pattern from the analog) and seeds `svc.brands[brand_key]` with a `DynamicBrand(brand_key=..., brand_name="Levi's", domain="www.levi.com.br", engine=engine, is_active=is_active, mappings=mappings or [])`.

    Implement `class TestBrandContract` with these methods (each asserts one truth of COMP-01):
    - `test_engine_is_vtex`: factory with engine="vtex"; assert `svc.get_brand("levis").engine == "vtex"`.
    - `test_brand_is_active`: factory with is_active=True; assert `.is_active is True`.
    - `test_mappings_persisted`: build sample mappings `[CategoryMapping(canonical_slug="calcas", vtex_fq_path="/roupas/jeans", label="Jeans"), CategoryMapping(canonical_slug="polos", vtex_fq_path="/roupas/polos", label="Polos")]`; call `svc.update_mappings("levis", sample)` inside `with unittest.mock.patch.object(svc, "_save")`; assert `len(brand.mappings) > 0` and every `m.canonical_slug in VALID_SLUGS`.
    - `test_brand_in_active_list`: factory is_active=True; assert `"levis" in [b.brand_key for b in svc.list_brands(active_only=True)]`.
    - `test_vtex_fq_path_is_relative`: factory with a mapping whose `vtex_fq_path="/roupas/jeans"`; assert every `m.vtex_fq_path.startswith("/")` (guards Pitfall 3 — FQ `C:/...` would fail). Include the failure message naming the offending value.
    - `test_resolve_category_returns_valid_url`: seed a brand with a mapping for slug "calcas" (`vtex_fq_path="/roupas/jeans"`); monkeypatch `services.category_mapping.brand_service` to the in-memory `svc` (use `unittest.mock.patch.object(category_mapping_module, "brand_service", svc)` after `import services.category_mapping as category_mapping_module`); call `resolve_category_for_brands("calcas", ["levis"])`; assert the returned dict's `"levis"` entry `"url"` equals `"https://www.levi.com.br/roupas/jeans"` (relative path correctly concatenated). This is the key link that proves vtex_fq_path stored as a relative path produces a valid scan URL.

    Use plain `assert` with descriptive messages (project style). No fenced shell, no network, no real file writes.
  </action>
  <verify>
    <automated>python -m pytest tests/test_vtex_brand_onboarding_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_vtex_brand_onboarding_contract.py exists and contains `class TestBrandContract`
    - `python -m pytest tests/test_vtex_brand_onboarding_contract.py -q` collects with ZERO import errors (no `CANONICAL_SLUGS` import)
    - The collected suite contains exactly the 6 named test methods (`test_engine_is_vtex`, `test_brand_is_active`, `test_mappings_persisted`, `test_brand_in_active_list`, `test_vtex_fq_path_is_relative`, `test_resolve_category_returns_valid_url`)
    - All 6 tests PASS (they assert a hand-seeded final state, so they are green at scaffold time and become the executable contract the seed script must satisfy)
    - Test exercises `resolve_category_for_brands` and asserts URL == "https://www.levi.com.br/roupas/jeans" (relative-path concatenation proven)
    - No network call and no real `_save`/`brands.json` write occurs (grep the file: `_save` is only referenced inside a `patch.object(...,"_save")` context; `_check_reload` is a MagicMock)
  </acceptance_criteria>
  <done>tests/test_vtex_brand_onboarding_contract.py exists, collects cleanly, and all 6 contract tests pass offline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → BrandManagerService | In-memory only; no external input crosses into persistence (all I/O sinks mocked) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-IO | Tampering | test touching real brands.json / Supabase | mitigate | `_check_reload` and `_save` are mocked; service built via `__new__` with empty in-memory `svc.brands` — test cannot read or write real persistence (mirrors test_brand_active.py) |
| T-26-01-IMP | Tampering | broken import (`CANONICAL_SLUGS`) silently skipping coverage | mitigate | Acceptance criterion forbids the non-existent import; valid-slug set derived from `_RAW_CATEGORIES` (source of truth, D-04) |
</threat_model>

<verification>
- `python -m pytest tests/test_vtex_brand_onboarding_contract.py -q` → 6 passed, 0 errors
- Full suite still green: `python -m pytest tests/ -q`
</verification>

<success_criteria>
- Contract test file exists, collects without ImportError, and all 6 tests pass offline.
- The test encodes every COMP-01 truth except the manual live smoke (criterion 1, D-10a).
</success_criteria>

<output>
Create `.planning/phases/26-onboarding-das-5-marcas-vtex/26-01-SUMMARY.md` when done.
</output>
