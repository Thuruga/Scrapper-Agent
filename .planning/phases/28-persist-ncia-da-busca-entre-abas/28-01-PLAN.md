---
phase: 28-persist-ncia-da-busca-entre-abas
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
autonomous: true
requirements: [PERS-01]
must_haves:
  truths:
    - "Ao desmontar a CategoryPage (troca de aba durante scrape), o WebSocket é fechado e nenhum log novo aparece no console após a saída"
    - "Nenhum handler do WS chama setState em componente desmontado depois da troca de aba"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "useEffect de cleanup do WebSocket na CategoryPage que fecha wsRef.current no unmount"
      contains: "wsRef.current.onmessage = null"
  key_links:
    - from: "CategoryPage useEffect cleanup"
      to: "wsRef.current"
      via: "onmessage = null + close() + null no unmount"
      pattern: "wsRef\\.current\\.close\\(\\)"
---

<objective>
Adicionar o `useEffect` de cleanup do WebSocket na `CategoryPage` (critério #4 / D-02 / D-09). Hoje o WS só é fechado dentro do handler `ws.onmessage` quando recebe `done`/`error_done` (`App.tsx:476-478`); se o usuário trocar de aba durante o scrape, a `CategoryPage` desmonta, o WS fica aberto e o handler `onmessage` continua chamando `setState` (`setProgress`/`setLogs`/`setIsScraping`) em um componente desmontado.

Esta é a única mudança travada para vir ANTES do store zustand (D-09 / STATE.md [PERS-01]). A `CategoryPage` NÃO persiste estado (D-02) — recebe APENAS o fix de cleanup. O job de scrape continua rodando no backend; o componente apenas abandona o feed ao vivo ao desmontar.

Purpose: Atender o critério #4 de sucesso da phase — cleanup do WS executado ao desmontar, sem conexões pendentes nem logs intercalados após a saída.
Output: Um `useEffect` de cleanup (~5 linhas) na `CategoryPage`.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- **`frontend/src/App.tsx` › `CategoryPage`** — novo `useEffect(() => { return () => { ... } }, [])` que no unmount:
  - seta `wsRef.current.onmessage = null` (previne `setState` pós-unmount)
  - chama `wsRef.current.close()`
  - seta `wsRef.current = null`

Nenhum símbolo novo exportado. Nenhuma nova prop. Nenhuma alteração de assinatura.
</artifacts_produced>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-CONTEXT.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-RESEARCH.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-PATTERNS.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: useEffect de cleanup do WebSocket na CategoryPage</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - `frontend/src/App.tsx` — REVERIFICAR os números de linha antes de editar (monolito grande; linhas da pesquisa são aproximadas). Localizar a `CategoryPage` por `const CategoryPage = ({ brands }` (≈ linha 366). Confirmar: `wsRef` declarado via `useRef<WebSocket | null>(null)` (≈ linha 374); `logEndRef` (≈ linha 376); o único `useEffect` atual da CategoryPage faz `logEndRef.current?.scrollIntoView` com dep `[logs]` (≈ linhas 378-380); `wsRef.current = ws` em `startScrape` (≈ linha 468); handler `ws.onmessage` que chama `setProgress`/`setLogs`/`setIsScraping` (≈ linhas 470-487); fechamento único do WS no branch `msg.type === 'done' || msg.type === 'error_done'` (≈ linha 478).
    - `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-PATTERNS.md` › seção "Fix de Cleanup de WebSocket na CategoryPage" — excerto concreto do `useEffect` de cleanup e posicionamento recomendado.
    - `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-RESEARCH.md` › "Padrão 4: WebSocket Cleanup na CategoryPage" e "Armadilha 5" — racional do `onmessage = null` antes do `close()`.
  </read_first>
  <action>
    Adicionar um `useEffect` de cleanup na `CategoryPage`, posicionado após os `useRef` existentes (`wsRef` e `logEndRef`) e o `useEffect` de scroll já presente, e antes de `fetchBrandCategories` — dentro do corpo do componente, NÃO dentro de `startScrape`.

    O `useEffect` deve usar array de dependências vazio `[]` (executa o cleanup apenas no unmount) e retornar uma função de cleanup que, se `wsRef.current` existir: (1) atribui `wsRef.current.onmessage = null` PRIMEIRO (previne que mensagens em trânsito disparem `setState` após o unmount — ordem importa, ver Armadilha 5 do RESEARCH); (2) chama `wsRef.current.close()`; (3) atribui `wsRef.current = null`.

    NÃO mover o WS para o store, NÃO persistir estado da CategoryPage (D-02) — só o cleanup. NÃO remover nem alterar o fechamento existente dentro de `ws.onmessage` no branch `done`/`error_done` (≈ linha 478) — ele continua válido para o caminho de conclusão normal. NÃO tocar em `AnimatePresence` (D-07). Seguir a forma exata do excerto em PATTERNS.md (a forma é discricionária por D-02/Claude's Discretion, desde que feche `wsRef.current` e zere `onmessage`).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `frontend/src/App.tsx` contém, no corpo da `CategoryPage`, um `useEffect` com dep array `[]` cuja função de retorno faz `wsRef.current.onmessage = null`, `wsRef.current.close()` e `wsRef.current = null` (guardados por `if (wsRef.current)`).
    - Source: a atribuição `wsRef.current.onmessage = null` ocorre ANTES do `wsRef.current.close()`.
    - Source: o branch `msg.type === 'done' || msg.type === 'error_done'` que já chama `ws.close()` permanece intacto (não removido).
    - Build: `cd frontend && npm run build` sai com código 0 (tsc -b + vite build verdes).
    - UAT manual (28-VALIDATION.md › Critério #4): abrir DevTools › Console, ir à aba Categorias, iniciar varredura; com WS ativo (logs aparecendo), navegar para outra aba; verificar que NENHUM log novo aparece no console após a troca e que DevTools › Network › WS mostra a conexão como "Closed".
  </acceptance_criteria>
  <done>O `useEffect` de cleanup existe na CategoryPage, fecha `wsRef.current` no unmount com `onmessage = null` antes do `close()`, o build passa, e o UAT do critério #4 confirma ausência de logs pós-troca-de-aba.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Esta mudança é puramente frontend: um `useEffect` de cleanup de ciclo de vida do componente. Não cria rota, endpoint, autenticação, persistência nem manipula dado sensível. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | (n/a) | CategoryPage WS cleanup | accept | Nenhuma ameaça nova introduzida. O cleanup do WS REDUZ risco de vazamento de recurso (conexão WS órfã) e de `setState` em componente desmontado. RESEARCH.md › Domínio de Segurança conclui: fase puramente de frontend state-management, sem novas rotas/endpoints/auth/dados sensíveis. O WS já existente (`/ws/${jobId}?api_key=...`) não é alterado por esta mudança. |
</threat_model>

<verification>
- `cd frontend && npm run build` verde (type-check + build).
- UAT manual do critério #4 (28-VALIDATION.md › "WS cleanup da CategoryPage"): sem logs novos após troca de aba; WS "Closed" no Network.
</verification>

<success_criteria>
- Critério #4 da phase atendido: cleanup do WS executado ao desmontar a CategoryPage, sem conexões pendentes nem logs intercalados após a saída.
- D-02 respeitado: CategoryPage recebe APENAS o cleanup; estado do scrape NÃO persiste.
- D-09 respeitado: este fix é o prerequisito que vem ANTES do store (Wave 1, sem dependência do store).
</success_criteria>

<output>
Create `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-01-SUMMARY.md` when done
</output>
