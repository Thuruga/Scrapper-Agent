---
phase: 28-persist-ncia-da-busca-entre-abas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/src/api/client.ts
  - frontend/src/stores/searchStore.ts
autonomous: true
requirements: [PERS-01]
must_haves:
  truths:
    - "Um store zustand module-scoped existe e expõe os slices search e cross com todo o estado de D-03"
    - "As actions startSearch / startCrossSearch cancelam o request anterior via AbortController ao iniciar uma nova busca"
    - "Ao concluir uma busca, a action dispara toast.success globalmente (sonner) e ignora AbortError sem notificar erro"
    - "ApiClient.search e ApiClient.crossMarketplaceSearch aceitam um signal?: AbortSignal opcional sem quebrar callers existentes"
  artifacts:
    - path: "frontend/src/stores/searchStore.ts"
      provides: "useSearchStore (zustand create) com slices search + cross, setSearch, setCross, startSearch, startCrossSearch"
      contains: "export const useSearchStore"
      min_lines: 60
    - path: "frontend/src/api/client.ts"
      provides: "request/search/crossMarketplaceSearch com parâmetro opcional signal?: AbortSignal"
      contains: "signal?: AbortSignal"
    - path: "frontend/package.json"
      provides: "dependência zustand"
      contains: "zustand"
  key_links:
    - from: "frontend/src/stores/searchStore.ts startSearch"
      to: "ApiClient.search"
      via: "await ApiClient.search(payload, controller.signal)"
      pattern: "ApiClient\\.search\\(.*signal"
    - from: "frontend/src/stores/searchStore.ts startCrossSearch"
      to: "ApiClient.crossMarketplaceSearch"
      via: "await ApiClient.crossMarketplaceSearch(payload, controller.signal)"
      pattern: "ApiClient\\.crossMarketplaceSearch\\(.*signal"
    - from: "startSearch / startCrossSearch"
      to: "sonner toast.success"
      via: "toast de conclusão global no bloco try após set(results)"
      pattern: "toast\\.success"
---

<objective>
Instalar `zustand@5.0.14` (D-10), adicionar suporte a `AbortSignal` no `ApiClient` (Padrão 2 do RESEARCH) e criar o store `frontend/src/stores/searchStore.ts` — o store zustand module-scoped que sobrevive ao ciclo de vida dos componentes (vive no escopo do módulo JS, fora da árvore React) e é a fonte de verdade do estado das duas buscas persistentes (D-01, D-06).

Este plano NÃO migra os componentes ainda — ele entrega o store pronto e o `signal` no client. A migração das páginas (`SearchPage`/`CrossMarketplacePage`) acontece no Plan 03 (Wave 2), que consome o store criado aqui.

Arquivos disjuntos do Plan 01 (este toca `package.json`/`package-lock.json`/`client.ts`/`searchStore.ts`; o Plan 01 toca apenas `App.tsx`) — por isso ambos rodam na mesma Wave 1.

Purpose: Estabelecer o contrato (store + signal) que desacopla o estado da busca do ciclo de vida do componente, habilitando os critérios #1, #2 e #3.
Output: `zustand` nas dependências, `signal?` no client, e `searchStore.ts` com `useSearchStore`.
</objective>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

**Novo arquivo `frontend/src/stores/searchStore.ts`:**
- `useSearchStore` — hook criado via `create<SearchStoreState>()((set, get) => ({ ... }))` (zustand v5, named import `import { create } from 'zustand'`).
- Slice `search`: campos `query: string`, `sort: string`, `inStock: boolean`, `zipcode: string`, `selectedBrands: string[]`, `results: any | null`, `loading: boolean`, `abortController: AbortController | null`.
- Slice `cross`: campos `targetSku: string`, `zipcode: string`, `results: any | null`, `selectedItems: Set<string>`, `selectionMode: boolean`, `loading: boolean`, `abortController: AbortController | null`.
- Action `setSearch: (patch: Partial<SearchSlice>) => void` — merge raso no slice `search`.
- Action `setCross: (patch: Partial<CrossSlice>) => void` — merge raso no slice `cross`.
- Action `startSearch: (payload) => Promise<void>` — aborta controller anterior, cria novo, set loading=true/results=null, `await ApiClient.search(payload, signal)`, salva results, `toast.success('Busca Comparativa concluída')`; em erro ignora `AbortError` e `toast.error` nos demais.
- Action `startCrossSearch: (payload) => Promise<void>` — análogo via `ApiClient.crossMarketplaceSearch`, `toast.success('Busca por SKU concluída')`.

**Modificação `frontend/src/api/client.ts`:**
- Novo parâmetro opcional `signal?: AbortSignal` em `request<T>`, `search` e `crossMarketplaceSearch`.

**Modificação `frontend/package.json`:**
- `"zustand": "^5.0.14"` em `dependencies`.
</artifacts_produced>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-CONTEXT.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-RESEARCH.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-PATTERNS.md
@.planning/phases/28-persist-ncia-da-busca-entre-abas/28-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar zustand e adicionar signal?: AbortSignal ao ApiClient</name>
  <files>frontend/package.json, frontend/package-lock.json, frontend/src/api/client.ts</files>
  <read_first>
    - `frontend/package.json` — confirmar dependências atuais (sonner ^2.0.7 presente; zustand AUSENTE). Versão alvo confirmada no RESEARCH.md: `5.0.14`.
    - `frontend/src/api/client.ts` — REVERIFICAR linhas antes de editar. Confirmar: `request<T>(endpoint, options = {})` (≈ linhas 21-31) monta headers e chama `fetch(`${API_BASE_URL}${endpoint}`, { ...options, headers })`; `crossMarketplaceSearch(payload)` (≈ linhas 77-82); `search(payload)` (≈ linhas 91-96). NOTA: o método `crossMarketplaceSearch` no código real recebe `{ target_sku; search_query?; broad_query?; min_score?; zipcode? }` — preservar a assinatura de payload existente, apenas ADICIONAR o segundo parâmetro `signal?`.
    - `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-PATTERNS.md` › seção "frontend/src/api/client.ts (adicionar signal?: AbortSignal)" — excerto concreto da modificação mínima.
    - `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-RESEARCH.md` › "Padrão 2: AbortController no ApiClient.request" e "Auditoria de Legitimidade de Pacotes" (zustand aprovado — slopcheck [SLOP] foi falso positivo por confusão PyPI/npm).
  </read_first>
  <action>
    Instalar a dependência rodando `npm install zustand@5.0.14` dentro de `frontend/` (atualiza `package.json` e `package-lock.json`). Usar named import no consumidor (`import { create } from 'zustand'`) — o default export foi removido no v5.

    No `frontend/src/api/client.ts`: adicionar um terceiro parâmetro opcional `signal?: AbortSignal` a `request<T>` e repassá-lo ao `fetch` nativo nas options (espalhar condicionalmente: incluir `signal` apenas quando definido, para não alterar o comportamento de callers que não passam signal — A1 do RESEARCH). Adicionar um parâmetro opcional `signal?: AbortSignal` a `search` e a `crossMarketplaceSearch`, repassando-o como terceiro argumento de `this.request(...)`. NÃO alterar a assinatura do `payload` de nenhum dos dois métodos. Os demais métodos do ApiClient NÃO mudam (parâmetro é opcional → callers existentes continuam funcionando).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `frontend/package.json` lista `zustand` em `dependencies` (≈ `^5.0.14`); `frontend/package-lock.json` resolve zustand 5.0.14.
    - Source: `frontend/src/api/client.ts` › `request<T>` tem assinatura com terceiro parâmetro `signal?: AbortSignal` e o `signal` é repassado ao `fetch` apenas quando definido.
    - Source: `search(payload, signal?: AbortSignal)` e `crossMarketplaceSearch(payload, signal?: AbortSignal)` repassam `signal` a `this.request(...)` como terceiro argumento.
    - Source: nenhuma assinatura de `payload` foi alterada; nenhum outro método do ApiClient foi modificado.
    - Build: `cd frontend && npm run build` sai com código 0.
  </acceptance_criteria>
  <done>zustand@5.0.14 instalado, `signal?: AbortSignal` adicionado a `request`/`search`/`crossMarketplaceSearch` sem quebrar callers, build verde.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar searchStore.ts (zustand store unificado com slices search + cross)</name>
  <files>frontend/src/stores/searchStore.ts</files>
  <read_first>
    - `frontend/src/stores/searchStore.ts` — arquivo NOVO; o diretório `frontend/src/stores/` ainda NÃO existe e deve ser criado (PATTERNS.md › "Pontos de Atenção", item 7). NÃO há análogo de store no projeto — usar o RESEARCH.md como referência primária.
    - `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-RESEARCH.md` › "Padrão 1: Store Unificado com Slices por Aba" — excerto completo do store (tipos `SearchSlice`/`CrossSlice`/`SearchStoreState`, valores iniciais, `setSearch`/`setCross`, `startSearch`/`startCrossSearch` com AbortController, toast e guarda de AbortError). Esta é a referência primária para este arquivo.
    - `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-PATTERNS.md` › seção "frontend/src/stores/searchStore.ts" — convenções do projeto a copiar: comentário `/* eslint-disable @typescript-eslint/no-explicit-any */` no topo; `import { toast } from 'sonner'`; `import { ApiClient } from '../api/client'`; lista exata dos campos que migram de `SearchPage` (App.tsx:831-839) e `CrossMarketplacePage` (App.tsx:1106-1114).
    - `frontend/src/api/client.ts` — confirmar (após Task 1) que `search`/`crossMarketplaceSearch` aceitam `signal`. ATENÇÃO ao payload real de `handleSearch`: Comparativa envia `{ query, sort, only_in_stock: inStock, brands: selectedBrands.length>0 ? selectedBrands : undefined, zipcode: <8díg sanitizado | undefined>, include_shipping: <true se 8díg | undefined> }` (App.tsx:873-880); SKU envia `{ target_sku, zipcode: <8díg | undefined> }` (App.tsx:1249-1252). O store recebe o payload já montado pelo componente.
  </read_first>
  <action>
    Criar `frontend/src/stores/searchStore.ts` com o cabeçalho `/* eslint-disable @typescript-eslint/no-explicit-any */`, importando `create` de `zustand`, `toast` de `sonner` e `ApiClient` de `../api/client`. (Importar `useShallow` de `zustand/react/shallow` é opcional aqui — ele será usado pelos componentes no Plan 03; o store em si não precisa.)

    Definir as interfaces `SearchSlice`, `CrossSlice` e `SearchStoreState` exatamente com os campos listados em D-03 / artifacts_produced. Documentar via comentário que `cross.selectedItems` é `Set<string>` e NÃO é serializável (Armadilha 2 do RESEARCH) — é compatível apenas com store em memória (D-05 proíbe `persist`).

    Exportar `useSearchStore = create<SearchStoreState>()((set, get) => ({ ... }))` com:
    - `search` e `cross` inicializados nos defaults (strings vazias, `sort: 'relevance'`, `inStock: false`, `selectedBrands: []`, `results: null`, `loading: false`, `abortController: null`; `selectedItems: new Set()`, `selectionMode: false`).
    - `setSearch(patch)` / `setCross(patch)` — merge raso (`set((s) => ({ search: { ...s.search, ...patch } }))`).
    - `startSearch(payload)`: abortar `get().search.abortController?.abort()`; criar novo `AbortController`; `set` loading=true/results=null/abortController=controller; `try { const data = await ApiClient.search(payload, controller.signal); set(results, loading=false, abortController=null); toast.success('Busca Comparativa concluída') } catch (err) { if (err.name === 'AbortError') return; set(loading=false, abortController=null); toast.error('Erro na busca: ' + err.message) }`.
    - `startCrossSearch(payload)`: análogo via `ApiClient.crossMarketplaceSearch`; ao iniciar, também zerar `selectedItems: new Set()` e `selectionMode: false` (espelha o reset que `handleSearch` da SKU faz hoje em App.tsx:1246-1247); toast de sucesso `'Busca por SKU concluída'`.

    O AbortController vive SEMPRE dentro da action (não nos componentes) — Padrão 2 / Ponto de Atenção 6. O toast é disparado DENTRO da action (não em `useEffect([loading])` no componente) para ser garantidamente global e seguro contra unmount (D-04 + anti-padrão do RESEARCH). NÃO adicionar middleware `persist` (D-05). NÃO converter para polling/job assíncrono (D-08).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Source: `frontend/src/stores/searchStore.ts` existe e começa com `/* eslint-disable @typescript-eslint/no-explicit-any */`.
    - Source: exporta `useSearchStore` criado via `create<SearchStoreState>()(...)` com named import `import { create } from 'zustand'` (NÃO default import).
    - Source: o estado tem os slices `search` e `cross` com todos os campos de D-03 (incl. `abortController: AbortController | null` em cada slice e `selectedItems: Set<string>` no cross).
    - Source: `startSearch` chama `get().search.abortController?.abort()`, cria `new AbortController()`, e `await ApiClient.search(payload, controller.signal)`.
    - Source: `startCrossSearch` chama `await ApiClient.crossMarketplaceSearch(payload, controller.signal)` e zera `selectedItems`/`selectionMode` ao iniciar.
    - Source: ambas as actions têm `if (err.name === 'AbortError') return;` antes de qualquer `toast.error` (Armadilha 3).
    - Source: ambas as actions chamam `toast.success(...)` no bloco try após salvar os results (D-04).
    - Source: NÃO há import de `persist` nem de `zustand/middleware` (D-05).
    - Build: `cd frontend && npm run build` sai com código 0.
  </acceptance_criteria>
  <done>`searchStore.ts` existe, exporta `useSearchStore` com slices search+cross, actions com AbortController + toast.success + guarda de AbortError, sem middleware persist, build verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → API (`/search`, `/search/cross-marketplace`) | Fronteira já existente; este plano apenas passa um `AbortSignal` opcional ao mesmo `fetch` com o mesmo header `X-API-Key`. Nenhuma nova fronteira de confiança é criada. |
| dependência externa (npm: zustand) | Instalação de pacote — coberto pelo gate de legitimidade abaixo. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-SC | Tampering | npm install zustand@5.0.14 | accept | RESEARCH.md › "Auditoria de Legitimidade de Pacotes" verificou manualmente no registry npm correto: zustand criado 2019, mantido por pmndrs (github.com/pmndrs/zustand), `time.modified` 2026-05-28, downloads altíssimos. O veredicto `[SLOP]` do slopcheck foi FALSO POSITIVO por confusão de ecossistema (verificou PyPI/Python em vez de npm). Pacote legítimo e amplamente adotado — disposição: aceitar. Versão fixada `5.0.14`. |
| T-28-02 | Information Disclosure | store em memória (`useSearchStore`) | accept | O store é module-scoped e SOMENTE em memória (D-05) — não persiste em `localStorage`/`sessionStorage`, logo não expõe `ComparisonResult` (potencialmente grande) a storage do navegador. `selectedItems` é `Set<string>` não serializável, reforçando a ausência de persistência. Zera em reload. Nenhum dado sensível novo. |
| T-28-03 | (n/a) | AbortController nas actions | accept | O `AbortController` apenas cancela o fetch em rede; não expõe estado sensível (RESEARCH.md › Domínio de Segurança). Cancelamento intencional (`AbortError`) é silenciado para não vazar ruído ao usuário. |

> RESEARCH.md › Domínio de Segurança: fase puramente de frontend state-management — sem novas rotas, endpoints, autenticação ou dados sensíveis. Categorias ASVS V2-V6 não se aplicam.
</threat_model>

<verification>
- `cd frontend && npm run build` verde após cada task (type-check + build como porta de fumaça — 28-VALIDATION.md).
- Não há suíte automatizada no frontend; a validação comportamental do store ocorre via UAT no Plan 03 (após a migração dos componentes que consomem o store).
</verification>

<success_criteria>
- zustand@5.0.14 nas dependências (D-10), import named `create` (v5).
- `ApiClient.request/search/crossMarketplaceSearch` aceitam `signal?: AbortSignal` sem quebrar callers (Padrão 2 / A1).
- `useSearchStore` existe com slices `search` + `cross` (D-03), actions com AbortController (cancelamento — critério #3) e toast.success global (D-04 — critério #2).
- D-05 respeitado: store apenas em memória, sem `persist`. D-06: zustand module-scoped. D-08: busca permanece síncrona.
</success_criteria>

<output>
Create `.planning/phases/28-persist-ncia-da-busca-entre-abas/28-02-SUMMARY.md` when done
</output>
