---
phase: quick-260624-d65
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.tsx
autonomous: true
requirements: [QUICK-260624-d65]
must_haves:
  truths:
    - "The brands screen no longer shows a 'Cadastrar Nova Marca' create form"
    - "The brands screen is framed as managing brands (title 'Gerenciar Marcas'), not adding them"
    - "Each brand in the list still has delete (Trash2) and enable/disable (Power) actions that work"
    - "The remaining brand-management card uses the full page width (not squeezed to half)"
    - "frontend type-check/build passes with no dangling references to removed symbols"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "SettingsPage reframed as manage-brands only; create form and its dead code removed"
      contains: "Gerenciar Marcas"
  key_links:
    - from: "SettingsPage brand list delete button"
      to: "ApiClient.deleteBrand"
      via: "handleDeleteBrand onClick"
      pattern: "handleDeleteBrand"
    - from: "SettingsPage brand list toggle button"
      to: "ApiClient.setBrandActive"
      via: "handleToggleActive onClick"
      pattern: "handleToggleActive"
---

<objective>
On the brands screen (the `settings` tab, component `SettingsPage` in `frontend/src/App.tsx`), remove the "Cadastrar Nova Marca" create form and reframe the screen as "Gerenciar Marcas". The per-brand actions DELETE (apagar) and DISABLE/ENABLE (desativar/ativar) already exist in the brand list and must remain working.

Purpose: The screen should be a management surface, not a creation surface. Removing the form prevents new brand registration from this UI while keeping the existing delete/disable controls.

Output: Updated `frontend/src/App.tsx` — form card removed, dead code cleaned up, layout widened to single column, page header and nav reframed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@frontend/src/App.tsx

Project conventions (from .claude/CLAUDE.md): Conventional Commits with scope, Clean Code, NO over-engineering. This is a deletion + rename + dead-code cleanup task only. Do NOT add new features, confirmation modals, or refactors beyond what is required. The existing `confirm(...)` for delete and the existing Power toggle are sufficient.

Verified facts (scouted; re-confirm line numbers before editing — the file is large and may shift):
- `SettingsPage` spans ~1837-1978. It renders a `<div className="grid-2">` (~1881) containing TWO `<GlassCard>`s:
  - "Cadastrar Nova Marca" (~1882-1923): the create `<form onSubmit={handleSubmit}>`. REMOVE this entire card.
  - "Marcas Cadastradas" (~1925-1974): the brand list with Power toggle (`handleToggleActive`) and Trash2 delete (`handleDeleteBrand`). KEEP. Rename its title.
- Dead-code analysis (confirmed via grep within the component range 1837-1978):
  - `newBrand`/`setNewBrand` (~1838): only used by the form. Remove.
  - `handleSubmit` (~1863-1876) + `ApiClient.saveBrand`: only used by the form. Remove.
  - `loading`/`setLoading` (~1839): only referenced by `handleSubmit` and the form's submit button. After removal it is orphaned. Remove.
  - `status`/`setStatus` (~1840): `setStatus` is still called inside `handleToggleActive` (~1857) which is KEPT, but the `<StatusBanner>` render (~1883) lives only inside the form card. After removing the form, `status` would never render. Resolution: rewire `handleToggleActive`'s error path to use `toast.error(...)` (already imported from 'sonner', established pattern at lines 943/1120/1434/1464), then remove `status`/`setStatus` entirely.
  - `StatusBanner` component (defined ~86) and import `Plus` (~13) and `PlusIcon` alias (~7) are ALSO used elsewhere (StatusBanner at line 261; `Plus` at line 2105; `PlusIcon` at nav 2414). Do NOT remove these definitions/imports — only remove the in-form usages.
- Page header (~2428-2437): the fall-through title currently resolves the settings tab to `'Adicionar Marca'`. Change to `'Gerenciar Marcas'`.
- Nav tab (~2413-2418): `SidebarItem` with `label="Marcas"`, `icon={PlusIcon}`, `onClick={() => navigateTab('settings')}`. Keep `label="Marcas"`. The `PlusIcon` (a plus) implies "add"; swap it for an already-imported icon that fits management — use `Package` (imported line 10). Do NOT add any new dependency or import.
- Backend: NO change. `DELETE /brands/{brand_key}` and `PATCH /brands/{brand_key}/active` exist; `ApiClient.deleteBrand` / `ApiClient.setBrandActive` are already wired.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove create form, clean dead code, reframe to "Gerenciar Marcas"</name>
  <files>frontend/src/App.tsx</files>
  <action>
In component `SettingsPage` (~line 1837):

1. Remove the "Cadastrar Nova Marca" `<GlassCard>` entirely (the card containing `<form onSubmit={handleSubmit}>` and its three inputs + "Cadastrar Marca" button, ~1882-1923), including the `{status && <StatusBanner .../>}` line inside it (~1883).

2. Remove now-orphaned state and handler:
   - `const [newBrand, setNewBrand] = ...` (~1838).
   - `const [loading, setLoading] = ...` (~1839).
   - `handleSubmit` (~1863-1876) — the only caller of `ApiClient.saveBrand`.

3. Rewire `handleToggleActive` (~1852-1859): replace its `setStatus({ type: 'error', message: ... })` error line with `toast.error(...)` carrying the same message text (`Erro ao ${brand.is_active ? 'desativar' : 'ativar'} marca: ${err.message}`). This matches the existing `toast.error` pattern in the file (e.g. lines 1120/1434). `toast` is already imported from 'sonner' (line 38) — do NOT add an import.

4. Remove `const [status, setStatus] = ...` (~1840) now that nothing references it. Do NOT remove the `StatusBanner` component definition (~86) or its other usage (~261). Do NOT remove the `Plus` import (used at ~2105) or the `PlusIcon` alias import line (still used at the nav until Task adjusts it below — keep the import line regardless since `Package` is what we switch to, not removal of the alias; if `PlusIcon` becomes unused after step 6, leave the import as-is to avoid touching shared import block — but prefer removing it ONLY if grep confirms zero remaining `PlusIcon` references).

5. Rename the remaining card title from `"Marcas Cadastradas"` to `"Gerenciar Marcas"` (~1925). Keep the entire brand list, the Power toggle button, and the Trash2 delete button exactly as-is (they already call `handleToggleActive` / `handleDeleteBrand`).

6. Layout: with only one card left, change the wrapper `<div className="grid-2">` (~1881) so the card is full-width single-column. Match how other single-card pages lay out in this file — prefer simply removing the `grid-2` wrapper div so the single `<GlassCard>` sits directly inside `<div className="page-content">`, OR replace `grid-2` with a single-column class already used in the file. Do not invent a new CSS class.

7. Page header (~2428-2437): change the settings-tab fall-through title string from `'Adicionar Marca'` to `'Gerenciar Marcas'`.

8. Nav tab (~2413-2418): keep `label="Marcas"`; change `icon={PlusIcon}` to `icon={Package}` (already imported, line 10). If this leaves `PlusIcon` (the `Plus as PlusIcon` alias on line 7) with zero references, remove that single alias from the import — but ONLY after grep confirms no other usage. Do NOT touch the `Plus` (line 13) import — it is still used at ~2105.

Do not add confirmation modals, new endpoints, or any feature. This is removal + rename + cleanup only.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>
- The "Cadastrar Nova Marca" form card is gone from the rendered `SettingsPage`.
- `newBrand`/`setNewBrand`, `loading`/`setLoading`, `status`/`setStatus`, and `handleSubmit` no longer exist in `SettingsPage`; no reference to `ApiClient.saveBrand` remains.
- `handleToggleActive` reports errors via `toast.error(...)`.
- The remaining card title reads "Gerenciar Marcas"; its brand list with Power toggle and Trash2 delete is unchanged and still wired to `handleToggleActive` / `handleDeleteBrand`.
- The card renders full-width (single column), not in `grid-2`.
- Page header for the settings tab reads "Gerenciar Marcas".
- Nav tab labeled "Marcas" uses the `Package` icon.
- `npm run build` (frontend) completes with no TypeScript errors and no unused-symbol / dangling-reference errors.
  </done>
</task>

</tasks>

<verification>
- Run the frontend build to confirm no dangling references after removing `newBrand`, `loading`, `status`, `handleSubmit`, and the in-form `StatusBanner`/`Plus` usages: `cd frontend && npm run build`.
- Grep `frontend/src/App.tsx` to confirm zero remaining references to `newBrand`, `setNewBrand`, `handleSubmit`, and `saveBrand`.
- Grep to confirm `StatusBanner` is still defined and still used at the other call site (~line 261), and `Plus` is still used (~line 2105) — these must NOT have been removed.
- Confirm `Gerenciar Marcas` appears both as the remaining card title and as the settings-tab header string.
</verification>

<success_criteria>
- Brands screen shows only the management card titled "Gerenciar Marcas" at full width, with no create form.
- Delete (apagar) and disable/enable (desativar/ativar) actions per brand remain functional and unchanged.
- No new features, modals, dependencies, or imports added.
- `cd frontend && npm run build` passes cleanly.
</success_criteria>

<output>
Create `.planning/quick/260624-d65-na-tela-de-adicionar-marcas-retire-cadas/260624-d65-SUMMARY.md` when done.
</output>
