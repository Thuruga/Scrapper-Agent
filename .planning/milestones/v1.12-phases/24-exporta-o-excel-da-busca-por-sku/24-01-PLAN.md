---
phase: 24-exporta-o-excel-da-busca-por-sku
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_export_cross_marketplace.py
autonomous: true
requirements: [EXPORT-04, EXPORT-05, EXPORT-06]
must_haves:
  truths:
    - "A test file tests/test_export_cross_marketplace.py exists and is collected by pytest"
    - "Every backend behavior (10 PT columns, null shipping, booleans, score rounding, formula injection, display order, fidelity, filename, empty→400/422, oversized→422, auth) has a dedicated failing test"
    - "Tests fail with a clear error referencing the missing endpoint/route (RED state), not import or syntax errors"
  artifacts:
    - path: "tests/test_export_cross_marketplace.py"
      provides: "Backend behavior contract for POST /search/cross-marketplace/export (TDD RED scaffold)"
      contains: "class TestExportEndpoint"
      min_lines: 120
  key_links:
    - from: "tests/test_export_cross_marketplace.py"
      to: "app (app.py:68)"
      via: "from app import app + TestClient"
      pattern: "from app import app"
    - from: "tests/test_export_cross_marketplace.py"
      to: "api.auth.verify_api_key"
      via: "app.dependency_overrides"
      pattern: "dependency_overrides\\[verify_api_key\\]"
---

<objective>
Create the Wave 0 test scaffold `tests/test_export_cross_marketplace.py` that fully specifies the behavior of the not-yet-built endpoint `POST /search/cross-marketplace/export`. This is the TDD RED step: every test describes a required behavior and MUST fail (route does not exist yet) — but must fail on a 404/missing-route assertion, NOT on import/collection errors.

Purpose: Lock the backend contract (10 PT columns, null-shipping rule, boolean mapping, score rounding, formula-injection sanitization, display-order sorting, fidelity, filename pattern, empty→400/422, oversized→422, auth 403) before any implementation, so Plan 02 has a precise, executable definition of done. Covers requirements EXPORT-04, EXPORT-05, EXPORT-06 plus security edge cases.

Output: One new test file. No production code is touched in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-RESEARCH.md
@.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-PATTERNS.md
@.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the RED test scaffold tests/test_export_cross_marketplace.py</name>
  <files>tests/test_export_cross_marketplace.py</files>
  <read_first>
    - tests/test_relevance_gates.py (lines 1-16 — class-based pytest structure, pure-function import style; the new file follows this structural template)
    - tests/test_cross_marketplace_service.py (lines 1-60 — existing import conventions and async-safe test patterns)
    - app.py (lines 68-73 — confirm the FastAPI instance is named `app` and importable as `from app import app`)
    - api/auth.py (lines 17-24 — `verify_api_key` raises HTTP 403 when X-API-Key is missing/wrong; this is the auth test's expected status)
    - api/__init__.py (line 23 — `api_router = APIRouter(dependencies=[Depends(verify_api_key)])`; confirms the new route inherits auth)
    - .planning/phases/24-exporta-o-excel-da-busca-por-sku/24-RESEARCH.md (Validation Architecture section, lines 718-810 — full test map, TestClient strategy, Assumptions Log A1/A2/A3)
    - .planning/phases/24-exporta-o-excel-da-busca-por-sku/24-PATTERNS.md (lines 598-712 — concrete test skeleton with ITEM_BASE fixture and example assertions)
  </read_first>
  <behavior>
    - Test test_happy_path: POST a single valid item → 200; openpyxl loads workbook; sheet name is "Busca SKU"; header row (row 1, cols 1-10) equals exactly ["Plataforma","Vendedor","Título","Preço","Frete","Preço Total","Frete Grátis","Score de Match","Similar","URL"].
    - Test test_null_shipping: item with shipping_price=null and is_free_shipping=false → "Frete" cell == "A calcular" and "Preço Total" cell == item price (never 0).
    - Test test_free_shipping: item with is_free_shipping=true → "Frete Grátis" cell == "Sim", "Frete" cell == 0, "Preço Total" == landed_price.
    - Test test_boolean_mapping: is_free_shipping and is_similar render as "Sim"/"Não" strings (not True/False).
    - Test test_score_rounding: final_match_score=87.4 → "Score de Match" cell == 87 (integer, rounded); also assert match_score fallback used when final_match_score==0.
    - Test test_formula_injection: an item whose seller/title/marketplace starts with "=", "+", "-", or "@" → the corresponding cell value begins with a leading single-quote (sanitized).
    - Test test_display_order: three items sent in shuffled order with _display_order 2,0,1 → rows appear in ascending _display_order (0,1,2) in the sheet.
    - Test test_fidelity: every scalar field sent (marketplace, seller, title, price, url) appears unchanged in the corresponding output cell — backend recomputes nothing.
    - Test test_filename: Content-Disposition filename matches regex ^busca_sku_.+_\d{8}_\d{6}\.xlsx$ and the query token portion is derived from search_query (fallback target_sku).
    - Test test_empty_items: POST items=[] → status 400 OR 422 (assert response.status_code in (400, 422)) per CONTEXT decision + Pydantic min_length=1.
    - Test test_oversized_payload: POST 501 items → 422 (Pydantic max_length=500).
    - Test test_auth: remove the dependency override, POST without X-API-Key → 403; restore override afterward.
  </behavior>
  <action>
    Create tests/test_export_cross_marketplace.py following the structural template in 24-PATTERNS.md lines 598-712. Module docstring in Portuguese describing the TDD strategy. Imports: `io`, `re`, `pytest`, `openpyxl`, `from fastapi.testclient import TestClient`, `from app import app`, `from api.auth import verify_api_key`. At module level set `app.dependency_overrides[verify_api_key] = lambda: "test-key"` and `client = TestClient(app)`. Define a module-level `ITEM_BASE` dict with all eleven fields (marketplace, seller, title, price, shipping_price, landed_price, is_free_shipping, final_match_score, match_score, is_similar, url) plus `_display_order`, using realistic values (e.g. "Mercado Livre", "Polo Piquet Aramis", price 199.90, final_match_score 87.4). Build all helper assertions against the route path `/search/cross-marketplace/export`. Implement exactly the twelve test methods named in <behavior> inside a single `class TestExportEndpoint`, plus a `class TestSanitizeHelper` with one test that imports `_sanitize_cell` from `api.routes_search` and asserts a "=SUM(A1)" string becomes "'=SUM(A1)" and a plain string is unchanged. Use the expected PT column header list as a module constant `EXPECTED_HEADERS`. For workbook reads use `openpyxl.load_workbook(io.BytesIO(response.content))` then `wb["Busca SKU"]`. IMPORTANT — this is the RED step: these tests are EXPECTED to fail because the route, models, and `_sanitize_cell` do not exist yet. Verify failures are route-not-found (404) / ImportError on `_sanitize_cell`, NOT collection/syntax errors. If `from app import app` triggers heavy startup side effects that break collection, fall back to the alternative documented in 24-RESEARCH.md Assumptions A1: drop TestClient and instead unit-test the (future) pure helpers `_sanitize_cell` and a `_build_row`-style mapping plus the Pydantic model `CrossMarketplaceExportRequest` directly via `from api.routes_search import ...`; keep the same behavior coverage and the same test method names so Plan 02 can satisfy them. Do NOT write any production code in this plan.
  </action>
  <verify>
    <automated>python -m pytest tests/test_export_cross_marketplace.py -q 2>&1 | grep -v '^#' | grep -Eq 'failed|error' && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_export_cross_marketplace.py exists and is at least 120 lines.
    - `python -m pytest tests/test_export_cross_marketplace.py --collect-only -q` collects ≥ 13 test items with NO collection error (no SyntaxError, no unresolved import other than the intentionally-missing `_sanitize_cell`/route).
    - Running the suite produces failures/errors (RED) referencing the missing route or missing `_sanitize_cell` import — not a green pass.
    - The header-equality assertion in test_happy_path uses the exact 10 PT-BR strings in order: Plataforma, Vendedor, Título, Preço, Frete, Preço Total, Frete Grátis, Score de Match, Similar, URL.
    - test_empty_items asserts status in (400, 422); test_oversized_payload asserts 422; test_auth asserts 403.
    - Running the full baseline suite `python -m pytest tests/ -q` still passes its prior 130 tests (the new file only adds failing tests for the unbuilt endpoint; it must not break unrelated tests at collection time).
  </acceptance_criteria>
  <done>
    tests/test_export_cross_marketplace.py exists, collects cleanly, and every listed behavior has a dedicated test that fails in a RED-meaningful way (route/helper missing), giving Plan 02 an executable contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app | Tests exercise the FastAPI app in-process via TestClient; no real network or scraper calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | Excel cell content (formula injection) | mitigate | test_formula_injection asserts cells starting with `= + - @` are prefixed with `'`; locks the requirement for Plan 02's `_sanitize_cell` |
| T-24-02 | Denial of Service | items payload size | mitigate | test_oversized_payload asserts >500 items → 422; locks Pydantic `max_length=500` |
| T-24-03 | Spoofing / Elevation | endpoint auth | mitigate | test_auth asserts missing X-API-Key → 403; confirms inherited `verify_api_key` covers the new route |
| T-24-04 | Information Disclosure (empty payload) | items=[] | mitigate | test_empty_items asserts 400/422; locks Pydantic `min_length=1` |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No new packages introduced (24-RESEARCH §Package Legitimacy Audit: zero new deps). All test libs (pytest, openpyxl, TestClient) pre-installed and verified. |
</threat_model>

<verification>
- `python -m pytest tests/test_export_cross_marketplace.py --collect-only -q` collects ≥ 13 items, zero collection errors.
- `python -m pytest tests/test_export_cross_marketplace.py -q` is RED (failures referencing missing route / `_sanitize_cell`).
- `python -m pytest tests/ -q` baseline 130 tests still pass.
</verification>

<success_criteria>
The backend behavior contract for the export endpoint is fully captured in an executable, collectible, currently-RED pytest file covering EXPORT-04, EXPORT-05, EXPORT-06 and the four security edge cases.
</success_criteria>

<output>
Create `.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-01-SUMMARY.md` when done.
</output>
