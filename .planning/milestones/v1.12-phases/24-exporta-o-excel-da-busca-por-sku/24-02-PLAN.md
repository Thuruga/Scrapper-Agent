---
phase: 24-exporta-o-excel-da-busca-por-sku
plan: 02
type: execute
wave: 1
depends_on: ["24-01"]
files_modified:
  - api/routes_search.py
autonomous: true
requirements: [EXPORT-04, EXPORT-05, EXPORT-06]
must_haves:
  truths:
    - "POST /search/cross-marketplace/export returns a valid .xlsx with one row per item and exactly the 10 PT-BR columns in order"
    - "The endpoint never calls any search engine, scraper, or cross_marketplace_service — it only maps the items it receives"
    - "Empty items array returns 400/422; payload over 500 items returns 422; missing X-API-Key returns 403"
    - "All RED tests from Plan 01 turn GREEN"
  artifacts:
    - path: "api/routes_search.py"
      provides: "ExportItem + CrossMarketplaceExportRequest Pydantic models, _sanitize_cell helper, export_cross_marketplace endpoint (POST /cross-marketplace/export)"
      contains: "async def export_cross_marketplace"
  key_links:
    - from: "POST /search/cross-marketplace/export"
      to: "pd.ExcelWriter(engine='openpyxl')"
      via: "DataFrame → BytesIO → StreamingResponse"
      pattern: "ExcelWriter\\(.*openpyxl"
    - from: "export_cross_marketplace"
      to: "_sanitize_cell"
      via: "applied to marketplace, seller, title string fields"
      pattern: "_sanitize_cell\\("
    - from: "StreamingResponse headers"
      to: "browser filename"
      via: "Content-Disposition + Access-Control-Expose-Headers"
      pattern: "Access-Control-Expose-Headers"
---

<objective>
Add the backend export endpoint `POST /search/cross-marketplace/export` to `api/routes_search.py`, reusing the xlsx-streaming tail of the existing `export_search_products` while explicitly NOT re-executing any search or scrape. Add the two Pydantic models (`ExportItem`, `CrossMarketplaceExportRequest`) and the `_sanitize_cell` formula-injection guard. Turn all RED tests from Plan 01 green.

Purpose: Deliver EXPORT-04 (valid .xlsx, 10 PT columns), EXPORT-05 (fidelity — backend recomputes nothing, preserves `_display_order`), and EXPORT-06 (meaningful filename `busca_sku_<query>_<timestamp>.xlsx`). The endpoint receives pre-computed items in the request body; fidelity is guaranteed by construction.

Output: One modified file (`api/routes_search.py`) gaining one helper function, two Pydantic models, and one endpoint. Plan 01's test suite goes fully green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-RESEARCH.md
@.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-PATTERNS.md
@.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-VALIDATION.md
@api/routes_search.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ExportItem, CrossMarketplaceExportRequest models and _sanitize_cell helper</name>
  <files>api/routes_search.py</files>
  <read_first>
    - api/routes_search.py (lines 1-24 — confirm existing imports: `io`, `pandas as pd`, `datetime`, `APIRouter`, `StreamingResponse`, `BaseModel`, `Field`, `List`, `Optional`; verify whether `re` is already imported)
    - api/routes_search.py (lines 75-81 — `CrossMarketplaceRequest` model is the structural template for the new request model)
    - .planning/phases/24-exporta-o-excel-da-busca-por-sku/24-PATTERNS.md (lines 53-87 — exact model + helper definitions to add)
    - .planning/phases/24-exporta-o-excel-da-busca-por-sku/24-RESEARCH.md (Pitfall 4, lines 514-522 — Pydantic v2 strips `_`-prefixed fields; use Field(alias="_display_order") + populate_by_name)
    - .planning/phases/24-exporta-o-excel-da-busca-por-sku/24-RESEARCH.md (Security Domain, lines 450-477 — concrete mitigations: min_length=1, max_length=500, _sanitize_cell on string fields only)
  </read_first>
  <action>
    In api/routes_search.py, add `import re` if not already present (check lines 1-24 first; do not duplicate). Define a module-level compiled regex `FORMULA_CHARS_RE = re.compile(r'^[=+\-@]')` and a function `_sanitize_cell(value)` that returns `"'" + value` when `value` is a `str` matching the regex, else returns `value` unchanged (per D-Security: formula/CSV injection guard). Add Pydantic model `ExportItem(BaseModel)` with fields exactly: `marketplace: str`, `seller: str`, `title: str`, `price: float`, `shipping_price: Optional[float] = None`, `landed_price: float`, `is_free_shipping: bool = False`, `final_match_score: float = 0.0`, `match_score: float = 0.0`, `is_similar: bool = False`, `url: str`, and `display_order: Optional[int] = Field(None, alias="_display_order")`, with `model_config = {"extra": "allow", "populate_by_name": True}` (handles unknown frontend fields and the `_`-prefixed display_order per Pitfall 4). Add `CrossMarketplaceExportRequest(BaseModel)` with `items: List[ExportItem] = Field(..., min_length=1, max_length=500)`, `search_query: Optional[str] = None`, and `target_sku: str = Field(..., min_length=1)`. Place these after the existing `CrossMarketplaceRequest` class (after line 81) and before the first endpoint that references them. No fenced code copied verbatim — match the field names exactly as listed.
  </action>
  <verify>
    <automated>python -c "from api.routes_search import ExportItem, CrossMarketplaceExportRequest, _sanitize_cell; print(_sanitize_cell('=SUM(A1)'), _sanitize_cell('ok'))"</automated>
  </verify>
  <acceptance_criteria>
    - `from api.routes_search import ExportItem, CrossMarketplaceExportRequest, _sanitize_cell` succeeds with no ImportError.
    - `_sanitize_cell("=SUM(A1)")` returns "'=SUM(A1)"; `_sanitize_cell("+1")`, `_sanitize_cell("-x")`, `_sanitize_cell("@cmd")` all gain a leading `'`; `_sanitize_cell("Mercado Livre")` is unchanged; `_sanitize_cell(199.9)` (non-str) returns the float unchanged.
    - `CrossMarketplaceExportRequest(items=[], target_sku="x")` raises a Pydantic ValidationError (min_length=1).
    - `ExportItem(**{"marketplace":"M","seller":"S","title":"T","price":1.0,"landed_price":1.0,"url":"u","_display_order":3})` populates `.display_order == 3` (alias works).
    - `re` is imported exactly once in the module (no duplicate import).
  </acceptance_criteria>
  <done>
    The two models and the sanitizer exist and behave per the locked CONTEXT decisions and the security mitigations, importable from api.routes_search.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the export_cross_marketplace endpoint (POST /cross-marketplace/export)</name>
  <files>api/routes_search.py</files>
  <read_first>
    - api/routes_search.py (lines 274-296 — the xlsx-streaming tail: BytesIO → ExcelWriter(openpyxl) → to_excel → safe_query/timestamp/filename → StreamingResponse with Content-Disposition + Access-Control-Expose-Headers; copy ONLY this tail)
    - api/routes_search.py (lines 181-272 — the SCRAPING portion of export_search_products that MUST NOT be copied: engine_factory / asyncio.gather re-fetch)
    - .planning/phases/24-exporta-o-excel-da-busca-por-sku/24-PATTERNS.md (lines 114-170 — exact endpoint skeleton, sort key, row mapping, null-shipping branch, filename token; plus the "Anti-pattern to avoid" note at lines 170)
    - .planning/phases/24-exporta-o-excel-da-busca-por-sku/24-RESEARCH.md (Pattern 2 lines 202-251 — null-shipping and boolean translation rules; Pattern 3 lines 252-278 — StreamingResponse)
    - tests/test_export_cross_marketplace.py (the RED contract from Plan 01 — the endpoint must satisfy every assertion)
  </read_first>
  <action>
    In api/routes_search.py, add `@router.post("/cross-marketplace/export", summary="Exporta busca por SKU em Excel")` decorating `async def export_cross_marketplace(request: CrossMarketplaceExportRequest)`, placed immediately after the existing `/cross-marketplace` endpoint. Auth is inherited from `api_router` (api/__init__.py:23) — do NOT add `Depends(verify_api_key)` to this decorator. Sort `request.items` by `i.display_order if i.display_order is not None else 0` (ascending, preserves on-screen order per EXPORT-05). For each item build a row dict in this exact key order: "Plataforma" (=_sanitize_cell(marketplace)), "Vendedor" (=_sanitize_cell(seller)), "Título" (=_sanitize_cell(title)), "Preço" (=price), "Frete", "Preço Total", "Frete Grátis" ("Sim"/"Não" from is_free_shipping), "Score de Match" (=round(final_match_score or match_score)), "Similar" ("Sim"/"Não" from is_similar), "URL" (=url, not sanitized — http-prefixed by structure). Null-shipping branch (locked CONTEXT): when `shipping_price is None and not is_free_shipping` → Frete = "A calcular" and Preço Total = price; elif is_free_shipping → Frete = 0.0 and Preço Total = landed_price; else → Frete = shipping_price and Preço Total = landed_price. Build `pd.DataFrame(rows)`, write to `io.BytesIO()` via `pd.ExcelWriter(engine='openpyxl')` with `sheet_name='Busca SKU'`, `output.seek(0)`. Compute `query_token = request.search_query or request.target_sku`, `safe_query = "".join(c if c.isalnum() else "_" for c in query_token)`, timestamp via `datetime.now().strftime('%Y%m%d_%H%M%S')`, `filename = f"busca_sku_{safe_query}_{timestamp}.xlsx"`. Return `StreamingResponse` with headers `Content-Disposition: attachment; filename="..."` AND `Access-Control-Expose-Headers: Content-Disposition` (required for CORS — Pitfall 3), media_type `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`. The endpoint MUST NOT import or call `engine_factory`, `cross_marketplace_service`, `asyncio.gather`, or any scraper — it processes only the items it received (EXPORT-05 fidelity by construction).
  </action>
  <verify>
    <automated>python -m pytest tests/test_export_cross_marketplace.py -q 2>&1 | grep -v '^#' | grep -Eq '([1-9][0-9]* passed)' && python -m pytest tests/test_export_cross_marketplace.py -q 2>&1 | grep -v '^#' | grep -Evq 'failed|error'</automated>
  </verify>
  <acceptance_criteria>
    - All tests in `python -m pytest tests/test_export_cross_marketplace.py -q` pass (GREEN), including test_happy_path, test_null_shipping, test_free_shipping, test_boolean_mapping, test_score_rounding, test_formula_injection, test_display_order, test_fidelity, test_filename, test_empty_items, test_oversized_payload, test_auth.
    - Source-grep proof of no re-scrape: `grep -nE 'engine_factory|cross_marketplace_service|asyncio\.gather' api/routes_search.py` shows ZERO matches inside the `export_cross_marketplace` function body (matches elsewhere in the file are fine).
    - The StreamingResponse headers include both `Content-Disposition` and `Access-Control-Expose-Headers: Content-Disposition`.
    - Full suite green: `python -m pytest tests/ -q` reports 130 baseline + new tests all passing.
  </acceptance_criteria>
  <done>
    POST /search/cross-marketplace/export streams a valid .xlsx with the 10 PT columns, honors the null-shipping/boolean/score/sort/filename rules, never re-scrapes, and turns Plan 01's suite fully green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend → API | Client sends an untrusted items array (string fields, numbers, URLs) crossing into the export endpoint |
| API → xlsx file | String cells written into a spreadsheet a user may later open in Excel/LibreOffice |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | xlsx string cells (marketplace, seller, title) | mitigate | `_sanitize_cell` prefixes `'` on values starting with `= + - @` before DataFrame build; verified by test_formula_injection |
| T-24-02 | Denial of Service | items list size | mitigate | `Field(..., min_length=1, max_length=500)` on `items`; >500 → 422 (test_oversized_payload). ~500×500B ≈ 250KB, within memory tolerance |
| T-24-03 | Elevation of Privilege | endpoint access | mitigate | inherited `Depends(verify_api_key)` from api_router (api/__init__.py:23); missing key → 403 (test_auth); no per-route auth code needed |
| T-24-04 | Information Disclosure | empty payload | mitigate | `min_length=1` → 400/422 (test_empty_items) |
| T-24-05 | Tampering (SSRF) | client-supplied URLs | accept | endpoint NEVER fetches URLs by design — no re-scrape; URL field only written as a cell value. Verified by no-re-scrape grep gate |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No new packages (24-RESEARCH §Package Legitimacy Audit). pandas 2.3.3 / openpyxl 3.1.5 / FastAPI 0.132.0 pre-installed & version-verified |
</threat_model>

<verification>
- `python -m pytest tests/test_export_cross_marketplace.py -q` → all GREEN.
- `python -m pytest tests/ -q` → 130 baseline + new tests pass.
- `grep -nE 'engine_factory|cross_marketplace_service|asyncio\.gather' api/routes_search.py` shows none inside `export_cross_marketplace`.
- StreamingResponse exposes Content-Disposition via Access-Control-Expose-Headers.
</verification>

<success_criteria>
EXPORT-04, EXPORT-05, EXPORT-06 are satisfied at the API layer: a valid 10-column PT-BR .xlsx is streamed with a meaningful filename, content is faithful to the submitted items, and no search/scrape is re-executed. All Plan 01 tests pass.
</success_criteria>

<output>
Create `.planning/phases/24-exporta-o-excel-da-busca-por-sku/24-02-SUMMARY.md` when done.
</output>
