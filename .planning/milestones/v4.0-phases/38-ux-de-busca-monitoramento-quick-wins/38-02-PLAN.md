---
phase: 38-ux-de-busca-monitoramento-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/App.css
  - frontend/src/App.tsx
autonomous: true
requirements: [UX-01, UX-06]
must_haves:
  truths:
    - "At 768px viewport, .grid-category collapses to a single column with no horizontal overflow on both the category-monitor and category-sweep screens (UX-01)"
    - "A top-right History icon button is visible without scrolling on both search tabs (comparativa and SKU) (UX-06)"
    - "Clicking the History icon toggles the existing HistoryList panel without duplicating its fetch logic (UX-06)"
    - "The icon's corner badge count matches HistoryList's type-filtered item count, not the raw unfiltered list length (UX-06, Pitfall 3)"
  artifacts:
    - path: "frontend/src/App.css"
      provides: "@media (max-width: 768px) rule collapsing .grid-category to 1fr"
      contains: "grid-template-columns: 1fr"
    - path: "frontend/src/App.tsx"
      provides: "Top-right History icon button wired to HistoryList collapsed state on both search tabs"
      contains: "Ver histórico de buscas"
  key_links:
    - from: "frontend/src/App.tsx (content-header / search-tab header)"
      to: "HistoryList collapsed state"
      via: "shared toggle (lifted state, store slice, or exposed toggle) driven by the top-right icon"
      pattern: "History"
    - from: "frontend/src/App.css @media 768px"
      to: ".grid-category"
      via: "grid-template-columns override"
      pattern: "grid-category"
---

<objective>
Frontend Wave-1 plan: the two lowest-risk, dependency-free visual fixes. UX-01 adds the missing responsive breakpoint to `.grid-category` (shared by `MonitoredCategoriesPage` and `CategoryPage`), and UX-06 surfaces the search history behind a top-right icon on both search tabs. Neither depends on backend field names, so this runs in parallel with Plan 01.

Purpose: `.grid-category` is a fixed `350px 1fr` grid with zero media queries (`App.css:366-370`), so it overflows at tablet widths — every other multi-column layout in the app already collapses at 980px/768px. `HistoryList` today renders as a full-width collapsible panel BELOW the search form (defaults `collapsed: true`), not "in the top-right corner" as UX-06 requires.
Output: One new CSS media block and a shared top-right History toggle wired identically on both tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-CONTEXT.md
@.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-UI-SPEC.md
@.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from source-drift verification):
- New `@media (max-width: 768px)` block targeting `.grid-category` in `frontend/src/App.css`
- New top-right icon-only History button element(s) in `frontend/src/App.tsx` (className `.btn-icon`, `title="Ver histórico de buscas"`, `<History size={18} />`)
- A shared history-panel toggle mechanism (lifted prop, small zustand slice mirroring `useSearchStore`, or an exposed imperative toggle — implementation shape is Claude's discretion per CONTEXT.md D-05/A1). New state identifier name at executor discretion.
- Possibly a new scoped CSS class for the products-modal grid `minmax(200px, 1fr)` override at ≤768px (only if overflow observed — UI-SPEC §1)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Responsive .grid-category breakpoint at 768px (UX-01)</name>
  <files>frontend/src/App.css</files>
  <read_first>
    - frontend/src/App.css — line 366-370 (`.grid-category` fixed `350px 1fr`, no breakpoint); lines 1052-1057 (existing `@media (max-width: 980px)` collapsing `.search-main-row`/`.search-control-row` to `1fr` — the exact pattern to mirror); lines 181-206 and 1270-1279 (existing `768px` blocks — colocate the new rule near these)
    - frontend/src/App.tsx — line 2983 (products-modal grid `gridTemplateColumns: 'repeat(auto-fill, minmax(250px, 1fr))'`) and line 2944 (products modal `max-width: 1200px; width: 95%`) — check for overflow at 768px
    - .planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-UI-SPEC.md — §1 (surface contract for UX-01, acceptance bar)
  </read_first>
  <action>
    Add a `@media (max-width: 768px)` block setting `.grid-category { grid-template-columns: 1fr; }` so the 350px sidebar column stacks above the content column (mirrors the existing `980px` collapse of `.search-main-row`). Colocate near the existing 768px blocks (`App.css:181-206` or `1270-1279`) or the 980px search block (`1052-1057`) — do NOT introduce container queries or any new responsiveness methodology (only `@media (max-width: Npx)` is used in this file). If, when checked at 768px, the products-modal product grid (`App.tsx:2983`, `minmax(250px, 1fr)`) causes horizontal overflow inside the modal, add a scoped CSS class override reducing it to `minmax(200px, 1fr)` at `≤768px` (UI-SPEC §1) and apply that class to the grid — do NOT duplicate inline styles. This same `.grid-category` rule fixes BOTH `MonitoredCategoriesPage` and `CategoryPage` since they share the class (Pitfall 4 — the fix must be verified on both surfaces, not one).
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <human-check>At exactly 768px viewport (devtools responsive mode), open the category monitor screen AND the category-sweep screen: confirm no horizontal scrollbar on `.grid-category`, no element overlap, and the sidebar/tree column stacks ABOVE the content column on both. Recorded in 38-HUMAN-UAT.md.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "grid-category" frontend/src/App.css` shows the class inside a `@media (max-width: 768px)` block with `grid-template-columns: 1fr`
    - `cd frontend && npm run build` succeeds (no TypeScript/Vite errors)
    - No new CSS methodology introduced (no `@container` / container-query syntax added) — `grep -c "@container" frontend/src/App.css` returns 0
    - Manual UAT: both `.grid-category` consumers show no horizontal scroll at 768px
  </acceptance_criteria>
  <done>`.grid-category` collapses to one column at ≤768px on both consuming screens; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Top-right History icon toggling the HistoryList panel on both search tabs (UX-06)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    - frontend/src/App.tsx — lines 779-885 (`HistoryList`: `collapsed` state `useState(true)` at line 781, type filter `all.filter(h => h.type === type)` at line 787, `filteredCount` badge at lines 821/854-856, internal toggle button at 834); line 1602 (`SearchPage` renders `HistoryList type="search"`); line 2177 (`CrossMarketplacePage` renders `HistoryList type="cross"`); lines 3215-3231 (`content-header` app shell — `justify-content: space-between`, right side empty)
    - frontend/src/App.tsx — lines 1-57 (imports; `History` and `AlertTriangle` are ALREADY imported — zero new icon imports needed)
    - frontend/src/stores/searchStore.ts — the existing `useSearchStore` zustand slice pattern (App.tsx:54), the reference shape if a store-slice toggle is chosen (RESEARCH.md Pattern 4 option 2 / A1)
    - frontend/src/App.css — lines 230-235 (`content-header` flex layout); `.btn-icon` and `.monitor-badge` class definitions (badge pill visual: `--primary`, `rgba(99,102,241,0.12)` bg, `2px 8px` padding, `20px` radius)
    - .planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-UI-SPEC.md — §3 (surface contract), Copywriting Contract (`title="Ver histórico de buscas"`, D-07), Color section (icon accent/hover uses `--primary`)
  </read_first>
  <action>
    Add an icon-only button `<History size={18} />` with `className="btn-icon"` and `title="Ver histórico de buscas"` (D-07, verbatim — no other tooltip copy), positioned top-right of each search tab so it lands opposite the page title. Preferred target is the shared `content-header` right slot (`App.tsx:3215-3231`, already `justify-content: space-between` with an empty right side) OR a header slot inside `SearchPage`/`CrossMarketplacePage` — executor's discretion (RESEARCH.md Pattern 4 / A1). Clicking it toggles the EXISTING `HistoryList` panel's `collapsed` boolean — do NOT duplicate the `ApiClient.getHistoryList()` fetch or the `HistoryList` component; only move/mirror the trigger. Choose ONE of: (a) lift `collapsed` out of `HistoryList` into the parent as a controlled prop, (b) add a small zustand slice mirroring `useSearchStore`, or (c) expose an imperative toggle from `HistoryList` (`forwardRef`+`useImperativeHandle`). Wire BOTH tabs identically (`type="search"` at line 1602, `type="cross"` at line 2177) — only the `type` prop differs. The icon's corner badge MUST use the same type-filtered count `HistoryList` already computes (`filteredCount` = `items.length` AFTER `all.filter(h => h.type === type)`), rendered with the existing `.monitor-badge` pill visual — NOT the raw `getHistoryList()` length (Pitfall 3: the SKU-tab icon badge must NOT equal the comparativa-tab icon badge when the underlying type-filtered counts differ). Icon hover/active state uses `--primary` (UI-SPEC Color). No visible text label (icon-only). Reuse already-imported `History` — add no new icon import.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
    <human-check>On BOTH the comparativa and SKU search tabs: confirm the History icon is visible top-right without scrolling on page load; clicking it opens/closes the existing history panel; the corner badge count matches the number of history entries FOR THAT TAB's type (the two tabs may show different counts). Recorded in 38-HUMAN-UAT.md.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -n "Ver histórico de buscas" frontend/src/App.tsx` returns the new button title (D-07 copy verbatim)
    - `cd frontend && npm run build` succeeds
    - No second `getHistoryList` call added — `grep -c "getHistoryList" frontend/src/App.tsx` is unchanged from before this task (the fetch is not duplicated)
    - The badge count source is the type-filtered `filteredCount` (or an identical re-derivation of `all.filter(h => h.type === type).length`), not the raw list length — verifiable by reading the badge expression
    - Both tabs (`type="search"` and `type="cross"`) wire the identical toggle mechanism
  </acceptance_criteria>
  <done>A top-right History icon toggles the existing panel on both tabs with a correct type-scoped badge; no fetch duplication; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → browser UI | Pure client-side layout/state; no new server input crosses here. History data is already fetched via the existing authenticated `getHistoryList()` call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03 | Information Disclosure | History icon badge revealing entries across tab types | accept | Both `HistoryList` instances already fetch the full history and filter by `type` client-side (existing behavior, single shared-key system per RESEARCH.md V4); the icon badge reuses the same type filter — no new disclosure surface. |
| T-38-SC | Tampering | npm installs | mitigate | N/A — zero package installs this plan (`History`/`AlertTriangle` already imported). |
</threat_model>

<verification>
- `cd frontend && npm run build` green
- Manual UAT rows for UX-01 (768px, both screens) and UX-06 (icon + badge, both tabs) completed in 38-HUMAN-UAT.md
</verification>

<success_criteria>
- UX-01: `.grid-category` responsive at ≤768px on both consuming screens, no overflow
- UX-06: top-right History icon on both search tabs toggles the existing panel; badge count is type-scoped (Pitfall 3 avoided)
- Frontend build green; no new dependency, no duplicated fetch, no new CSS methodology
</success_criteria>

<output>
Create `.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-02-SUMMARY.md` when done.
</output>
