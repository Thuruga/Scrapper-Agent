---
phase: 38-ux-de-busca-monitoramento-quick-wins
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_price_monitor.py
  - backend/tests/test_brand_active.py
  - backend/tests/test_category_monitor.py
  - backend/core/models.py
  - backend/services/price_monitor_service.py
autonomous: true
requirements: [UX-02, UX-08, COMP-08]
must_haves:
  truths:
    - "A promo-only price change (price_full unchanged, discount added) produces a PriceHistoryEntry and a price_update WS message (D-01)"
    - "The price_update WebSocket payload includes the discount field alongside the full/effective price (D-03)"
    - "PriceMonitorConfig and PriceHistoryEntry persist a discount field so GET /monitors serializes it with no new endpoint (UX-02)"
    - "Lacoste (is_active=false) never appears in list_brands(active_only=True) (COMP-08)"
    - "run_category_scan populates last_scraped_at on the monitor row, which is the frontend completion signal (UX-08 backend proof)"
  artifacts:
    - path: "backend/core/models.py"
      provides: "last_price_discount field on PriceMonitorConfig and PriceHistoryEntry"
      contains: "last_price_discount"
    - path: "backend/services/price_monitor_service.py"
      provides: "D-01 effective-price + discount-aware change detection and D-03 WS payload"
      contains: "price_discount"
    - path: "backend/tests/test_price_monitor.py"
      provides: "D-01/D-03 promo-only-change regression test"
      contains: "test_price_monitor_promo_only_change_triggers_history"
    - path: "backend/tests/test_brand_active.py"
      provides: "COMP-08 Lacoste exclusion regression test class"
      contains: "TestLacosteExcludedFromActiveOnly"
    - path: "backend/tests/test_category_monitor.py"
      provides: "UX-08 backend coverage that run_category_scan writes last_scraped_at"
      contains: "last_scraped_at"
  key_links:
    - from: "backend/services/price_monitor_service.py"
      to: "backend/core/models.py"
      via: "PriceHistoryEntry(last_price_discount=...) constructor + config.last_price_discount"
      pattern: "last_price_discount"
    - from: "backend/tests/test_brand_active.py"
      to: "services.brand_service.brand_service.list_brands"
      via: "active_only=True chokepoint assertion"
      pattern: "list_brands\\(active_only=True\\)"
---

<objective>
Backend + test-scaffold plan for Phase 38. Delivers the UX-02 data/logic change (promo price now flows through change-detection, history and the WebSocket payload) and creates the three Wave-0 failing tests the whole phase depends on (UX-02, UX-08 backend proof, COMP-08 regression). COMP-08 needs NO production code — the `list_brands(active_only=True)` chokepoint already excludes inactive brands (per STATE.md `[ARCH]` / `[D-07/25-02]`); this plan only adds the regression test that guards it. UX-08's backend trigger also already exists (`background_tasks.add_task(run_category_scan, row)` in `routes_monitor.py:73-75`); this plan only adds the test proving `run_category_scan` writes the `last_scraped_at` signal the frontend (Plan 03) polls for.

Purpose: Without the discount field on the monitor models, UX-02's frontend render (Plan 03) has nothing to read; without the D-01/D-03 change-detection fix, a promo-only price drop is silently ignored (confirmed at `price_monitor_service.py:178,193`). The Wave-0 tests establish the RED baseline so the phase satisfies Nyquist Dimension 8.
Output: Two extended Pydantic models, a discount-aware `_monitor_loop`, and three test files that go RED→GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-CONTEXT.md
@.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-RESEARCH.md
@.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-PATTERNS.md
@.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols created by THIS plan (exclude from source-drift verification):
- `last_price_discount` — new `Optional[float]` field on `PriceMonitorConfig` (`backend/core/models.py`)
- `last_price_discount` — new `Optional[float]` field on `PriceHistoryEntry` (`backend/core/models.py`)
- `test_price_monitor_promo_only_change_triggers_history` — new test function (`backend/tests/test_price_monitor.py`)
- `TestLacosteExcludedFromActiveOnly` — new test class + `test_lacoste_absent_from_active_only` method (`backend/tests/test_brand_active.py`)
- `backend/tests/test_category_monitor.py` — new test file (function name at executor discretion, e.g. `test_run_category_scan_populates_last_scraped_at`)
- New keys added to the `price_update` WS payload dict: `price_discount`, `price_full` (D-03)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — create the three failing baseline tests (COMP-08, UX-02, UX-08 backend)</name>
  <files>backend/tests/test_brand_active.py, backend/tests/test_price_monitor.py, backend/tests/test_category_monitor.py</files>
  <read_first>
    - backend/tests/test_brand_active.py — read `TestMarketplacesInBrandsJson` (lines ~180-231) for the exact real-singleton assertion pattern to copy for the new class
    - backend/tests/test_price_monitor.py — read the existing hermetic async test style (lines 1-60): mocked `engine_factory.get_engine`, mocked `manager.send_message`, `asyncio.sleep` stop-loop trick
    - backend/services/price_monitor_service.py — lines 140-238, so the test targets the real `_monitor_loop` (note: existing tests call `get_product_details`, but `_monitor_loop` calls `get_pdp_product` per RESEARCH.md sketch — verify the actual method name used inside `_monitor_loop` before mocking)
    - backend/services/category_monitor_service.py — lines 43-93 (`run_category_scan` writes `last_scraped_at` + `last_stock_summary` onto the row; `_load_local`/`_save_local` do disk I/O — must be patched in the test)
    - backend/data/brands.json — confirm `lacoste.is_active` is `false` (COMP-08 test reads the real singleton)
  </read_first>
  <behavior>
    - test_brand_active.py: class `TestLacosteExcludedFromActiveOnly` with `test_lacoste_absent_from_active_only` — asserts `"lacoste" not in {b.brand_key for b in brand_service.list_brands(active_only=True)}`. Uses the real `brand_service` singleton, no mocking (mirrors `TestMarketplacesInBrandsJson`). Passes immediately (no prod code needed) — it is a regression guard, not RED.
    - test_price_monitor.py: `test_price_monitor_promo_only_change_triggers_history` — mocked engine returns `price_full=100.0, price_discount=20.0` (a discount DELTA per Pitfall 1). Config starts with `last_price=100.0` and no prior discount. Assert `len(config.history) == 1` (promo-only change generates history, D-01) and that a `price_update` WS message was sent whose payload dict contains key `price_discount` (D-03). This is RED until Task 2/3 land.
    - test_category_monitor.py (new file): assert `run_category_scan(row)` writes a non-null `last_scraped_at` onto the monitor row (patch `_load_local`/`_save_local` and the engine's `run_bulk_scrape` async generator so it yields a small fixture list; patch `persist_monitor_stock_summary` + file writes to avoid disk I/O). This is GREEN immediately (backend trigger already exists) — it documents UX-08's backend contract for the Plan 03 poll.
  </behavior>
  <action>
    Create `TestLacosteExcludedFromActiveOnly` in `test_brand_active.py` copying the real-`brand_service`-singleton assertion shape of `TestMarketplacesInBrandsJson` (assert the negated membership on the `active_only=True` key set). Add `test_price_monitor_promo_only_change_triggers_history` to `test_price_monitor.py` using the same hermetic style as the existing tests: patch `services.price_monitor_service.engine_factory.get_engine`, patch `services.price_monitor_service.manager.send_message` with `AsyncMock`, patch `service._save_monitors` (or `patch.object(service, "_save_monitors")`), and patch `services.price_monitor_service.asyncio.sleep` with a side-effect that sets `config.active = False` after one iteration so `_monitor_loop(job_id)` runs exactly once. The mocked engine method MUST match the real method `_monitor_loop` calls (verify — RESEARCH.md sketch uses `get_pdp_product`; the older test uses `get_product_details` outside the loop). Create new file `test_category_monitor.py` with a `@pytest.mark.asyncio` test that runs `run_category_scan` against a patched `_load_local`/`_save_local` and a mocked engine whose `run_bulk_scrape` yields 1-2 fixture product dicts; assert the row passed through `_save_local` carries a truthy `last_scraped_at`. Do NOT implement the model/service change in this task — RED must be observable for the price-monitor test.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_brand_active.py::TestLacosteExcludedFromActiveOnly tests/test_category_monitor.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_brand_active.py::TestLacosteExcludedFromActiveOnly -x` PASSES (regression guard green from the start)
    - `pytest tests/test_category_monitor.py -x` PASSES (backend trigger already writes `last_scraped_at`)
    - `pytest tests/test_price_monitor.py::test_price_monitor_promo_only_change_triggers_history -x` FAILS (RED) with an assertion on `config.history` length or missing `price_discount` WS key — proving the test exercises the not-yet-implemented D-01/D-03 behavior
    - New test file `backend/tests/test_category_monitor.py` exists and does no real disk/network I/O (all persistence + engine calls patched)
  </acceptance_criteria>
  <done>All three test artifacts exist; COMP-08 + UX-08 backend tests are green; the UX-02 price-monitor test is RED and pinned to D-01/D-03 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add last_price_discount field to PriceMonitorConfig and PriceHistoryEntry (D-04)</name>
  <files>backend/core/models.py</files>
  <read_first>
    - backend/core/models.py — lines 281-318 (`PriceHistoryEntry` and `PriceMonitorConfig`; note the sibling optional-field convention: `available_colors`/`available_sizes` use `Field(default_factory=list)`, `last_price` uses `Optional[float] = None`)
    - backend/data/price_monitors.json — confirm current shape has only `last_price` (D-02: existing monitors have no discount history; new field must default to None so old records load)
  </read_first>
  <action>
    Add a single new field `last_price_discount: Optional[float] = None` to BOTH `PriceMonitorConfig` (near `last_price`, line ~304) and `PriceHistoryEntry` (near `price`, line ~287). Rationale (RESEARCH.md D-04 recommendation): one delta field, keeping `last_price` as the effective/current price so the existing frontend that reads `last_price` stays backward-compatible, and the pre-discount price is reconstructed downstream as `last_price + last_price_discount` (the exact formula the working `App.tsx:3009-3018` render already uses with `price_full + price_discount`). Do NOT add a redundant `last_price_full`. Default MUST be `None` (not 0.0) so existing `price_monitors.json` records without the key still validate (D-02, no retroactive re-scrape). Use `Optional[float]` and the existing `from typing import Optional` already imported in the file. `last_price_discount` here holds the discount DELTA amount (positive number), matching the per-engine `price_discount` convention (VTEX/ML/Shopify: `list_price - sale_price`), NOT a standalone discounted price (Pitfall 1).
  </action>
  <verify>
    <automated>cd backend && python -c "from core.models import PriceMonitorConfig, PriceHistoryEntry; c=PriceMonitorConfig(job_id='x', url='http://e', brand='b'); assert c.last_price_discount is None; e=PriceHistoryEntry(price=1.0, available=True); assert e.last_price_discount is None; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "last_price_discount" backend/core/models.py` returns at least 2 hits (one per model)
    - Constructing `PriceMonitorConfig(job_id=..., url=..., brand=...)` with no discount arg yields `last_price_discount is None`
    - Constructing `PriceHistoryEntry(price=..., available=...)` with no discount arg yields `last_price_discount is None`
    - Loading the existing `backend/data/price_monitors.json` via `PriceMonitorConfig.model_validate` on each record does not raise (backward compat, D-02)
  </acceptance_criteria>
  <done>Both models carry `last_price_discount: Optional[float] = None`; existing persisted records still validate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Discount-aware change detection + WS payload in _monitor_loop (D-01, D-03) — GREEN</name>
  <files>backend/services/price_monitor_service.py</files>
  <read_first>
    - backend/services/price_monitor_service.py — lines 175-238 (the `if product:` block: `current_price` assignment line 178, `has_change` checks 191-203, history-append + `price_update` WS message 206-231)
    - backend/core/models.py — the `last_price_discount` field added in Task 2 (both models)
    - backend/tests/test_price_monitor.py — the RED test from Task 1 (this task makes it GREEN)
    - .planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-RESEARCH.md — Pitfall 1 (price_discount is a DELTA, not a discounted price) before wiring any comparison
  </read_first>
  <action>
    Implement D-01 and D-03 so the Task-1 price-monitor test goes GREEN. Read the scraped discount as `current_discount = product.price_discount if product.price_discount and product.price_discount > 0 else None` (delta convention — `RawProductBronze.price_discount` is `Optional[float]`, a positive discount amount, NOT a final price; Pitfall 1). Keep `current_price = product.price_full` as the effective/current price (RawProductBronze `price_full` is already the selling price). Extend the `has_change` detection so a change in the discount delta ALSO flags a change: set `has_change = True` when `config.last_price_discount != current_discount` (this is D-01's core behavior — a promo-only change where `price_full` is unchanged must now register). In the `if has_change:` block: pass `last_price_discount=current_discount` to the `PriceHistoryEntry(...)` constructor, set `config.last_price_discount = current_discount`, and add both `"price_discount": current_discount` and `"price_full": current_price` keys to the `manager.send_message({"type": "price_update", ...})` payload dict (D-03), mirroring exactly how `available_colors`/`available_sizes` are already threaded through both the `PriceHistoryEntry` constructor and the WS dict. Keep `config.last_price = current_price` unchanged (D-04 back-compat: `last_price` still means effective price). Do not alter the `else`/`info` no-change branch or the `blocked`/`error` branches.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_price_monitor.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_price_monitor.py::test_price_monitor_promo_only_change_triggers_history -x` PASSES (was RED in Task 1) — promo-only change (price_full=100 unchanged, discount 20) produced exactly one history entry and a `price_update` WS payload containing `price_discount`
    - `grep -n "price_discount" backend/services/price_monitor_service.py` shows the field in the `has_change` comparison, the `PriceHistoryEntry` constructor, and the WS payload dict
    - `grep -n "price_full" backend/services/price_monitor_service.py` shows `price_full` added to the `price_update` WS payload dict (D-03)
    - The full pre-existing `test_price_monitor.py` suite still passes (e.g. `test_price_monitor_recording_change` — a `price_full`-only change still flags `has_change`)
  </acceptance_criteria>
  <done>UX-02 backend is complete: promo-only changes register in history and notify via WS with the discount field; existing price-change behavior is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scraped-site → price_monitor_service | Untrusted product payload (`price_full`, `price_discount`) already validated by `RawProductBronze` before reaching the loop |
| brands.json → brand-selector surfaces | `is_active` flag controls visibility; `list_brands(active_only=True)` is the single enforcement chokepoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-01 | Tampering | `last_price_discount` from scraped payload | accept | Value is already type-validated as `Optional[float]` by `RawProductBronze`; it is display/analytics data, not a security decision. No injection surface (JSON-file persistence, no SQL). |
| T-38-02 | Information Disclosure | Lacoste (inactive brand) leaking into a selector | mitigate | COMP-08 regression test (`TestLacosteExcludedFromActiveOnly`) locks the `list_brands(active_only=True)` chokepoint; no new client-side name filter permitted (would be the exact regression this test guards). |
| T-38-SC | Tampering | npm/pip installs | mitigate | N/A this phase — zero package installs (RESEARCH.md Package Legitimacy Audit: not applicable). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_price_monitor.py tests/test_brand_active.py tests/test_category_monitor.py -q` all green
- `cd backend && python -m pytest -q` full backend suite green (no regression)
- `grep -c "last_price_discount" backend/core/models.py` ≥ 2
</verification>

<success_criteria>
- UX-02 backend: promo-only price change generates a history entry + WS `price_update` with the discount field (D-01/D-03); models carry `last_price_discount` (D-04) with `None` default (D-02 back-compat)
- UX-08 backend proof: `run_category_scan` writes `last_scraped_at` (covered by test, no code change — trigger pre-exists)
- COMP-08: Lacoste absent from `list_brands(active_only=True)`, locked by regression test (no production code change)
- Full backend suite green
</success_criteria>

<output>
Create `.planning/phases/38-ux-de-busca-monitoramento-quick-wins/38-01-SUMMARY.md` when done.
</output>
