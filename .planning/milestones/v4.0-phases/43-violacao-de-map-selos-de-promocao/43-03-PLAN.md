---
phase: 43-violacao-de-map-selos-de-promocao
plan: 03
type: execute
wave: 3
depends_on: [43-01]
files_modified:
  - backend/api/routes_search.py
  - backend/services/product_contract.py
  - backend/services/cross_marketplace_service.py
  - backend/services/vtex_api_scraper.py
  - backend/services/shopify_api_client.py
  - backend/services/engines/wake_engine.py
  - backend/services/engines/sfcc_engine.py
  - backend/services/engines/sfcc_parser.py
  - backend/services/engines/zara_parser.py
  - backend/services/engines/amazon_engine.py
  - backend/services/engines/mercado_livre_engine.py
  - backend/services/engines/netshoes_engine.py
  - backend/tests/test_phase43_search_contract.py
autonomous: true
requirements: [MAP-01, PROMO-01]
---

<objective>
Wire MAP verdicts and structured promotions into the real search/cross/export flows without breaking current contracts.

Purpose: make the Phase 43 data actually appear in responses and exports, using existing result builders and engine seams.
Output: backend result payloads and export paths carry additive MAP/promotion metadata for brand search and cross-marketplace flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-CONTEXT.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-RESEARCH.md
@backend/api/routes_search.py
@backend/services/product_contract.py
@backend/services/cross_marketplace_service.py
@backend/services/engines/seller_extraction.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression tests for additive MAP/promotion search contracts</name>
  <files>backend/tests/test_phase43_search_contract.py, backend/tests/test_export_search_contract.py, backend/tests/test_export_cross_marketplace.py</files>
  <behavior>
    - Comparative search responses can include `promotions` and MAP fields without breaking older payloads.
    - Cross-marketplace result rows can include MAP metadata and seller fallback semantics.
    - Export paths still succeed when Phase 43 fields are absent and can serialize additive MAP/promotion data when present.
  </behavior>
  <action>
    Write contract/regression tests before wiring production code. Reuse the style already used in export/search contract suites.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_phase43_search_contract.py tests/test_export_search_contract.py tests/test_export_cross_marketplace.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire MAP evaluation into brand search, cross-marketplace, and export transforms</name>
  <files>backend/api/routes_search.py, backend/services/product_contract.py, backend/services/cross_marketplace_service.py</files>
  <behavior>
    - Effective advertised price is the only MAP verdict input.
    - First-party brand products use brand/storefront identity as infractor fallback.
    - Marketplace rows distinguish real seller vs default fallback semantics when available.
  </behavior>
  <action>
    Integrate `map_evaluator_service.py` into:
    - comparative search response normalization,
    - cross-marketplace result formatting,
    - search export/cross export transforms where additive columns or stable serialized fields are needed.

    Keep the evaluation logic centralized in the service; routes/builders should only pass product-like data and merge the returned metadata.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_phase43_search_contract.py tests/test_export_search_contract.py tests/test_export_cross_marketplace.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feed structured promotions from existing engine seams</name>
  <files>backend/services/vtex_api_scraper.py, backend/services/shopify_api_client.py, backend/services/engines/wake_engine.py, backend/services/engines/sfcc_engine.py, backend/services/engines/sfcc_parser.py, backend/services/engines/zara_parser.py, backend/services/engines/amazon_engine.py, backend/services/engines/mercado_livre_engine.py, backend/services/engines/netshoes_engine.py</files>
  <behavior>
    - Engines that can cheaply expose promotion text feed `promotion_parser.py`.
    - Products with no promotion evidence return `promotions=[]`.
    - Unparseable promotion text still surfaces as `generic_badge` with `raw_text`.
  </behavior>
  <action>
    Touch engine files only where there is a low-cost seam to expose raw promotion text or payment-condition text.

    Preferred order:
    - reuse already available listing/PDP text in first-party engines,
    - piggyback on cross-marketplace PDP enrichment where seller/price enrichment already exists,
    - avoid introducing heavy new network passes solely for promotion extraction when the result can safely remain `[]`.

    The planner/executor may choose a small helper layer if several engines need the same raw-text harvesting pattern.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_phase43_search_contract.py tests/test_export_search_contract.py tests/test_export_cross_marketplace.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
Run:

```powershell
cd backend
python -m pytest tests/test_phase43_search_contract.py tests/test_export_search_contract.py tests/test_export_cross_marketplace.py -x -q
```
</verification>

<success_criteria>
- Comparative search, cross-marketplace results, and exports all carry Phase 43 metadata additively.
- Promotion extraction is best-effort, centralized, and never blocks the response contract.
</success_criteria>
