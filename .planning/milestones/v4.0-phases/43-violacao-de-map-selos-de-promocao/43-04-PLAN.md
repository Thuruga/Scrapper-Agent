---
phase: 43-violacao-de-map-selos-de-promocao
plan: 04
type: execute
wave: 4
depends_on: [43-02, 43-03]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/App.tsx
autonomous: true
requirements: [MAP-01, PROMO-01]
---

<objective>
Expose Phase 43 to the operator and to readers of search results: manage MAP rules from the existing settings surface and render MAP/promotion badges on result cards.

Purpose: make the backend capabilities operable and visible without introducing a new frontend screen or design system.
Output: typed MAP rules client methods, settings management UI, and result-card rendering for MAP/promotion metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-CONTEXT.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-RESEARCH.md
@frontend/src/api/client.ts
@frontend/src/App.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add typed client methods for MAP rules CRUD</name>
  <files>frontend/src/api/client.ts</files>
  <behavior>
    - Frontend has typed helpers to list/create/update/delete MAP rules.
    - No existing API client methods regress.
  </behavior>
  <action>
    Add typed request helpers for the `/map-rules` endpoints using the same style as existing brand CRUD methods.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add MAP rules management to SettingsPage</name>
  <files>frontend/src/App.tsx</files>
  <behavior>
    - Operator can view existing rules, create a rule, edit/delete a rule, and receive toast feedback.
    - UI lives in the existing settings surface or a modal launched from it, not on a new page.
  </behavior>
  <action>
    Reuse `SettingsPage`, `GlassCard`, modal, and confirm-dialog patterns already present in `App.tsx`.

    Keep the form narrow: scope, target identity, minimum price, active toggle, and optional note.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Render MAP and promotion metadata on result cards</name>
  <files>frontend/src/App.tsx</files>
  <behavior>
    - Search and cross-marketplace cards show a clear MAP violation state when present.
    - Seller/infractor context is readable.
    - Promotion entries render compactly without overwhelming the card.
  </behavior>
  <action>
    Extend the existing result-card branches in both `SearchPage` and `CrossMarketplacePage`.

    Recommended rendering:
    - small warning/error badge for MAP violation,
    - one short line for infractor / floor price,
    - compact chips or short stacked rows for up to a few promotion entries.

    Avoid creating a parallel details panel; stay within the established card rhythm.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
</task>

</tasks>

<verification>
Run:

```powershell
cd frontend
npm run build
```
</verification>

<success_criteria>
- The operator can manage MAP rules without editing JSON directly.
- End users of search results can see violations and promotions in the existing UI.
- The frontend compiles cleanly against the new typed backend contracts.
</success_criteria>
