---
phase: 43-violacao-de-map-selos-de-promocao
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - backend/api/routes_map_rules.py
  - backend/api/__init__.py
  - backend/tests/test_map_rules_routes.py
autonomous: true
requirements: [MAP-01]
---

<objective>
Expose Phase 43 MAP rules as a first-class protected API so the operator UI and later search wiring can manage rules without manual JSON edits.

Purpose: ship the CRUD surface independently of search rendering, keeping route validation and persistence isolated.
Output: `/map-rules` router, registration in `api/__init__.py`, and route tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-CONTEXT.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-RESEARCH.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-PATTERNS.md
@backend/api/routes_brands.py
@backend/api/__init__.py
@backend/services/map_rules_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route tests and request/response models for MAP rules CRUD</name>
  <files>backend/api/routes_map_rules.py, backend/tests/test_map_rules_routes.py</files>
  <behavior>
    - List returns an empty array when no rules exist.
    - Create persists a valid rule and rejects invalid scope/payload.
    - Update mutates an existing rule and 404s unknown IDs.
    - Delete removes an existing rule and 404s unknown IDs.
  </behavior>
  <action>
    Build route-level tests first, using the existing protected-router testing style already used in other API route suites.

    Create `routes_map_rules.py` with local request/response models and protected CRUD endpoints under a dedicated prefix such as `/map-rules`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_map_rules_routes.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the router and keep it independent from search logic</name>
  <files>backend/api/__init__.py, backend/tests/test_map_rules_routes.py</files>
  <behavior>
    - The new router is reachable through the protected API aggregator.
    - No search endpoint changes are required for CRUD-only behavior.
  </behavior>
  <action>
    Register the new router in `backend/api/__init__.py` using the same API-key-protected aggregator as the other protected routes.

    Keep precedence and price evaluation inside services, not in the route module.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_map_rules_routes.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
Run:

```powershell
cd backend
python -m pytest tests/test_map_rules_routes.py -x -q
```
</verification>

<success_criteria>
- The operator has a protected backend CRUD surface for MAP rules.
- Route validation and error handling are isolated from search/render flows.
</success_criteria>
