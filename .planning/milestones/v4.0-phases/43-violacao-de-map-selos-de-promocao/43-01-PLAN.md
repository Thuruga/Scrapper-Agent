---
phase: 43-violacao-de-map-selos-de-promocao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/core/models.py
  - backend/services/map_rules_service.py
  - backend/services/map_evaluator_service.py
  - backend/services/promotion_parser.py
  - backend/tests/test_map_rules_service.py
  - backend/tests/test_map_evaluator_service.py
autonomous: true
requirements: [MAP-01, PROMO-01]
must_haves:
  truths:
    - "MAP comparison uses the effective advertised product price, never the full/riscado price."
    - "MAP precedence is product > category > brand."
    - "Products without rules or without effective price never become false violations."
    - "Promotions default to [] and preserve raw_text whenever raw promotion text exists."
  artifacts:
    - path: "backend/core/models.py"
      provides: "Additive Phase 43 models/fields for promotions and MAP metadata"
      contains: "promotions"
    - path: "backend/services/map_rules_service.py"
      provides: "Atomic JSON CRUD + precedence selection for MAP rules"
      exports: ["map_rules_service"]
    - path: "backend/services/map_evaluator_service.py"
      provides: "Pure MAP verdict computation over product-like input"
      exports: ["evaluate_map_violation", "resolve_effective_advertised_price"]
    - path: "backend/services/promotion_parser.py"
      provides: "Pure promotion normalization helpers"
      exports: ["parse_promotions", "parse_promotion_text"]
---

<objective>
Create the shared backend foundations for Phase 43: additive contracts, JSON-backed MAP rule persistence, a pure MAP evaluator, and a pure promotion parser.

Purpose: prevent later route/search/frontend plans from inventing their own field names, precedence rules, or promotion semantics.
Output: deterministic services and models that later plans can wire into routes and UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-CONTEXT.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-RESEARCH.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-PATTERNS.md
@.planning/phases/43-violacao-de-map-selos-de-promocao/43-VALIDATION.md
@backend/core/models.py
@backend/services/brand_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive Phase 43 product/rule/promotion contracts</name>
  <files>backend/core/models.py, backend/tests/test_map_rules_service.py, backend/tests/test_map_evaluator_service.py</files>
  <behavior>
    - Constructing historical `RawProductBronze` and `SearchProductResult` payloads without Phase 43 fields still validates.
    - New promotion and MAP metadata fields default safely (`[]`, `None`, `False`).
    - MAP rule model accepts scopes `product`, `category`, `brand` only.
  </behavior>
  <action>
    Add additive Phase 43 models/fields to `backend/core/models.py`. Recommended symbols:
    - `PromotionInfo`
    - `MapRule`
    - optional compact `MapViolationInfo` if the planner prefers a nested helper model

    Extend `RawProductBronze` and `SearchProductResult` with additive fields for:
    - `promotions`
    - `map_violation`
    - `map_price_floor`
    - `map_rule_scope`
    - `map_rule_id`
    - `map_infractor`
    - `map_infractor_is_default` (or equivalent boolean/type field)

    Defaults must preserve backward compatibility with old search/history/export records.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_map_rules_service.py tests/test_map_evaluator_service.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement atomic MAP rule persistence and precedence selection</name>
  <files>backend/services/map_rules_service.py, backend/tests/test_map_rules_service.py</files>
  <behavior>
    - Rules are persisted under `backend/data/map_rules.json` using temp-file replace.
    - Product rules beat category rules; category rules beat brand rules.
    - Product-rule matching prefers `product_code` and falls back to normalized URL.
    - Listing/loading rules with an empty or missing file returns a safe empty collection.
  </behavior>
  <action>
    Create `map_rules_service.py` following the house JSON-service pattern from `brand_service.py`.

    Include helpers for:
    - list/load
    - create/save/update/delete
    - applicable-rule selection by precedence
    - normalized URL matching for product rules when `product_code` is absent

    Keep route concerns out of this file.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_map_rules_service.py -x -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement pure effective-price MAP evaluator and promotion parser</name>
  <files>backend/services/map_evaluator_service.py, backend/services/promotion_parser.py, backend/tests/test_map_evaluator_service.py</files>
  <behavior>
    - A product with `price_full=300` and `price_discount=250` is compared using `250`, not `300`.
    - Delta-style prices still resolve through existing effective-price semantics.
    - No price or no rule means `map_violation=False`.
    - Promotion parser can classify at least `pix_discount`, `percentage_discount`, `bundle`, `installments`, and `generic_badge`.
    - Unparseable promotion text still yields a `generic_badge` with `raw_text`.
  </behavior>
  <action>
    Create:
    - `map_evaluator_service.py` as a pure module that consumes product-like input, resolves the effective advertised price using the existing model semantics, applies the best rule, and returns additive MAP metadata.
    - `promotion_parser.py` as a pure text parser/normalizer with no I/O or engine imports.

    Reuse first-party brand/storefront fallback and marketplace-default-seller semantics in a later wiring plan; this task only defines the pure rules.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_map_evaluator_service.py -x -q</automated>
  </verify>
</task>

</tasks>

<verification>
Run:

```powershell
cd backend
python -m pytest tests/test_map_rules_service.py tests/test_map_evaluator_service.py -x -q
```
</verification>

<success_criteria>
- Phase 43 has a single source of truth for MAP precedence and price-basis semantics.
- Promotion normalization exists as a pure reusable module.
- All new fields are additive and backward compatible.
</success_criteria>
