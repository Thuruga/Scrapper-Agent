---
phase: 37-paridade-de-atributos-fundacao-sqlite
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - backend/services/vtex_api_scraper.py
  - backend/services/shopify_api_client.py
  - backend/services/engines/wake_engine.py
  - backend/services/engines/sfcc_parser.py
  - backend/services/engines/zara_parser.py
  - backend/services/engines/amazon_engine.py
  - backend/services/engines/mercado_livre_engine.py
  - backend/services/engines/netshoes_engine.py
  - backend/tests/test_phase37_engine_contract.py
  - backend/tests/test_vtex_api_client.py
  - backend/tests/test_shopify.py
  - backend/tests/test_wake_engine.py
  - backend/tests/test_sfcc_engine.py
  - backend/tests/test_zara_engine.py
  - backend/tests/test_amazon_engine.py
  - backend/tests/test_netshoes_engine.py
autonomous: true
requirements: [PARID-02, PARID-03]
must_haves:
  truths:
    - "Rich engines populate canonical typed fields when the source exposes them."
    - "Sparse engines return blanks/nulls rather than invented values."
    - "Alias normalization remains additive; raw/source keys still survive."
    - "No marketplace or engine reuses internal IDs as product_code."
---

<objective>
Bring the actual extraction paths up to the contract established in Plan 01. This plan focuses on engine and parser parity, especially for the brands explicitly called out as currently deficient.

Purpose: the shared projector alone does not improve parity if the engines never supply richer fields. This plan fills the contract where the source already exposes the data and preserves blanks where it does not.
Output: engine/parsers populate or safely omit the canonical fields under one tested contract.
</objective>

<context>
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-CONTEXT.md
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-RESEARCH.md
@.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-PATTERNS.md
@backend/core/models.py
@backend/services/product_contract.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - characterization tests for representative engines/parsers</name>
  <files>backend/tests/test_phase37_engine_contract.py</files>
  <action>
    Create a new characterization suite that feeds representative payloads/fixtures through the real mappers or parser helpers and asserts the canonical contract behavior. Cover at least:

    - VTEX as the rich reference
    - Shopify as mid-rich
    - Wake as sparse
    - SFCC and Zara parsers as sparse browser engines
    - one marketplace engine case showing blanks rather than invented `product_code`/`composition`
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_phase37_engine_contract.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rich-engine parity improvements</name>
  <files>backend/services/vtex_api_scraper.py, backend/services/shopify_api_client.py, backend/services/engines/wake_engine.py</files>
  <action>
    Improve the richest engines first:

    - VTEX: expose `product_code` when present in visible specs/source data and route any divergent spec names through additive alias normalization
    - Shopify: promote visible product/category/spec data into the canonical seam where possible without fabricating values
    - Wake: keep sparse fields blank where necessary, but populate any clearly available product code/category/spec signal
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_vtex_api_client.py backend/tests/test_shopify.py backend/tests/test_wake_engine.py backend/tests/test_phase37_engine_contract.py -q</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sparse-engine and marketplace alignment</name>
  <files>backend/services/engines/sfcc_parser.py, backend/services/engines/zara_parser.py, backend/services/engines/amazon_engine.py, backend/services/engines/mercado_livre_engine.py, backend/services/engines/netshoes_engine.py</files>
  <action>
    Align the sparse engines and marketplaces with the contract:

    - surface richer category/rating/review signals where already present
    - leave `product_code`, `composition`, or other absent fields blank rather than synthetic
    - route any source-specific attribute names through additive aliases if those engines expose them
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_sfcc_engine.py backend/tests/test_zara_engine.py backend/tests/test_amazon_engine.py backend/tests/test_netshoes_engine.py backend/tests/test_phase37_engine_contract.py -q</automated>
  </verify>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/test_phase37_engine_contract.py -q`
- `python -m pytest backend/tests/test_vtex_api_client.py backend/tests/test_shopify.py backend/tests/test_wake_engine.py backend/tests/test_sfcc_engine.py backend/tests/test_zara_engine.py backend/tests/test_amazon_engine.py backend/tests/test_netshoes_engine.py -q`
</verification>

<success_criteria>
- Engines now feed the canonical projector richer typed/source data where the source exposes it.
- Sparse engines remain truthful by leaving blanks rather than inventing values.
- Additive aliasing is preserved across all touched engines/parsers.
</success_criteria>

<output>
Create `.planning/phases/37-paridade-de-atributos-funda-o-sqlite/37-02-SUMMARY.md` when done.
</output>
