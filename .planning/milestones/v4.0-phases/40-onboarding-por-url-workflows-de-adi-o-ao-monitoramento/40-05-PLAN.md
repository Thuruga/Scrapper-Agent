---
phase: 40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento
plan: 05
type: execute
wave: 4
depends_on: ["40-02", "40-03", "40-04"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/App.tsx
autonomous: false
requirements: [UX-03, UX-04, UX-05]
must_haves:
  truths:
    - "Pasting a URL in onboarding calls POST /brands/identify and shows a pre-filled, editable confirmation form (name + engine override) before saving (UX-03, D-01/D-02/D-03)"
    - "Confirm saves via POST /brands/ passing the confirmed engine (never engine='auto') (Pitfall 7)"
    - "An 'Adicionar ao monitoramento' button appears on all 3 surfaces (comparative search, SKU search, category monitor) and calls the same dedup flow (UX-04, D-07)"
    - "The button reads response.status and toasts already_active / reactivated / created distinctly (D-09)"
    - "Marketplace toggles render in SettingsPage and call PATCH /brands/{key}/active (UX-05, D-10)"
    - "CategoryPage (l.531) and BannersPage (l.886) marketplace filters are KEPT; only the SettingsPage VIRTUAL guard is removed (Pitfalls 2/3/4)"
  artifacts:
    - path: "frontend/src/api/client.ts"
      provides: "identifyBrand() and addToMonitor() client methods"
      contains: "identifyBrand"
    - path: "frontend/src/App.tsx"
      provides: "onboarding confirmation form, add-to-monitor button x3, marketplace toggles"
      contains: "handleAddToMonitor"
  key_links:
    - from: "frontend/src/App.tsx onboarding form"
      to: "ApiClient.identifyBrand → ApiClient.saveBrand"
      via: "identify dry-run then confirm with confirmed engine"
      pattern: "identifyBrand"
    - from: "frontend/src/App.tsx product cards (3 surfaces)"
      to: "ApiClient.addToMonitor → POST /monitor/start"
      via: "handleAddToMonitor(url, brand)"
      pattern: "handleAddToMonitor"
    - from: "frontend/src/App.tsx SettingsPage"
      to: "ApiClient.setBrandActive (PATCH /brands/{key}/active)"
      via: "toggle rendered for marketplaces"
      pattern: "setBrandActive"
---

<objective>
Wire the three flows into the UI (UX-03/04/05). Add `identifyBrand()` and `addToMonitor()` to the
API client. Build the onboarding-by-URL confirmation form: paste URL → `POST /brands/identify`
(dry-run) → pre-filled, editable form (name + engine override, warning on unknown) → confirm via
`POST /brands/` passing the CONFIRMED engine (never `auto`, Pitfall 7). Add the "Adicionar ao
monitoramento" button to all 3 product surfaces (comparative search cards ~l.1524, cross-marketplace
cards ~l.2132, category-monitor modal cards ~l.2667), all calling the same idempotent
`ApiClient.addToMonitor(url, brand)` and toasting based on `response.status` (already_active /
reactivated / created). Expose the marketplace toggles by removing ONLY the SettingsPage `VIRTUAL`
guard (~l.2325/2332) — keeping the CategoryPage (l.531) and BannersPage (l.886) filters intact
(Pitfalls 2/3/4).

This plan is non-autonomous: it ends with a human-verify checkpoint for the cross-surface UI per
40-VALIDATION.md's Manual-Only Verifications (rendering/interaction cannot be asserted by the
backend test suite).

Purpose: Operator-facing delivery of all three success criteria, consistent with the app's visual
language (reuse the MGMT-02 inactive-row distinction).
Output: extended client.ts + App.tsx; human verification of the 3 flows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-CONTEXT.md
@.planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-RESEARCH.md
@.planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-PATTERNS.md
@.planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates these frontend symbols (recorded for convergence source-grounding):
- `ApiClient.identifyBrand(url)` (NEW client method)
- `ApiClient.addToMonitor(url, brand)` (NEW client method)
- `handleAddToMonitor(url, brand)` handler(s) wired on the 3 product surfaces (NEW)
- onboarding confirmation form/modal consuming identify → saveBrand (NEW UI)
- SettingsPage `VIRTUAL`/`canToggle` guard REMOVED (marketplace toggles now render)
- KEEP CategoryPage (l.531) + BannersPage (l.886) marketplace filters (unchanged)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add identifyBrand + addToMonitor to client.ts</name>
  <read_first>
    - frontend/src/api/client.ts (l.7-64 — request<T> generic + getBrands/saveBrand/setBrandActive; l.261-265 startMonitor)
    - .planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-PATTERNS.md (§ client.ts — identifyBrand + addToMonitor exact shapes; request generic pattern)
    - .planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-RESEARCH.md (Code Examples: ApiClient.identifyBrand, ApiClient.addToMonitor; D-05 defaults interval=10 duration=24)
  </read_first>
  <files>frontend/src/api/client.ts</files>
  <action>
    Add `static identifyBrand(url: string)` in the Brands section (after `setBrandActive`) calling
    `this.request<{ engine: string; inferred_name: string; domain: string; warning?: string }>(
    '/brands/identify', { method: 'POST', body: JSON.stringify({ url }) })`. Add
    `static addToMonitor(url: string, brand: string)` in the Monitors section (after `startMonitor`)
    calling `this.request<{ job_id: string; status: string; config: any }>('/monitor/start',
    { method: 'POST', body: JSON.stringify({ url, brand, interval: 10, duration: 24 }) })` (D-05
    defaults). Reuse the existing `request` generic; do NOT add a second HTTP layer. Keep `saveBrand`
    and `startMonitor` as-is (addToMonitor is a semantic helper over /monitor/start).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/src/api/client.ts` contains `static identifyBrand(` and `static addToMonitor(` (grep -c on each >= 1).
    - `identifyBrand` POSTs to `/brands/identify`; `addToMonitor` POSTs to `/monitor/start` with `interval: 10, duration: 24`.
    - `cd frontend && npx tsc --noEmit` passes (no type errors).
  </acceptance_criteria>
  <done>Client exposes identifyBrand and addToMonitor over the existing request generic; types compile.</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding confirmation form + add-to-monitor button on 3 surfaces + expose marketplace toggles</name>
  <read_first>
    - frontend/src/App.tsx (l.1524-1531 SearchPage card `<a href={p.url}>` with brandKey in scope; l.1578-1588 shipping button preventDefault/stopPropagation analog; l.2119-2135 CrossMarketplacePage cards with `marketplace` + `item.url`; l.2647-2704 MonitoredCategoriesPage modal cards with `selectedMonitor.brand` + `p.url`; l.2305-2376 SettingsPage VIRTUAL guard + toggle render; l.531 CategoryPage filter KEEP; l.886 BannersPage filter KEEP)
    - .planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-RESEARCH.md (Pattern 8 button inside <a>; Pitfall 2 SettingsPage VIRTUAL removal; Pitfalls 3/4 KEEP CategoryPage+BannersPage; Pitfall 6 button targets /monitor/start NOT category; Pitfall 7 confirmed engine never auto)
    - .planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-PATTERNS.md (§ App.tsx — button pattern, handleAddToMonitor handler, VIRTUAL removal, onboarding form: identify → pre-fill → saveBrand with confirmed engine)
    - .planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-CONTEXT.md (D-01..D-11 source of truth; Claude's Discretion on exact UI)
  </read_first>
  <files>frontend/src/App.tsx</files>
  <action>
    (a) Onboarding form (UX-03, D-01/D-02/D-03): add a URL-paste onboarding UI (a form/modal — place
    consistent with the existing brand-management area; visual style at Claude's discretion). On
    submit, call `ApiClient.identifyBrand(url)`; render a pre-filled, EDITABLE confirmation form with
    fields name (`inferred_name`), engine (a select allowing manual override), domain. If the
    response has a `warning` (engine='unknown'), show it as a non-blocking notice and still allow
    save (D-03). On confirm, call `ApiClient.saveBrand({ brand_name, domain, engine:
    <confirmedEngine>, ... })` passing the confirmed engine — NEVER `engine: 'auto'` (Pitfall 7) —
    then refresh the brand list and toast success. Name field always editable (D-01).
    (b) Add-to-monitor button (UX-04, D-04/D-07): add a `handleAddToMonitor = async (url, brand) =>`
    handler that calls `ApiClient.addToMonitor(url, brand)` and toasts by `result.status`:
    `already_active` → `toast.info('Produto já está em monitoramento')`; `reactivated` →
    `toast.success('Monitor reativado')`; else → `toast.success('Adicionado ao monitoramento')`;
    catch → `toast.error`. Render a small button (e.g. `<Plus size={14}/>`, title "Adicionar ao
    monitoramento") inside the product card on ALL THREE surfaces, each with
    `onClick={(e)=>{ e.preventDefault(); e.stopPropagation(); handleAddToMonitor(url, brand); }}`
    (mandatory because cards are `<a href>`): SearchPage cards (~l.1524, brand = `brandKey` in
    scope); CrossMarketplacePage cards (~l.2132, url = `item.url`, brand = the marketplace's brand_key
    derived from `marketplace` — map "Mercado Livre"→`mercado_livre`, "Netshoes"→`netshoes`,
    "Amazon"→`amazon`); MonitoredCategoriesPage modal cards (~l.2667, url = `p.url`, brand =
    `selectedMonitor.brand`). The button targets `/monitor/start` ONLY — never the category monitor
    (Pitfall 6).
    (c) Marketplace toggles (UX-05, D-10): in SettingsPage REMOVE the `const VIRTUAL = [...]` array
    and the `canToggle = !VIRTUAL.includes(...)` guard so the Power toggle renders for
    mercado_livre/netshoes/amazon (now real brands.json entries). Keep the inactive-row visual
    distinction (opacity 0.55 on `.brand-info` + "Inativa" badge) for marketplaces too. DO NOT touch
    the CategoryPage filter (l.531) or the BannersPage filter (l.886) — those are intentional and
    KEPT (Pitfalls 3/4).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `App.tsx` contains `handleAddToMonitor` and references `ApiClient.addToMonitor` (grep -c each >= 1).
    - The add-to-monitor button appears on all 3 surfaces: grep shows the button/onClick near the SearchPage (~l.1524), CrossMarketplacePage (~l.2132), and MonitoredCategoriesPage modal (~l.2667) cards (3 distinct call sites of handleAddToMonitor).
    - Onboarding form calls `ApiClient.identifyBrand` and then `ApiClient.saveBrand` with an explicit engine value, never the literal `engine: 'auto'` in the confirm path (grep -c "engine: 'auto'" in the onboarding/confirm code == 0).
    - SettingsPage no longer defines the `VIRTUAL` guard (grep -c "const VIRTUAL" frontend/src/App.tsx == 0) while `.filter(b => !['mercado_livre'` still appears for CategoryPage and the `virtualMarketplaces` Set still appears for BannersPage (both KEPT).
    - `cd frontend && npx tsc --noEmit && npm run build` succeeds.
  </acceptance_criteria>
  <done>Onboarding form (identify→confirm→save with confirmed engine), add-to-monitor button on 3 surfaces with status-aware toasts, and marketplace toggles all wired; build green; CategoryPage/BannersPage filters preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the 3 UI flows</name>
  <what-built>
    Onboarding-by-URL confirmation form (UX-03), "Adicionar ao monitoramento" button on comparative
    search / SKU search / category monitor (UX-04), and marketplace activate/deactivate toggles in
    settings (UX-05). All backend endpoints (identify dry-run, monitor dedup, per-request marketplace
    enforcement) shipped in Plans 02-04.
  </what-built>
  <how-to-verify>
    Start backend and frontend, then:
    1. UX-03: Open the brand onboarding UI. Paste a known brand URL (e.g. `https://www.hugoboss.com.br`).
       Confirm the form appears PRE-FILLED with an inferred name + detected engine, the name field is
       editable, and an engine override is available. Paste an unrecognized URL and confirm a
       non-blocking warning appears but you can still save. Save and confirm the brand appears in the
       brand list. Verify nothing was persisted by the identify step itself (only the explicit save).
    2. UX-04: Run a comparative search → click "Adicionar ao monitoramento" on a product card →
       confirm a success toast and the product appears once in the price monitors. Click it again on
       the same product → confirm an "já está em monitoramento" info toast and NO duplicate is created.
       Repeat the click test from the SKU search and from the category-monitor products modal — all
       three add to the SAME price-monitor list with dedup.
    3. UX-05: Open settings → confirm Mercado Livre / Netshoes / Amazon now show activate/deactivate
       toggles (with the inactive-row visual distinction). Deactivate one marketplace → run a
       cross-marketplace search → confirm that marketplace is absent from the results on the very next
       run. Reactivate → confirm it returns.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (which flow, expected vs actual).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → backend API | Frontend calls /brands/identify, /brands/, /monitor/start, /brands/{key}/active |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-08 | Tampering | onboarding confirm path | mitigate | Confirm sends the engine value chosen by the operator (never `auto`), avoiding a second server-side detect_engine that could yield a different result (Pitfall 7); operator reviews all fields before save |
| T-40-09 | Spoofing/Info Disclosure | URL passed to identify | transfer | SSRF/scheme validation is enforced server-side in Plan 02 (T-40-SSRF); the frontend only forwards the URL — no client-side trust assumed |
| T-40-SC | Tampering | npm installs | accept | No new frontend packages — reuses existing ApiClient.request, lucide `Plus` icon, and toast already imported; RESEARCH Package Legitimacy Audit: zero new deps |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` passes (Task 1 + Task 2).
- `cd frontend && npm run build` succeeds.
- Human-verify checkpoint (Task 3) approves all 3 flows per 40-VALIDATION.md Manual-Only Verifications.
- Backend regression (Plans 01-04): `cd backend && python -m pytest tests/ -x` green.
</verification>

<success_criteria>
- UX-03: paste URL → pre-filled editable confirmation form (engine override, unknown warning) → save with confirmed engine.
- UX-04: add-to-monitor button on all 3 surfaces → single dedup flow with status-aware toasts.
- UX-05: marketplace toggles visible in settings; deactivation excludes the marketplace on the next cross-marketplace search.
</success_criteria>

<output>
Create `.planning/phases/40-onboarding-por-url-workflows-de-adi-o-ao-monitoramento/40-05-SUMMARY.md` when done.
</output>
