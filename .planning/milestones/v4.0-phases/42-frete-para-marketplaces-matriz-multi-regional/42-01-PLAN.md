---
phase: 42-frete-para-marketplaces-matriz-multi-regional
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/shipping/base.py
  - backend/services/shipping/resolver.py
  - backend/config.py
  - backend/services/shipping/mercado_livre.py
  - backend/services/shipping/amazon.py
  - backend/services/shipping/netshoes.py
  - backend/services/engines/mercado_livre_engine.py
  - backend/services/engines/amazon_engine.py
  - backend/tests/test_shipping_resolver.py
  - backend/tests/test_marketplace_shipping.py
autonomous: true
requirements: [FRET-08]
must_haves:
  truths:
    - "resolve_shipping_provider returns MercadoLivreShipping / AmazonShipping / NetshoesShipping for engine strings mercadolivre / amazon / netshoes"
    - "A Mercado Livre shipping calculation returns ShippingState.AVAILABLE with cost and (when the API exposes it) delivery-time populated"
    - "An Amazon shipping calculation extracts delivery-time text alongside price when the delivery block contains it"
    - "A Netshoes shipping calculation returns ShippingState.BLOCKED (never a fake 0.0/free) when the Playwright flow yields no result"
    - "ShippingState.BLOCKED exists with message 'Bloqueado (anti-bot)'"
  artifacts:
    - path: "backend/services/shipping/base.py"
      provides: "ShippingState.BLOCKED constant + DEFAULT_MESSAGES entry 'Bloqueado (anti-bot)'"
      contains: "BLOCKED"
    - path: "backend/services/shipping/resolver.py"
      provides: "3 new engine branches (mercadolivre/amazon/netshoes)"
      contains: "mercadolivre"
    - path: "backend/services/shipping/mercado_livre.py"
      provides: "MercadoLivreShipping BaseShipping adapter"
      exports: ["MercadoLivreShipping"]
    - path: "backend/services/shipping/amazon.py"
      provides: "AmazonShipping BaseShipping adapter"
      exports: ["AmazonShipping"]
    - path: "backend/services/shipping/netshoes.py"
      provides: "NetshoesShipping BaseShipping adapter with BLOCKED mapping"
      exports: ["NetshoesShipping"]
    - path: "backend/config.py"
      provides: "SHIPPING_MATRIX_THROTTLE_SECONDS + SHIPPING_MATRIX_CACHE_TTL_SECONDS settings"
      contains: "SHIPPING_MATRIX_THROTTLE_SECONDS"
  key_links:
    - from: "backend/services/shipping/resolver.py"
      to: "backend/services/shipping/mercado_livre.py"
      via: "lazy import inside if engine == 'mercadolivre' branch"
      pattern: "from services.shipping.mercado_livre import MercadoLivreShipping"
    - from: "backend/services/shipping/mercado_livre.py"
      to: "backend/services/engines/mercado_livre_engine.py"
      via: "delegate to engine.calculate_shipping_advanced"
      pattern: "calculate_shipping_advanced"
---

<objective>
Extend the Phase 41 shipping abstraction so `resolve_shipping_provider` dispatches the three marketplace engines (Mercado Livre, Amazon, Netshoes) to new `BaseShipping` adapters (D-03) that wrap each engine's already-proven scraping logic, add delivery-time extraction (D-02), and map Netshoes' documented Akamai block to a new explicit `ShippingState.BLOCKED` (D-01). Also add the `SHIPPING_MATRIX_*` config settings that Plan 02's regional matrix consumes.

Purpose: FRET-08 — the system must return `shipping_cost` and `shipping_time` for all three marketplaces, never a fake zero/free value on failure. This plan is the backend foundation both Plan 02 (matrix) and Plan 03 (frontend) build on.
Output: `ShippingState.BLOCKED`; three new provider files; 3 resolver branches; delivery-time extension in the ML and Amazon engines; two `SHIPPING_MATRIX_*` config settings; Wave-0 hermetic tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-CONTEXT.md
@.planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-RESEARCH.md
@.planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-PATTERNS.md
@.planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Symbols created or modified across the whole of Phase 42 (this section is identical in all Phase 42 plans; the source-grounding pass uses it to exclude newly-created symbols from drift verification):

**Backend — Python**
- `ShippingState.BLOCKED` constant (value `"blocked"`) and `DEFAULT_MESSAGES[ShippingState.BLOCKED] = "Bloqueado (anti-bot)"` in `backend/services/shipping/base.py`
- `MercadoLivreShipping` class in `backend/services/shipping/mercado_livre.py`
- `AmazonShipping` class in `backend/services/shipping/amazon.py`
- `NetshoesShipping` class in `backend/services/shipping/netshoes.py`
- New resolver branches for engine strings `mercadolivre`, `amazon`, `netshoes` in `backend/services/shipping/resolver.py`
- Settings `SHIPPING_MATRIX_THROTTLE_SECONDS` (float) and `SHIPPING_MATRIX_CACHE_TTL_SECONDS` (int) in `backend/config.py`
- Delivery-time keys added to marketplace engine shipping dicts: `estimated_delivery_days` and/or `delivery_raw_text` in `MercadoLivreEngine._fetch_shipping_options` and `AmazonEngine._parse_shipping_text`
- `calculate_regional_matrix(product, brand, cep_list, *, triggered_by)` in `backend/services/shipping/regional_matrix.py` (Plan 02)
- `_read_matrix_cache` / `_write_matrix_cache` / `_stable_product_identity` helpers in `regional_matrix.py` (Plan 02)
- Route handler `calculate_shipping_matrix` + models `CalculateShippingMatrixRequest`, `ShippingMatrixRegionResult`, `CalculateShippingMatrixResponse` in `backend/api/routes_search.py` (Plan 02)
- New data files `backend/data/cep_matrix.json`, `backend/data/shipping_matrix_cache.json` (Plan 02)

**Frontend — TypeScript**
- `requestMatrix` handler, `matrixState`/`matrixModal` state, and a "Matriz Regional" button + "Frete por região" modal in `frontend/src/App.tsx` (Plan 03)
- `blocked` state branch in the shipping-state rendering + `mercadolivre`/`amazon`/`netshoes` added to `isBrandShippingSupported` (Plan 03)

**Tests**
- `backend/tests/test_marketplace_shipping.py` (Plan 01)
- `backend/tests/test_shipping_regional_matrix.py` (Plan 02)
- New cases in `backend/tests/test_shipping_resolver.py`, `backend/tests/test_non_vtex_shipping_route.py` (Plans 01/02)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 tests + ShippingState.BLOCKED + config settings + resolver branches</name>
  <files>backend/services/shipping/base.py, backend/services/shipping/resolver.py, backend/config.py, backend/tests/test_shipping_resolver.py, backend/tests/test_marketplace_shipping.py</files>
  <read_first>
    - backend/services/shipping/base.py (current ShippingState — has AVAILABLE, UNAVAILABLE_FOR_CEP, TEMPORARY_FAILURE, UNSUPPORTED; DEFAULT_MESSAGES has no BLOCKED)
    - backend/services/shipping/resolver.py (current chain: only shopify + wake, lazy imports inside each branch, falls through to UnsupportedShipping)
    - backend/services/stock_depth/base.py (StockDepthState.BLOCKED precedent to mirror)
    - backend/config.py lines 126-154 (DEFAULT_CEP + the Phase 44 STOCK_PROBE_* block — copy the Field(default=..., description="...") style)
    - backend/tests/test_shipping_resolver.py (existing resolver test shape: _brand(engine) helper builds a DynamicBrand with engine=engine)
    - backend/data/brands.json lines 567-627 (confirm engine field values: "mercadolivre", "netshoes", "amazon" — NO underscore; brand_key uses underscore for mercado_livre)
  </read_first>
  <behavior>
    - test_resolver_returns_mercado_livre_provider: resolve_shipping_provider(_brand("mercadolivre")) is a MercadoLivreShipping instance.
    - test_resolver_returns_amazon_provider: resolve_shipping_provider(_brand("amazon")) is an AmazonShipping instance.
    - test_resolver_returns_netshoes_provider: resolve_shipping_provider(_brand("netshoes")) is a NetshoesShipping instance.
    - test_resolver_underscore_mercado_livre_falls_through: resolve_shipping_provider(_brand("mercado_livre")) is an UnsupportedShipping instance (the underscore brand_key form is NOT an engine value; guards against Pitfall 2).
    - test_shipping_state_blocked_exists: ShippingState.BLOCKED == "blocked" and DEFAULT_MESSAGES[ShippingState.BLOCKED] == "Bloqueado (anti-bot)".
    - test_config_has_shipping_matrix_settings: settings.SHIPPING_MATRIX_THROTTLE_SECONDS and settings.SHIPPING_MATRIX_CACHE_TTL_SECONDS exist and are numeric.
  </behavior>
  <action>
    Write the failing tests first in a NEW file backend/tests/test_marketplace_shipping.py for the BLOCKED-state and config assertions, and ADD the three resolver-branch cases plus the underscore-fall-through case to the existing backend/tests/test_shipping_resolver.py (reuse its _brand(engine) helper). Run them to confirm RED, then implement:

    In backend/services/shipping/base.py: add `BLOCKED = "blocked"` to the ShippingState class (after UNSUPPORTED), and add `ShippingState.BLOCKED: "Bloqueado (anti-bot)"` to DEFAULT_MESSAGES. Use the exact string "Bloqueado (anti-bot)" (verbatim vocabulary from frontend/src/App.tsx:437 — do not paraphrase). No other change to base.py: apply_shipping_calculation and status_shipping already fall through generically via DEFAULT_MESSAGES.get(state).

    In backend/services/shipping/resolver.py: add three branches AFTER the wake branch and BEFORE the UnsupportedShipping fallback, matching the exact engine strings `"mercadolivre"`, `"amazon"`, `"netshoes"` (NO underscore — verified in brands.json). Each branch does a lazy import inside the branch body exactly like the existing shopify/wake branches: `from services.shipping.mercado_livre import MercadoLivreShipping` → `return MercadoLivreShipping()`, and likewise for amazon.py → AmazonShipping and netshoes.py → NetshoesShipping. Keep the `engine = str(get_field(brand, "engine", "") or "").lower()` line unchanged.

    In backend/config.py: add a `# Phase 42 - Matriz de frete multi-regional (FRET-09)` comment block immediately after the Phase 44 STOCK_PROBE block (after line 153, before `model_config`), with two settings following the exact Field style: `SHIPPING_MATRIX_THROTTLE_SECONDS: float = Field(default=2.0, description="Throttle fixo entre chamadas de frete da matriz regional (uma por CEP).")` and `SHIPPING_MATRIX_CACHE_TTL_SECONDS: int = Field(default=21600, description="TTL do cache (produto, CEP) da matriz regional — 6h default, curto por D-09.")`.

    The three provider files (mercado_livre.py, amazon.py, netshoes.py) are created in Tasks 2 and 3; import them lazily in the resolver so the resolver tests only pass once those files exist — sequence Task 2/3 before running the full resolver suite green. If you prefer resolver tests green immediately, create the three provider files as minimal stubs subclassing BaseShipping in this task and flesh them out in Tasks 2-3.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_shipping_resolver.py tests/test_marketplace_shipping.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `ShippingState.BLOCKED == "blocked"` (source assertion) and `DEFAULT_MESSAGES[ShippingState.BLOCKED] == "Bloqueado (anti-bot)"` (exact string).
    - resolver.py contains three new `if engine == "mercadolivre"` / `"amazon"` / `"netshoes"` branches with lazy imports; the underscore form `mercado_livre` is NOT matched anywhere (resolves to UnsupportedShipping).
    - `settings.SHIPPING_MATRIX_THROTTLE_SECONDS` and `settings.SHIPPING_MATRIX_CACHE_TTL_SECONDS` are defined with numeric defaults.
    - `cd backend && python -m pytest tests/test_shipping_resolver.py tests/test_marketplace_shipping.py -x -q` passes.
  </acceptance_criteria>
  <done>All resolver-branch, BLOCKED-state, and config tests pass; BLOCKED state + config settings + 3 resolver branches exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MercadoLivreShipping + AmazonShipping adapters with delivery-time extraction (D-02)</name>
  <files>backend/services/shipping/mercado_livre.py, backend/services/shipping/amazon.py, backend/services/engines/mercado_livre_engine.py, backend/services/engines/amazon_engine.py, backend/tests/test_marketplace_shipping.py</files>
  <read_first>
    - backend/services/shipping/wake.py (the exact house adapter pattern to mirror: calculate() → normalize_zipcode ValueError → UNAVAILABLE_FOR_CEP; is_url_allowed_for_brand → UNSUPPORTED; try/except → TEMPORARY_FAILURE logging only brand_key + type(exc).__name__, never the CEP; success → build ShippingInfo list, sorted_shipping_options, state AVAILABLE)
    - backend/services/engines/mercado_livre_engine.py lines 649-829 (calculate_shipping, _fetch_shipping_options returning {"is_free_shipping": bool, "shipping_price": float|None} from data["options"][*]["cost"]; calculate_shipping_advanced with Playwright/Anubis fallback)
    - backend/services/engines/amazon_engine.py lines 471-632 (calculate_shipping_advanced returning {"is_free_shipping", "shipping_price"} OR {"error": "..."} on CAPTCHA; _parse_shipping_text; _read_delivery_text selectors)
    - backend/core/models.py lines 13-48 (ShippingInfo fields: price, status, estimated_delivery_days, raw_text, service_name, service_id, estimate_display, estimate_unit, is_free_shipping)
    - backend/services/shipping/base.py (ShippingCalculation, ShippingState, get_field, is_url_allowed_for_brand, normalize_zipcode, sorted_shipping_options)
    - .planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-RESEARCH.md Assumptions Log A1 + Open Question 1 (ML delivery-time field shape is MEDIUM confidence — log one real response before finalizing the parser; degrade to cost-only if the field is absent)
  </read_first>
  <behavior>
    - test_mercado_livre_provider_available_maps_cost_and_time: given a fake MercadoLivreEngine whose calculate_shipping_advanced returns a dict with a price and a delivery-time value, MercadoLivreShipping.calculate(product, cep, brand) returns state AVAILABLE with one ShippingInfo whose price matches and whose estimated_delivery_days/estimate_display/raw_text carries the delivery-time when present.
    - test_mercado_livre_provider_free_shipping: is_free_shipping True and shipping_price 0.0 → ShippingInfo.price == 0.0, is_free_shipping True, state AVAILABLE.
    - test_mercado_livre_provider_invalid_cep: a non-8-digit CEP → state UNAVAILABLE_FOR_CEP, no engine call.
    - test_mercado_livre_provider_url_host_mismatch: product URL host not matching the brand domain → state UNSUPPORTED before any engine call.
    - test_mercado_livre_provider_none_result: engine returns None → state TEMPORARY_FAILURE (never a fake 0.0).
    - test_amazon_provider_extracts_delivery_time: _parse_shipping_text on a delivery-block string containing both a price and a "Receba ... N dias" phrase returns a dict that also carries the delivery-time (e.g. estimated_delivery_days or delivery_raw_text); AmazonShipping maps it into ShippingInfo.
    - test_amazon_provider_captcha_is_temporary_not_blocked: engine returns {"error": "...CAPTCHA..."} → AmazonShipping returns state TEMPORARY_FAILURE (NOT BLOCKED — CAPTCHA is transient per D-02/PATTERNS, unlike Netshoes' permanent Akamai block).
  </behavior>
  <action>
    Write the tests above in backend/tests/test_marketplace_shipping.py first (RED). Use a fake engine object injected via each provider's __init__ (mirror the WakeShipping session_factory injection style) so tests are hermetic with NO live network — build providers as MercadoLivreShipping(engine=FakeEngine()).

    Extend the delivery-time extraction in the ENGINE layer (D-02 says extend _parse_shipping_text / _fetch_shipping_options, not just the adapter):
    - In backend/services/engines/mercado_livre_engine.py _fetch_shipping_options: after computing is_free/shipping_price from data["options"], also read a delivery-time value from each option. Per RESEARCH.md A1, the ML API exposes an `estimated_delivery_time` object (MEDIUM confidence: fields like `unit`/`time_frame`/`date`). Add the delivery value to the returned dict as `estimated_delivery_days` (int, when a day count is derivable) and/or `delivery_raw_text` (str fallback). Guard every field access with .get() so a missing field degrades gracefully to cost-only (matches today's behavior). Add a debug log of one raw response shape gated behind logger.debug — do NOT log the CEP.
    - In backend/services/engines/amazon_engine.py _parse_shipping_text: add a delivery-time regex branch that also matches phrases like "Receba ... em até N dias", "Chegará entre DD e DD", "amanhã" from the SAME text blob already read — no new page reads. Return the delivery info in the dict as `estimated_delivery_days` (when parseable) and/or `delivery_raw_text` (the raw phrase). Preserve the existing free/paid return keys; only ADD delivery keys (additive — do not break the existing test_amazon_parses_free_shipping_text / test_amazon_parses_paid_shipping_text which assert exact dict equality: update those two existing tests to the new expected dict shape OR make the delivery keys optional so equality still holds — prefer adding keys and updating the two existing assertions to include the new keys).

    Create backend/services/shipping/mercado_livre.py (D-03 — thin adapter over the existing engine, not a rewrite): a MercadoLivreShipping(BaseShipping) class following wake.py's calculate() skeleton verbatim (CEP validation → UNAVAILABLE_FOR_CEP; URL guard via is_url_allowed_for_brand → UNSUPPORTED; try/except → TEMPORARY_FAILURE with brand_key+exc-type logging only). __init__ accepts an optional engine param defaulting to a lazily-imported MercadoLivreEngine (import inside __init__, not module top — Pitfall 1). Delegate to `self.engine.calculate_shipping_advanced(product_url, cep)`. Map the returned dict into a single ShippingInfo (price, is_free_shipping, status "Gratis"/"Disponivel", estimated_delivery_days/estimate_display/raw_text from the delivery-time keys), wrap in a one-element list, return ShippingCalculation(state=AVAILABLE, shipping_options=sorted_shipping_options([info])). None result → TEMPORARY_FAILURE.

    Create backend/services/shipping/amazon.py: an AmazonShipping(BaseShipping) class with the same skeleton. Delegate to `self.engine.calculate_shipping_advanced(product_url, cep)`. If the result dict has an "error" key (CAPTCHA), return ShippingCalculation(state=TEMPORARY_FAILURE, message=result["error"]) — explicitly NOT BLOCKED. Otherwise map into a ShippingInfo including delivery-time keys.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_marketplace_shipping.py tests/test_shipping_engines.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/services/shipping/mercado_livre.py` exports `MercadoLivreShipping(BaseShipping)`; `backend/services/shipping/amazon.py` exports `AmazonShipping(BaseShipping)`.
    - Both providers reject invalid CEP with UNAVAILABLE_FOR_CEP and host-mismatch URLs with UNSUPPORTED before calling the engine (behavior assertion via test).
    - Amazon `{"error": ...}` (CAPTCHA) maps to TEMPORARY_FAILURE, never BLOCKED (behavior assertion).
    - `MercadoLivreEngine._fetch_shipping_options` and `AmazonEngine._parse_shipping_text` return delivery-time information additively; no CEP appears in any log statement in the new/modified code.
    - `cd backend && python -m pytest tests/test_marketplace_shipping.py tests/test_shipping_engines.py -x -q` passes.
  </acceptance_criteria>
  <done>ML and Amazon providers exist, map cost+delivery-time into ShippingInfo, handle CEP/URL guards and CAPTCHA-as-temporary; delivery-time extraction added to both engines; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NetshoesShipping adapter with BLOCKED-state mapping (D-01)</name>
  <files>backend/services/shipping/netshoes.py, backend/tests/test_marketplace_shipping.py</files>
  <read_first>
    - backend/services/shipping/wake.py (adapter skeleton to mirror)
    - backend/services/engines/netshoes_engine.py lines 553-635 (_run_playwright_shipping returns None both on Akamai block AND on "no shipping element found"; calculate_shipping_advanced wraps it via asyncio.to_thread)
    - backend/services/shipping/base.py (ShippingState.BLOCKED added in Task 1)
    - .planning/debug/monitor-marketplace-pendente.md (Netshoes "Access Denied" ~343-byte Akamai signature — documented, reproducible, infra-only; the basis for D-01's "default conservatively to BLOCKED")
    - .planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-PATTERNS.md lines 92-101 (Netshoes wrapper guidance: engine does not distinguish block vs no-result today; default conservatively to BLOCKED for Netshoes)
  </read_first>
  <behavior>
    - test_netshoes_provider_blocked_when_engine_returns_none: given a fake NetshoesEngine whose calculate_shipping_advanced returns None, NetshoesShipping.calculate(product, cep, brand) returns state BLOCKED with message "Bloqueado (anti-bot)" — NEVER a ShippingInfo with price 0.0 and NEVER TEMPORARY_FAILURE.
    - test_netshoes_provider_available_when_engine_returns_price: if the engine ever returns a real {"is_free_shipping": False, "shipping_price": 19.9} dict, NetshoesShipping returns state AVAILABLE with that price (block is not hard-coded — D-01 "tentar ao vivo").
    - test_netshoes_provider_invalid_cep: non-8-digit CEP → UNAVAILABLE_FOR_CEP, no engine call.
    - test_netshoes_provider_url_host_mismatch: URL host not matching brand domain → UNSUPPORTED before engine call.
  </behavior>
  <action>
    Write the tests above in backend/tests/test_marketplace_shipping.py first (RED). Inject a fake engine via NetshoesShipping(engine=FakeEngine()) for hermetic tests — no live network, no Playwright.

    Create backend/services/shipping/netshoes.py: a NetshoesShipping(BaseShipping) class following wake.py's calculate() skeleton (CEP validation → UNAVAILABLE_FOR_CEP; URL guard → UNSUPPORTED). __init__ accepts an optional engine param defaulting to a lazily-imported NetshoesEngine (import inside __init__ — Pitfall 1). Delegate to `self.engine.calculate_shipping_advanced(product_url, cep)`.

    Result mapping (D-01): because the engine returns None both on Akamai block and on genuine no-result, and the documented reality is a permanent infra block, map a None result to ShippingState.BLOCKED with message "Bloqueado (anti-bot)" (use DEFAULT_MESSAGES via the message default, or pass the string explicitly). A truthy dict with a price maps to AVAILABLE (build a ShippingInfo like the ML/Amazon providers). Wrap any exception in try/except → TEMPORARY_FAILURE (network blip during the attempt), logging only brand_key + type(exc).__name__ — never the CEP. This preserves the "tentar ao vivo, cair em blocked" ordering: exception during attempt = temporary; clean None = blocked; real dict = available.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_marketplace_shipping.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/services/shipping/netshoes.py` exports `NetshoesShipping(BaseShipping)`.
    - Engine None result maps to state BLOCKED with message "Bloqueado (anti-bot)" — never price 0.0, never free (behavior assertion).
    - A real price dict maps to AVAILABLE; invalid CEP → UNAVAILABLE_FOR_CEP; host mismatch → UNSUPPORTED (behavior assertions).
    - No CEP appears in any log statement in netshoes.py.
    - `cd backend && python -m pytest tests/test_marketplace_shipping.py -x -q` passes, and the full resolver suite `cd backend && python -m pytest tests/test_shipping_resolver.py -x -q` passes (all 3 provider classes now importable).
  </acceptance_criteria>
  <done>NetshoesShipping exists, maps documented block to BLOCKED (never fake free), and all Wave-0 marketplace-provider + resolver tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API client → /search/* routes | Untrusted request body (brand_key, product_url, zipcode) crosses into shipping providers |
| Provider → marketplace host (api.mercadolibre.com / amazon.com.br / netshoes.com.br) | Outbound HTTP/Playwright to external hosts derived from product URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Spoofing/Tampering (SSRF) | product_url passed to the 3 new providers | mitigate | Each provider calls `is_url_allowed_for_brand(product_url, brand)` (existing Phase 41 helper in base.py) before any outbound call, exactly as WakeShipping does — host must match the persisted brand domain. Covered by url-host-mismatch tests in Tasks 2 and 3. |
| T-42-02 | Information Disclosure | CEP/PII in provider throttle/error logs | mitigate | New/modified provider and engine code logs only `brand_key` + `type(exc).__name__` (Phase 41 D-21 precedent), never the raw CEP. Verified by acceptance criterion "no CEP appears in any log statement". |
| T-42-03 | Tampering (false-zero shipping) | Netshoes block path | mitigate | Block maps to explicit `ShippingState.BLOCKED`, never to `price=0.0`/free (inherited Phase 33/41 rule). Covered by test_netshoes_provider_blocked_when_engine_returns_none. |
| T-42-04 | Repudiation/Regression | Existing marketplace engine shipping paths + Phase 41 VTEX/Wake/Shopify providers | mitigate | New providers wrap (do not rewrite) engine methods; engine changes are additive (delivery-time keys only). Full backend suite runs at wave merge to confirm no regression. |
| T-42-SC | Tampering | npm/pip/cargo installs | mitigate | Phase installs zero new packages (RESEARCH.md Package Legitimacy Audit: "Not applicable"). No install task exists; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_shipping_resolver.py tests/test_marketplace_shipping.py tests/test_shipping_engines.py -x -q` passes.
- `cd backend && python -m pytest -q` (full suite) green before the wave merge — confirms no regression to existing shipping/VTEX/Wake/Shopify tests.
- No `slopcheck`/package-legitimacy checkpoint needed (zero new dependencies).
</verification>

<success_criteria>
- `resolve_shipping_provider` returns MercadoLivreShipping / AmazonShipping / NetshoesShipping for the exact engine strings `mercadolivre` / `amazon` / `netshoes`, and UnsupportedShipping for the underscore `mercado_livre` form.
- ML and Amazon providers return AVAILABLE with cost + delivery-time (when the source exposes it); Amazon CAPTCHA maps to TEMPORARY_FAILURE.
- Netshoes maps its documented block to BLOCKED ("Bloqueado (anti-bot)"), never a fake free value.
- `SHIPPING_MATRIX_THROTTLE_SECONDS` and `SHIPPING_MATRIX_CACHE_TTL_SECONDS` exist in config for Plan 02 to consume.
</success_criteria>

<output>
Create `.planning/phases/42-frete-para-marketplaces-matriz-multi-regional/42-01-SUMMARY.md` when done
</output>
