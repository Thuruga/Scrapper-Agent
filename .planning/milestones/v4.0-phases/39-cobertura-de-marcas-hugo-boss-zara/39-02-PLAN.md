---
phase: 39-cobertura-de-marcas-hugo-boss-zara
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/010-zara-product-price/experiment.py
  - .planning/spikes/010-zara-product-price/REPORT.md
autonomous: false
requirements: [COMP-07]
user_setup: []

must_haves:
  truths:
    - "Existe um spike isolado em .planning/spikes/010-zara-product-price/ que prova (ou refuta) extração pública de produto+preço da Zara via browser stealth (D-05, D-06, COMP-07 critério #3)"
    - "experiment.py não importa de backend/ como efeito colateral nem grava qualquer arquivo dentro de backend/ (regra de gate D-08/D-10)"
    - "REPORT.md contém um veredito GO/NO-GO/GO_TECHNICAL explícito + evidência reprodutível (probes, status, produtos extraídos por rodada) (D-05, D-09)"
    - "O fluxo usa apenas browser público + playwright-stealth, queries camiseta/calça e filtro masculino — sem proxy pago/CAPTCHA/login (D-06, D-07)"
    - "O veredito é registrado como decisão do operador que destrava (GO) ou cancela (NO-GO) o Plano 03"
  artifacts:
    - path: ".planning/spikes/010-zara-product-price/experiment.py"
      provides: "Probe Playwright-stealth da busca pública da Zara BR; extrai título+URL+preço; 2 rodadas; choose_verdict; write_report"
      contains: "Stealth"
    - path: ".planning/spikes/010-zara-product-price/REPORT.md"
      provides: "Veredito GO/NO-GO + evidência reprodutível (gerado pelo experiment.py)"
      contains: "GO"
  key_links:
    - from: ".planning/spikes/010-zara-product-price/experiment.py"
      to: "playwright_stealth.Stealth"
      via: "apply_stealth_sync no context Playwright público"
      pattern: "Stealth\\(\\)"
    - from: ".planning/spikes/010-zara-product-price/experiment.py"
      to: ".planning/spikes/010-zara-product-price/REPORT.md"
      via: "write_report grava o veredito em REPORT.md no finally"
      pattern: "REPORT_PATH"
---

<objective>
Gate de viabilidade GO/NO-GO da Zara (COMP-07, critério #3): construir um spike isolado em `.planning/spikes/010-zara-product-price/` que navega à busca pública da Zara BR via browser + `playwright-stealth`, tenta extrair título + URL + preço de ≥3 produtos reais para as queries `camiseta`/`calça` (filtro masculino), repete o fluxo uma segunda vez e emite um veredito GO/NO-GO explícito com evidência reprodutível em `REPORT.md`. NENHUM código de engine Zara é escrito neste plano — o veredito deste spike destrava (GO) ou cancela (NO-GO) o Plano 03.

Purpose: Decidir com base em evidência (não suposição) se vale construir o `InditexEngine`, espelhando o gate da Phase 32 (Wake) e da Phase 36 (Lacoste). Hard gate D-08: zero código de engine antes do GO.
Output: `experiment.py` + `REPORT.md` no diretório do spike 010, fora de `backend/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-CONTEXT.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-RESEARCH.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-PATTERNS.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-VALIDATION.md
@.planning/spikes/008-lacoste-antibot-zara-recheck/experiment.py
@.planning/spikes/008-lacoste-antibot-zara-recheck/REPORT.md
</context>

## Artifacts this phase produces (Plano 02 — Zara spike 010)

**Arquivos novos:**
- `.planning/spikes/010-zara-product-price/experiment.py` — probe Playwright-stealth da busca pública da Zara BR.
- `.planning/spikes/010-zara-product-price/REPORT.md` — veredito GO/NO-GO + evidência (gerado pelo `experiment.py`).

**Símbolos/funções novos (dentro de experiment.py, fora de backend/):**
- `ProbeResult` (dataclass), `_new_context`, `_detect_block`, `choose_verdict`, `write_report`, `main` — adaptados do spike 008.
- Constante `ZARA_SEARCH_URL = "https://www.zara.com/br/pt/search?searchTerm={query}&section=MAN"`.

**Decisão registrada:**
- Veredito GO / NO-GO / GO_TECHNICAL da viabilidade pública da Zara (alimenta a decisão de execução do Plano 03).

<tasks>

<task type="auto">
  <name>Task 1: Spike 010 — experiment.py de viabilidade pública da Zara</name>
  <files>.planning/spikes/010-zara-product-price/experiment.py</files>
  <read_first>
    - .planning/spikes/008-lacoste-antibot-zara-recheck/experiment.py (analog exato — imports/CHROMIUM_ARGS/STEALTH_ARGS/HEADERS L1–49; `ProbeResult` L52–66; `_new_context` L69–77; `_detect_block` L80–91; stealth apply L144–160; cleanup finally L178–184; `choose_verdict` L252–257; `write_report` L284–357; `main` try/except/finally L360–406)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-PATTERNS.md (seção "experiment.py" — todos os snippets + URL base e choose_verdict adaptado a D-05)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-RESEARCH.md (Padrão 3 + Armadilhas 3/4: section=MAN, JSON-LD na busca ≠ home)
  </read_first>
  <action>
    Criar `experiment.py` espelhando o spike 008, focado SÓ na Zara. Configurar contexto pt-BR coerente: `viewport={"width":1366,"height":768}`, `locale="pt-BR"`, `timezone_id="America/Sao_Paulo"`, USER_AGENT Chrome + HEADERS Accept-Language pt-BR, e `Stealth().apply_stealth_sync(context)` (browser público headless, sem proxy/login — D-06). Usar `ZARA_SEARCH_URL = "https://www.zara.com/br/pt/search?searchTerm={query}&section=MAN"` com as queries `camiseta` e `calça` (D-07; filtro masculino `section=MAN`, NÃO `WOMAN` — Armadilha 3/CAT-01). Navegar com `wait_until="domcontentloaded"`, aguardar carregamento dinâmico (`time.sleep` curto + opcionalmente `networkidle`), capturar `page.content()`. Para extrair produtos, investigar nesta ordem de fallback (Armadilha 4): (a) todos os `<script type="application/ld+json">` da página de BUSCA procurando `ItemList`/array de produtos; (b) interceptação de request/response de rede para um endpoint JSON de busca; (c) parsing de tiles de produto via seletores CSS. De cada produto extrair título (não vazio), URL absoluta em `zara.com/br` e preço numérico positivo. Rodar o fluxo DUAS vezes (first_round, second_round). Implementar `_detect_block` (status 403, "access denied", html < 1000 bytes), `choose_verdict(first, second)` por D-05 (GO se `len(first)>=3 and len(second)>=3`; GO_TECHNICAL se ≥1 mas sem repetição estável; senão NO-GO) e `write_report(...)` gravando `REPORT.md` com veredito explícito + tabelas de probes e produtos. `main()` envolve tudo em try/except/finally que SEMPRE grava o REPORT e SEMPRE fecha page/context/browser. Baixa frequência (poucas tentativas, sleeps). REGRA DE GATE: `experiment.py` fica exclusivamente em `.planning/spikes/010-*/`; NÃO importar de `backend/` como efeito colateral nem gravar nada dentro de `backend/` (D-08/D-10/T-39-SPIKE).
  </action>
  <verify>
    <automated>cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -c "import ast,sys; src=open('.planning/spikes/010-zara-product-price/experiment.py',encoding='utf-8').read(); ast.parse(src); sys.exit(0 if ('section=MAN' in src and 'Stealth' in src and 'choose_verdict' in src and 'write_report' in src and 'zara.com/br' in src and 'WOMAN' not in src and 'from backend' not in src and 'import backend' not in src) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo parseia como Python válido.
    - Usa `section=MAN` e NÃO contém `WOMAN` (filtro masculino — D-07/CAT-01).
    - Contém `Stealth`, `choose_verdict`, `write_report` e referência a `zara.com/br`.
    - NÃO importa de `backend/` (`from backend`/`import backend` ausentes — regra de gate D-08/D-10).
    - `choose_verdict` retorna "GO" apenas quando ambas as rodadas têm ≥3 produtos (D-05).
  </acceptance_criteria>
  <done>experiment.py existe, parseia, respeita o envelope D-06/D-07 e a regra de gate de isolamento.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Executar o spike (2 rodadas), gerar REPORT.md e registrar o veredito GO/NO-GO</name>
  <read_first>
    - .planning/spikes/010-zara-product-price/experiment.py (criado na Task 1)
    - .planning/spikes/008-lacoste-antibot-zara-recheck/REPORT.md (forma de referência do REPORT)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-VALIDATION.md (Manual-Only Verifications — veredito de viabilidade pública da Zara)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-CONTEXT.md (D-05/D-09 — critério de GO e comportamento em NO-GO)
  </read_first>
  <what-built>
    `experiment.py` (Task 1) que probes a busca pública da Zara BR via stealth, extrai produtos em 2 rodadas, avalia o gate D-05 e grava `REPORT.md` com veredito explícito.
  </what-built>
  <how-to-verify>
    1. Garantir Chromium instalado: `python -m playwright install chromium` (se necessário).
    2. Rodar o spike: `cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python .planning/spikes/010-zara-product-price/experiment.py`
    3. Conferir o JSON de saída no stdout (`{"verdict": "...", "report": "..."}`) e abrir o `REPORT.md` gerado.
    4. Validar o critério D-05: GO somente se ≥3 produtos reais (título não vazio + URL `zara.com/br` + `price_full > 0`) em AMBAS as rodadas. Se ≥1 mas sem repetição estável → GO_TECHNICAL (anotar). Senão → NO-GO.
    5. Em NO-GO/GO_TECHNICAL: confirmar que o `REPORT.md` registra técnicas testadas + assinatura do bloqueio (status, blocked_signals) — evidência para deferir COMP-07 (D-09). NÃO mascarar "0 produtos" como sucesso e NÃO rotular a Zara como SFCC/Wake/VTEX.
    6. Confirmar que NENHUM arquivo foi criado/alterado dentro de `backend/` por este spike (`git status` — só `.planning/spikes/010-*` deve aparecer).
  </how-to-verify>
  <acceptance_criteria>
    - `REPORT.md` existe e contém um veredito explícito (GO | GO_TECHNICAL | NO-GO).
    - Em GO: ≥3 produtos com título + URL `zara.com/br` + preço positivo nas duas rodadas, documentados no REPORT.
    - Em NO-GO/GO_TECHNICAL: técnicas testadas + assinatura do bloqueio registradas; COMP-07 marcado para deferimento.
    - `git status` não mostra alterações dentro de `backend/` originadas por este spike.
  </acceptance_criteria>
  <resume-signal>Responda com o veredito do spike: "approved GO" (destrava o Plano 03), "approved NO-GO" (cancela o Plano 03 e defere COMP-07), ou "approved GO_TECHNICAL" + decisão de construir ou deferir. Em qualquer caso, descreva a evidência registrada no REPORT.md.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| experiment.py → storefront público da Zara (`zara.com/br`) | Conteúdo HTML/JSON externo não confiável entra via browser |
| experiment.py (spike) → árvore `backend/` | Fronteira de isolamento de código experimental (não pode vazar para produção antes do GO) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-SPIKE | Tampering | spike 010 vazando código/estado para `backend/` | mitigate | Regra D-08/D-10: `experiment.py` vive exclusivamente em `.planning/spikes/010-*/`; sem `import backend`/`from backend`; sem gravação dentro de `backend/`; verificado por `git status` na Task 2 |
| T-39-03 | Information Disclosure | dados de produto externos da Zara (título/URL/preço) | mitigate | Dados tratados como não confiáveis no spike; em GO (Plano 03) serão tipados por `SearchProductResult` com `price_full > 0` e `url` apenas armazenada/exibida; no spike apenas registrados como evidência |
| T-39-ENV | Tampering | uso de proxy pago / login / CAPTCHA solver fora do envelope | accept (prevenir) | Envelope D-06 trava o spike a browser público + `playwright-stealth` sem proxy/login; qualquer técnica fora disso exige aprovação explícita posterior e está fora deste plano |
| T-39-SC | Tampering | instalações npm/pip/cargo | accept | Nenhum pacote novo — `playwright`/`playwright-stealth` já em `requirements.txt` (RESEARCH § "Auditoria de Legitimidade de Pacotes"); `playwright install chromium` baixa apenas binários do browser, não pacotes |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('.planning/spikes/010-zara-product-price/experiment.py',encoding='utf-8').read())"` — parseia.
- Gate de isolamento: `experiment.py` sem `import backend`/`from backend`; `git status` sem alterações em `backend/` originadas pelo spike.
- Manual (Task 2): `REPORT.md` com veredito GO/NO-GO explícito + evidência reprodutível (2 rodadas).
</verification>

<success_criteria>
- COMP-07 critério #3: spike documentado (GO/NO-GO) registrado em `spikes/010-zara-product-price/REPORT.md` valida (ou refuta) extração pública de produto+preço ANTES de qualquer código de engine.
- O veredito é claro e acionável: GO destrava o Plano 03; NO-GO defere COMP-07 com evidência; GO_TECHNICAL exige decisão explícita do operador.
- Nenhum código de engine Zara foi escrito ou commitado neste plano (hard gate D-08).
</success_criteria>

<output>
Criar `.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-02-SUMMARY.md` ao concluir — incluir o veredito do spike e se o Plano 03 deve executar.
</output>
