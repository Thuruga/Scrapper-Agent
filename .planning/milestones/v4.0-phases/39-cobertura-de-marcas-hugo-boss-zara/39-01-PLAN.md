---
phase: 39-cobertura-de-marcas-hugo-boss-zara
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/scripts/onboard_hugoboss_categories.py
  - backend/data/brands.json
  - backend/data/monitored_categories.json
  - backend/tests/test_hugoboss_category_mapping.py
  - backend/tests/test_hugoboss_vtex_scan.py
autonomous: false
requirements: [COMP-06]
user_setup: []

must_haves:
  truths:
    - "O operador seleciona uma categoria da Hugo Boss no monitor e a varredura retorna produtos reais (título + URL + preço) com o schema canônico das demais marcas VTEX (D-02, D-03)"
    - "get_canonical_categories() inclui 'hugoboss' em available_brands para as categorias que a Hugo Boss realmente possui (D-01, D-02)"
    - "resolve_category_for_brands('camisas', ['hugoboss']) retorna URL https://www.hugoboss.com.br/... válida construída a partir de brand.mappings dinâmicos (D-01)"
    - "Todo vtex_fq_path persistido em hugoboss.mappings começa com '/' (path relativo, nunca URL completa) (T-39-01, D-03)"
    - "O scheduler de 10 min inclui a Hugo Boss e re-execuções de categoria inalterada não disparam falso 'produto novo' (Discrição do Claude — critério #2)"
  artifacts:
    - path: "backend/scripts/onboard_hugoboss_categories.py"
      provides: "Descoberta + auto_match + persistência única do de/para de categorias da Hugo Boss reusando o pipeline VTEX"
      contains: "www.hugoboss.com.br"
    - path: "backend/data/brands.json"
      provides: "hugoboss.mappings populado com CategoryMapping reais (canonical_slug + vtex_fq_path relativo + label)"
      contains: "vtex_fq_path"
    - path: "backend/data/monitored_categories.json"
      provides: "Entrada(s) de monitoramento da Hugo Boss para o scheduler de 10 min"
      contains: "hugoboss"
    - path: "backend/tests/test_hugoboss_category_mapping.py"
      provides: "Cobre COMP-06-b (resolve via mappings dinâmicos) e COMP-06-c (get_canonical_categories inclui hugoboss)"
      exports: ["test_resolve_category_returns_valid_url_hugoboss", "test_get_canonical_categories_includes_hugoboss"]
    - path: "backend/tests/test_hugoboss_vtex_scan.py"
      provides: "Cobre COMP-06-d (scan VTEX retorna SearchProductResult válido com mock de VtexApiClient)"
      exports: ["test_vtex_scan_returns_valid_schema"]
  key_links:
    - from: "backend/scripts/onboard_hugoboss_categories.py"
      to: "services.engines.vtex_engine.VTEXEngine.discover_categories"
      via: "descoberta da árvore VTEX de www.hugoboss.com.br"
      pattern: "VTEXEngine\\([\"']hugoboss[\"']\\)"
    - from: "backend/scripts/onboard_hugoboss_categories.py"
      to: "services.brand_service.brand_service.update_mappings"
      via: "persistência dual dev/prod via persist_mappings"
      pattern: "update_mappings|persist_mappings"
    - from: "backend/data/brands.json (hugoboss.mappings)"
      to: "services.category_mapping.resolve_category_for_brands"
      via: "fallback dinâmico em brand.mappings"
      pattern: "vtex_fq_path"
---

<objective>
Fechar o gap da Hugo Boss (COMP-06): hoje a marca está cadastrada e ativa como `engine="vtex"` (`www.hugoboss.com.br`) mas com `mappings: []` em `brands.json`, então selecionar uma categoria não aponta para nenhuma URL. Este plano descobre o de/para real de categorias VTEX, valida com varredura-amostra ao vivo, persiste estaticamente em `brands.json` e inclui a Hugo Boss no scheduler de 10 min — tudo reusando o pipeline existente, sem novo engine e sem tocar no código de roteamento.

Purpose: A Hugo Boss entra na comparação "banana com banana" por categoria junto das demais marcas VTEX, sem fragmentar o vocabulário canônico (D-02).
Output: `onboard_hugoboss_categories.py`, `hugoboss.mappings` populado em `brands.json`, entrada(s) em `monitored_categories.json`, dois arquivos de teste herméticos novos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-CONTEXT.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-RESEARCH.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-PATTERNS.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-VALIDATION.md
@backend/scripts/onboard_vtex_brands.py
@backend/services/category_mapping.py
@backend/services/engines/vtex_engine.py
@backend/data/brands.json
</context>

## Artifacts this phase produces (Plano 01 — Hugo Boss)

**Arquivos novos:**
- `backend/scripts/onboard_hugoboss_categories.py` — script offline de descoberta-e-persistência do de/para da Hugo Boss.
- `backend/tests/test_hugoboss_category_mapping.py` — testes herméticos COMP-06-b / COMP-06-c.
- `backend/tests/test_hugoboss_vtex_scan.py` — teste hermético COMP-06-d.

**Arquivos modificados (dados):**
- `backend/data/brands.json` — `hugoboss.mappings` populado (de `[]` para a lista descoberta de `CategoryMapping`).
- `backend/data/monitored_categories.json` — entrada(s) de monitoramento da Hugo Boss.

**Símbolos/funções novos:**
- Funções de orquestração em `onboard_hugoboss_categories.py` (ex.: `discover_hugoboss_mappings`, `main`) reusando `discover_and_match`, `auto_match`, `print_and_confirm`, `persist_mappings` de `onboard_vtex_brands.py`.
- Testes: `test_resolve_category_returns_valid_url_hugoboss`, `test_get_canonical_categories_includes_hugoboss`, `test_vtex_scan_returns_valid_schema`.

**Dados persistidos:**
- Entradas `CategoryMapping` em `hugoboss.mappings` (subset dos slugs canônicos `camisas`/`polos`/`camisetas`/`calcas`/`bermudas`/`jaquetas`/`infantil` que a marca realmente possui).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — scaffolds de teste herméticos para o de/para da Hugo Boss</name>
  <files>backend/tests/test_hugoboss_category_mapping.py, backend/tests/test_hugoboss_vtex_scan.py</files>
  <read_first>
    - backend/tests/test_vtex_brand_onboarding_contract.py (analog exato — `_make_service_with_vtex_brand` linhas 36–64; `test_resolve_category_returns_valid_url` linhas 185–207; monkeypatch de `brand_service` no módulo `category_mapping` linhas 196–198)
    - backend/services/category_mapping.py (`get_canonical_categories` L143; `resolve_category_for_brands` L191 — destino dos asserts)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-PATTERNS.md (seções "test_hugoboss_category_mapping.py" e "test_hugoboss_vtex_scan.py")
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-VALIDATION.md (mapa req→teste: COMP-06-b/c/d)
  </read_first>
  <behavior>
    - test_resolve_category_returns_valid_url_hugoboss: com `mappings=[CategoryMapping(canonical_slug="camisas", vtex_fq_path="/masculino/roupas/camisas", label="Camisas")]` num service em memória, `resolve_category_for_brands("camisas", ["hugoboss"])` retorna dict contendo "hugoboss" cuja `url` começa com "https://www.hugoboss.com.br/" e termina com "/masculino/roupas/camisas" (COMP-06-b).
    - test_get_canonical_categories_includes_hugoboss: com o mesmo service mockado, `get_canonical_categories()` lista "hugoboss" em `available_brands` da categoria mapeada (COMP-06-c).
    - test_vtex_scan_returns_valid_schema: com `VtexApiClient.search_products` mockado retornando uma lista de `SearchProductResult(brand="hugoboss", ...)`, `VTEXEngine("hugoboss").search("camisa", max_results=3)` retorna `BrandSearchResult` com ≥1 produto, cada um com `brand=="hugoboss"`, `url` começando com "https://www.hugoboss.com.br/" e `price_full > 0` (COMP-06-d).
  </behavior>
  <action>
    Criar os dois arquivos de teste herméticos seguindo o analog `test_vtex_brand_onboarding_contract.py`. Reusar o helper de construção de service em memória (`BrandManagerService.__new__` + `svc.brands` + `svc._check_reload = MagicMock()` + `svc.updated_event = asyncio.Event()`) com `brand_key="hugoboss"`, `brand_name="Hugo Boss"`, `domain="www.hugoboss.com.br"`, `engine="vtex"`, `is_active=True`. Para os testes de resolução, aplicar `unittest.mock.patch.object(category_mapping_module, "brand_service", svc)` antes de chamar `resolve_category_for_brands` / `get_canonical_categories`. Para o teste de scan, mockar `services.engines.vtex_engine.VtexApiClient.search_products` (NÃO bater na rede) e rodar `asyncio.run(engine.search(...))`. Importar `CategoryMapping`, `DynamicBrand`, `SearchProductResult`, `BrandSearchResult` de `core.models`. Zero I/O de arquivo, zero rede. Estes testes testam o mecanismo de resolução com mappings injetados em memória (não o conteúdo de `brands.json`), portanto devem PASSAR já nesta task.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_hugoboss_category_mapping.py tests/test_hugoboss_vtex_scan.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Comando acima passa (3 testes verdes).
    - `test_resolve_category_returns_valid_url_hugoboss` afirma `url.startswith("https://www.hugoboss.com.br/")` e `url.endswith("/masculino/roupas/camisas")`.
    - `test_get_canonical_categories_includes_hugoboss` afirma `"hugoboss" in available_brands`.
    - `test_vtex_scan_returns_valid_schema` afirma `isinstance(result, BrandSearchResult)`, `len(result.products) >= 1`, e por produto `p.price_full > 0` e `p.url.startswith("https://www.hugoboss.com.br/")`.
    - Nenhum teste faz I/O de rede (mock de `VtexApiClient.search_products` presente).
  </acceptance_criteria>
  <done>Os dois arquivos de teste existem, são herméticos e passam com mappings injetados em memória.</done>
</task>

<task type="auto">
  <name>Task 2: Script de descoberta-e-persistência do de/para da Hugo Boss</name>
  <files>backend/scripts/onboard_hugoboss_categories.py</files>
  <read_first>
    - backend/scripts/onboard_vtex_brands.py (analog exato — imports L15–29; `discover_and_match` L313–330 com `urlparse(item.get("path") or "").path` na L329; `print_and_confirm` L337–352; `persist_mappings` L359–378; `main` L385+ com per-brand live smoke)
    - backend/services/engines/vtex_engine.py (`discover_categories` → `VtexApiClient.fetch_categories` → `_flatten_vtex_tree`)
    - backend/data/brands.json (linhas 456–468 — estado atual da Hugo Boss: `domain="www.hugoboss.com.br"`, `engine="vtex"`, `mappings: []`)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-PATTERNS.md (seção "onboard_hugoboss_categories.py")
  </read_first>
  <action>
    Criar `onboard_hugoboss_categories.py` reusando as funções já existentes em `onboard_vtex_brands.py` via import (`from scripts.onboard_vtex_brands import auto_match, persist_mappings, print_and_confirm`) — NÃO reimplementar `auto_match` nem `persist_mappings`. O script deve: (1) instanciar `VTEXEngine("hugoboss")` e chamar `discover_categories()` (passa por `VtexApiClient.fetch_categories("www.hugoboss.com.br")` — domínio COM `www.`, nunca sem); (2) aplicar `item["rel_path"] = urlparse(item.get("path") or "").path` em cada item descoberto (Pitfall 3 DEFUSED — `item["path"]` é URL completa, o path relativo deve começar com `/`); (3) chamar `auto_match(raw)` para propor o de/para restrito aos slugs canônicos existentes (`camisas`, `polos`, `camisetas`, `calcas`, `bermudas`, `jaquetas`, `infantil`); (4) imprimir as propostas e os slugs SEM match via `print_and_confirm` (gate humano interativo — não auto-confirmar); (5) somente após confirmação, chamar `persist_mappings(brand_service, "hugoboss", proposals)` que filtra `rel_path` não-relativo e grava via `brand_service.update_mappings`. O script é de descoberta ÚNICA (D-04) — não rodar a cada varredura. NÃO tocar em `_RAW_CATEGORIES` de `category_mapping.py` (D-01 — hardcoded é só aramis/reserva/tommy). Slugs sem categoria no catálogo da Hugo Boss são simplesmente omitidos (comportamento de `auto_match`, A1 do RESEARCH).
  </action>
  <verify>
    <automated>cd backend && python -c "import ast,sys; src=open('scripts/onboard_hugoboss_categories.py',encoding='utf-8').read(); ast.parse(src); sys.exit(0 if ('www.hugoboss.com.br' in src and 'urlparse' in src and 'persist_mappings' in src and 'auto_match' in src and '_RAW_CATEGORIES' not in src) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo parseia como Python válido (ast.parse sem erro).
    - Contém literal `"www.hugoboss.com.br"` (domínio com www).
    - Usa `urlparse(...).path` para extrair path relativo.
    - Reusa `persist_mappings` e `auto_match` (import de `scripts.onboard_vtex_brands`).
    - NÃO contém referência a `_RAW_CATEGORIES` (não hardcoda paths — D-01).
    - Tem um gate humano de confirmação antes de persistir (chamada a `print_and_confirm` ou `input`).
  </acceptance_criteria>
  <done>O script existe, parseia, reusa o pipeline VTEX existente e respeita D-01/D-03/D-04.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Executar descoberta ao vivo, validar varredura-amostra e persistir + monitorar Hugo Boss</name>
  <read_first>
    - backend/scripts/onboard_hugoboss_categories.py (criado na Task 2)
    - backend/data/brands.json (hugoboss.mappings — alvo da gravação)
    - backend/data/monitored_categories.json (entrada de monitoramento a criar)
    - backend/services/category_monitor_service.py (`run_category_scan`, `category_monitor_job` — sobrescreve `monitored_products_{id}.json` a cada ciclo, sem diff automático → re-execução inalterada não gera falso positivo)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-VALIDATION.md (seção "Manual-Only Verifications")
  </read_first>
  <what-built>
    Script `onboard_hugoboss_categories.py` (Task 2) que descobre a árvore VTEX real da Hugo Boss, propõe o de/para canônico e persiste em `brands.json` após confirmação. Os testes herméticos (Task 1) já validam o mecanismo de resolução com mappings injetados.
  </what-built>
  <how-to-verify>
    1. Rodar a descoberta: `cd backend && python -m scripts.onboard_hugoboss_categories`
    2. Conferir as propostas impressas: cada `canonical_slug` deve apontar para um `vtex_fq_path` que começa com `/` e parece masculino (não feminino). Slugs sem match aparecem em "[SEM MATCH]".
    3. ANTES de confirmar a persistência, validar com varredura-amostra ao vivo que pelo menos as categorias propostas retornam produtos reais (título + URL + preço) na Hugo Boss — usar o preview/scan de categoria ou um scan direto por uma das URLs mapeadas. NÃO persistir paths que não retornem produtos.
    4. Confirmar (`s`) para gravar em `brands.json`; verificar que `hugoboss.mappings` deixou de ser `[]` e que todo `vtex_fq_path` começa com `/`.
    5. Criar entrada(s) de monitoramento da Hugo Boss via `POST /monitor/category` (brand="hugoboss") para 1–2 categorias mapeadas representativas (ex.: `camisas`, `camisetas`) — escolher categorias com produtos confirmados no passo 3.
    6. Observar 2 ciclos do scheduler de 10 min (ou disparar `run_category_scan` manualmente 2×) sobre uma categoria inalterada e confirmar que NÃO há falso "produto novo" — o arquivo `monitored_products_{id}.json` é sobrescrito com o mesmo set; a UI não deve alarmar (critério #2).
    7. Rodar a suíte completa: `cd backend && python -m pytest tests/ -x -q`.
  </how-to-verify>
  <acceptance_criteria>
    - `hugoboss.mappings` em `brands.json` ≠ `[]`; todo `vtex_fq_path` começa com `/`.
    - Varredura-amostra ao vivo retornou produtos reais (título + URL + preço) para cada categoria persistida.
    - Pelo menos 1 entrada de monitoramento da Hugo Boss em `monitored_categories.json`.
    - 2 ciclos sobre categoria inalterada → nenhum falso "produto novo".
    - `cd backend && python -m pytest tests/ -x -q` verde.
  </acceptance_criteria>
  <resume-signal>Digite "approved" se mappings persistidos (todos `/`-relativos), varredura-amostra retornou produtos reais, entrada(s) de monitoramento criadas e 2 ciclos inalterados sem falso positivo; caso contrário, descreva os problemas.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script de descoberta → árvore VTEX pública da Hugo Boss | Dados externos (paths/URLs) entram via `VtexApiClient.fetch_categories` |
| operador → `POST /monitor/category` / `POST /scrape-category-multi` | Entrada de URL/marca cruza a fronteira da API (protegida por `X-API-Key`) |
| brands.json (`vtex_fq_path`) → `resolve_category_for_brands` (montagem de URL) | Path persistido é concatenado ao domínio para formar a URL de varredura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | `vtex_fq_path` em `persist_mappings` / `brands.json` | mitigate | `persist_mappings` filtra propostas cujo `rel_path` não começa com `/`; `urlparse(...).path` na descoberta garante path relativo; regressão por `test_vtex_fq_path_is_relative` (em `test_vtex_brand_onboarding_contract.py`) detecta `http`-prefixed |
| T-39-02 | Tampering | `custom_url` / categoria em `ScrapeCategoryRequest` (rotas de categoria) | mitigate | `clean_url()` corrige duplicação de esquema; `brand_service.get_brand()` valida marca existente; rotas protegidas por `X-API-Key` via `verify_api_key` |
| T-39-03 | Information Disclosure | dados de produto VTEX externos (Hugo Boss) | mitigate | `SearchProductResult` (Pydantic) tipa os campos; `price_full` validado `> 0`; `url` apenas armazenada/exibida, nunca executada |
| T-39-SC | Tampering | instalações npm/pip/cargo | accept | Nenhum pacote novo nesta phase — `playwright-stealth`/`pydantic`/`fastapi`/`APScheduler` já em `requirements.txt`, sem instalação; auditoria de legitimidade vazia (RESEARCH § "Auditoria de Legitimidade de Pacotes") |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_hugoboss_category_mapping.py tests/test_hugoboss_vtex_scan.py -x -q` — verde (COMP-06-b/c/d herméticos).
- `cd backend && python -m pytest tests/test_vtex_brand_onboarding_contract.py -x -q` — verde (contrato `auto_match`/`update_mappings`/`VALID_SLUGS` + `test_vtex_fq_path_is_relative` não regrediu).
- `cd backend && python -m pytest tests/ -x -q` — suíte completa verde antes de `/gsd-verify-work`.
- Manual (Task 3): mappings persistidos `/`-relativos; varredura-amostra ao vivo com produtos reais; 2 ciclos de categoria inalterada sem falso "produto novo".
</verification>

<success_criteria>
- COMP-06-a: `hugoboss.mappings` deixou de ser `[]` e contém apenas slugs do vocabulário canônico.
- COMP-06-b: `resolve_category_for_brands("camisas", ["hugoboss"])` retorna URL válida `https://www.hugoboss.com.br/...`.
- COMP-06-c: `get_canonical_categories()` inclui `hugoboss` em `available_brands` das categorias mapeadas.
- COMP-06-d: scan VTEX da Hugo Boss retorna `SearchProductResult` válido (mock verde + varredura-amostra ao vivo confirmada).
- Critério #2 do roadmap: Hugo Boss no scheduler de 10 min sem falso positivo de "produto novo" em re-execução inalterada.
</success_criteria>

<output>
Criar `.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-01-SUMMARY.md` ao concluir.
</output>
