---
phase: 39-cobertura-de-marcas-hugo-boss-zara
plan: 03
type: execute
wave: 2
depends_on: ["39-02"]
files_modified:
  - backend/services/engines/inditex_engine.py
  - backend/services/engines/factory.py
  - backend/data/brands.json
  - backend/tests/test_inditex_engine.py
autonomous: false
conditional: "GO-only — executar SOMENTE se o veredito do spike 010 (Plano 02 / REPORT.md) for GO. Em NO-GO: NÃO executar nenhuma task; deferir COMP-07 ao backlog com a evidência do REPORT.md e commitar ZERO código de engine."
requirements: [COMP-07]
user_setup: []

must_haves:
  truths:
    - "[GO] EngineFactory.get_engine('zara') retorna InditexEngine via branch lazy engine_type == 'inditex' (D-08)"
    - "[GO] InditexEngine.search('camiseta') retorna ≥1 SearchProductResult com url em zara.com/br e price_full > 0 (mock) (D-05, D-10)"
    - "[GO] zara é cadastrada em brands.json com engine='inditex' e is_active=true APENAS após o engine integrado e o smoke verde (Armadilha 6)"
    - "[GO] O contrato SearchProductResult (brand, product_name, url, price_full, image_url opcional) é preservado e o filtro masculino respeitado (D-10)"
    - "[NO-GO] Nenhum arquivo de engine Zara é criado/commitado; COMP-07 é deferido com a evidência do REPORT.md (D-09)"
  artifacts:
    - path: "backend/services/engines/inditex_engine.py"
      provides: "[GO] Engine net-new da Zara/Inditex; search() retorna BrandSearchResult; extração baseada no que o spike 010 revelou"
      contains: "class InditexEngine"
    - path: "backend/services/engines/factory.py"
      provides: "[GO] Branch lazy engine_type == 'inditex' que instancia InditexEngine"
      contains: "inditex"
    - path: "backend/data/brands.json"
      provides: "[GO] Entrada zara (brand_key='zara', engine='inditex', is_active=true) criada só após smoke verde"
      contains: "zara"
    - path: "backend/tests/test_inditex_engine.py"
      provides: "[GO] Cobre COMP-07 em GO (factory retorna InditexEngine + search mock retorna SearchProductResult válido)"
      exports: ["TestInditexFactory", "TestInditexEngineSearch"]
  key_links:
    - from: "backend/services/engines/factory.py"
      to: "backend.services.engines.inditex_engine.InditexEngine"
      via: "lazy import no branch engine_type == 'inditex'"
      pattern: "engine_type == [\"']inditex[\"']"
    - from: "backend/services/engines/inditex_engine.py"
      to: "core.models.SearchProductResult"
      via: "produtos extraídos retornados como SearchProductResult dentro de BrandSearchResult"
      pattern: "SearchProductResult"
---

<objective>
**Plano CONDICIONAL — GO-only.** Em caso de veredito GO do spike 010 (Plano 02), construir o engine Zara dentro desta phase (D-08): `InditexEngine` net-new, branch `inditex` na `EngineFactory`, cadastro da `zara` em `brands.json` e smoke de busca retornando produtos reais (título + URL + preço). Em caso de NO-GO: **nenhuma task deste plano executa** — COMP-07 é formalmente deferido ao backlog com a evidência do `REPORT.md` (D-09) e ZERO código de engine é commitado.

O executor DEVE ler o veredito em `.planning/spikes/010-zara-product-price/REPORT.md` (e o SUMMARY do Plano 02) ANTES de qualquer ação. Se o veredito não for GO, parar e executar apenas a Task 0 (deferimento). Esta separação garante que execute-phase não construa um engine sobre um NO-GO e nunca deixe um engine incompleto commitado.

Purpose: Entregar COMP-07 (operador onboarda e busca a Zara) somente quando há evidência de viabilidade, espelhando o gate da Phase 32.
Output (GO): `inditex_engine.py`, branch na `factory.py`, entrada `zara` em `brands.json`, `test_inditex_engine.py`. Output (NO-GO): deferimento documentado, sem engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-CONTEXT.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-RESEARCH.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-PATTERNS.md
@.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-VALIDATION.md
@.planning/spikes/010-zara-product-price/REPORT.md
@backend/services/engines/wake_engine.py
@backend/services/engines/factory.py
@backend/core/models.py
</context>

## Artifacts this phase produces (Plano 03 — Zara engine, GO-only)

**Em GO — arquivos novos:**
- `backend/services/engines/inditex_engine.py` — engine net-new da Zara (analog `wake_engine.py`).
- `backend/tests/test_inditex_engine.py` — testes COMP-07 (factory + search mock).

**Em GO — arquivos modificados:**
- `backend/services/engines/factory.py` — branch `if engine_type == "inditex": from services.engines.inditex_engine import InditexEngine; return InditexEngine(brand_key)` (lazy import, após o bloco wake L55–57).
- `backend/data/brands.json` — entrada `zara` (`brand_key="zara"`, `engine="inditex"`, `is_active=true`) criada SÓ após smoke verde.

**Em GO — símbolos novos:**
- Classe `InditexEngine(BaseEngine)` com `__init__`, `get_engine_name() -> "Inditex"`, `search(...) -> BrandSearchResult`, `_fetch_products(...)`.
- Branch de factory `engine_type == "inditex"`.
- Testes `TestInditexFactory`, `TestInditexEngineSearch`.

**Em NO-GO:**
- Nenhum artefato de engine. Apenas o deferimento documentado de COMP-07 (Task 0).

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Ler o veredito do spike 010 e ramificar GO / NO-GO</name>
  <read_first>
    - .planning/spikes/010-zara-product-price/REPORT.md (veredito do spike — fonte da decisão)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-02-SUMMARY.md (veredito + recomendação do Plano 02)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-CONTEXT.md (D-08 GO / D-09 NO-GO)
  </read_first>
  <decision>O spike 010 emitiu GO? Construir o engine Zara nesta phase (D-08) ou deferir COMP-07 ao backlog (D-09)?</decision>
  <context>
    Hard gate D-08: nenhum código de engine Zara antes do veredito GO. O `REPORT.md` é a fonte de verdade. Se GO → executar Tasks 1–4. Se NO-GO ou GO_TECHNICAL sem decisão de construir → executar APENAS o deferimento abaixo e PARAR (não criar `inditex_engine.py`, não tocar `factory.py`, não cadastrar `zara`).
  </context>
  <options>
    <option id="go">
      <name>GO — construir o engine</name>
      <pros>Entrega COMP-07 completo nesta phase; evidência de ≥3 produtos em 2 rodadas justifica o investimento.</pros>
      <cons>Engine net-new; extração da Zara pode exigir manutenção se o anti-bot mudar.</cons>
    </option>
    <option id="no-go">
      <name>NO-GO — deferir COMP-07</name>
      <pros>Não commita engine incompleto; respeita o envelope público (D-06); evidência preservada no REPORT.md.</pros>
      <cons>Zara permanece não suportada; COMP-07 vai ao backlog.</cons>
    </option>
  </options>
  <action>
    Em NO-GO (ou GO_TECHNICAL sem decisão de construir): registrar o deferimento de COMP-07 ao backlog citando a evidência do `REPORT.md` (técnicas testadas + assinatura do bloqueio) — atualizar STATE.md (Blockers/Concerns + Decisions) e o SUMMARY desta phase; NÃO criar nenhum arquivo de engine, NÃO alterar `factory.py`, NÃO cadastrar `zara` em `brands.json`, NÃO rotular a Zara como SFCC/Wake/VTEX (D-09). Encerrar o plano aqui. Em GO: prosseguir para a Task 1.
  </action>
  <resume-signal>Selecione: "go" (executar Tasks 1–4) ou "no-go" (deferir COMP-07 e encerrar o plano).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: [GO] Wave 0 — test_inditex_engine.py (factory + search mock)</name>
  <files>backend/tests/test_inditex_engine.py</files>
  <read_first>
    - backend/tests/test_vtex_brand_onboarding_contract.py (analog de hermetismo — service em memória, monkeypatch de brand_service)
    - backend/services/engines/factory.py (`get_engine` L40–59 — branches lazy sfcc/wake; alvo do `TestInditexFactory`)
    - backend/core/models.py (`SearchProductResult`, `BrandSearchResult` — contrato a afirmar)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-PATTERNS.md (seções "inditex_engine.py" e "factory.py")
    - .planning/spikes/010-zara-product-price/REPORT.md (formato real de produto revelado pelo spike — informa o shape do mock)
  </read_first>
  <behavior>
    - TestInditexFactory: para uma marca com `engine="inditex"`, `EngineFactory.get_engine("zara")` retorna uma instância de `InditexEngine` (não `VTEXEngine`).
    - TestInditexEngineSearch: com `InditexEngine._fetch_products` mockado retornando ≥1 `SearchProductResult(brand="zara", product_name=<não vazio>, url="https://www.zara.com/br/...", price_full=499.0)`, `InditexEngine("zara").search("camiseta")` retorna `BrandSearchResult` com ≥1 produto, cada um com `url` contendo "zara.com/br" e `price_full > 0`. Sem rede.
    - Caminho de erro: quando a marca não existe, `search` retorna `BrandSearchResult` com `error` preenchido (canal de falha, nunca 0 produtos silenciosos).
  </behavior>
  <action>
    Criar `test_inditex_engine.py` hermético (mock de `InditexEngine._fetch_products`, zero rede). `TestInditexFactory` valida o branch de factory (pode mockar `brand_service.get_brand`/usar service em memória com `engine="inditex"`). `TestInditexEngineSearch` valida o contrato de `search` e o canal de erro `BrandSearchResult.error`. Modelar o shape do `SearchProductResult` mock conforme o produto real registrado no `REPORT.md` do spike 010. Importar de `core.models` e `services.engines.inditex_engine`/`factory`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_inditex_engine.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `TestInditexFactory` afirma que `get_engine("zara")` é instância de `InditexEngine`.
    - `TestInditexEngineSearch` afirma `len(result.products) >= 1`, `"zara.com/br" in p.url`, `p.price_full > 0`.
    - Há um teste para o canal de erro (`BrandSearchResult.error` preenchido quando marca ausente).
    - Nenhuma chamada de rede (mock de `_fetch_products`).
  </acceptance_criteria>
  <done>test_inditex_engine.py existe, é hermético e passa após Tasks 2–3.</done>
</task>

<task type="auto">
  <name>Task 2: [GO] InditexEngine net-new</name>
  <files>backend/services/engines/inditex_engine.py</files>
  <read_first>
    - backend/services/engines/wake_engine.py (analog de estrutura — imports L44–56, construtor, get_engine_name, search com error como canal de falha)
    - backend/services/engines/factory.py (`_search_one` L89–102 — padrão de BrandSearchResult.error)
    - backend/core/models.py (`SearchProductResult`, `BrandSearchResult` — contrato obrigatório)
    - backend/core/browser_manager.py (BrowserManager + playwright-stealth — base da extração pública)
    - .planning/spikes/010-zara-product-price/experiment.py (lógica de extração validada — JSON-LD/XHR/tiles + URL section=MAN)
    - .planning/spikes/010-zara-product-price/REPORT.md (caminho de extração confirmado pelo spike)
  </read_first>
  <action>
    Criar `InditexEngine(BaseEngine)` espelhando `wake_engine.py`: `__init__(self, brand_key)`, `get_engine_name() -> "Inditex"`, e `async def search(self, query, max_results=10, **kwargs) -> BrandSearchResult` que resolve `brand = brand_service.get_brand(self.brand_key)` (import lazy de `brand_service`), retorna `BrandSearchResult(..., error="Marca não encontrada")` se ausente, e em sucesso chama `await self._fetch_products(brand.domain, query, max_results)` envolto em try/except que captura exceções como `BrandSearchResult(..., error=str(exc))`. Implementar `_fetch_products` PORTANDO a lógica de extração já validada no `experiment.py` do spike 010 (mesmo caminho que produziu o GO — JSON-LD / endpoint JSON / tiles), usando `BrowserManager` + `playwright-stealth` (sem proxy/login — D-06), filtro masculino `section=MAN` (D-07/CAT-01), e construindo `SearchProductResult(brand=self.brand_key, product_name=<título>, url=<URL absoluta em zara.com/br>, price_full=<float>0>, image_url=<quando disponível>)`. Catálogo + preço apenas — frete `None` (sem checkout/CEP). Preservar Quality Gates e o contrato `SearchProductResult`.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_inditex_engine.py::TestInditexEngineSearch -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `class InditexEngine(BaseEngine)` com `get_engine_name()` retornando "Inditex".
    - `search` usa `BrandSearchResult.error` como canal de falha (nunca exceção propagada / nunca 0 produtos silencioso).
    - `_fetch_products` produz `SearchProductResult` com `url` em `zara.com/br` e `price_full > 0`.
    - `TestInditexEngineSearch` verde.
  </acceptance_criteria>
  <done>InditexEngine existe, respeita o contrato e o envelope público, e o teste de search passa.</done>
</task>

<task type="auto">
  <name>Task 3: [GO] Branch inditex na EngineFactory + cadastro da zara</name>
  <files>backend/services/engines/factory.py, backend/data/brands.json</files>
  <read_first>
    - backend/services/engines/factory.py (branches lazy sfcc L48–50 e wake L52–57 — padrão a espelhar; acrescentar APÓS o bloco wake, antes do `return VTEXEngine(brand_key)` L59)
    - backend/data/brands.json (entradas de marca existentes — formato de cadastro; ver hugoboss L456–468 como molde de campos)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-RESEARCH.md (Armadilha 6 — não ativar zara antes do engine pronto)
  </read_first>
  <action>
    Acrescentar em `factory.py`, após o bloco `wake` (L55–57) e antes do `return VTEXEngine(brand_key)`, o branch lazy: `if engine_type == "inditex": from services.engines.inditex_engine import InditexEngine  # noqa: PLC0415` seguido de `return InditexEngine(brand_key)` (mesmo padrão dos branches sfcc/wake; comentário citando "D-08 Phase 39 — Inditex/Zara"). Cadastrar a `zara` em `brands.json` com `brand_key="zara"`, `brand_name="Zara"`, `domain` da Zara BR (ex.: `www.zara.com`), `engine="inditex"`, campos opcionais (`review_provider="none"`, `mappings: []`, etc. conforme o molde), e `is_active=true`. CRÍTICO (Armadilha 6): só setar `is_active=true` DEPOIS de Tasks 1–2 verdes e do smoke (Task 4) — caso contrário a busca geral/scheduler tentaria instanciar `InditexEngine` antes de pronto. O chokepoint `list_brands(active_only=True)` só inclui a Zara após este cadastro ativo.
  </action>
  <verify>
    <automated>cd backend && python -c "import ast,sys,json; src=open('services/engines/factory.py',encoding='utf-8').read(); ast.parse(src); b=json.load(open('data/brands.json',encoding='utf-8')); sys.exit(0 if (\"engine_type == \\\"inditex\\\"\" in src or \"engine_type=='inditex'\" in src.replace(' ','')) and 'zara' in b and b['zara'].get('engine')=='inditex' else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `factory.py` contém o branch `engine_type == "inditex"` com lazy import de `InditexEngine` (após wake, antes do fallback VTEX).
    - `brands.json` tem a entrada `zara` com `engine="inditex"`.
    - O arquivo `factory.py` parseia; `brands.json` é JSON válido.
  </acceptance_criteria>
  <done>Branch inditex integrado e zara cadastrada; ativação confirmada no smoke (Task 4).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [GO] Smoke ao vivo da busca Zara + ativação + suíte completa</name>
  <read_first>
    - backend/services/engines/inditex_engine.py (Task 2)
    - backend/services/engines/factory.py (branch inditex — Task 3)
    - backend/data/brands.json (entrada zara — Task 3)
    - .planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-VALIDATION.md (Manual-Only Verifications)
  </read_first>
  <what-built>
    `InditexEngine` (Task 2) integrado na `EngineFactory` (Task 3) e a marca `zara` cadastrada em `brands.json`. Os testes herméticos (Task 1) já validam factory + contrato de search.
  </what-built>
  <how-to-verify>
    1. Smoke ao vivo: executar uma busca real `query="camiseta"` para a marca `zara` (via endpoint de busca da API ou `asyncio.run(InditexEngine("zara").search("camiseta"))`).
    2. Confirmar ≥1 produto real (idealmente ≥3, espelhando o GO) com `product_name` não vazio, `url` em `zara.com/br` e `price_full > 0` — filtro masculino respeitado.
    3. Confirmar `is_active=true` na entrada `zara` de `brands.json` SOMENTE após o smoke retornar produtos; confirmar que a Zara passa a aparecer em `list_brands(active_only=True)`.
    4. Rodar a suíte completa: `cd backend && python -m pytest tests/ -x -q` — verde (incluindo `test_inditex_engine.py`).
    5. Confirmar que, se o smoke falhar, a Zara NÃO fica `is_active=true` (não deixar engine quebrado ativo — Armadilha 6).
  </how-to-verify>
  <acceptance_criteria>
    - Smoke ao vivo retorna ≥1 produto real da Zara (título + URL `zara.com/br` + preço > 0), filtro masculino respeitado.
    - `zara.is_active=true` em `brands.json` apenas com smoke verde.
    - `cd backend && python -m pytest tests/ -x -q` verde.
  </acceptance_criteria>
  <resume-signal>Digite "approved" se o smoke retornou produtos reais da Zara, a marca está ativa e a suíte completa está verde; caso contrário, descreva a falha (e mantenha `is_active=false`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| InditexEngine → storefront público da Zara | Conteúdo HTML/JSON externo não confiável entra via BrowserManager + stealth |
| operador/scheduler → EngineFactory.get_engine("zara") | Roteamento de engine pela marca; `is_active` controla inclusão na busca geral |
| dados de produto Zara → SearchProductResult | Campos externos tipados antes de armazenar/exibir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-03 | Information Disclosure | dados de produto externos da Zara (título/URL/preço) | mitigate | `SearchProductResult` (Pydantic) tipa os campos; `price_full` validado `> 0`; `url` apenas armazenada/exibida, nunca executada; catálogo+preço apenas (sem checkout/CEP) |
| T-39-04 | Denial of Service | engine Zara ativado antes de pronto (`is_active=true` prematuro) | mitigate | Armadilha 6: `is_active=true` só após smoke verde; até lá `is_active=false` mantém a Zara fora de `list_brands(active_only=True)` e do scheduler |
| T-39-05 | Spoofing | rotular a Zara como SFCC/Wake/VTEX para mascarar falha | mitigate | D-09: a Zara só recebe `engine="inditex"`; em NO-GO nenhum cadastro é feito; falhas viram `BrandSearchResult.error`, nunca 0 produtos silenciosos |
| T-39-ENV | Tampering | uso de proxy pago / login / CAPTCHA fora do envelope | accept (prevenir) | Envelope D-06: apenas browser público + `playwright-stealth`; qualquer técnica fora disso exige aprovação explícita posterior (fora deste plano) |
| T-39-SC | Tampering | instalações npm/pip/cargo | accept | Nenhum pacote novo — `playwright`/`playwright-stealth` já em `requirements.txt` (RESEARCH § "Auditoria de Legitimidade de Pacotes") |
</threat_model>

<verification>
- `cd backend && python -m pytest tests/test_inditex_engine.py -x -q` — verde (factory + search mock).
- `cd backend && python -m pytest tests/ -x -q` — suíte completa verde antes de `/gsd-verify-work`.
- Gate de integração: `factory.py` contém o branch `inditex`; `brands.json` tem `zara` com `engine="inditex"`.
- Manual (Task 4): smoke ao vivo retorna produtos reais; ativação só com smoke verde.
- NO-GO: nenhum dos arquivos de engine existe; COMP-07 deferido com evidência (Task 0).
</verification>

<success_criteria>
- [GO] COMP-07 critério #4: operador onboarda a Zara e a busca retorna produtos reais (título + URL + preço).
- [GO] `EngineFactory.get_engine("zara")` retorna `InditexEngine`; `InditexEngine.search("camiseta")` retorna ≥1 `SearchProductResult` válido.
- [GO] Contrato `SearchProductResult` preservado, filtro masculino respeitado, frete `None` (catálogo+preço apenas).
- [NO-GO] COMP-07 critério #4: requisito formalmente deferido ao backlog com evidência; nenhum engine incompleto commitado (D-09).
</success_criteria>

<output>
Criar `.planning/phases/39-cobertura-de-marcas-hugo-boss-zara/39-03-SUMMARY.md` ao concluir — registrar o caminho tomado (GO com engine entregue, ou NO-GO com deferimento + evidência).
</output>
