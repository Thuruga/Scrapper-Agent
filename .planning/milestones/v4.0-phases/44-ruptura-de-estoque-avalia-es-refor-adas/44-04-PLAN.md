---
phase: 44-ruptura-de-estoque-avalia-es-refor-adas
plan: 04
type: execute
wave: 4
depends_on: [44-01, 44-02, 44-03]
files_modified:
  - backend/core/models.py
  - backend/data/brands.json
  - backend/services/review_service.py
  - backend/services/vtex_api_scraper.py
  - backend/api/routes_monitor.py
  - backend/tests/test_review_comments_service.py
  - backend/tests/test_phase44_routes.py
autonomous: true
requirements: [REVW-01]
user_setup: []

must_haves:
  truths:
    - "REVW-01 D-10: normal search and category scans remain light with only rating and review_count fetched inline."
    - "REVW-01 D-11/D-14: on-demand comments use compact ReviewComment fields and do not persist raw provider payloads."
    - "REVW-01 D-12: comment pagination is capped by settings.MAX_REVIEW_PAGES and comments are deduped by review_id or stable hash."
    - "REVW-01 D-13: backend/data/brands.json is audited so every brand has supported review provider evidence or an explicit unsupported rationale."
    - "REVW-01 D-13: brands without supported Trustvox/VTEX-native evidence return reviews_state=unsupported and do not break search or scan flows."
  artifacts:
    - path: "backend/data/brands.json"
      provides: "D-13 provider coverage audit with supported evidence or unsupported rationale per registered brand"
      contains: "review_provider"
    - path: "backend/core/models.py"
      provides: "Additive DynamicBrand audit metadata for review provider evidence and unsupported rationale"
      contains: "review_unsupported_reason"
    - path: "backend/services/review_service.py"
      provides: "On-demand review comments provider router, normalization, dedup, and scan product persistence"
      exports: ["get_review_comments", "fetch_scan_product_review_comments", "dedupe_review_comments", "review_comment_key"]
    - path: "backend/services/vtex_api_scraper.py"
      provides: "review_product_id on VTEX RawProductBronze and SearchProductResult without fetching full comments"
      contains: "review_product_id"
    - path: "backend/api/routes_monitor.py"
      provides: "POST /monitor/category/{monitor_id}/products/{scan_product_id}/reviews"
      contains: "reviews"
  key_links:
    - from: "backend/data/brands.json"
      to: "backend/services/review_service.py"
      via: "brand_service loads audited review_provider, review_store_id, and unsupported rationale"
      pattern: "review_provider"
    - from: "backend/api/routes_monitor.py"
      to: "backend/services/review_service.py"
      via: "route delegates on-demand comments to fetch_scan_product_review_comments"
      pattern: "fetch_scan_product_review_comments"
    - from: "backend/services/vtex_api_scraper.py"
      to: "backend/services/review_service.py"
      via: "normal search still calls get_bulk_reviews only; on-demand path uses review_product_id later"
      pattern: "get_bulk_reviews"
---

<objective>
Add REVW-01 on-demand full comment retrieval for persisted monitor scan products while preserving the existing lightweight rating/count behavior.

Purpose: Operators can fetch compact, page-limited, deduped comments for a specific scanned product when a provider is supported. Unsupported providers are explicit states and normal search does not fetch full comments.
Output: review comment service functions, VTEX `review_product_id` propagation, an authenticated monitor-product review endpoint, and hermetic tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-RESEARCH.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-VALIDATION.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-PATTERNS.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-01-SUMMARY.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-02-SUMMARY.md
@.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-03-SUMMARY.md
@backend/core/models.py
@backend/config.py
@backend/services/review_service.py
@backend/services/vtex_api_scraper.py
@backend/services/category_monitor_service.py
@backend/api/routes_monitor.py
@backend/data/brands.json
</context>

## Artifacts this phase produces

**Endpoint paths created:**
- `POST /monitor/category/{monitor_id}/products/{scan_product_id}/reviews`

**Brand provider audit/config updated:**
- `backend/data/brands.json` has explicit `review_provider`, `review_store_id`, `review_provider_evidence`, and `review_unsupported_reason` coverage per registered brand.
- Supported providers are configured only when Trustvox store id or VTEX-native comments evidence exists; all others are explicit `none` with unsupported rationale.

**Symbols created or extended:**
- `core.models.DynamicBrandCreate.review_provider_evidence`
- `core.models.DynamicBrandCreate.review_unsupported_reason`
- `services.review_service.ReviewState`
- `services.review_service.review_comment_key`
- `services.review_service.dedupe_review_comments`
- `services.review_service.get_review_comments`
- `services.review_service.fetch_scan_product_review_comments`
- `services.review_service._fetch_trustvox_comments`
- `services.review_service._fetch_vtex_native_comments`
- `services.vtex_api_scraper.VtexApiClient.parse_product_dict` sets `review_product_id`
- `services.vtex_api_scraper.VtexApiClient.search` sets `review_product_id`

**Fields updated on scan product records:**
- `review_product_id`
- `reviews_state`
- `review_comments`
- `rating`
- `review_count`

<tasks>


<task type="auto" tdd="true">
  <name>Task 1: Audit and configure review provider coverage per D-13</name>
  <files>backend/core/models.py, backend/data/brands.json, backend/tests/test_review_comments_service.py</files>
  <read_first>
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md (D-13 and D-14)
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-RESEARCH.md (`Open Questions` provider coverage notes)
    - backend/core/models.py (`DynamicBrandCreate`, `DynamicBrand`)
    - backend/data/brands.json (`review_provider`, `review_store_id`, `engine`, `domain`)
    - backend/services/review_service.py (`get_bulk_reviews` provider router)
  </read_first>
  <behavior>
    - Test 1: the real `backend/data/brands.json` validates through `BrandDatabase` after provider audit metadata is added.
    - Test 2: every brand with `review_provider="trustvox"` has non-empty `review_store_id` and `review_provider_evidence`.
    - Test 3: every brand with `review_provider="vtex_native"` has explicit `review_provider_evidence`; `engine="vtex"` alone is not enough.
    - Test 4: every brand with `review_provider="none"` has non-empty `review_unsupported_reason`.
    - Test 5: Aramis remains configured as Trustvox with store id `78800` unless stronger local evidence contradicts that existing configuration.
  </behavior>
  <action>
    Add optional additive fields `review_provider_evidence: Optional[str] = None` and `review_unsupported_reason: Optional[str] = None` to `DynamicBrandCreate` so provider audit metadata round-trips through `BrandManagerService`. Audit every registered entry in `backend/data/brands.json`. Keep or set `review_provider="trustvox"` only when a Trustvox store id is present and evidence is recorded. Set `review_provider="vtex_native"` only when concrete repository or verified provider evidence identifies a VTEX-native comments endpoint for that brand; do not infer VTEX-native support from `engine="vtex"` alone. For every brand without supported-provider evidence, set or keep `review_provider="none"`, set `review_store_id=null`, and fill `review_unsupported_reason` with a concrete rationale such as missing Trustvox store id and no verified VTEX-native comments endpoint. Preserve brand keys, domains, engines, activation states, mappings, and other unrelated config.
  </action>
  <verify>
    <automated>python -c "import json; json.load(open('backend/data/brands.json', encoding='utf-8'))"</automated>
    <automated>python -m pytest backend/tests/test_review_comments_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `DynamicBrandCreate` contains optional `review_provider_evidence` and `review_unsupported_reason` fields.
    - Every registered brand in `backend/data/brands.json` has an explicit provider state: supported with evidence or `none` with unsupported rationale.
    - No brand is marked `vtex_native` solely because its engine is VTEX.
    - Aramis remains the known Trustvox-supported brand with `review_store_id="78800"` unless execution records stronger contradictory evidence.
    - The coverage tests fail if a brand lacks both supported provider evidence and unsupported rationale.
  </acceptance_criteria>
  <done>D-13 provider coverage is audited and configured before comment-fetching code routes provider states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend review_service with compact on-demand comments</name>
  <files>backend/services/review_service.py, backend/tests/test_review_comments_service.py</files>
  <read_first>
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-CONTEXT.md (D-10 through D-14)
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-RESEARCH.md (`Review comments action`, `Open Questions`, `Common Pitfalls`)
    - .planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-PATTERNS.md (`backend/services/review_service.py`)
    - backend/services/review_service.py (`get_bulk_reviews`, Trustvox and VTEX native summary drivers)
    - backend/core/models.py (`ReviewComment`, `ReviewCommentsResult`)
    - backend/config.py (`MAX_REVIEW_PAGES`)
    - backend/data/brands.json (`review_provider_evidence`, `review_unsupported_reason`)
  </read_first>
  <behavior>
    - Test 1: unknown brand and provider `none` return `reviews_state="unsupported"`, `comments=[]`, and no exception.
    - Test 2: `max_pages` is capped to `settings.MAX_REVIEW_PAGES` even when caller requests more.
    - Test 3: duplicate provider comments collapse by `review_id`.
    - Test 4: comments without stable provider ID receive deterministic `review_id` from a hash of rating, title, text, author, and created_at.
    - Test 5: normalized comments contain only compact fields from `ReviewComment`.
  </behavior>
  <action>
    Before editing source files, perform the Backstage MCP coding standards check if available and record the result in `44-04-SUMMARY.md`. Keep `get_bulk_reviews` and `get_single_review` summary behavior intact. Add `ReviewState` constants `available`, `unsupported`, and `temporary_failure`. Add `review_comment_key(item)` and `dedupe_review_comments(comments)` helpers. Add `get_review_comments(brand_key, product_id, max_pages=None) -> ReviewCommentsResult` that resolves audited `DynamicBrand.review_provider`, `review_store_id`, `review_provider_evidence`, and `review_unsupported_reason`; caps pages through `settings.MAX_REVIEW_PAGES`; returns unsupported for `none`, missing provider config, or missing evidence; and dispatches only supported providers to provider-specific comment fetchers. Implement `_fetch_trustvox_comments` using the existing Trustvox `widget/root` request shape plus page parameter when present; parse comment arrays from keys named `reviews`, `comments`, `items`, or nested `data` when those keys contain dict/list review entries. Implement `_fetch_vtex_native_comments` using the persisted brand domain and the VTEX Reviews path family already used for summary; if the comments endpoint responds 404/401 or lacks comment entries, return `temporary_failure` or `unsupported` based on status instead of persisting raw payload. Provider fetchers must return normalized `ReviewComment` objects only.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_review_comments_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Existing `get_bulk_reviews` tests and behavior are unchanged except for safe helper imports.
    - `get_review_comments` returns `ReviewCommentsResult` with `reviews_state`, `comments`, `rating`, `review_count`, `source_provider`, and `max_pages`.
    - No field named `raw_reviews`, `raw_payload`, or `payload` is introduced into persisted product/comment contracts.
    - Dedup by `review_id` and hash fallback are covered by tests.
    - Provider absence or missing audit evidence is `unsupported`, not a failed search.
  </acceptance_criteria>
  <done>Review comments are fetched on demand, capped, deduped, compact, and explicitly unsupported when no provider exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist on-demand review comments for monitor scan products</name>
  <files>backend/services/review_service.py, backend/services/vtex_api_scraper.py, backend/api/routes_monitor.py, backend/tests/test_review_comments_service.py, backend/tests/test_phase44_routes.py</files>
  <read_first>
    - backend/services/vtex_api_scraper.py (`parse_product_dict`, `search`, `productId` usage)
    - backend/services/category_monitor_service.py (`monitored_products_{monitor_id}.json` artifact)
    - backend/api/routes_monitor.py (stock-depth route from Plan 44-03)
    - backend/services/review_service.py (`get_review_comments`)
    - backend/data/brands.json (`review_provider`, `review_store_id`)
  </read_first>
  <behavior>
    - Test 1: VTEX category scan products and normal search products include `review_product_id` from VTEX `productId`.
    - Test 2: `fetch_scan_product_review_comments(monitor_id, scan_product_id, max_pages)` rejects missing product identity with `reviews_state="unsupported"` and no provider call.
    - Test 3: successful on-demand comments update only the matching scan product with `reviews_state` and `review_comments`.
    - Test 4: `POST /monitor/category/{monitor_id}/products/{scan_product_id}/reviews` caps max_pages and returns the persisted compact comments.
    - Test 5: normal search files do not call `get_review_comments` or fetch full comments.
  </behavior>
  <action>
    In `VtexApiClient.parse_product_dict`, set `review_product_id=str(p.productId)` on `RawProductBronze`. In `VtexApiClient.search`, set `review_product_id=pid` on each `SearchProductResult` while keeping existing `get_bulk_reviews` summary calls unchanged. In `review_service.py`, add `fetch_scan_product_review_comments(monitor_id, scan_product_id, max_pages=None) -> ReviewCommentsResult`: load the monitor row, load `monitored_products_{monitor_id}.json`, find the matching product by `scan_product_id`, resolve the brand, read `review_product_id`, call `get_review_comments`, persist `reviews_state`, `review_comments`, `rating`, and `review_count` back onto only that product, and return the result. In `routes_monitor.py`, add `POST /monitor/category/{monitor_id}/products/{scan_product_id}/reviews` with a small request model containing only optional `max_pages`; route body must not accept provider, raw URL, domain, product_id override, or raw payload. The route calls `fetch_scan_product_review_comments`.
  </action>
  <verify>
    <automated>python -m pytest backend/tests/test_review_comments_service.py backend/tests/test_phase44_routes.py -q</automated>
    <automated>powershell -NoProfile -Command "$m = rg -n 'get_review_comments|fetch_scan_product_review_comments|review_comments' backend/api/routes_search.py backend/services/vtex_api_scraper.py; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - New route path is exactly `/monitor/category/{monitor_id}/products/{scan_product_id}/reviews`.
    - Route request body contains only optional `max_pages`.
    - On-demand comments persist on the matched scan product record only.
    - `backend/services/vtex_api_scraper.py` may set `review_product_id`, `rating`, and `review_count`, but it does not import or call `get_review_comments`.
    - `backend/api/routes_search.py` does not import or call full-comment functions.
  </acceptance_criteria>
  <done>On-demand review comments are available for persisted scan products without making normal search heavy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API caller -> review action | Caller can choose monitor_id, scan_product_id, and max_pages only. |
| provider response -> persisted comments | External provider payloads are untrusted and may contain large or unexpected fields. |
| normal search -> review summary | Search must keep summary-only provider calls and avoid comment payload collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-14 | Information Disclosure | review provider payloads | mitigate | Normalize to `ReviewComment` fields only; do not persist raw provider payloads. |
| T-44-15 | Denial of Service | review pagination | mitigate | Cap pages by `settings.MAX_REVIEW_PAGES`; route accepts no unbounded page/limit values. |
| T-44-16 | Tampering | review product identity | mitigate | Use `review_product_id` from persisted scan/search product data; route accepts no product_id override. |
| T-44-17 | Spoofing / Access Control | review endpoint | mitigate | Endpoint lives in `routes_monitor.py`, already included under the authenticated API router with `X-API-Key`. |
| T-44-18 | Repudiation | unsupported providers | mitigate | Audit `backend/data/brands.json`; return and persist `reviews_state="unsupported"` for brands whose provider is `none` or lacks supported-provider evidence, with unsupported rationale recorded in brand config. |
</threat_model>

<verification>
Run:

```powershell
python -c "import json; json.load(open('backend/data/brands.json', encoding='utf-8'))"
python -m pytest backend/tests/test_review_comments_service.py backend/tests/test_phase44_routes.py -q
powershell -NoProfile -Command "$m = rg -n 'get_review_comments|fetch_scan_product_review_comments|review_comments' backend/api/routes_search.py backend/services/vtex_api_scraper.py; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"
```

Manual low-frequency UAT: after a monitor product has `review_product_id`, call the reviews endpoint once for a supported provider brand and confirm `review_comments` contains compact entries or `reviews_state` is explicit. For provider `none`, confirm `unsupported` and no search failure.
</verification>

<success_criteria>
- Normal search remains summary-only with `rating` and `review_count`.
- Full comments are available only through the monitor-product action.
- Comments are compact, page-limited, deduped, and provider absence is explicit.
- D-13 provider coverage is documented in `backend/data/brands.json` with evidence for supported providers and unsupported rationale for the rest.
</success_criteria>

<output>
Create `.planning/phases/44-ruptura-de-estoque-avalia-es-refor-adas/44-04-SUMMARY.md` when done.
</output>
