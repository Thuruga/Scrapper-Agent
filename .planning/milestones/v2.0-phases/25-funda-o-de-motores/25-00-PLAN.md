---
phase: 25-funda-o-de-motores
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_engine_detection.py
  - tests/test_brand_active.py
autonomous: true
requirements: [COMP-02, MGMT-01]
must_haves:
  truths:
    - "tests/test_engine_detection.py exists and fails RED (detect_engine + create_brand unknown scenarios)"
    - "tests/test_brand_active.py exists and fails RED (list_brands active_only + set_active + GET /brands/ inactive)"
  artifacts:
    - path: "tests/test_engine_detection.py"
      provides: "RED test stubs for COMP-02"
      contains: "class TestDetectEngine"
    - path: "tests/test_brand_active.py"
      provides: "RED test stubs for MGMT-01"
      contains: "class TestListBrandsActiveOnly"
  key_links:
    - from: "tests/test_engine_detection.py"
      to: "api.routes_brands.detect_engine"
      via: "import + asyncio.run"
      pattern: "from api.routes_brands import detect_engine"
    - from: "tests/test_brand_active.py"
      to: "services.brand_service.BrandManagerService"
      via: "in-memory service construction"
      pattern: "BrandManagerService"
---

<objective>
Wave 0 RED test scaffold. Create the two test files named in 25-VALIDATION.md
(`tests/test_engine_detection.py`, `tests/test_brand_active.py`) covering every
COMP-02 and MGMT-01 behavior BEFORE any implementation exists. The tests MUST fail
RED (they reference behavior that does not yet exist: `detect_engine` still returns
`"vtex"` as fallback, `list_brands` has no `active_only` param, `set_active` does
not exist, `create_brand` does not auto-deactivate unknown engines).

Purpose: Establish the Nyquist feedback loop. Every Wave 1/Wave 2 implementation task
references one of these test commands as its `<automated>` verify. No implementation
proceeds without a failing test to turn green.

Output: Two new test files in `tests/`, both failing RED against current code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-funda-o-de-motores/25-CONTEXT.md
@.planning/phases/25-funda-o-de-motores/25-RESEARCH.md
@.planning/phases/25-funda-o-de-motores/25-VALIDATION.md
</context>

<artifacts_this_phase_produces>
Wave 0 creates only test files (no production symbols). The test files reference the
following symbols that Wave 1/Wave 2 will create — they are EXPECTED to be undefined
now (RED):
- `set_active` method on `BrandManagerService`
- `active_only` parameter on `list_brands`
- `BrandActiveUpdate` Pydantic model (in `core/models.py` or `api/routes_brands.py`)
- `PATCH /brands/{brand_key}/active` route
- `"unknown"` return value from `detect_engine`
- `create_brand` auto-deactivation of unknown-engine brands
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: RED scaffold tests/test_engine_detection.py (COMP-02)</name>
  <files>tests/test_engine_detection.py</files>
  <read_first>
    - tests/test_cross_marketplace_service.py (project async test style + asyncio.run usage)
    - tests/test_brand_gate.py (project module-import test convention, no pytest.ini)
    - api/routes_brands.py (detect_engine L14-53 current behavior; create_brand L56-66)
    - .planning/phases/25-funda-o-de-motores/25-RESEARCH.md (§"Code Examples" — the `_make_mock_response`/`_make_mock_session` helpers and the four TestDetectEngine cases)
    - .planning/phases/25-funda-o-de-motores/25-VALIDATION.md (Per-Task Verification Map — exact -k filters: shopify, vtex, wake, all_probes_fail, CreateBrandUnknown)
  </read_first>
  <action>
    Create `tests/test_engine_detection.py`. Include the two mock helpers from RESEARCH §"Code Examples":
    `_make_mock_response(status, json_data, text_data)` (sets `.status`, `.json` AsyncMock, `.text` AsyncMock,
    and `__aenter__`/`__aexit__` AsyncMocks) and `_make_mock_session(responses_dict)` (substring URL match on `session.get`,
    `session.post` raising aiohttp.ClientError). Patch target is `api.routes_brands.SessionManager.get_session` via
    `unittest.mock.patch(..., new=AsyncMock(return_value=mock_session))`. Run async calls with `asyncio.run(detect_engine(domain))`.

    Class `TestDetectEngine` with the four cases mapped in VALIDATION:
    - `test_shopify_detected_via_collections_json` — collections.json → 200 with `{"collections":[...]}` → asserts `== "shopify"`.
    - `test_vtex_detected_via_category_tree` — collections.json 404, category/tree/1 → 200 → asserts `== "vtex"`.
    - `test_wake_commerce_returns_unknown` — all API probes 404, home HTML contains `fbitsstatic.net` marker → asserts `== "unknown"` (this is the COMP-02 SC-1 anchor; today the code returns `"vtex"` so this MUST be RED).
    - `test_all_probes_fail_returns_unknown` — all probes 404, generic HTML with no platform marker → asserts `== "unknown"` (today returns `"vtex"` fallback at L53 → RED).

    Class `TestCreateBrandUnknown` — integration: call `create_brand` (mock `detect_engine` to return `"unknown"` and mock
    `brand_service.add_brand`/`set_active` or use an in-memory brand_service) and assert the persisted/returned brand has
    `engine == "unknown"` and `is_active is False`, and that no HTTP exception is raised (per D-04). This MUST be RED now
    because `create_brand` does not yet branch on `"unknown"`.

    Do NOT implement any production code. Tests must reference the target behavior so they fail against current code.
    Do NOT write fenced implementation code into production files.
  </action>
  <verify>
    <automated>cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -m pytest tests/test_engine_detection.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_engine_detection.py contains `class TestDetectEngine` and `class TestCreateBrandUnknown`
    - `python -m pytest tests/test_engine_detection.py -k wake -x` collects and FAILS (RED — current code returns "vtex")
    - `python -m pytest tests/test_engine_detection.py -k all_probes_fail -x` FAILS RED
    - `python -m pytest tests/test_engine_detection.py -k CreateBrandUnknown -x` FAILS RED
    - No production file under api/ or services/ is modified by this task
  </acceptance_criteria>
  <done>Both detect_engine and create_brand RED tests collect cleanly and fail (not error on import/collection); failures are assertion failures or AttributeError on missing behavior, proving the contract is expressed.</done>
</task>

<task type="auto">
  <name>Task 2: RED scaffold tests/test_brand_active.py (MGMT-01)</name>
  <files>tests/test_brand_active.py</files>
  <read_first>
    - tests/test_brand_gate.py (project module-import test convention)
    - services/brand_service.py (BrandManagerService.__init__ L33-40; list_brands L207-209; _save L180-186; get_brand L211-213)
    - core/models.py (DynamicBrand L228-232: is_active default True; DynamicBrandCreate L210-225)
    - .planning/phases/25-funda-o-de-motores/25-RESEARCH.md (§"Code Examples" — `_make_service_with_brands`, TestListBrandsActiveOnly, TestSetActive)
    - .planning/phases/25-funda-o-de-motores/25-VALIDATION.md (Per-Task Verification Map — -k filters: default_returns_all, active_only_excludes, deactivate, reactivate, RouteReturnsInactive)
  </read_first>
  <action>
    Create `tests/test_brand_active.py`. Include `_make_service_with_brands()` helper from RESEARCH §"Code Examples":
    builds `BrandManagerService` via `__new__` (no `__init__`, no file I/O), sets `svc.brands = {}`, `svc.last_modified = 0`,
    `svc.updated_event = asyncio.Event()`, and inserts two `DynamicBrand`: one `is_active=True` (engine="vtex") and one
    `is_active=False` (engine="unknown"). Patch `svc._save` with `unittest.mock.patch.object` in set_active tests to avoid I/O.

    Class `TestListBrandsActiveOnly` (RED — `list_brands` has no `active_only` param yet):
    - `test_default_returns_all_brands` — `svc.list_brands()` returns 2.
    - `test_active_only_excludes_inactive` — `svc.list_brands(active_only=True)` returns 1, the active one (RED: TypeError today).
    - `test_active_only_false_returns_all` — `svc.list_brands(active_only=False)` returns 2.

    Class `TestSetActive` (RED — `set_active` does not exist yet):
    - `test_deactivate_brand` — `set_active("active_brand", False)` returns brand with `is_active is False`, mutates dict.
    - `test_reactivate_brand` — `set_active("inactive_brand", True)` returns brand with `is_active is True`.
    - `test_set_active_unknown_key_returns_none` — `set_active("nonexistent", True)` returns None (drives the 404 path; security: arbitrary brand_key → no corruption, per RESEARCH §Security Domain Tampering threat).

    Class `TestBrandRouteReturnsInactive` (integration, MGMT-01 SC-4 / D-08): assert that `GET /brands/` route logic
    returns inactive brands — i.e. the route calls `list_brands()` with default `active_only=False`. Use FastAPI TestClient
    against the brands router OR call the route function directly with an in-memory brand_service containing one inactive brand,
    asserting the inactive brand IS present in the result. This guards against the Pitfall-6 regression (default flipped to True).

    Do NOT implement production code. Tests must reference target behavior so they fail against current code.
    Do NOT write fenced implementation code into production files.
  </action>
  <verify>
    <automated>cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -m pytest tests/test_brand_active.py -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_brand_active.py contains `class TestListBrandsActiveOnly`, `class TestSetActive`, `class TestBrandRouteReturnsInactive`
    - `python -m pytest tests/test_brand_active.py -k active_only_excludes -x` FAILS RED (list_brands has no active_only param)
    - `python -m pytest tests/test_brand_active.py -k deactivate -x` FAILS RED (set_active does not exist)
    - `python -m pytest tests/test_brand_active.py -k RouteReturnsInactive -x` collects (may pass today since default is unfiltered — this is the SC-4 guard, document expectation in the test)
    - No production file under api/ or services/ is modified by this task
  </acceptance_criteria>
  <done>All MGMT-01 RED tests collect; list_brands(active_only) and set_active tests fail with TypeError/AttributeError proving the contracts are expressed; RouteReturnsInactive guards default opt-in behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in Wave 0) | Test scaffold touches no runtime trust boundary; tests mock all network and I/O. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-W0-01 | Tampering | test fixtures mocking aiohttp | accept | Tests mock all HTTP via unittest.mock; no real network, no live redirect risk. The redirect/spoofing threat (T-25-01-SR) is exercised at the implementation task in Plan 01, not here. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH §"Package Legitimacy Audit" confirms zero new dependencies (aiohttp/fastapi/pydantic already in use). |
</threat_model>

<verification>
Both test files collect under pytest and fail RED against current code. Run:
`cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -m pytest tests/test_engine_detection.py tests/test_brand_active.py -q`
Expected: tests collected, multiple failures (RED), zero collection/import errors.
</verification>

<success_criteria>
- tests/test_engine_detection.py and tests/test_brand_active.py exist
- All named -k cases from VALIDATION Per-Task Verification Map are present and collect
- COMP-02 and MGMT-01 behavior tests fail RED (no production code changed)
- No import/collection errors (failures are assertion/Attribute/TypeError on missing behavior)
</success_criteria>

<output>
Create `.planning/phases/25-funda-o-de-motores/25-00-SUMMARY.md` when done.
</output>
