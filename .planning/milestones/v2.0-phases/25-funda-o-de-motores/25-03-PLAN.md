---
phase: 25-funda-o-de-motores
plan: 03
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - api/routes_brands.py
  - api/routes_search.py
  - services/engines/factory.py
  - api/routes_category.py
  - core/models.py
autonomous: true
requirements: [MGMT-01]
must_haves:
  truths:
    - "PATCH /brands/{brand_key}/active with {is_active: bool} sets the flag and returns the updated brand; unknown key → 404"
    - "A non-bool is_active body is rejected with HTTP 422 by Pydantic"
    - "Search, scheduler and category-scan enumerate brands via list_brands(active_only=True); a deactivated brand drops out next cycle"
    - "GET /brands/ still returns inactive brands (active_only opt-in, not the global default) — SC-4"
  artifacts:
    - path: "api/routes_brands.py"
      provides: "PATCH /brands/{brand_key}/active endpoint + BrandActiveUpdate model usage; GET /brands/ unchanged"
      contains: "@router.patch(\"/brands/{brand_key}/active\""
    - path: "api/routes_search.py"
      provides: "active_only=True at the three search call sites"
      contains: "list_brands(active_only=True)"
    - path: "services/engines/factory.py"
      provides: "active_only=True for scheduler default brand list"
      contains: "list_brands(active_only=True)"
  key_links:
    - from: "api/routes_brands.py:set_brand_active"
      to: "services.brand_service.set_active"
      via: "route delegates to service (thin route)"
      pattern: "brand_service\\.set_active"
    - from: "api/routes_search.py"
      to: "services.brand_service.list_brands"
      via: "active_only=True chokepoint call"
      pattern: "list_brands\\(active_only=True\\)"
---

<objective>
Implement MGMT-01 wiring (D-06, D-08) — the API surface plus the opt-in chokepoint adoption:
1. Add `PATCH /brands/{brand_key}/active` to `api/routes_brands.py` with a `BrandActiveUpdate`
   Pydantic body `{ is_active: bool }` (D-06). Thin route: delegate to `brand_service.set_active`;
   None → HTTPException 404; otherwise return the updated `DynamicBrand`. Non-bool body → FastAPI 422.
2. Flip the search/scheduler/category-scan call sites to `list_brands(active_only=True)` per D-08:
   `api/routes_search.py` L144, L209, L228; `services/engines/factory.py` L70; and
   `api/routes_category.py` L176 (`scrape_category_multi` validator — RESEARCH Open Question 2 decision: YES,
   enforce active_only for consistency with search).
3. Leave `GET /brands/` (routes_brands.py L72) at the default `active_only=False` so inactive brands stay
   visible/reactivatable (SC-4, D-08). Leave `services/category_mapping.py:161` at default `False` (D-08 — gate
   is at search time; revisit Phase 29). Virtual marketplaces (mercado_livre/netshoes/amazon) remain appended
   after the list call and are always active.

Purpose: Connect the chokepoint to its real consumers so deactivation/reactivation is observable end-to-end,
while keeping the management surface unfiltered. This wave consumes `set_active` (Plan 02) and the hardened
`detect_engine`/create_brand (Plan 01) — hence Wave 2.

Output: PATCH endpoint live + four call sites switched to active_only=True + GET unchanged. Turns RED
integration tests green (test_brand_active.py: TestBrandRouteReturnsInactive and any PATCH/422 cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-funda-o-de-motores/25-CONTEXT.md
@.planning/phases/25-funda-o-de-motores/25-RESEARCH.md
@.planning/phases/25-funda-o-de-motores/25-VALIDATION.md

@api/routes_brands.py
@api/routes_search.py
@services/engines/factory.py
@api/routes_category.py
@core/models.py

@.planning/phases/25-funda-o-de-motores/25-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates/changes (exclude from drift verification):
- `BrandActiveUpdate` Pydantic model (`{ is_active: bool }`)
- `PATCH /brands/{brand_key}/active` route (`set_brand_active` handler) in api/routes_brands.py
- `list_brands(active_only=True)` call at routes_search.py L144/L209/L228, factory.py L70, routes_category.py L176
- (unchanged on purpose) GET /brands/ at default; category_mapping.py:161 at default; virtual marketplaces always active
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PATCH /brands/{brand_key}/active endpoint + BrandActiveUpdate model (D-06)</name>
  <files>api/routes_brands.py, core/models.py</files>
  <read_first>
    - api/routes_brands.py (update_brand_mappings L137-145 — analog thin route with 404 handling; delete_brand L148-160; list_brands route L69-103; imports L1-8)
    - services/brand_service.py (set_active — created in Plan 02; update_mappings L215-221 analog)
    - core/models.py (DynamicBrand L228-232; DynamicBrandCreate L210-225 — place BrandActiveUpdate near these or inline in the route module)
    - .planning/phases/25-funda-o-de-motores/25-RESEARCH.md (§"PATCH /brands/{brand_key}/active Endpoint"; §"Security Domain" — V5 Input Validation, Tampering on non-bool body)
    - .planning/phases/25-funda-o-de-motores/25-02-SUMMARY.md (set_active signature/return contract)
    - tests/test_brand_active.py (PATCH/route contract from Plan 00)
  </read_first>
  <action>
    Define a Pydantic model `BrandActiveUpdate` with a single field `is_active: bool` (put it in `core/models.py` near
    DynamicBrand, or inline in `api/routes_brands.py` — choose one and import accordingly; if added to core/models.py, that is
    why core/models.py is in files_modified). Add an async route `@router.patch("/brands/{brand_key}/active", response_model=DynamicBrand)`
    handler `set_brand_active(brand_key: str, payload: BrandActiveUpdate)` that calls `brand_service.set_active(brand_key, payload.is_active)`;
    if the result is None raise `HTTPException(status_code=404, detail="Marca não encontrada")`; otherwise return the brand.

    Keep the route thin — no business logic in the route (Architectural Responsibility Map). The bool typing on `BrandActiveUpdate.is_active`
    is the V5 input-validation mitigation: a non-bool body yields FastAPI 422 automatically (Tampering). Idempotent set semantics (not toggle)
    come from set_active. Do NOT modify GET /brands/, create_brand, or delete_brand in this task. No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -m pytest tests/test_brand_active.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - api/routes_brands.py contains `@router.patch("/brands/{brand_key}/active"` with handler delegating to `brand_service.set_active`
    - The handler raises HTTPException 404 when set_active returns None
    - `BrandActiveUpdate` model exists with `is_active: bool` (in core/models.py or routes_brands.py)
    - GET /brands/ route at routes_brands.py is unchanged (still calls list_brands() with no args)
    - `python -m pytest tests/test_brand_active.py -x -q` exits 0 (full MGMT-01 file green, incl. route + any 422 case)
  </acceptance_criteria>
  <done>PATCH endpoint sets/clears is_active idempotently via the service, returns the updated brand or 404, and rejects malformed bodies with 422; GET /brands/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Adopt active_only=True at search/scheduler/category-scan call sites (D-08)</name>
  <files>api/routes_search.py, services/engines/factory.py, api/routes_category.py</files>
  <read_first>
    - api/routes_search.py (L144 search_products validation list; L209 search_products_get all_brands; L228 export_search_products validation list — all build `[b.brand_key for b in brand_service.list_brands()]` then extend with virtual marketplaces)
    - services/engines/factory.py (L70 search_all_brands default target_brands from list_brands())
    - api/routes_category.py (L176 scrape_category_multi builds all_brands dict from list_brands() for validation)
    - .planning/phases/25-funda-o-de-motores/25-RESEARCH.md (§"Call-Site Audit" table; §"Common Pitfalls" 4 — both validation list AND factory must pass True; Open Question 2 decision)
    - .planning/phases/25-funda-o-de-motores/25-CONTEXT.md (D-08 — exact call sites; GET /brands/ and category_mapping.py:161 stay False; virtual marketplaces always active)
  </read_first>
  <action>
    Change `brand_service.list_brands()` to `brand_service.list_brands(active_only=True)` at exactly these call sites:
    routes_search.py L144 (search_products), L209 (search_products_get), L228 (export_search_products); factory.py L70
    (search_all_brands default brand list); routes_category.py L176 (scrape_category_multi validator). Keep the
    `.extend(["mercado_livre","netshoes","amazon"])` virtual-marketplace appends exactly as-is (they are added AFTER the list call
    and stay always-active — do not filter them).

    Per Pitfall 4: in routes_search.py both the validation list and the factory default path must be active_only=True — verify all
    three search occurrences are switched, not just one, so an inactive brand is neither a valid filter value nor a default target.

    Decision on RESEARCH Open Question 2: routes_category.py:176 scrape_category_multi DOES enforce active_only=True (consistency with
    search — an inactive brand should not be a valid scan target). This is a deliberate task decision; record it in the SUMMARY.

    Do NOT touch `GET /brands/` (routes_brands.py L72 — stays default False, SC-4). Do NOT touch `services/category_mapping.py:161`
    (D-08 — stays False; revisit Phase 29). Do NOT add per-call-site `if b.is_active` filters — rely solely on the chokepoint param.
    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>cd "c:/Users/arthur.correia/Documents/Pessoal/scrapper" && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - api/routes_search.py contains `list_brands(active_only=True)` at all three search call sites (search_products, search_products_get, export_search_products)
    - services/engines/factory.py search_all_brands default branch calls `list_brands(active_only=True)`
    - api/routes_category.py scrape_category_multi validator calls `list_brands(active_only=True)`
    - api/routes_brands.py GET /brands/ still calls `list_brands()` with no args (default False) — grep confirms unchanged
    - services/category_mapping.py:161 still calls list_brands without active_only=True (unchanged)
    - Virtual marketplace appends (mercado_livre/netshoes/amazon) unchanged at every search call site
    - `python -m pytest tests/ -x -q` full suite exits 0 (no regression)
  </acceptance_criteria>
  <done>Search, export, scheduler and category-scan exclude inactive brands via the single chokepoint param; GET /brands/ and category mapping keep inactive brands; virtual marketplaces unaffected; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PATCH /brands/{brand_key}/active | Untrusted URL path `brand_key` and JSON body `is_active` cross into the service. |
| client → search/category endpoints | Untrusted `brands` filter values validated against the active brand list. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-BODY | Tampering | PATCH body is_active | mitigate | `BrandActiveUpdate.is_active: bool` → FastAPI/Pydantic returns 422 for non-bool input (V5 Input Validation). Implemented in Task 1. |
| T-25-03-KEY | Tampering | PATCH brand_key path | mitigate | set_active (Plan 02) normalizes + returns None → route raises 404; no corruption from arbitrary keys. Surfaced in Task 1. |
| T-25-03-VAL | Information Disclosure | search/export brand validation | mitigate | active_only=True at validation lists (Pitfall 4) so inactive brands are not valid filter targets — no confusing empty-result leakage of inactive-brand existence via search. Implemented in Task 2. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH §"Package Legitimacy Audit" confirms zero new dependencies. |
</threat_model>

<verification>
Per-task: Task 1 → `python -m pytest tests/test_brand_active.py -x -q`; Task 2 → full suite `python -m pytest tests/ -x -q`.
Wave-merge gate: `python -m pytest tests/ -x -q` fully green.
Manual (VALIDATION Manual-Only): PATCH a brand inactive, run a search → it is absent; PATCH it active → present next call;
GET /brands/ shows it throughout. Production: confirm Supabase `is_active` column exists (ALTER TABLE ... IF NOT EXISTS) before release.
</verification>

<success_criteria>
- ROADMAP SC-2: deactivating via PATCH removes the brand from search/export/scheduler with no other route modified — exclusion only via the chokepoint
- ROADMAP SC-3: reactivating restores the brand on the next search call
- ROADMAP SC-4: GET /brands/ still returns inactive brands (opt-in active_only, not global default)
- PATCH rejects non-bool bodies (422) and unknown keys (404)
- Full test suite green
</success_criteria>

<output>
Create `.planning/phases/25-funda-o-de-motores/25-03-SUMMARY.md` when done.
</output>
