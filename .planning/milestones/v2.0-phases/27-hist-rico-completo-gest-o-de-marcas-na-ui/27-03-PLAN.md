---
phase: 27-hist-rico-completo-gest-o-de-marcas-na-ui
plan: 03
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - frontend/src/App.tsx
autonomous: true
requirements: [HIST-02]
must_haves:
  truths:
    - "App.tsx owns a preloadedJobId state and renderTab passes preloadedJobId + onClearPreloadedJob to BOTH SearchPage and CrossMarketplacePage (Success Criterion #2)"
    - "Each tab shows a history list filtered to its own type (search vs cross) — comparative history in SearchPage, SKU history in CrossMarketplacePage (D-01)"
    - "Each entry shows label + type badge + date/time + status (D-02); only COMPLETED entries are clickable to reopen (D-06)"
    - "Each history entry exposes two per-entry actions — reopen (click a COMPLETED row) and delete (ApiClient.deleteHistory with confirm + refetch, DELETE /history/{job_id}) (D-03)"
    - "Clicking a COMPLETED entry re-displays the saved results in the correct tab without re-scraping, overwriting the current view (D-04/D-05)"
    - "A failed reopen does not crash; FAILED entries show an error badge and PENDING entries show an in-progress indicator, neither clickable"
  artifacts:
    - path: "frontend/src/App.tsx"
      provides: "App-level preloadedJobId state + handleReopen; renderTab prop propagation; reusable HistoryList component mounted in both pages"
      contains: "const [preloadedJobId, setPreloadedJobId]"
  key_links:
    - from: "frontend/src/App.tsx (renderTab)"
      to: "SearchPage / CrossMarketplacePage props"
      via: "preloadedJobId={preloadedJobId} onClearPreloadedJob={() => setPreloadedJobId(null)}"
      pattern: "preloadedJobId=\\{preloadedJobId\\}"
    - from: "frontend/src/App.tsx (HistoryList row click)"
      to: "App.handleReopen"
      via: "onReopen(jobId) bubbles → setActiveTab(type) + setPreloadedJobId(jobId)"
      pattern: "setPreloadedJobId\\("
    - from: "frontend/src/App.tsx (HistoryList)"
      to: "ApiClient.getHistoryList / getHistoryDetail / deleteHistory"
      via: "fetch on mount/after-search/after-delete; filter client-side by type; reopen via getHistoryDetail in child useEffect"
      pattern: "getHistoryList\\("
---

<objective>
HIST-02: make every saved search reopenable. Today `preloadedJobId` is a dead prop — the child pages already have the loading `useEffect`, but `App.tsx` never owns the state nor passes it (Success Criterion #2 names `App.tsx` propagation literally). And no history-list UI exists anywhere yet. This plan (1) adds App-level `preloadedJobId` state + a `handleReopen` callback and wires both into `renderTab` for `SearchPage` and `CrossMarketplacePage` (including the `onReopen` prop type on both pages so the call sites type-check on their own), and (2) builds ONE reusable `HistoryList` component mounted per tab (D-01) that lists `getHistoryList()` filtered by type, renders label/badge/date/status (D-02), and reopens only COMPLETED entries (D-06) by bubbling to `handleReopen` (D-04/D-05).

Purpose: Close the reopen loop so comparative searches persisted by 27-01 (and existing SKU searches) can be re-displayed without re-scraping.
Output: `frontend/src/App.tsx` gains `preloadedJobId` state + propagation + a `HistoryList` component mounted in both `SearchPage` and `CrossMarketplacePage`. Depends on 27-02 (same file — App.tsx; must run after to avoid conflict) and 27-01 (comparative persistence must exist for the comparative reopen path to have data).

> EXECUTOR NOTE: Anchor every edit by SYMBOL NAME (`renderTab`, `SearchPage`, `CrossMarketplacePage`, `HistoryList`, each child's `useEffect([preloadedJobId])`, `handleSearch`), NOT by exact line number — the live `App.tsx` drifts a few lines from the line references below (e.g. renderTab cases ~1796-1799, CrossMarketplacePage useEffect ~974-985). Treat all line numbers as approximate hints only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-RESEARCH.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-PATTERNS.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-level preloadedJobId state + handleReopen + renderTab propagation, including the onReopen prop-type on both pages (SC#2)</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    NOTE: Anchor every edit by SYMBOL NAME, not by exact line number — live App.tsx drifts a few lines from the references below. Locate `App()`, `renderTab` (the `case 'search'`/`case 'cross'` switch), the `SearchPage` arrow-function declaration, the `CrossMarketplacePage` arrow-function declaration, and each child's `useEffect([preloadedJobId])` by name/signature and edit there.
    - frontend/src/App.tsx — `App()` (~1779): declares `activeTab`/`setActiveTab` (~1780), `brands`, sidebar flags, `refreshBrands`. `renderTab` currently renders `case 'search': <SearchPage brands={brands} />` and `case 'cross': <CrossMarketplacePage />` (~1798-1799) — NEITHER passes preloadedJobId/onClearPreloadedJob.
    - frontend/src/App.tsx — `SearchPage` signature (~641): inline destructured props `{ brands, preloadedJobId, onClearPreloadedJob }: { brands: any[], preloadedJobId?: string | null, onClearPreloadedJob?: () => void }` — NO index signature, so TS excess-property checking rejects any extra JSX prop until the prop type declares it. Its reopen `useEffect([preloadedJobId])` (~651-660) calls `ApiClient.getHistoryDetail(preloadedJobId)` and `setResults({ results: res.results, query: res.query, brands_searched: res.brands })`; `onClearPreloadedJob()` fires on a fresh `handleSearch` (~678). DO NOT modify this useEffect — 27-01 already made the stored shape match it.
    - frontend/src/App.tsx — `CrossMarketplacePage` signature (~910): inline destructured props `{ preloadedJobId, onClearPreloadedJob }: { preloadedJobId?: string | null, onClearPreloadedJob?: () => void }` — also NO index signature. Its reopen useEffect (~974-985) uses the full-dict shape (`withDisplayOrder`, `results?.results`). DO NOT modify it — the SKU full-dict contract is already correct.
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-RESEARCH.md — §"App.tsx Reopen Wiring" (the exact `useState`/`handleReopen`/`renderTab` pattern) and the edge-case note (shared preloadedJobId is safe because tabs are mutually exclusive; child self-clears on fresh search; optionally clear on manual tab switch).
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-PATTERNS.md — Edit 1 snippet.
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-UI-SPEC.md — §B (App-level reopen wiring, MANDATORY).
  </read_first>
  <action>
    Wire reopen propagation at the App level (D-04, SC#2), AND make Task 1 independently type-clean by declaring the `onReopen` prop on both page signatures in the SAME change. The JSX in step 3 passes `onReopen` to both pages; both signatures use inline destructured prop types with NO index signature, so TS excess-property checking will fail `npm run build` at the call site unless the prop type declares `onReopen` here. That is why the prop-type addition is folded into THIS task (not deferred to Task 2) — Task 1's `npm run build` acceptance criterion must be independently satisfiable.

    In `App()` (near the existing `activeTab` state):
    1. Add `const [preloadedJobId, setPreloadedJobId] = useState<string | null>(null);`.
    2. Add `const handleReopen = (jobId: string, type: 'search' | 'cross') => { setActiveTab(type === 'cross' ? 'cross' : 'search'); setPreloadedJobId(jobId); };` — the destination tab is inherent to the entry type (D-01).
    3. In `renderTab`, change `case 'search'` to render `<SearchPage brands={brands} preloadedJobId={preloadedJobId} onClearPreloadedJob={() => setPreloadedJobId(null)} onReopen={(jobId) => handleReopen(jobId, 'search')} />` and `case 'cross'` to render `<CrossMarketplacePage preloadedJobId={preloadedJobId} onClearPreloadedJob={() => setPreloadedJobId(null)} onReopen={(jobId) => handleReopen(jobId, 'cross')} />`.
    4. Add `onReopen?: (jobId: string) => void` to BOTH the `SearchPage` and `CrossMarketplacePage` inline prop-type signatures (alongside the existing `brands`/`preloadedJobId`/`onClearPreloadedJob` props), and add `onReopen` to each page's destructured parameter list. In THIS task the pages only need to RECEIVE and hold `onReopen` so the call sites in step 3 type-check; the value is forwarded down to the `HistoryList` that Task 2 mounts (data flow: App → page (`onReopen`) → HistoryList). Do NOT add any HistoryList usage here — just thread the prop through.
    5. Optionally, in the tab-switch handler that sets `activeTab` on manual navigation, also `setPreloadedJobId(null)` to avoid a stale reopen firing (low risk; RESEARCH note — keep it simple, this is discretionary).

    Do NOT introduce a zustand/global store for this (that is Phase 28 / PERS-01 scope — RESEARCH anti-pattern). App-level `useState` is the SC#2-satisfying choice.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 — independently satisfiable in THIS task: the `onReopen` prop type is declared here, so the new `onReopen={...}` JSX props on both pages pass TS excess-property checking without depending on Task 2.
    - `frontend/src/App.tsx` `App()` contains `const [preloadedJobId, setPreloadedJobId] = useState<string | null>(null);` and a `handleReopen` that sets the tab + preloadedJobId.
    - `renderTab` passes `preloadedJobId={preloadedJobId}` and `onClearPreloadedJob={() => setPreloadedJobId(null)}` to BOTH `<SearchPage>` and `<CrossMarketplacePage>` (grep for `preloadedJobId={preloadedJobId}` returns 2 hits).
    - Both `SearchPage` and `CrossMarketplacePage` prop-type signatures declare `onReopen?: (jobId: string) => void` and destructure `onReopen` (grep for `onReopen` in the two signatures returns hits on both).
    - The child reopen `useEffect`s (SearchPage ~651-660, CrossMarketplacePage ~974-985) are unchanged.
    - No zustand/global store added.
  </acceptance_criteria>
  <done>App.tsx owns and propagates preloadedJobId to both pages, and both pages declare/receive the `onReopen` prop so the renderTab call sites type-check on their own (`npm run build` exits 0 without depending on Task 2). Setting preloadedJobId triggers the existing child useEffect to load + display saved results without re-scraping (Success Criterion #2 satisfied at the propagation level).</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable HistoryList component and mount it per-tab in SearchPage and CrossMarketplacePage</name>
  <files>frontend/src/App.tsx</files>
  <read_first>
    NOTE: Anchor edits by SYMBOL NAME (`HistoryList`, `SearchPage`, `CrossMarketplacePage`, `handleSearch`, `SettingsPage` brand row), not by exact line number — live App.tsx drifts a few lines from the references below.
    - frontend/src/App.tsx — `SettingsPage` brand row (~1423-1448) for the `.brand-item`/`.brand-info`/`.brand-name-text`/`.brand-domain-text`/`.brand-actions`/`.btn-icon.text-error` geometry to reuse; `handleDeleteBrand` (~1349-1357) for the confirm()+refetch delete pattern.
    - frontend/src/App.tsx — `SearchPage` reopen useEffect (~651-660), its `handleSearch` (~676+), and the success path (`setResults(data)` ~691). `onClearPreloadedJob()` is already called at ~678. `CrossMarketplacePage` reopen useEffect (~974-985), its `handleSearch` (~1042+), and the success path (`setResults(withDisplayOrder(data))` ~1056). The HistoryList in each page must refetch after a successful search (Pitfall 4).
    - frontend/src/App.tsx — both pages now receive the `onReopen?: (jobId: string) => void` prop added in Task 1; Task 2 forwards it to the mounted `HistoryList`. Do NOT re-add the prop type — it already exists from Task 1.
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-UI-SPEC.md — §A (HistoryList contract): collapsible `GlassCard` panel above results, collapsed by default; header = "Histórico de buscas" + count badge + chevron (`ChevronDown`/`ChevronRight`); row layout left→right = label / type badge / date `DD/MM/YYYY HH:mm` pt-BR / status badge / delete button; row interaction states (COMPLETED clickable `<button>`, FAILED dimmed+error badge non-clickable, PENDING spinner badge non-clickable); reopen flow bubbles via `onReopen(job_id)`; copy contract (empty/loading/labels/confirmation strings); accessibility (real `<button>` for COMPLETED, aria-labels, status by text+icon not color alone, delete `stopPropagation`).
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-RESEARCH.md — §"Per-Tab History List": fetch on mount + after search + after delete, filter client-side by `type`, only COMPLETED reopens; Open Q1 recommendation = ONE shared component parameterized by `type`. §"Comparative History Label Derivation" + Pitfall 2: `query` is the raw term; compose the label in the FE from `brands` + `query`.
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-PATTERNS.md — Edit 2 (HistoryList row reuse of `.brand-item`; classes/icons to reuse; data + interaction rules).
    - frontend/src/api/client.ts — `getHistoryList()` (~91-93, returns full array newest-first), `getHistoryDetail(jobId)` (~95-97), `deleteHistory(jobId)` (~99-103). Each `SearchHistory` record has: job_id, query, brands, type ('search'|'cross'), status ('COMPLETED'|'FAILED'|'PENDING'), created_at.
  </read_first>
  <action>
    Build ONE reusable presentational component `HistoryList` and mount it per-tab (D-01). Implement entirely within `frontend/src/App.tsx` (no new files; hand-rolled CSS, no Tailwind). NOTE: this task lives in the same file (App.tsx) as Task 1 and must run after it; that same-file serialization is intentional — do not attempt to parallelize.

    1. Component signature: `const HistoryList = ({ type, onReopen, refreshKey }: { type: 'search' | 'cross'; onReopen: (jobId: string) => void; refreshKey: number }) => {...}`. Internal state: `items` (the fetched list), `loading`, `collapsed` (default true — collapsible panel, UI-SPEC §A discretion). Fetch via `ApiClient.getHistoryList()` in a `useEffect` whose deps include `refreshKey`, filtering client-side to `items.filter(h => h.type === type)`. Expose a refetch (re-run the same fetch) and call it after a delete.

       REFRESH MECHANISM (resolves Pitfall 4 — a newly completed search must appear in the history list): use a `refreshKey: number` prop. Each parent page owns a local `const [historyRefreshKey, setHistoryRefreshKey] = useState(0);` and, immediately after a SUCCESSFUL `handleSearch` (right after the success-path `setResults(...)`), calls `setHistoryRefreshKey(k => k + 1)`. The page passes `refreshKey={historyRefreshKey}` to its `<HistoryList>`. Because `refreshKey` is in the fetch `useEffect` deps, the bump triggers a refetch and the just-created entry appears. (Mount-fetch is the `refreshKey===0` first run; delete uses the local refetch function.) Document this chosen mechanism in the SUMMARY.

    2. Render a collapsible `GlassCard` panel mounted ABOVE the results container in each page's `.page-content`, after the search-bar card. Header: "Histórico de buscas" + a count badge + a chevron toggle (`ChevronDown`/`ChevronRight`). When collapsed, hide the list body.

    3. Each row reuses `.brand-item` geometry. Render:
       - Label (`.brand-name-text`): for `type==='cross'` use the stored `query` as-is (already `"SKU: …"`); for `type==='search'` compose in the FE per UI-SPEC §A — `{first 2 brand names joined ", "}{brands.length>2 ? ' · '+brands.length+' marcas' : ''}{query ? ' — "'+query+'"' : ''}` (Pitfall 2 — query is the raw term, never a pre-composed label). Example: `Reserva, Aramis · 3 marcas — "polo piquet"`.
       - Type badge (`.monitor-badge` style): "Comparativa" (primary tint) for `search`, "SKU" (accent tint) for `cross`.
       - Date/time (muted meta, `.brand-domain-text` style): format `created_at` as `DD/MM/YYYY HH:mm` (pt-BR; use `toLocaleString('pt-BR')` or equivalent).
       - Status badge (`.monitor-badge` style): "Concluída" (success) / "Falhou" (error) / "Em andamento" (warning, with `RefreshCw .animate-spin`).
       - Delete button (`.btn-icon.text-error`, `Trash2 size={18}`, aria-label "Excluir do histórico") that `stopPropagation`s, `confirm()`s with "Excluir esta busca do histórico? Esta ação é permanente.", calls `ApiClient.deleteHistory(job_id)`, then refetches.

    4. Row interaction (D-06): for `status === 'COMPLETED'`, render the row as a real `<button>` (or `role="button"` + `tabIndex=0`, Enter/Space activate) with `cursor: pointer` and a subtle `--primary` hover affordance; `onClick` → `onReopen(job_id)`. For `FAILED`, render non-clickable, opacity ~0.7, error badge, no reopen. For `PENDING`, render non-clickable with the "Em andamento" spinner badge. The delete button is always clickable and must `stopPropagation` so it never triggers reopen.

    5. Empty state: `.empty-state` with heading "Nenhuma busca ainda" + body per copy contract. Loading: `RefreshCw .animate-spin` + "Carregando histórico…".

    6. Mount: in `SearchPage` render `<HistoryList type="search" onReopen={onReopen!} refreshKey={historyRefreshKey} />`; in `CrossMarketplacePage` render `<HistoryList type="cross" onReopen={onReopen!} refreshKey={historyRefreshKey} />`. The `onReopen` value is the prop Task 1 already added to each page signature and that `renderTab` passes from App. After a successful `handleSearch` in each page, bump `historyRefreshKey` (per step 1) so the just-created entry shows (Pitfall 4).

    Import any missing lucide icons (`History`, `ChevronDown`, `ChevronRight`, `Power` already added in 27-02; `Trash2`, `RefreshCw`, `Globe` already imported). Reuse existing CSS classes; only add minimal new CSS rules if strictly required for the collapsible header or the COMPLETED-row hover, keeping to existing tokens.
  </action>
  <verify>
    <automated>cd frontend && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run build` exits 0 and `npm run lint` exits 0.
    - `frontend/src/App.tsx` defines a single `HistoryList` component parameterized by `type`, `onReopen`, and `refreshKey`, mounted once in `SearchPage` (`type="search"`) and once in `CrossMarketplacePage` (`type="cross"`).
    - HistoryList fetches via `ApiClient.getHistoryList()` and filters client-side by `type`; deletes via `ApiClient.deleteHistory` with a confirm + refetch; the delete button `stopPropagation`s.
    - HistoryList re-fetches when `refreshKey` changes: each page bumps its `historyRefreshKey` after a successful `handleSearch`, and the new entry appears in the list without a page reload (Pitfall 4). The mount fetch (initial `refreshKey`) and delete-refetch also work.
    - COMPLETED rows are real `<button>`s (or role/tabIndex) calling `onReopen(job_id)`; FAILED and PENDING rows are non-clickable with their badges (D-06).
    - Comparative label is composed in the FE from `brands` + raw `query` (no pre-composed label stored); SKU label uses the stored `"SKU: …"` query.
    - Manual UAT (recorded in SUMMARY, per VALIDATION.md): run a comparative search → it appears in the SearchPage history list (refreshKey refetch) → click it → results re-display identically with NO scrape network call; repeat for SKU in CrossMarketplacePage; confirm FAILED/PENDING entries are not clickable.
  </acceptance_criteria>
  <done>A reusable HistoryList shows each tab's own saved searches with label/badge/date/status; a successful search bumps refreshKey and the new entry appears; COMPLETED entries reopen via App.handleReopen and re-display saved results without re-scraping; FAILED/PENDING are non-clickable; delete works with confirmation. Build + lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (HistoryList) → GET /history, GET /history/{id}, DELETE /history/{id} | Reads/deletes saved records; same API-key gate as the rest of the app |
| stored results → JSX render on reopen | Saved search data re-rendered in the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering / XSS | Stored search results & labels re-rendered on reopen | mitigate | React auto-escapes JSX text and attribute values; results render via text/`src`, no `dangerouslySetInnerHTML` (verified RESEARCH Security Domain) — no change to this safe path |
| T-27-03-02 | Denial of Service (client) | Very large history list rendered | accept | Bounded by 30-day `cleanup_old_records` retention; client-side `type` filter sufficient (RESEARCH A3); collapsed-by-default panel limits initial render |
| T-27-03-03 | Error handling | Reopen of a FAILED/PENDING record (results null) | mitigate | Click gated on `status === 'COMPLETED'` (D-06); FAILED/PENDING non-clickable so `getHistoryDetail` is never called for null-results records (Pitfall 3) |
| T-27-03-SC | Tampering | npm installs | accept | This plan installs ZERO packages (RESEARCH Package Legitimacy Audit: zero external packages). All icons from already-installed lucide-react. No legitimacy gate required |

No HIGH threats. Reuses existing escaped-render path and existing history endpoints; the only new risk vector (reopening a null-results record) is closed by the D-06 click gate.
</threat_model>

<verification>
- `cd frontend && npm run build` → exits 0 (type gate). NOTE: Task 1's build is independently green — the `onReopen` prop type is added in Task 1, so its call sites do not depend on Task 2.
- `cd frontend && npm run lint` → exits 0.
- Manual UAT (per VALIDATION.md Manual-Only table): comparative reopen re-displays identical results with no scrape; SKU reopen works; FAILED/PENDING non-clickable; a new search bumps refreshKey and refreshes the list.
- Cross-check SC#2: `App.tsx` owns `preloadedJobId` and propagates it to both pages (verified in Task 1).
</verification>

<success_criteria>
HIST-02 complete: any saved search (comparative or SKU) reopens from its tab's history list and re-displays without re-scraping, with `App.tsx` owning and propagating `preloadedJobId` (Success Criteria #1 reopen-half and #2).
</success_criteria>

<output>
Create `.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-03-SUMMARY.md` when done
</output>
