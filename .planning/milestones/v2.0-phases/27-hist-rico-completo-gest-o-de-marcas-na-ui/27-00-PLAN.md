---
phase: 27-hist-rico-completo-gest-o-de-marcas-na-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_search_history_comparative.py
autonomous: true
requirements: [HIST-01]
must_haves:
  truths:
    - "A pytest test file exists asserting that POST /search persists a type='search' history record"
    - "A test asserts the persisted results field is the inner List[BrandSearchResult], NOT the ComparisonResult wrapper (shape contract / Pitfall 1)"
    - "A test asserts the search-exception path marks the record FAILED with error set"
    - "Tests collect without import errors and fail RED against the current unmodified POST /search"
  artifacts:
    - path: "tests/test_search_history_comparative.py"
      provides: "RED backend test scaffold for HIST-01 (persistence + shape contract + FAILED path + service round-trip)"
      contains: "def test_persisted_results_shape_is_inner_list"
      min_lines: 80
  key_links:
    - from: "tests/test_search_history_comparative.py"
      to: "api.routes_search.search_products"
      via: "asyncio.run(routes_search.search_products(fake_request)) with monkeypatched search_history_service + engine_factory"
      pattern: "asyncio\\.run\\(.*search_products"
    - from: "tests/test_search_history_comparative.py"
      to: "services.search_history_service.SearchHistoryService"
      via: "in-memory service via __new__ with patched _save_history"
      pattern: "SearchHistoryService.__new__"
---

<objective>
Wave 0: create the RED backend test scaffold `tests/test_search_history_comparative.py` that locks the HIST-01 contract BEFORE the persistence code is written (Nyquist: every verify needs an automated test; this file is the missing test). It encodes the single highest-risk decision in the phase — the stored-result-shape contract (Resolution A: store the INNER list, not the `ComparisonResult` wrapper) — as an executable assertion so 27-01 cannot regress it silently.

Purpose: Make the HIST-01 acceptance criteria machine-checkable. These tests MUST fail (RED) against the current unmodified `POST /search` (which has no persistence and no try/except) and turn GREEN once 27-01 lands.
Output: `tests/test_search_history_comparative.py` with 4 test functions following the project test idiom from `tests/test_brand_active.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-RESEARCH.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-PATTERNS.md
@.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/test_search_history_comparative.py (RED scaffold for HIST-01)</name>
  <files>tests/test_search_history_comparative.py</files>
  <read_first>
    - tests/test_brand_active.py — the EXACT project test idiom to copy: in-memory service via `SomeService.__new__(...)`, mocked I/O (`unittest.mock.patch.object(svc, "_save")`), monkeypatched module singleton (`module.brand_service = fake_svc` in try/finally), async route call via `asyncio.run(route_fn(...))`, plain `assert` inside `class Test*`. Lines 30-57 (in-memory builder), 184-203 (singleton patch + asyncio.run).
    - services/search_history_service.py — signatures to mock/use: `create_job(job_id, query, brands, type="search")` (line 52), `update_job(job_id, status, results=None, error=None)` (line 58), `get_job(job_id)` (line 67); persistence method `_save_history` (line 28) must be patched to a no-op for the in-memory service. The module singleton is `search_history_service` (line 83).
    - api/routes_search.py — the route under test is `search_products(request: SearchRequest)` (line 140); it currently has NO persistence (lines 140-179). Module-level singletons to monkeypatch: `search_history_service` (imported lazily today — for the test, patch the module attribute `api.routes_search.search_history_service` after the route imports it; OR patch `services.search_history_service.search_history_service` which the lazy `from ... import` resolves to) and `engine_factory` (used at line 165 `engine_factory.search_all_brands(...)`). `brand_service.list_brands(active_only=True)` is called at line 144 — patch `api.routes_search.brand_service` to a fake returning one active brand so `target_brands` is deterministic.
    - core/models.py — `ComparisonResult` (query, brands_searched, results: List[BrandSearchResult]), `BrandSearchResult` (the inner item shape), `SearchHistory` (job_id, query, brands, type, status, results: Optional[Any], error, created_at). Build canned `BrandSearchResult` instances for the engine mock to return.
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-RESEARCH.md — "Stored Result Shape Contract" table and Pitfall 1: stored `results` MUST equal `result.model_dump(mode="json")["results"]` (the inner array), NOT the whole `ComparisonResult`.
    - .planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-VALIDATION.md — the four test function names are fixed by the test map: test_post_search_persists_history, test_persisted_results_shape_is_inner_list, test_search_failure_marks_failed, test_history_service_search_type.
  </read_first>
  <action>
    Create `tests/test_search_history_comparative.py` following the `tests/test_brand_active.py` idiom verbatim (no I/O, monkeypatched singletons, `asyncio.run` route calls, plain asserts in `class Test*`). Implement exactly these four test functions (names are contract — referenced by 27-01 verify blocks and VALIDATION.md):

    1. `test_history_service_search_type` (unit, service round-trip): build an in-memory `SearchHistoryService` via `__new__`, set `history = {}`, patch `_save_history` to a no-op. Call `create_job(job_id, query="polo", brands=["aramis"], type="search")` then `update_job(job_id, status="COMPLETED", results=[{"brand": "aramis", "products": []}])`. Assert `get_job(job_id).type == "search"`, `.status == "COMPLETED"`, `.results == [...]`.

    2. `test_post_search_persists_history` (integration): build a fake in-memory history service (as above) and assign it to `api.routes_search.search_history_service` in try/finally. Patch `api.routes_search.engine_factory` with a MagicMock whose `search_all_brands` is an AsyncMock returning a list of canned `BrandSearchResult`s. Patch `api.routes_search.brand_service` so `list_brands(active_only=True)` returns one active brand. Construct a `SearchRequest(query="polo", brands=["aramis"])` (or the minimal valid fields). `asyncio.run(routes_search.search_products(req))`. Assert exactly one record was created and its `type == "search"`, `status == "COMPLETED"`, and `query == "polo"` (raw term, Pitfall 2 — NOT a composed label).

    3. `test_persisted_results_shape_is_inner_list` (integration, GUARDS Pitfall 1 — the highest-risk bug): same setup as test 2. After `asyncio.run(...)`, fetch the stored record's `results` and assert it `isinstance(stored.results, list)` AND that each element is a dict/object with the per-brand shape of a `BrandSearchResult` (e.g. has the brand/products keys) — i.e. it is the INNER list. Add an explicit negative assertion that it is NOT the wrapper: `assert not (isinstance(stored.results, dict) and "brands_searched" in stored.results)`. Include a comment citing RESEARCH "Stored Result Shape Contract" / Resolution A and that `SearchPage` reopen (App.tsx:655) requires `res.results` to BE the array.

    4. `test_search_failure_marks_failed` (integration, Pitfall 5): same setup but `engine_factory.search_all_brands` AsyncMock raises `RuntimeError("boom")`. Wrap the `asyncio.run(routes_search.search_products(req))` in `pytest.raises(RuntimeError)` (the route re-raises after marking FAILED). Assert the stored record `status == "FAILED"` and `error` contains "boom".

    Do NOT add any frontend test framework. Do NOT modify `routes_search.py` in this plan — these tests are expected to FAIL RED until 27-01.
  </action>
  <verify>
    <automated>python -m pytest tests/test_search_history_comparative.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_search_history_comparative.py --collect-only -q` exits 0 and lists exactly 4 tests: test_post_search_persists_history, test_persisted_results_shape_is_inner_list, test_search_failure_marks_failed, test_history_service_search_type (no import/collection errors).
    - Running `python -m pytest tests/test_search_history_comparative.py -x` FAILS RED (the three integration tests fail because POST /search has no persistence today); this is the expected RED state, not a defect.
    - File contains `def test_persisted_results_shape_is_inner_list` and an assertion that the stored `results` is a list AND not a dict containing "brands_searched".
    - File uses the `tests/test_brand_active.py` idiom: `SearchHistoryService.__new__`, patched `_save_history`, monkeypatched module singletons in try/finally, `asyncio.run(...)`.
    - No frontend test files or framework added.
  </acceptance_criteria>
  <done>The test file exists, collects cleanly (4 tests), and fails RED against the current code, encoding the shape contract, persistence, FAILED path, and service round-trip for HIST-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → service/route under test | Tests run trusted code paths in-memory with mocked I/O; no untrusted input crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-00-01 | Tampering | `_save_history` real file I/O during tests | mitigate | Patch `_save_history` to a no-op; tests never touch `data/search_history.json` (mirrors `test_brand_active.py` patching `_save`) |
| T-27-00-SC | Tampering | npm/pip installs | accept | This plan installs ZERO packages (pytest already present, per RESEARCH Package Legitimacy Audit). No package-legitimacy gate needed |

No HIGH threats. This is a test-only plan with no new runtime surface.
</threat_model>

<verification>
- `python -m pytest tests/test_search_history_comparative.py --collect-only -q` → 4 tests, 0 collection errors.
- `python -m pytest tests/test_search_history_comparative.py -x` → RED (expected; turns GREEN after 27-01).
- `python -m pytest tests/ --collect-only -q` → whole suite still collects (no import breakage introduced).
</verification>

<success_criteria>
The HIST-01 contract is captured as four executable pytest functions that fail RED today and define the exact shape/persistence/failure expectations for 27-01 to satisfy.
</success_criteria>

<output>
Create `.planning/phases/27-hist-rico-completo-gest-o-de-marcas-na-ui/27-00-SUMMARY.md` when done
</output>
