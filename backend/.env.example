APP_HOST=127.0.0.1
APP_PORT=8000
INTERNAL_API_KEY=dev-api-key
PLAYWRIGHT_ENABLED=true

# Optional Wake storefront token overrides. Keep real values in local env/.env only.
# WAKE_ACCESS_TOKEN_RICHARDS=
